---
phase: stage-1.5c-information-architecture
plan: 6
plan-name: admin-platform-admin
type: execute
status: not-started
wave: 3
depends_on: [1, 3]
autonomous: true
halt_after: false
threat_model_severity: low
requirements: []
files_modified:
  - src/app/admin/page.tsx
  - src/app/admin/users/page.tsx
  - src/app/admin/roles/page.tsx
  - src/app/admin/permissions/page.tsx
  - src/app/admin/org-chart/page.tsx
  - src/app/admin/approval-workflows/page.tsx
  - src/app/admin/notification-rules/page.tsx
  - src/app/admin/cost-codes/page.tsx
  - src/app/admin/templates/page.tsx
  - src/app/admin/integrations/page.tsx
  - src/app/admin/billing/page.tsx
  - src/app/admin/workflow-customization/page.tsx
  - src/app/admin/profile/page.tsx
  - src/app/platform-admin/page.tsx
requires:
  - .planning/phases/stage-1.5c-information-architecture/CONTEXT.md
  - .planning/phases/stage-1.5c-information-architecture/PATTERNS.md
  - .planning/phases/stage-1.5c-information-architecture/01.5c-1-nav-today-redirects-PLAN.md (Wave 1)
  - .planning/phases/stage-1.5c-information-architecture/01.5c-3-extraction-production-routes-PLAN.md (Wave 2)
  - .planning/phases/stage-1.5c-information-architecture/01.5c-4-section-placeholders-sub-portal-PLAN.md (PlaceholderCard from Plan 4)
  - src/app/settings/page.tsx (existing — analog for /admin)
  - src/app/settings/team/page.tsx (existing — analog for /admin/users)
  - src/app/settings/integrations/page.tsx (existing — analog for /admin/integrations)
  - src/app/settings/billing/page.tsx (existing — analog for /admin/billing)
  - src/app/settings/cost-codes/page.tsx (existing — analog for /admin/cost-codes)
  - src/app/admin/platform/page.tsx (existing — analog for /platform-admin)
provides:
  - 13-item Admin section at /admin/* (5 real-logic mounts + 8 placeholders)
  - /platform-admin/* placeholder (existing /admin/platform redirects here per Plan 1)
affects:
  - All /settings/* paths 301-redirect to /admin/* (per Plan 1's redirect map)
  - /admin/platform 301-redirects to /platform-admin (per Plan 1)
  - Admin ▾ dropdown in top nav (Plan 1) links to all 13 admin items
acceptance-criteria-target: 9 falsifiable items
estimated-duration: 0.5 day
must_haves:
  truths:
    - "Admin section has 13 routes per CONTEXT D-15 / Part 7 F: /admin (overview) + /admin/users + /admin/roles + /admin/permissions + /admin/org-chart + /admin/approval-workflows + /admin/notification-rules + /admin/cost-codes + /admin/templates + /admin/integrations + /admin/billing + /admin/workflow-customization + /admin/profile"
    - "/admin (overview) renders Card grid linking to all 12 sub-routes"
    - "/admin/users mounts existing /settings/team logic with terminology rename (Team → Users in user-visible labels)"
    - "/admin/integrations mounts existing /settings/integrations logic"
    - "/admin/billing mounts existing /settings/billing logic"
    - "/admin/cost-codes mounts existing /settings/cost-codes logic"
    - "/admin/roles + /admin/permissions + /admin/org-chart placeholders describe F2 implementation (15-role library + permission matrix + visual org chart)"
    - "/admin/approval-workflows + /admin/notification-rules placeholders describe F3 engines"
    - "/admin/profile placeholder shows the stubbed selector copy: 'Custom Builder / Remodeler / Commercial GC — coming F2' (per CONTEXT D-10 / Q12=A)"
    - "/admin/templates + /admin/workflow-customization placeholders describe Wave 3 / F3 future-state"
    - "/platform-admin/page.tsx exists as a placeholder describing cross-org tools"
    - "Existing /admin/platform/* paths redirect to /platform-admin/* (Plan 1's redirect map updated to point at /platform-admin)"
    - "Build + typecheck + hooks silent across all 14 files; privacy gate clean"
  artifacts:
    - path: "src/app/admin/page.tsx"
      provides: "Admin section overview Card grid linking to 12 sub-routes"
      exports: ["default"]
    - path: "src/app/admin/users/page.tsx"
      provides: "Users management (renamed from /settings/team — same logic, Bills/terminology rename = User-facing 'Users' label)"
      exports: ["default"]
    - path: "src/app/admin/roles/page.tsx"
      provides: "Roles placeholder (15 default roles + custom org roles — coming F2)"
      exports: ["default"]
    - path: "src/app/admin/profile/page.tsx"
      provides: "Org Profile placeholder showing stubbed Custom Builder / Remodeler / Commercial GC selector"
      exports: ["default"]
    - path: "src/app/platform-admin/page.tsx"
      provides: "Platform Admin placeholder describing cross-org tools (existing /admin/platform redirects here)"
      exports: ["default"]
  key_links:
    - from: "src/app/admin/users/page.tsx"
      to: "src/app/settings/team/page.tsx"
      via: "logic re-mount with terminology rename"
      pattern: "(Team UI label → Users)"
    - from: "src/app/admin/page.tsx"
      to: "All 12 sub-routes via Card grid"
      via: "Link components"
      pattern: "/admin/users"
    - from: "src/app/platform-admin/page.tsx"
      to: "src/app/admin/platform/page.tsx (existing — re-mount logic OR keep existing logic mounted under platform-admin path)"
      via: "wrapper or rename"
      pattern: "platform_admin"
---

<objective>
Build the 13-item Admin section + Platform Admin placeholder per CONTEXT D-15 / Part 7 F. Existing /settings/* routes are renamed + remounted at /admin/* (legacy paths redirect via Plan 1's `next.config.mjs` extension). New placeholders describe F2 (roles/permissions/org-chart/profile) + F3 (approval workflows/notification rules) future-states. Per CONTEXT D-09: Platform Admin badge (Plan 1) links to /platform-admin/* placeholder.

This plan is parallel-safe with Plan 4 + Plan 5 — different file sets, all in Wave 3.

Purpose:
- Admin ▾ dropdown items (Plan 1's AdminDropdown) all resolve to existing routes (no 404s).
- /settings/* logic preserved (no functional regression) — just remounted at /admin/* per Part 7 F restructure.
- F2/F3 future-state placeholders (Roles, Permissions, Org Chart, Profile, Approval Workflows, Notification Rules, Templates, Workflow Customization) describe the IA so reviewers can evaluate.
- Platform Admin placeholder gives platform_admins (Jake/Andrew) a defined destination from the badge.

Output:
- 13 admin page.tsx files (5 real-logic + 8 placeholders)
- 1 platform-admin page.tsx placeholder
</objective>

<execution_context>
@C:/Users/Jake/nightwork-platform/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jake/nightwork-platform/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/stage-1.5c-information-architecture/CONTEXT.md
@.planning/phases/stage-1.5c-information-architecture/PATTERNS.md
@.planning/phases/stage-1.5c-information-architecture/01.5c-1-nav-today-redirects-PLAN.md
@.planning/phases/stage-1.5c-information-architecture/01.5c-3-extraction-production-routes-PLAN.md
@.planning/phases/stage-1.5c-information-architecture/01.5c-4-section-placeholders-sub-portal-PLAN.md
@.planning/expansions/stage-1.5c-information-architecture-EXPANDED-SCOPE.md
@CLAUDE.md
@.planning/design/SYSTEM.md
@.planning/design/COMPONENTS.md
@.planning/design/PATTERNS.md
@src/components/nw/Card.tsx
@src/components/nw/Eyebrow.tsx
@src/components/nw/Badge.tsx
@src/components/nw/PlaceholderCard.tsx
@src/app/settings/page.tsx
@src/app/settings/team/page.tsx
@src/app/settings/integrations/page.tsx
@src/app/settings/billing/page.tsx
@src/app/settings/cost-codes/page.tsx
@src/app/admin/platform/page.tsx
@src/middleware.ts

<interfaces>
Admin section (per CONTEXT D-15 / Part 7 F):

  /admin                        — overview (NEW)
  /admin/users                  — REAL-LOGIC (mount /settings/team)
  /admin/roles                  — placeholder (F2)
  /admin/permissions            — placeholder (F2)
  /admin/org-chart              — placeholder (F2)
  /admin/approval-workflows     — placeholder (F3)
  /admin/notification-rules     — placeholder (F3)
  /admin/cost-codes             — REAL-LOGIC (mount /settings/cost-codes)
  /admin/templates              — placeholder (Wave 3)
  /admin/integrations           — REAL-LOGIC (mount /settings/integrations)
  /admin/billing                — REAL-LOGIC (mount /settings/billing)
  /admin/workflow-customization — placeholder (F3)
  /admin/profile                — placeholder (F2 — stubbed Custom Builder / Remodeler / Commercial GC selector)

Plan 1's redirect map handles legacy paths:
  /settings           → /admin
  /settings/team      → /admin/users
  /settings/integrations → /admin/integrations
  /settings/billing   → /admin/billing
  /settings/cost-codes → /admin/cost-codes
  (other /settings/* paths redirect to /admin per Plan 1's wildcard rule)

Platform Admin (per CONTEXT D-09):
  /platform-admin     — NEW placeholder; existing /admin/platform redirects here

Per Plan 1's middleware: existing platform_admin gate at /admin/platform/* still applies. The /platform-admin/* path inherits the same middleware semantics — Plan 6 does NOT modify middleware. The new placeholder is gated by middleware role check (existing isPlatformAdmin posture).

PlaceholderCard reuse from Plan 4.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 13 admin routes (5 real-logic mounts + 8 placeholders + 1 overview)</name>
  <files>src/app/admin/page.tsx, src/app/admin/users/page.tsx, src/app/admin/roles/page.tsx, src/app/admin/permissions/page.tsx, src/app/admin/org-chart/page.tsx, src/app/admin/approval-workflows/page.tsx, src/app/admin/notification-rules/page.tsx, src/app/admin/cost-codes/page.tsx, src/app/admin/templates/page.tsx, src/app/admin/integrations/page.tsx, src/app/admin/billing/page.tsx, src/app/admin/workflow-customization/page.tsx, src/app/admin/profile/page.tsx</files>

  <read_first>
    - src/app/settings/page.tsx (full file — analog for /admin overview if structure transfers)
    - src/app/settings/team/page.tsx (full file — to mount at /admin/users)
    - src/app/settings/integrations/page.tsx (full file)
    - src/app/settings/billing/page.tsx (full file)
    - src/app/settings/cost-codes/page.tsx (full file)
    - src/components/nw/PlaceholderCard.tsx (Plan 4)
    - src/app/pipeline/page.tsx (Plan 4 section overview pattern)
    - src/app/admin/page.tsx (existing — verify if exists; if so, replace with overview)
  </read_first>

  <action>
**Half A — REAL-LOGIC mounts (4 files):**

For each, COPY the rendering logic from /settings/* and rename to /admin/*. UI labels retain their existing semantic (Team → Users in `/admin/users` is a label rename; cost-codes / integrations / billing labels stay).

1. **`/admin/users/page.tsx`** — copy from `src/app/settings/team/page.tsx`. Rename "Team" → "Users" in user-visible labels (page title, headlines). Logic + state shape unchanged. Component file imports + variable names retain `team*` per CONTEXT D-01 scope clarification (UI labels visible; code identifiers not).

2. **`/admin/integrations/page.tsx`** — copy from `src/app/settings/integrations/page.tsx`. Page title stays "Integrations". Logic + state unchanged.

3. **`/admin/billing/page.tsx`** — copy from `src/app/settings/billing/page.tsx`. Page title stays "Billing". Logic + state unchanged.

4. **`/admin/cost-codes/page.tsx`** — copy from `src/app/settings/cost-codes/page.tsx`. Page title stays "Cost Codes". Logic + state unchanged.

**Half B — PLACEHOLDER mounts (8 files):**

5. **`/admin/page.tsx`** — section overview. RESTRUCTURE existing (if /admin/page.tsx exists from earlier code) OR create new. Card grid linking to all 12 sub-routes. Use /pipeline/page.tsx pattern (Plan 4 Task 1).

   Suggested grid items (with Wave badges where placeholder):
   ```
   Org Settings (overview)         — current page
   Users                           — REAL (manage org_members)
   Roles                           — Coming F2
   Permissions                     — Coming F2
   Org Chart                       — Coming F2
   Approval Workflows              — Coming F3
   Notification Rules              — Coming F3
   Cost Codes                      — REAL
   Templates                       — Coming Wave 3
   Integrations                    — REAL
   Billing                         — REAL
   Workflow Customization          — Coming F3
   Profile (Org Type)              — Coming F2
   ```

   The overview is interactive — each card is a Link to its sub-route. Show Wave badge for placeholders only (real-logic items don't show "Coming" badges).

6. **`/admin/roles/page.tsx`** — placeholder. Body: "15 default roles will live here, customizable per org. Default library: Owner, GM, Sr PM, PM, Estimator, Accountant, Foreman, Field, Designer, Sub Foreman, Sub Bookkeeper, Owner/Homeowner, Architect/External, Inspector, Banker. Companies can customize roles. Org chart drives reporting + approval routing + notification routing + permission inheritance." Wave: F2.

7. **`/admin/permissions/page.tsx`** — placeholder. Body: "Permissions matrix UI — granular per-role permissions. Read / Write / Approve / Admin per entity type." Wave: F2.

8. **`/admin/org-chart/page.tsx`** — placeholder. Body: "Visual org chart — drives reporting + approval routing + notification routing + permission inheritance per Principle 6." Wave: F2.

9. **`/admin/approval-workflows/page.tsx`** — placeholder. Body: "Approval workflow engine — customizable per company. PM approves bill → QA approves → Push to QB. Each company configures their own thresholds + routes." Wave: F3.

10. **`/admin/notification-rules/page.tsx`** — placeholder. Body: "Notification routing engine — who gets notified for what events. Configurable per role + per event type." Wave: F3.

11. **`/admin/templates/page.tsx`** — placeholder. Body: "Document + email templates — proposals, contracts, lien-release letters, daily-log emails. Per-org customizable." Wave: Wave 3.

12. **`/admin/workflow-customization/page.tsx`** — placeholder. Body: "Workflow customization — adjust per-company business rules (payment schedule, deposit %, fee rates) without code changes per CLAUDE.md 'Org-configurable, not hardcoded' rule." Wave: F3.

13. **`/admin/profile/page.tsx`** — placeholder with stubbed selector per CONTEXT D-10 / Q12=A.

   Body: "Org type — Custom Builder / Remodeler / Commercial GC. Drives feature flag visibility per Principle 8 — pre-con prominent for custom; lighter for remodel; RFIs/submittals prominent for commercial. Coming F2 — selector + filter wiring."

   Visually show 3 options (disabled/grayed) so the eventual selector shape is visible:

   ```typescript
   import PlaceholderCard from "@/components/nw/PlaceholderCard";
   import Card from "@/components/nw/Card";
   import Eyebrow from "@/components/nw/Eyebrow";
   import Badge from "@/components/nw/Badge";

   export default function AdminProfilePage() {
     return (
       <div className="px-6 py-8 max-w-[800px] mx-auto space-y-6">
         <PlaceholderCard
           eyebrow="Admin · Profile"
           headline="Org type"
           body="Drives feature flag visibility per Principle 8. Custom Builder shows full Pipeline; Remodeler hides Pipeline; Commercial GC promotes RFIs/Submittals. Coming F2 — selector + filter wiring."
           wave="F2"
         />

         {/* Stubbed selector shape — visually shows 3 options as disabled cards */}
         <div className="grid grid-cols-1 md:grid-cols-3 gap-3 opacity-60 pointer-events-none">
           <Card padding="md">
             <Eyebrow tone="default" className="mb-1">Option</Eyebrow>
             <div className="text-[14px] mb-1" style={{ fontFamily: "var(--font-space-grotesk)", fontWeight: 500, color: "var(--text-primary)" }}>Custom Builder</div>
             <p className="text-[12px]" style={{ color: "var(--text-secondary)" }}>Full Pipeline + Pre-con prominent</p>
             <Badge variant="neutral">Coming F2</Badge>
           </Card>
           <Card padding="md">
             <div className="text-[14px] mb-1" style={{ fontFamily: "var(--font-space-grotesk)", fontWeight: 500, color: "var(--text-primary)" }}>Remodeler</div>
             <p className="text-[12px]" style={{ color: "var(--text-secondary)" }}>Lighter pre-con; emphasis on Bills/Pay Apps</p>
             <Badge variant="neutral">Coming F2</Badge>
           </Card>
           <Card padding="md">
             <div className="text-[14px] mb-1" style={{ fontFamily: "var(--font-space-grotesk)", fontWeight: 500, color: "var(--text-primary)" }}>Commercial GC</div>
             <p className="text-[12px]" style={{ color: "var(--text-secondary)" }}>RFIs / Submittals prominent</p>
             <Badge variant="neutral">Coming F2</Badge>
           </Card>
         </div>
       </div>
     );
   }
   ```

   The disabled grid demonstrates the eventual selector shape without doing any actual filtering (per Q12=A — placeholder only).

**Per existing /admin/page.tsx (if exists):** verify before writing. If existing /admin/page.tsx mounts something specific (e.g., redirects to /admin/platform), restructure to be the section overview Card grid. Backup any existing logic in SUMMARY for review.
  </action>

  <verify>
    <automated>npm run build && ls src/app/admin/page.tsx src/app/admin/users/page.tsx src/app/admin/roles/page.tsx src/app/admin/permissions/page.tsx src/app/admin/org-chart/page.tsx src/app/admin/approval-workflows/page.tsx src/app/admin/notification-rules/page.tsx src/app/admin/cost-codes/page.tsx src/app/admin/templates/page.tsx src/app/admin/integrations/page.tsx src/app/admin/billing/page.tsx src/app/admin/workflow-customization/page.tsx src/app/admin/profile/page.tsx 2>&1 | grep -c 'page.tsx'</automated>
    Expected: build exits 0; ls returns 13.

    Real-logic mount verification:
    - `/admin/users/page.tsx` size resembles `/settings/team/page.tsx` (logic preserved)
    - `/admin/integrations/page.tsx` size resembles `/settings/integrations/page.tsx`
    - `/admin/billing/page.tsx` size resembles `/settings/billing/page.tsx`
    - `/admin/cost-codes/page.tsx` size resembles `/settings/cost-codes/page.tsx`

    Placeholder verification:
    - `grep -c 'PlaceholderCard' src/app/admin/roles/page.tsx` returns >=1
    - Same for /admin/permissions, /admin/org-chart, etc. (8 placeholders total)

    /admin/profile shows stubbed selector:
    - `grep -c 'Custom Builder' src/app/admin/profile/page.tsx` returns >=1
    - `grep -c 'Remodeler' src/app/admin/profile/page.tsx` returns >=1
    - `grep -c 'Commercial GC' src/app/admin/profile/page.tsx` returns >=1
    - `grep -c 'pointer-events-none' src/app/admin/profile/page.tsx` returns >=1 (disabled state)

    Hex check across all 13 files: 0 hardcoded hex.

    Visual smoke (Chrome DevTools MCP):
    - Visit /admin — section overview Card grid renders with 13 cards
    - Click /admin/users — renders Users page (existing /settings/team logic re-mounted)
    - Click /admin/roles — placeholder
    - Click /admin/profile — stubbed selector with 3 disabled options
    - Visit legacy /settings → 308 redirects to /admin (per Plan 1's redirect map)
  </verify>

  <done>
    - 13 admin routes exist (1 overview + 4 real-logic + 8 placeholders)
    - Real-logic mounts preserve /settings/* functionality at /admin/* paths
    - /admin/profile shows stubbed Custom Builder / Remodeler / Commercial GC selector
    - Build passes
    - Privacy gate clean
    - Hooks silent
  </done>
</task>

<task type="auto">
  <name>Task 2: Platform Admin placeholder + redirect destination</name>
  <files>src/app/platform-admin/page.tsx</files>

  <read_first>
    - src/app/admin/platform/page.tsx (existing — analog for /platform-admin)
    - src/middleware.ts (verify isPlatformAdmin gate; new /platform-admin/* path inherits same posture per CONTEXT D-09)
    - src/components/nw/PlaceholderCard.tsx (Plan 4)
    - .planning/phases/stage-1.5c-information-architecture/CONTEXT.md (D-09 — Platform Admin badge target)
    - .planning/expansions/stage-1.5c-information-architecture-EXPANDED-SCOPE.md Part 7 G (Platform Admin badge spec)
  </read_first>

  <action>
**Step A — Read existing /admin/platform/page.tsx.**

Determine if it has functional logic (e.g., cross-org tools, system monitoring) OR if it's a thin placeholder.

**Step B — Decide /platform-admin/page.tsx approach:**

**Option A (recommended): Placeholder.** Per CONTEXT D-09 + Part 7 G — Platform Admin section is itself placeholder for cross-org tooling (1.5c lands the badge + the destination; F1+ wires real cross-org operations). The existing /admin/platform/page.tsx may have stubs for cost-intelligence ops; those can stay where they are (Plan 1's redirect map updated to point at /platform-admin/cost-intelligence?tab=...).

**Option B: Re-mount existing /admin/platform/page.tsx logic at /platform-admin/page.tsx.** If existing /admin/platform has working logic that platform_admins use today, copy it.

**For 1.5c, Option A is simplest.** Create:

```typescript
// src/app/platform-admin/page.tsx
//
// Platform Admin landing page per CONTEXT D-09 + Part 7 G.
// Cross-org tooling for Jake / Andrew (and future support staff).
// Existing /admin/platform redirects here per Plan 1's redirect map.
//
// 1.5c: placeholder + links to existing platform admin tooling at
// /admin/platform/cost-intelligence?tab=... (the redirect map at Plan 1
// updated those destinations from /admin/platform/* to /platform-admin/*
// where appropriate; /admin/platform/cost-intelligence the consolidated
// CI tool may stay at the existing path since it's a complex existing surface).
//
// Per CLAUDE.md "Platform admin (cross-tenant)" section: existing posture
// preserved (no middleware change). Existing platform_admins query reused.

import Link from "next/link";
import Card from "@/components/nw/Card";
import Eyebrow from "@/components/nw/Eyebrow";
import Badge from "@/components/nw/Badge";
import PlaceholderCard from "@/components/nw/PlaceholderCard";

const PLATFORM_TOOLS = [
  // Existing /admin/platform/* tools (still mounted at original paths;
  // Plan 1's redirect map for /admin/platform → /platform-admin handles
  // the section root; sub-paths can stay at /admin/platform/*).
  // Document here as known platform-admin destinations:
  { href: "/admin/platform/cost-intelligence", label: "Cost Intelligence ops", desc: "Cross-org CI engine controls (existing tooling)", existing: true },
  // Placeholder destinations:
  { href: "#", label: "Org observability", desc: "Sentry / logs filtered by org_id (cross-org)", wave: "Wave 1.1-Lite" as const, existing: false },
  { href: "#", label: "Trial management", desc: "Extend / mark churned / impersonate per CLAUDE.md platform-admin runbook", wave: "Wave 1.1-Lite" as const, existing: false },
  { href: "#", label: "Cross-org support", desc: "Read any tenant's data for support cases (audit-logged)", wave: "Wave 1.1-Lite" as const, existing: false },
];

export default function PlatformAdminPage() {
  return (
    <div className="px-6 py-8 max-w-[1200px] mx-auto">
      <div className="mb-6">
        <Eyebrow tone="muted" className="mb-2">Platform Admin</Eyebrow>
        <h1 className="text-[28px] mb-1" style={{ fontFamily: "var(--font-space-grotesk)", fontWeight: 500, color: "var(--text-primary)" }}>
          Cross-org operations
        </h1>
        <p className="text-[14px]" style={{ color: "var(--text-secondary)" }}>
          For Jake / Andrew and future operations staff. Existing platform_admins gate enforced via middleware.
        </p>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-2 gap-5">
        {PLATFORM_TOOLS.map((tool) => (
          <div key={tool.label}>
            {tool.existing ? (
              <Link href={tool.href} className="block">
                <Card padding="md">
                  <Eyebrow tone="accent" className="mb-2">{tool.label}</Eyebrow>
                  <p className="text-[13px] mb-2" style={{ color: "var(--text-secondary)" }}>{tool.desc}</p>
                  <Badge variant="success">Live</Badge>
                </Card>
              </Link>
            ) : (
              <Card padding="md">
                <Eyebrow tone="muted" className="mb-2">{tool.label}</Eyebrow>
                <p className="text-[13px] mb-2" style={{ color: "var(--text-secondary)" }}>{tool.desc}</p>
                <Badge variant="neutral">Coming {tool.wave}</Badge>
              </Card>
            )}
          </div>
        ))}
      </div>
    </div>
  );
}
```

**Step C — Verify middleware behavior at /platform-admin/*.**

Per CONTEXT D-09 + CLAUDE.md "Platform admin (cross-tenant)" section: existing middleware checks `isPlatformAdmin` for /admin/platform/*. Verify the same check applies to /platform-admin/* — read src/middleware.ts. If the existing middleware's gating regex is `/admin/platform/*`, the new /platform-admin/* path may NOT be auto-gated. Two options:

**Option A:** Update middleware regex to also match `/platform-admin/*`. Add a comment citing CONTEXT D-09 + Plan 6.
**Option B:** Add a runtime check at /platform-admin/page.tsx (server component checks isPlatformAdmin via supabase server client; redirects non-admins).

Recommended: **Option A** — middleware-level gating is the existing posture per CLAUDE.md. Add /platform-admin to the regex list. Keep the change minimal.

If the executor verifies that the existing middleware uses a generic auth gate + role check approach (not path-based), no middleware change is needed — the page-level check inherits.

Document the chosen option in plan SUMMARY for Plan 7 documentation update.
  </action>

  <verify>
    <automated>npm run build && test -f src/app/platform-admin/page.tsx && grep -c 'Cross-org' src/app/platform-admin/page.tsx</automated>
    Expected: build exits 0; file exists; grep returns >=1.

    Manual smoke (Chrome DevTools MCP):
    - Logged in as Jake (platform_admin) — visit /platform-admin → renders cross-org operations page
    - Logged in as PM (non-platform-admin) — visit /platform-admin → middleware redirects to /dashboard or /today (per existing posture); OR page-level check redirects; verify the gate works
    - Visit legacy /admin/platform → 308 redirects to /platform-admin (per Plan 1's redirect map)

    Hex check + tenant-blind check + privacy gate per usual.
  </verify>

  <done>
    - /platform-admin/page.tsx exists as placeholder with cross-org operations description
    - Existing platform_admins gate works for the new path (middleware update OR page-level check)
    - Platform Admin badge from Plan 1 navigates to this page when clicked
    - Build passes
    - Privacy gate clean
    - Hooks silent
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /admin/* routes → existing role gate | owner / admin role required per Plan 1's ACCESS map; middleware enforces |
| /platform-admin/* → platform_admins gate | Existing isPlatformAdmin posture per CONTEXT D-09 + CLAUDE.md platform admin section |
| Real-logic re-mounts (/admin/users etc.) → original /settings/* logic | Same RLS, same auth, same audit-log writes; just renamed UI labels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.5c-6-01 | E (Elevation of privilege) | /platform-admin/* gate | mitigate | Verify existing middleware semantics at /platform-admin/* — either path-based regex updated to include /platform-admin OR generic auth-then-role check applies. Document in SUMMARY. Non-platform_admin users MUST be redirected away. |
| T-1.5c-6-02 | I (Information disclosure) | /platform-admin placeholder | accept | Placeholder content is generic (describes cross-org tooling category names + Wave badges); no actual cross-org data exposed. |
| T-1.5c-6-03 | T (Tampering) | /admin/* real-logic mounts | accept | /admin/users + /admin/integrations + /admin/billing + /admin/cost-codes preserve /settings/* logic verbatim. Existing optimistic-locking + audit-log writes preserved. |
| T-1.5c-6-04 | R (Repudiation) | /admin/* placeholder routes | accept | Placeholders are read-only; no audit-log writes. Existing middleware Sentry tagging applies. |
| T-1.5c-6-05 | S (Spoofing) | Admin section overview links | accept | Section overview Card grid links to /admin/* sub-routes via Next.js Link components. Tampering with Link href just routes elsewhere; existing role gates apply. |
</threat_model>

<verification>
After Task 1 + Task 2 complete:

1. 13 admin routes exist (4 real-logic + 8 placeholders + 1 overview)
2. Existing /settings/* logic preserved at /admin/* paths
3. /admin/profile shows stubbed Custom Builder / Remodeler / Commercial GC selector (disabled)
4. /platform-admin/page.tsx exists as placeholder
5. Existing platform_admins gate enforced at /platform-admin/* (middleware updated OR page-level check)
6. Plan 1's redirect map for /settings/* + /admin/platform → /admin/* + /platform-admin/* all resolve
7. Build + typecheck + hooks silent
8. Privacy gate clean across 14 new files

No halt — Wave 3 complete after Plan 6 ships. Plan 7 (Wave 4) starts.
</verification>

<success_criteria>
- 13 admin routes + 1 platform-admin placeholder
- Existing /settings/* logic preserved
- /admin/profile stubbed selector documents F2 future-state
- Platform Admin gate verified working at new path
- All 9 must_haves truths verified
- Build + typecheck + hooks + privacy gate clean
</success_criteria>

## Acceptance criteria

- [ ] AC-1.5c-6-01: 13 admin routes exist per CONTEXT D-15 / Part 7 F
- [ ] AC-1.5c-6-02: /admin/users mounts /settings/team logic (logic preserved; UI labels say Users)
- [ ] AC-1.5c-6-03: /admin/integrations + /admin/billing + /admin/cost-codes mount existing /settings/* logic
- [ ] AC-1.5c-6-04: /admin (overview) renders Card grid linking to all 12 sub-routes
- [ ] AC-1.5c-6-05: 8 placeholder admin routes (roles, permissions, org-chart, approval-workflows, notification-rules, templates, workflow-customization, profile) describe F2 / F3 / Wave 3 future-states
- [ ] AC-1.5c-6-06: /admin/profile shows disabled stubbed Custom Builder / Remodeler / Commercial GC selector per Q12=A
- [ ] AC-1.5c-6-07: /platform-admin/page.tsx exists as placeholder describing cross-org tools
- [ ] AC-1.5c-6-08: Existing platform_admins gate enforced at /platform-admin/* (middleware updated OR page-level check)
- [ ] AC-1.5c-6-09: Build + typecheck pass; T10c silent; tenant-blind silent; bouncy-easing silent; privacy gate clean

## Nyquist coverage

| Acceptance criterion | Verified by |
|---|---|
| AC-1.5c-6-01 | Task 1 verify (ls returns 13) |
| AC-1.5c-6-02 | Task 1 verify (file size resembles /settings/team + grep "Users") |
| AC-1.5c-6-03 | Task 1 verify (per-file grep) |
| AC-1.5c-6-04 | Task 1 verify (visual: section overview links to all 12) |
| AC-1.5c-6-05 | Task 1 verify (per-placeholder grep PlaceholderCard + Wave badge) |
| AC-1.5c-6-06 | Task 1 verify (grep "Custom Builder" + "Remodeler" + "Commercial GC" + "pointer-events-none") |
| AC-1.5c-6-07 | Task 2 verify (test -f /platform-admin/page.tsx) |
| AC-1.5c-6-08 | Task 2 verify (smoke test as PM = redirect; as Jake = renders) |
| AC-1.5c-6-09 | Plan-level (npm run build + privacy gate grep) |

## Decisions inherited (from CONTEXT.md)

- D-09: Platform Admin badge top-right for Jake/Andrew (Plan 1) — destination at /platform-admin/* per CONTEXT D-09
- D-10: Today screen role-aware logic SKELETON only; profile-based feature flags placeholder at /admin/profile per Q12=A
- D-15: 4 NEW canonical docs + 5 UPDATED docs (Plan 7); /admin/* structure per Part 7 F lands here

## Halt conditions

- No halt_after — Wave 3 complete after Plan 6
- Halt during execution if:
  - Existing /settings/* logic doesn't transfer cleanly (e.g., context provider scope issues, route-segment conflicts)
  - Existing /admin/page.tsx has critical logic that can't be replaced with section overview — surface to Jake
  - Middleware semantics for /platform-admin/* are surprising (e.g., the existing isPlatformAdmin posture doesn't apply by default + can't be wired without architectural change) — log to Wave 1.1-Lite if minor; escalate to Jake if blocking
  - Hook rejection on any new file
  - Privacy gate fail

## Output

After completion, create `.planning/phases/stage-1.5c-information-architecture/01.5c-6-admin-platform-admin-SUMMARY.md` per `.claude/get-shit-done/templates/summary.md`. Document:
- Files created (13 admin + 1 platform-admin)
- Acceptance criteria status (9 items)
- Real-logic vs placeholder counts
- Middleware change taken (if any) for /platform-admin/* gating
- Backed-up existing /admin/page.tsx content if replaced
- Hook silence + build / privacy gate pass
