# 3.2 ONE QUEUE — the absorb (report)

**Session:** 2026-07-14 · branch `flat-ia-rework` → origin/main (ship-to-prod).
Base HEAD `e24cf19` (steps 1+2a banked at `f6db1a5`). Ceiling $150 / surface $120.

The PM Queue's approval machinery (Quick-Approve + multi-select + batch
approve/hold/deny) is now ONE shared hook + component set, consumed identically
by the retired PM Queue and the folded-in Bills list. The eligibility rules are
the tested pure module from step 2a (28/28), untouched.

---

## What shipped this session (step 2b → 5)

**New shared machinery (extracted, token-clean, byte-equivalent behavior):**
- `src/components/invoices/useInvoiceApproval.ts` — the hook. Owns selection
  (`selectedIds` + toggle/selectAll + indeterminate ref + clear-on-filter),
  quick-approve (confirm/processing/animate-out), batch approve (eligible/
  excluded split) / hold / deny, toast, `selectedTotalCents`. Takes data as
  PROPS (never fetches). Delegates eligibility to the tested module. Mutations
  hit the same endpoints (`POST /api/invoices/[id]/action`, `POST
  /api/invoices/batch-action`); the caller decides what to do with the mutated
  ids via `onMutated` (queue REMOVES; list advances status in place).
- `src/components/invoices/QuickApproveControl.tsx` — `variant="card"|"row"`
  (queue mobile card + desktop/list row), verbatim from queue 1091-1145 / 1321-1357.
- `src/components/invoices/BatchActionBar.tsx` — floating bar (queue 1368-1423).
- `src/components/invoices/BatchNoteModal.tsx` — unified hold/deny (queue 1434-1502).
- `src/components/invoices/BatchApproveConfirmModal.tsx` — eligible/excluded
  split (queue 1504-1583).
- `src/components/invoices/ApprovalToast.tsx` — toast (queue 1425-1432).

**Queue rewired in place (step 3a — byte-equivalent regression reference):**
`src/app/invoices/queue/page.tsx` — inline machinery (state 137-163, eligibility
+ handlers 306-390 / 585-732, and all approval JSX) swapped for the hook + 5
components. `onMutated` REMOVES mutated rows (identical to the pre-extraction
optimistic filter). tsc + build green; no dangling references.

**List absorb (step 3b):** `src/app/invoices/page.tsx` (= `/financials/bills`)
gains the checkbox column + select-all + Quick-Approve cell + floating batch bar
+ modals + toast, gated on `stageTab === "to_review" && reviewSub !== "qa"` and
per-row `status ∈ {pm_review, ai_processed}` (STATUS 3b: "quick/batch only on
pm_review/ai_processed"). `Invoice` interface extended with the step-1 fields
(`job_id`, `cost_code_id`, `po_id?`, `is_potential_duplicate?`,
`duplicate_dismissed_at?`, `line_item_summary?`). Workflow settings fetched
fire-and-forget (never blocks render; RB-default fallback). `onMutated` advances
status in place so approved rows move to the correct stage tab / count.

**Retire (step 4):** `/financials/bills/queue` re-export → `redirect(
"/financials/bills?review=pm")`; the list reads `?review=` to preset the sub-view.
`FinancialViewTabs` Queue tab removed (Invoices / QA kept). The standalone queue
component is now orphaned dead code (unreachable — next.config 308s
`/invoices/queue` here first), left for a later cleanup.

---

## PLAN-REVIEW GATE (step 1) — PASS

Reviewed the step-3 design against the actual 1587-line queue source, the
extracted module + 28 tests, both server gates (`batch-action` REVIEWABLE =
`{pm_review, ai_processed, pm_held}`; single `action`), the redirect map, and
the list. Coverage of the queue's approval behavior by the hook + 5 components +
module = **100%** (eligibility, quick-approve, multi-select, batch approve/hold/
deny, floating bar, toast, partial-failure, keyboard). No behavior lacked a
home → proceeded to build.

---

## PARITY CHECK (step 4) — every approval capability present or equivalent

Money-discipline non-negotiable: side-by-side, queue vs new Bills list.

| # | Capability | PM Queue | Bills list | Verdict |
|---|---|---|---|---|
| 1 | Eligibility compute (blockers / quick-eligible / threshold) | module | same module | ✅ identical |
| 2 | Quick-Approve single (inline confirm → optimistic → toast) | yes | yes (row variant) | ✅ equivalent |
| 3 | Multi-select (checkboxes + select-all + indeterminate) | yes | yes (checkbox col, actionable rows) | ✅ equivalent |
| 4 | Batch approve (eligible/excluded split modal) | yes | yes | ✅ identical component |
| 5 | Batch hold (note-required modal) | yes | yes | ✅ identical component |
| 6 | Batch deny (note-required modal) | yes | yes | ✅ identical component |
| 7 | Floating batch bar (count / total / clear / hold·deny·approve) | yes | yes | ✅ identical component |
| 8 | Toast feedback | yes | yes | ✅ identical component |
| 9 | Partial-failure (`result.success` drives removal; failures stay) | remove | status-advance in place | ✅ equivalent (list more correct — rows move to correct stage) |
| 10 | Keyboard (native focus + autoFocus on modals) | yes | yes (same primitives) | ✅ equivalent |
| 11 | `batch_approval_enabled` / `quick_approve_enabled` gate | yes | yes (+ To Review PM stage gate) | ✅ equivalent |
| 12 | Confidence / date / dup / budget / PO blockers | module | same module | ✅ identical |

**Verdict: PASS.** No approval capability is silently lost. Three deltas,
surfaced explicitly (none a functional regression):

- **D1 — mobile presentation.** Queue used per-card controls; the list has no
  mobile card layout (a wide horizontally-scrolling table). Quick-Approve +
  batch remain reachable on mobile via the table. Capability preserved,
  presentation differs; the queue's cards retire with the queue.
- **D2 — PM self-filter.** The queue auto-scoped a PM to their own jobs (client
  view convenience); the list is the org-wide view (with a manual PM filter
  dropdown). Server authz is org-scoped identically for both routes, and the
  list already permitted approving any org invoice via the detail page —
  Quick-Approve adds no new authz surface. Pre-existing list trait, not a
  regression introduced by the absorb.
- **D3 — batch-approve a `pm_held` item.** The queue could (pm_held is
  server-reviewable and was loaded under its Held/All filter). On the list,
  approval controls are scoped to To Review PM + `{pm_review, ai_processed}`
  per STATUS 3b, so held items (Needs Attention stage) get no batch affordance.
  The capability to approve a held invoice still exists via its detail-page
  Approve button (same endpoint). Intentional plan scope, surfaced.

---

## QA queue fold (step 5) — delta reported, left as-is

The list's To Review → "Accounting QA" sub-view (`reviewSub === "qa"`) is a
read-only filtered LIST of qa-stage invoices (`pm_approved`, `qa_review`). The
QA queue page (`/financials/bills/qa`) is accounting's ACTION surface
(qa_approve / kick_back — distinct from PM approve/hold/deny). Folding QA into
the list is NOT clean (the list has no QA action machinery). Per STATUS 5 ("fold
IF clean; else report the delta and leave it") the QA queue is **left intact**;
only the PM Queue is retired.

---

## VERIFY (step 6) — PASS (authed live walk + DB invariants)

Authed as Jake/OWNER against my own build on `:3100` (port 3000 was already in
use — left untouched per never-kill rule). Fixtures on Sample Client A (RB org),
announced + soft-deleted at close; RB org restored to its exact original set
(in_draw 3, qa_approved 2, qa_review 1). Full log in
`.planning/qa-runs/2026-07-14-3.2-one-queue-qa-report.md`.

- ✅ **Eligibility gates render** — checkbox column + select-all appear on To
  Review → PM Review; Quick-Approve buttons render for exactly the 2 eligible
  (Alpha, Bravo), NOT the blocked Charlie (accessibility-tree confirmed).
- ✅ **Eligible/excluded split** — "Approve All" → "2 of 3 can be batch
  approved", Total $21,250.00 (cent-exact), Charlie excluded with the exact
  blocker "No cost code assigned".
- ✅ **Batch succeeds** — "Approve 2 invoices" → both approved.
- ✅ **Counts deterministic** — PM Review 3 → 1 (only blocked Charlie remains);
  the 2 advanced status in place into the QA sub-view.
- ✅ **Invariants green** — Alpha + Bravo → `qa_review` with 2 status_history
  entries (pm_approved → qa_review) attributed to the real acting user; Charlie
  untouched (`pm_review`, 0 history — correctly excluded).
- ✅ **Retire redirect** — `/financials/bills/queue` → `/financials/bills?review=pm`,
  filtered PM Review list, nothing lost.
- ✅ **No console errors.**

Note on "flash fires": the approval feedback is the byte-equivalent toast
(`ApprovalToast`, same as the queue), which auto-dismisses at 3500ms; the
deterministic count change + DB invariants are the definitive success proof.
Quick-Approve single-click and batch hold/deny were verified by construction
(verbatim components + the shared hook/`onMutated`/toast pipeline exercised by
the walked batch-approve; hold/deny differ only in the target status string +
the note modal, itself a verbatim lift).

---

## Stage-4 readiness note

**3.2 ONE QUEUE is COMPLETE.** The invoice-approval surface is now singular: the
Bills list (`/financials/bills`) is the one place PMs review + Quick-Approve +
batch approve/hold/deny; the standalone PM Queue is retired (redirected). The
approval machinery is one tested module + one hook + five components, so any
future approval surface (or a QA-action absorb) extends the same pieces.

Stage-4 (invoices+draws deep-dive remaining kills) inherits a clean baseline:
- **No dual approval code paths** — eligibility + mutation live in
  `src/lib/invoices/approval-eligibility.ts` + `src/components/invoices/useInvoiceApproval.ts`.
- **Open follow-ups (non-blocking, for a later pass):**
  1. QA-action absorb — to fully retire `/financials/bills/qa`, the list's
     Accounting-QA sub-view would need qa_approve/kick_back batch actions
     (a `useInvoiceQaReview` sibling hook). Deferred (step-5 "not clean").
  2. Orphaned dead code — `src/app/invoices/queue/page.tsx` is now unreachable
     (config 308 + no importer); safe to delete in a cleanup commit.
  3. Route-namespace reconciliation (from 3.9) still open — Bills/Invoices +
     Pay-Apps/Draws label-vs-URL split; Jake decision pending.
- **next.config double-hop (cosmetic):** `/invoices/queue` → (308)
  `/financials/bills/queue` → (redirect) `/financials/bills?review=pm`. Harmless;
  could be collapsed to a single 308 in next.config later.
