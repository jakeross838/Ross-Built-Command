# 00100 (B-1a) Pre-flight Probe Results

Generated by `scripts/b1a-probe.mjs` at execute time (B-1a executor).
Run timestamp: 2026-05-14 (project clock).

## Method

REST + service-role direct fetch against production project
`egxkffodxcefwpqmwrur`. Probes (a)/(b)/(d) use REST count headers;
probe (c) emulates GROUP BY in JS over the row payload.

## Results

```json
{
  "probe_a_distinct_clients": {
    "distinct": 25,
    "raw_rows": 26
  },
  "probe_b_jobs_with_client_data": 26,
  "probe_c_ambiguous_backfill": {
    "ambiguous_count": 0,
    "samples": []
  },
  "probe_d_fixture_org_jobs": 10
}
```

## Interpretation

- **Probe (a) 25 distinct clients from 26 raw rows.** One pair of jobs
  shares the same `(org_id, lower(trim(client_name)), lower(trim(client_email)))`
  key — collapses to a single client row on backfill. Expected: live DB
  has Michael Harllee with 2 jobs (per database-reviewer's BLK-1 finding
  that informed iter-2 §2.1).

- **Probe (b) 26 jobs with client data.** Includes fixture-harness-org's
  10 jobs + Ross Built production jobs (Drummond + ~15 others).

- **Probe (c) 0 ambiguous rows.** No `(name, email)` group has > 1 phone
  variant. The §2.1 partial unique index + `ON CONFLICT DO NOTHING` will
  not encounter any ambiguous cases in current data. PASS.

- **Probe (d) 10 fixture-harness-org jobs.** Matches Wave-E baseline.
  PASS.

## Disposition

All 4 probes PASS. No HALT triggered. Safe to apply migration 00100.

Expected backfill outcome: 25 `clients` rows inserted across all orgs
(of which 10 are fixture-harness-org Smoke Client A..J), bringing
`jobs.client_id` mapping coverage to 26 jobs (every non-NULL
`jobs.client_name` resolves to a clients row).
