// src/app/pipeline/contracts/page.tsx
// Wave 4 placeholder per Stage 1.5c Plan 4 — owner + sub master agreements.

import NwPlaceholderCard from "@/components/nw/NwPlaceholderCard";

export default function ContractsPage() {
  return (
    <div className="px-6 py-8 max-w-[800px] mx-auto">
      <NwPlaceholderCard
        eyebrow="Pipeline · Contracts"
        headline="Owner contracts + sub master agreements"
        body="Owner contracts created from signed proposals; sub master agreements maintained per vendor. Tracks effective dates, riders, insurance certificates, and renewal cadence."
        wave="Wave 4"
      />
    </div>
  );
}
