---
phase: stage-1.5c-verification-harness
plan_id: 11
plan_name: self-test
type: execute
wave: 7
depends_on: [01, 02, 03, 04, 05, 06, 07, 08a, 08b, 09, 10]
autonomous: false
halt_after: true
files_modified:
  - .planning/phases/stage-1.5c-verification-harness/SELF-TEST-LOG.md
estimated_days: 0.5
plan_review_watchpoints: [1, 2, 3, 4, 5, 6, 7]
requirements: []
must_haves:
  truths:
    - "Harness runs against this very phase's Vercel preview URL via scripts/verify-phase.ts"
    - "All 3 layers behave correctly: Layer 1 builds + typechecks + hook-sweeps + DOM/route-status; Layer 2 loads conservation rule + SKIPs cleanly; Layer 3 returns empty (no visual/semantic criteria on main HEAD 7607bbf)"
    - "Idempotency invariant verified: rerun on same commit returns vision_cost_usd=0 (Plan-review watchpoint #7)"
    - "Local mode (--mode local) works against localhost:3000 if running"
    - "GitHub Actions workflow fires on push (this branch's most recent commit triggers verify-phase.yml — first real CI run)"
    - "PR comment posted (if PR exists for this branch)"
    - "Commit status set on most recent commit"
    - "Self-test log written documenting all 7 plan-review watchpoint verifications"
  artifacts:
    - path: ".planning/phases/stage-1.5c-verification-harness/SELF-TEST-LOG.md"
      provides: "Self-test execution log + per-watchpoint verification + final ship-readiness verdict"
      contains: "watchpoint"
  key_links: []
---

<objective>
Wave 7 — self-test against the harness itself before merging to main. Per Jake nwrp47 Part 7 step 11 + EXPANDED-SCOPE §2 prereq #9: a harness that does not pass its own three layers is not safe to deploy.

This plan does NOT add new code. It runs the harness, observes results, and writes a self-test log that explicitly verifies all 7 plan-review watchpoints from nwrp48. On any failure, halt for Jake.

All 7 plan-review watchpoints get verified end-to-end here:

1. Standards library schema designed for forward extensibility
2. Loop-with-executor state machine cleanly bounded
3. gsd-research-standards agent reusable across phases
4. Calibration-log integration with custodian produces useful lessons not noise
5. gsd-planner mandate update enforces criteria sections without becoming a friction tax
6. Vercel preview URL discovery robust
7. Idempotency contract precise

Output:
- 1 self-test log at `.planning/phases/stage-1.5c-verification-harness/SELF-TEST-LOG.md`
- HALT signal to Jake (Wave 7 complete; ready for merge to main)
</objective>

<execution_context>
@C:/Users/Jake/nightwork-platform/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jake/nightwork-platform/.claude/get-shit-done/templates/summary.md
</execution_context>

<canonical_refs>
@.planning/STATE.md
@.planning/phases/stage-1.5c-verification-harness/01.5-c-verification-harness-CONTEXT.md
@.planning/expansions/stage-1.5c-verification-harness-EXPANDED-SCOPE.md
@.planning/architecture/VERIFICATION-PIPELINE.md
@CLAUDE.md
@.planning/lessons.md

<!-- All Wave 0-6 outputs available -->
@scripts/verify-phase.ts
@scripts/loop-with-harness.ts
@src/lib/verification/orchestrator.ts
@src/lib/verification/state-machine.ts
@.github/workflows/verify-phase.yml
@.claude/agents/gsd-research-standards.md
@.claude/agents/gsd-fix-executor.md
@.planning/templates/criteria-template.md
@.planning/calibration-log-schema.md
@src/lib/verification/orchestrator.test.md
</canonical_refs>

<tasks>

<task type="auto">
  <name>Task 1: Self-test execution + log</name>
  <files>.planning/phases/stage-1.5c-verification-harness/SELF-TEST-LOG.md</files>

  <read_first>
    - src/lib/verification/orchestrator.test.md (test fixtures Plan 5 documented)
    - .planning/architecture/VERIFICATION-PIPELINE.md (operational reference)
    - .planning/phases/stage-1.5c-verification-harness/01.5-c-verification-harness-CONTEXT.md (D-22 idempotency invariant)
  </read_first>

  <action>
**Self-test execution:**

This task runs the harness end-to-end + records observations. Each step's outcome goes into `SELF-TEST-LOG.md`.

**Step A — Confirm prerequisites:**

```bash
# 1. Vercel preview deploy is Ready for this branch
git rev-parse --abbrev-ref HEAD  # should be phase/1.5-c-verification-harness
git rev-parse HEAD               # capture current commit SHA
vercel ls --prod=false | grep "phase/1.5-c-verification-harness" | head -1

# 2. ANTHROPIC_API_KEY available in env
test -n "$ANTHROPIC_API_KEY" && echo "ANTHROPIC_API_KEY present" || echo "MISSING — set before run"

# 3. Build clean
npm run build 2>&1 | tail -3

# 4. Typecheck clean
npx tsc --noEmit 2>&1 | tail -3
```

If any prerequisite fails: halt + surface to Jake.

**Step B — Run harness once (fresh):**

```bash
COMMIT_SHA=$(git rev-parse HEAD)
npx tsx scripts/verify-phase.ts \
  --phase stage-1.5c-verification-harness \
  --commit-sha "$COMMIT_SHA" \
  --mode ci \
  --cost-cap-usd 1.0 > self-test-run-1.json
```

Capture:
- Exit code
- `total_vision_cost_usd` from the JSON output
- Layer 1 results: pass/fail counts (build, typecheck, hooks, route-status, dom)
- Layer 2 results: pass/fail counts (should have 1 SKIP for conservation/money-line-items-sum)
- Layer 3 results: should be empty (no visual/semantic criteria on this branch's PLAN files for main HEAD 7607bbf — actually, this phase's own PLANs DO have visual/semantic criteria entries marked N/A, which the criteria-loader skips. Verify the count.)

**Step C — Run harness again (rerun, idempotency check — Plan-review watchpoint #7):**

```bash
npx tsx scripts/verify-phase.ts \
  --phase stage-1.5c-verification-harness \
  --commit-sha "$COMMIT_SHA" \
  --mode ci \
  --cost-cap-usd 1.0 > self-test-run-2.json
```

Capture:
- Exit code
- `total_vision_cost_usd` from second run JSON output

**Idempotency invariant:** `self-test-run-2 total_vision_cost_usd` should equal 0 (or very close — fixed at vision_cost_usd=0 for cached results). If non-zero, idempotency cache broken — fail self-test.

In practice, since this phase's PLANs have N/A for visual/semantic, both runs should have `total_vision_cost_usd: 0` (no Layer 3 calls made). The invariant still holds; document it.

**Step D — Test discovery failure path (Plan-review watchpoint #6):**

```bash
npx tsx scripts/verify-phase.ts \
  --phase stage-1.5c-verification-harness \
  --commit-sha "$COMMIT_SHA" \
  --preview-url https://does-not-exist-1234567890.invalid \
  --mode ci 2>&1 | tail -10
```

Expected: exit code 3 + actionable error message listing all 4 attempted discovery paths.

**Step E — Test loop wrapper (Plan-review watchpoint #2):**

```bash
npx tsx scripts/loop-with-harness.ts \
  --phase stage-1.5c-verification-harness \
  --commit-sha "$COMMIT_SHA" \
  --cost-cap-usd 1.0 > self-test-loop.json
```

Capture: outcome.final_state, outcome.total_iterations, halt_artifact_path (if any).

Expected: final_state="passed", total_iterations=1 (no failures to retry).

**Step F — Verify state machine boundedness (watchpoint #2):**

Manually trace through `src/lib/verification/state-machine.ts`:

- MAX_ITERATIONS=3 hard cap → grep confirms
- transition() is pure-function (no I/O, no side effects) → grep confirms (only takes ctx + event, returns new ctx)
- isHaltState() → exists and returns boolean

**Step G — Verify standards library forward-extensibility (watchpoint #1):**

Manually verify by inspecting:

- `.planning/verification/standards/_schema.json` — JSON Schema present + 5-domain enum + required fields
- `src/lib/verification/layer2/loader.ts` — generic glob loader (no hardcoded rule IDs)
- `src/lib/verification/registry.ts` — `registerVerifyFn`/`resolveVerifyFn` shape unchanged from Plan 1
- `src/lib/verification/layer2/index.ts` — side-effect imports list (currently 1 rule, future rules append)

Confirm: adding a hypothetical new rule = JSON file + TS module + 1-line import in index.ts. No changes to runner.ts or loader.ts.

**Step H — Verify gsd-research-standards reusability (watchpoint #3):**

Manually verify `.claude/agents/gsd-research-standards.md`:

- Caching strategy section: domain+subset+tenant key + TTL brackets (90/30/7 days)
- Search authority hierarchy section: NAHB / AIA / FL statutes / IRS / GAAP
- Output format section: cache reuse table + per-rule sections + JSON skeleton + verifyFn algorithm

**Step I — Verify calibration log usefulness (watchpoint #4):**

Manually verify `.planning/calibration-log-schema.md`:

- 13+ JSON front-matter fields
- 5 markdown body sections
- Reader rules (4 heuristic adjustments)
- Writer rules (extraction-from-artifacts, no-speculation)

**Step J — Verify criteria mandate friction-tax (watchpoint #5):**

Manually verify `.planning/templates/criteria-template.md`:

- Drop-in template + 5 categories
- Specificity rubric (vague vs specific table)
- Friction-tax check (~5 min/plan target)
- Worked-example references to this phase's PLANs 1-11

Calculate per-plan criteria authoring time on this phase (subjective): how long did it take to author the `<criteria>` block on each PLAN.md? If >5 min/plan average, flag as friction-tax warning.

**Step K — Run GitHub Actions workflow:**

The most recent push to phase/1.5-c-verification-harness branch should have triggered `.github/workflows/verify-phase.yml`. Verify:

```bash
gh run list --workflow=verify-phase.yml --branch phase/1.5-c-verification-harness --limit 1
```

Confirm: most recent run completed (success / failure / error). Read the run logs:

```bash
RUN_ID=$(gh run list --workflow=verify-phase.yml --branch phase/1.5-c-verification-harness --limit 1 --json databaseId --jq '.[0].databaseId')
gh run view "$RUN_ID"
```

Expected: workflow ran ubuntu-latest, installed Playwright, ran scripts/verify-phase.ts, posted PR comment (if PR exists), set commit status.

**Step L — Write SELF-TEST-LOG.md:**

```markdown
# Self-test log — stage-1.5c-verification-harness

**Date:** 2026-05-XX
**Branch:** `phase/1.5-c-verification-harness`
**Commit SHA:** `<sha>`
**Self-test status:** PASS | FAIL (per Jake nwrp47 Part 7 step 11)

## Prerequisites
- [x] Vercel preview deploy Ready: <preview-url>
- [x] ANTHROPIC_API_KEY in env
- [x] npm run build clean
- [x] npx tsc --noEmit clean

## Run 1 — fresh harness invocation

Command: `npx tsx scripts/verify-phase.ts --phase stage-1.5c-verification-harness --commit-sha <sha> --mode ci`

Exit code: <0 | 1 | 2 | 3 | 4>
Duration: <Xs>
Total vision cost: $<amount>

Layer 1 results:
- Build: <PASS|FAIL>
- Typecheck: <PASS|FAIL>
- Hook sweep: <PASS|FAIL>
- Route status: <N PASS / M FAIL out of total routes>
- DOM assertions: <empty (no DOM criteria on this phase) | N PASS / M FAIL>

Layer 2 results:
- conservation/money-line-items-sum: <SKIP — introspection HEAD probe non-2xx/3xx (expected on main HEAD 7607bbf)>

Layer 3 results:
- <empty (no visual/semantic criteria; all N/A) | N PASS / M FAIL / K AMBIGUOUS>

## Run 2 — rerun on same commit (Plan-review watchpoint #7)

Command: same as Run 1

Exit code: <should match Run 1>
Total vision cost: $<should equal 0; if Run 1 had vision calls, all should be cached>

**Idempotency invariant:** Run 2 vision cost = 0 → ✅ PASS | ❌ FAIL (cache broken)

## Run 3 — discovery failure path (Plan-review watchpoint #6)

Command: `npx tsx scripts/verify-phase.ts --phase ... --preview-url https://does-not-exist-...invalid`

Exit code: <should be 3>
stderr: <actionable error listing all 4 discovery paths attempted>

**Discovery hierarchy verified:** ✅ PASS | ❌ FAIL

## Loop wrapper test (Plan-review watchpoint #2)

Command: `npx tsx scripts/loop-with-harness.ts --phase stage-1.5c-verification-harness`

Outcome:
- final_state: <passed | failed-fixable | failed-ambiguous>
- total_iterations: <N>
- halt_artifact_path: <none | path>

**State machine bounded:** ✅ PASS (MAX_ITERATIONS=3 hard cap; pure-function transitions; isHaltState exists)

## Manual watchpoint verifications

### Watchpoint #1 — Standards library forward-extensibility
- _schema.json: 5-domain enum + required fields ✅
- loader.ts: generic glob (no hardcoded rule IDs) ✅
- registry.ts: registerVerifyFn / resolveVerifyFn unchanged ✅
- layer2/index.ts: side-effect imports list (1 rule currently; appends are 1 line) ✅

### Watchpoint #3 — gsd-research-standards reusability
- gsd-research-standards.md: caching (domain+subset+tenant + TTL) + search authority hierarchy (NAHB/AIA/FL/IRS/GAAP) + output format ✅

### Watchpoint #4 — Calibration log useful not noise
- calibration-log-schema.md: 13+ JSON fields + 5 body sections + reader rules + writer rules ✅
- nightwork-custodian.md: extraction rules table + don't list + self-check anchor ✅

### Watchpoint #5 — Criteria mandate friction-tax
- criteria-template.md: drop-in + rubric + ~5 min/plan target ✅
- gsd-planner.md: criteria mandate section + template reference ✅
- nightwork-plan-review: enforcement check + 1.5c IA exemption + friction-tax meta-finding ✅
- Per-plan authoring time on this phase: ~3-5 min average → within target ✅

### Watchpoint #6 — Vercel discovery robust
- vercel-discovery.ts: 4-tier hierarchy + fail-loudly with actionable error ✅
- Run 3 above empirically verified discovery failure path ✅

### Watchpoint #7 — Idempotency contract precise
- idempotency.ts: deriveIdempotencyKey + canonicalCriterionHash + cacheReadJson + cacheWriteJson ✅
- Run 2 above empirically verified rerun-no-op (vision cost = 0) ✅

## GitHub Actions workflow

Run ID: <run-id>
URL: <run-url>
Status: <success | failure | error>
Duration: <Xs>
PR comment posted: <yes | no | N/A (no PR yet)>
Commit status set: <success | failure | error>

**Workflow integrity:** ✅ PASS

## Final verdict

All 7 plan-review watchpoints verified: ✅ PASS / ❌ FAIL <with details>

Self-test PASS criteria (per nwrp47 Part 7 step 11):
- [ ] Harness runs end-to-end against Vercel preview URL
- [ ] All 3 layers behave correctly
- [ ] Idempotency invariant holds
- [ ] Discovery failure path returns actionable error
- [ ] Loop state machine bounded (MAX_ITERATIONS=3)
- [ ] All 7 watchpoints individually verified
- [ ] GitHub Actions workflow runs without error

If all checked: SHIP READY → Jake merges this branch to main.

If any unchecked: HALT for Jake review. Document failure(s) above. /np or manual fix as Jake directs.
```

**Step M — Halt for Jake:**

After Task 1 commits, Plan 11 is complete. This is the FINAL halt before merge to main. Surface to Jake:

> Wave 7 (self-test) complete. SELF-TEST-LOG.md written with all 7 plan-review watchpoint verifications.
>
> Self-test verdict: <PASS|FAIL>
>
> If PASS: ready to merge `phase/1.5-c-verification-harness` → main. Per Jake nwrp47 Part 7 sequence:
> 1. Merge to main (squash or merge-commit per Jake preference)
> 2. 1.5c IA branch rebases / merges main (~1 hour)
> 3. 1.5c IA criteria retrofit (~1 hour, per Q6=B)
> 4. Plan 3a-now spawns through harness (first real production use)
> 5. Wave 3 (Plans 4+5) ships through harness
> 6. 1.5c ships
>
> If FAIL: halt for Jake review. Document failures in self-test log; /np or manual fix as Jake directs.
>
> Halt for Jake review per D-25 + Plan 11 acceptance criterion.

**STANDING RULES:**
- Process discipline (D-23): the self-test runs the harness; the harness has its own timeouts; we don't kill processes manually.
- Per D-29: this self-test does NOT modify _fixtures/drummond/, sanitize-drummond.ts, design-system playground, or 1.5c IA Plans 1/2/2-amend/3 commits. It runs the harness READING those resources, never modifying.

**COMMIT:**
```
docs(stage-1.5c-vh): Plan 11 — self-test execution + log + Wave 7 halt

Ran harness end-to-end against this phase's Vercel preview URL. Verified
all 7 plan-review watchpoints from nwrp48:

- #1: Standards library forward-extensibility (loader/registry/schema unchanged)
- #2: Loop state machine bounded (MAX_ITERATIONS=3, pure transitions)
- #3: gsd-research-standards reusable (caching + TTL + authority hierarchy)
- #4: Calibration log useful (13+ fields + writer rules + don't-fabricate)
- #5: Criteria mandate friction-tax (~5min/plan target met on this phase)
- #6: Vercel discovery robust (4-tier hierarchy + fail-loudly empirically tested)
- #7: Idempotency contract precise (rerun-no-op empirically tested)

GitHub Actions workflow ran on most recent push (first real CI run on main
from this branch).

Self-test verdict: <PASS|FAIL>. If PASS, ready to merge to main.

Wave 7 complete. Halt for Jake (final pre-merge halt) per D-25.
```
  </action>

  <verify>
    <automated>
test -f .planning/phases/stage-1.5c-verification-harness/SELF-TEST-LOG.md &&
grep -q "Plan-review watchpoint #1\|Watchpoint #1" .planning/phases/stage-1.5c-verification-harness/SELF-TEST-LOG.md &&
grep -q "Watchpoint #7\|Idempotency invariant" .planning/phases/stage-1.5c-verification-harness/SELF-TEST-LOG.md &&
grep -q "Final verdict\|Self-test PASS\|SHIP READY\|HALT for Jake" .planning/phases/stage-1.5c-verification-harness/SELF-TEST-LOG.md
    </automated>
  </verify>

  <done>
    - SELF-TEST-LOG.md exists with all 7 watchpoint verifications + final verdict
    - Halt signal surfaced for Jake (final pre-merge halt)
    - Either: PASS verdict + "ship ready" surfaced
    - Or: FAIL verdict + halt for Jake to direct next step
  </done>
</task>

</tasks>

<acceptance_criteria>

- [ ] **AC-1.5c-vh-11-1:** Harness runs end-to-end against this phase's Vercel preview URL via scripts/verify-phase.ts
- [ ] **AC-1.5c-vh-11-2:** Layer 1 reports per-route HTTP status for all auto-discovered routes; build clean; typecheck clean; hook sweep silent
- [ ] **AC-1.5c-vh-11-3:** Layer 2 loads conservation/money-line-items-sum rule + returns SKIP cleanly (no /api/_introspect/* on main HEAD 7607bbf)
- [ ] **AC-1.5c-vh-11-4:** Layer 3 returns empty (no visual/semantic criteria on this phase's PLANs that aren't N/A)
- [ ] **AC-1.5c-vh-11-5:** Idempotency invariant verified: Run 2 (rerun on same commit) total vision cost = 0 (Plan-review watchpoint #7)
- [ ] **AC-1.5c-vh-11-6:** Discovery failure path tested: invalid preview URL → exit 3 + actionable error message (Plan-review watchpoint #6)
- [ ] **AC-1.5c-vh-11-7:** Loop wrapper test passes: scripts/loop-with-harness.ts → outcome.final_state="passed", total_iterations=1
- [ ] **AC-1.5c-vh-11-8:** State machine bounded: MAX_ITERATIONS=3, pure-function transitions, isHaltState exists (Plan-review watchpoint #2)
- [ ] **AC-1.5c-vh-11-9:** Standards library forward-extensibility verified: schema/loader/registry unchanged from Plan 1 (Plan-review watchpoint #1)
- [ ] **AC-1.5c-vh-11-10:** gsd-research-standards reusability verified: caching + TTL + authority hierarchy documented (Plan-review watchpoint #3)
- [ ] **AC-1.5c-vh-11-11:** Calibration log useful-not-noise verified: 13+ fields + writer rules + don't-fabricate (Plan-review watchpoint #4)
- [ ] **AC-1.5c-vh-11-12:** Criteria mandate friction-tax verified: ~5 min/plan target met on this phase (Plan-review watchpoint #5)
- [ ] **AC-1.5c-vh-11-13:** GitHub Actions workflow ran on most recent push: first real CI run on main from this branch
- [ ] **AC-1.5c-vh-11-14:** SELF-TEST-LOG.md written with all observations + per-watchpoint verification + final verdict
- [ ] **AC-1.5c-vh-11-15:** Wave 7 halt signal surfaced for Jake review (final pre-merge halt)

</acceptance_criteria>

<criteria>

```yaml
criteria:
  mechanical:
    - "npm run build clean (0 warnings, 0 errors)"
    - "npx tsc --noEmit returns 0 errors in src/"
    - "All hooks silent on every commit"
    - "Privacy gate clean across SELF-TEST-LOG.md"
    - "Drummond grep gate silent (system .githooks/pre-commit)"
  dom:
    - "N/A — Layer 1 dom assertions tested by self-test execution but no NEW dom criteria authored in this plan"
  visual:
    - "N/A — no UI surfaces in this plan (self-test execution + log only)"
  behavioral:
    - "scripts/verify-phase.ts run against this phase's Vercel preview URL completes with exit code 0 (passed) — confirms end-to-end harness behavior"
    - "scripts/verify-phase.ts second run on same commit_sha returns total_vision_cost_usd=0 — confirms idempotency invariant (D-22 / watchpoint #7)"
    - "scripts/verify-phase.ts with invalid --preview-url exits with code 3 and stderr lists all 4 discovery paths attempted — confirms watchpoint #6"
    - "scripts/loop-with-harness.ts run against this phase returns outcome.final_state='passed' AND total_iterations=1 (no failures to retry)"
    - "GitHub Actions workflow ran on most recent push to this branch (first real CI run on main)"
    - "SELF-TEST-LOG.md final verdict matches actual run outcomes (PASS or FAIL with explicit failure attribution)"
  semantic:
    - "Self-test design: covers all 7 plan-review watchpoints individually + end-to-end harness behavior + GH Actions integration — no watchpoint left unverified before merge to main"
    - "Halt-for-Jake design: final halt before merge gives Jake explicit go/no-go decision; PASS verdict + ship-ready summary surfaces; FAIL verdict + failure attribution surfaces"
```

</criteria>

<halt_conditions>

- **HARD HALT after Task 1 commits.** Wave 7 complete. This is the FINAL halt before merge to main.
- Surface to Jake: SELF-TEST-LOG.md verdict (PASS or FAIL).
- If PASS: ready to merge `phase/1.5-c-verification-harness` to main per Jake nwrp47 Part 7 sequence.
- If FAIL: halt for Jake to direct next step (manual fix / /np re-spawn / scope adjustment).
- Soft halt if: harness can't run (Vercel preview not Ready, ANTHROPIC_API_KEY missing, build/typecheck broken).

</halt_conditions>

<canonical_refs>

- `.planning/phases/stage-1.5c-verification-harness/01.5-c-verification-harness-CONTEXT.md` (D-22 idempotency invariant; all 7 watchpoints)
- `.planning/expansions/stage-1.5c-verification-harness-EXPANDED-SCOPE.md` §2 prereq #9 (self-test mandate); §8.5 Approved confirmations (all 7 watchpoints)
- `.planning/architecture/VERIFICATION-PIPELINE.md` (operational reference)
- `src/lib/verification/orchestrator.test.md` (Plan 5 test fixtures referenced)
- All Wave 0-6 outputs (this plan exercises them end-to-end)

</canonical_refs>
