---
phase: stage-1.5c-verification-harness
plan_id: 10
plan_name: documentation
type: execute
wave: 6
depends_on: [01, 02, 03, 04, 05, 06, 07, 08a, 08b, 09]
autonomous: false
halt_after: true
files_modified:
  - .planning/architecture/VERIFICATION-PIPELINE.md
  - .planning/MASTER-PLAN.md
  - .planning/design/PHILOSOPHY.md
  - CLAUDE.md
  - .planning/phases/stage-1.5c-verification-harness/01.5-c-verification-harness-CONTEXT.md  # cross-ref additions
  - .planning/phases/stage-1.5c-verification-harness/deferred-items.md  # NEW (iter-1 amendment) — Wave-1.1+ deferrals captured
estimated_days: 0.5
plan_review_watchpoints: []
requirements: []
must_haves:
  truths:
    - ".planning/architecture/VERIFICATION-PIPELINE.md is the canonical doc for the harness (NEW)"
    - "MASTER-PLAN.md DECISIONS LOG entries D-073..D-077 added per Jake nwrp48 item 10"
    - "PHILOSOPHY.md updated with verification-first development pattern note"
    - "CLAUDE.md Testing Rule updated per Q2=C deprecation-path (hybrid posture during weeks 1-3; re-eval at end of Wave 3)"
    - "CONTEXT.md cross-refs added (links to VERIFICATION-PIPELINE.md, calibration-log-schema.md, etc.)"
  artifacts:
    - path: ".planning/architecture/VERIFICATION-PIPELINE.md"
      provides: "Canonical doc — pipeline overview, layer responsibilities, criteria mandate, idempotency, loop, costs, agents"
      contains: "## Layer 1\\|## Layer 2\\|## Layer 3"
    - path: ".planning/MASTER-PLAN.md"
      provides: "Updated DECISIONS LOG with D-073..D-077"
      contains: "D-073\\|D-074\\|D-075"
    - path: ".planning/design/PHILOSOPHY.md"
      provides: "Verification-first development pattern note"
      contains: "verification"
    - path: "CLAUDE.md"
      provides: "Updated Testing Rule per Q2=C deprecation-path"
      contains: "harness will replace localhost\\|verification-eligible"
  key_links:
    - from: ".planning/architecture/VERIFICATION-PIPELINE.md"
      to: "All Wave 0-5 outputs (types, layers, orchestrator, loop, agents, criteria template, calibration log)"
      via: "documentation links"
      pattern: "src/lib/verification\\|scripts/verify-phase"
    - from: ".planning/MASTER-PLAN.md"
      to: ".planning/architecture/VERIFICATION-PIPELINE.md"
      via: "DECISIONS LOG entry citation"
      pattern: "VERIFICATION-PIPELINE"
---

<!-- ITER-1 PLAN-REVIEW AMENDMENT (2026-05-06)
Per Jake nwrp49: applies these surgical amendments to address iter-1 plan-review:
- C5 (multi-tenant) + WARNING-3 (compliance): VERIFICATION-PIPELINE.md adds report-sanitization-rules section + tenant-safety boundary section + D-30 cross-ref.
- WARNING-2 (compliance): Anthropic-as-data-processor disclosure / DPA section in VERIFICATION-PIPELINE.md.
- WARNING-4 (compliance): Data retention table in VERIFICATION-PIPELINE.md.
- WARNING-5 (compliance): SOC2 control numbers (CC6.1, CC6.7, CC7.2, PI1.1) cited.
- WARNING-1 (compliance): API key redaction documented (overlap with Plan 4 + Plan 5 inline; this docs-pass references both).
- W4 (enterprise-readiness): final report overwrite-on-rerun versioning (final-<run-iso-date>.md symlink/alias).
- W6 (enterprise-readiness): Vercel ls stdout grep fragility — recommend `vercel inspect <branch> --json` as future addition.
- D-30 reference: VERIFICATION-PIPELINE.md cross-references CONTEXT.md D-30 + PHILOSOPHY.md §6.9.
- iter-1 amendment summary: VERIFICATION-PIPELINE.md adds final 'Iter-1 amendments anchored here' section enumerating all CRIT/HIGH/WARN coverage.
-->

<objective>
Wave 6 (parallel with Plan 9) — documentation. Per nwrp48 item 10 + Q2=C: VERIFICATION-PIPELINE.md as canonical doc + MASTER-PLAN D-073..D-077 entries + PHILOSOPHY addition + CLAUDE.md Testing Rule update.

This plan consumes outputs from all prior plans (1-9) — it documents what was built. No new code; only documentation.

Output:
- 1 new canonical doc at `.planning/architecture/VERIFICATION-PIPELINE.md`
- Updated MASTER-PLAN.md (DECISIONS LOG + maybe NEXT PLANNED)
- Updated PHILOSOPHY.md (verification-first note)
- Updated CLAUDE.md (Testing Rule per Q2=C)
- Updated phase CONTEXT.md (cross-refs to forward documents)
- HALT signal to Jake (Wave 6 complete)
</objective>

<execution_context>
@C:/Users/Jake/nightwork-platform/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jake/nightwork-platform/.claude/get-shit-done/templates/summary.md
</execution_context>

<canonical_refs>
@.planning/STATE.md
@.planning/MASTER-PLAN.md
@.planning/phases/stage-1.5c-verification-harness/01.5-c-verification-harness-CONTEXT.md
@.planning/expansions/stage-1.5c-verification-harness-EXPANDED-SCOPE.md
@.planning/design/PHILOSOPHY.md
@CLAUDE.md
@.planning/architecture/VISION.md

<!-- All Wave 0-5 outputs being documented -->
@src/lib/verification/types.ts
@src/lib/verification/orchestrator.ts
@scripts/verify-phase.ts
@scripts/loop-with-harness.ts
@.github/workflows/verify-phase.yml
@.claude/agents/gsd-research-standards.md
@.claude/agents/gsd-fix-executor.md
@.planning/templates/criteria-template.md
@.planning/calibration-log-schema.md
</canonical_refs>

<tasks>

<task type="auto">
  <name>Task 1: VERIFICATION-PIPELINE.md canonical doc</name>
  <files>.planning/architecture/VERIFICATION-PIPELINE.md</files>

  <read_first>
    - .planning/architecture/WORKFLOW-INTELLIGENCE.md (format precedent — 39-pattern catalogue)
    - .planning/architecture/VISION.md (cross-doc reference style)
    - All .planning/phases/stage-1.5c-verification-harness/*-PLAN.md (sources for what to document)
  </read_first>

  <action>
**`.planning/architecture/VERIFICATION-PIPELINE.md`:**

```markdown
# Verification pipeline

**Status:** SHIPPED (stage-1.5c-verification-harness, 2026-05-XX). Canonical doc.
**Last updated:** Plan 10 of stage-1.5c-verification-harness.

This document is the single source of truth for the 3-layer verification pipeline that catches per-commit bugs against Vercel preview URLs. It supersedes ad-hoc verification descriptions in earlier docs.

## Why this exists

Per Jake nwrp47 + nwrp48: prior verification was Jake-on-localhost-after-print-and-paste. Bugs slipped through three rounds of review (e.g., the cost-code-missing-on-each-line-item issue in 1.5c IA). The harness catches:

- Static check failures (build, typecheck, hooks, privacy) — Layer 1
- Industry-standards violations (money conservation, AIA G702/G703, accounting identities, lien law, dates) — Layer 2
- Visual / semantic drift from PLAN-file criteria — Layer 3 (Claude vision)

Per-commit, on Vercel preview URLs, in CI via GitHub Actions. Loop-with-executor (max 3 iter) auto-fixes mechanical failures; halts for Jake review on ambiguous (confidence < 0.7) or iter-3-reached failures.

## Pipeline overview

```
Push to phase/* branch
  ↓
Vercel preview deploy (auto)
  ↓
GitHub Action: verify-phase.yml
  ↓
scripts/verify-phase.ts → src/lib/verification/orchestrator.ts → runHarness
  ↓
  Layer 1: build/typecheck/hooks/privacy + DOM (3 viewports) + route-status
  ↓
  Layer 2: industry-standards rules (loaded from .planning/verification/standards/<domain>/*.json)
  ↓
  Layer 3: Claude vision against PLAN-file criteria (visual + semantic)
  ↓
  Report: per-commit (gitignored) + final (git-tracked)
  ↓
Inside /gsd-execute-phase: scripts/loop-with-harness.ts wraps with state machine
  ↓
  passed → continue
  failed-fixable → spawn gsd-fix-executor → iter+1
  failed-ambiguous → halt for Jake (HALT-AMBIGUOUS-VISION.md or HALT-ITER-3.md artifact)
  ↓
After execute completes → /nightwork-qa (unchanged)
```

## Layer 1 — static / mechanical / DOM

**Source:** `src/lib/verification/layer1/` (Plan 2 of stage-1.5c-verification-harness)

**What it checks:**
- `mechanical` category criteria: `npm run build` clean, `npx tsc --noEmit` clean, system pre-commit hook silent (Drummond grep)
- `dom` category criteria: Playwright chromium across 3 viewports (1920×1080 / 1280×800 / 393×852); selector + text assertions
- Auto-discovered route status (every `src/app/**/page.tsx` route returns 2xx/3xx)

**Convention v1 for `dom` criteria:**
```yaml
criteria:
  dom:
    - 'Page /financials/bills/[id] contains element "th[scope=col]:has-text(\"Cost Code\")"'
    - 'Page /dashboard contains text "Recent invoices"'
```

**Loop semantics:** Layer 1 FAIL → state machine `failed-fixable` → spawn `gsd-fix-executor`. Most Layer 1 failures are file:line errors with clear remediation.

**Cost:** Free (no API calls).

**Forward-extensibility:** Per D-02. Add a new mechanical check = wrap a CLI command in `runLayer1`. Add a new DOM convention = parser branch in `dom-assertions.ts`.

## Layer 2 — industry-standards

**Source:** `src/lib/verification/layer2/` (Plan 3) + `.planning/verification/standards/<domain>/*.json`

**Domains scaffolded:**
- `aia` — AIA G702/G703 standards (e.g., row-7 identity)
- `accounting` — debits/credits, retainage formulas, GC fee math
- `lien-law` — Florida 4-statute waiver structure (713.20(2)(a-d))
- `dates` — payment schedule cutoffs, business-day calc
- `conservation` — money sums, state machine completeness

**Currently shipped:**
- `conservation/money-line-items-sum` — invoice/draw/CO line items sum to total within 1 cent. Phase placeholder; SKIPs cleanly until `/api/_introspect/*` ships in Wave 1.1+.

**Forward-extensibility (per D-02 + Plan-review watchpoint #1):** Adding a Layer 2 rule:

1. Drop `<rule-id>.json` matching `_schema.json` at `.planning/verification/standards/<domain>/<rule-id>.json`.
2. Drop `<rule-id>.ts` at `src/lib/verification/layer2/standards/<domain>/<rule-id>.ts` calling `registerVerifyFn(...)`.
3. Append `import "./standards/<domain>/<rule-id>"` to `src/lib/verification/layer2/index.ts`.

That's it. No framework code changes per rule. `gsd-research-standards` agent (Plan 7) automates research of new rules at /np time.

**Convention for `behavioral` criteria:**
```yaml
criteria:
  behavioral:
    - "Sum of invoice.line_items[*].amount equals invoice.total_amount within 1 cent"
    - "Approval transitions invoice.status from pm_review to pm_approved"
```

**Loop semantics:** Layer 2 FAIL on `severity: "blocking"` rule → state machine `failed-fixable`. `severity: "warning"` rules log but don't halt loop.

**Cost:** Free (no API calls; standards are local checks).

## Layer 3 — Claude vision

**Source:** `src/lib/verification/layer3/` (Plan 4)

**What it checks:**
- `visual` category criteria: design system tokens (Slate Set B), typography (Space Grotesk / Inter / JetBrains Mono), layout (right-rail, audit timeline)
- `semantic` category criteria: meaning-level checks not catchable by DOM selectors (e.g., "cost code is visible on each invoice line item")

**Page URL routing convention:**
```yaml
criteria:
  visual:
    - "Page /financials/invoices/[id]: Site Office signature visible (tracking-eyebrow 0.18em UPPERCASE eyebrows)"
  semantic:
    - "Page /financials/invoices/[id]: cost code is visible on each line item (no blank cells)"
```

**Confidence-based halt (per D-07):** If vision returns confidence < 0.7 EVEN ON PASS verdict, the loop halts for Jake review. Reasoning: model said "I think yes but I'm not sure" — a human should look. Implementation: `state-machine.ts` transitions to `failed-ambiguous` on any Layer 3 result with confidence < 0.7.

**Idempotency contract (per D-22 + Plan-review watchpoint #7):** `idempotency_key = sha256(commit_sha + canonical_criterion_hash)`. Cached at `.planning/verification/runs/<phase>/<commit>/vision-<key-prefix>.json` (gitignored). Rerun on same commit = cache hit, vision_cost_usd=0. Plan 11 self-tests this invariant.

**Cost cap (per D-22):** Default $1 per harness run. CostCap class enforces; calls beyond cap return SKIP. Org-monthly cap deferred to F3.

**Cost guidance:**
- ~$0.05 per call (Claude 3.5 Sonnet vision)
- 20 criteria × 1 commit ≈ $1; idempotency caps reruns at ~$0
- Calibration log (Plan 9) tracks actual `vision_cost_usd` per phase

## State machine (per D-05/D-06/D-07)

```
idle → running-layer-1 → running-layer-2 → running-layer-3 → passed | failed-fixable | failed-ambiguous
```

**Transitions:**
- L1/L2 FAIL → failed-fixable (unless iter-3, then failed-ambiguous)
- L3 FAIL with confidence ≥ 0.7 → failed-fixable (unless iter-3)
- L3 result with confidence < 0.7 (ANY verdict) → failed-ambiguous
- All layers PASS → passed

**Iter-N loop (per D-06):** MAX_ITERATIONS = 3. ADDITIVE within current execute iteration. Does NOT consume halt-after budget. The harness loop is an inner loop; halt-after gates are outer-loop.

**Halt-for-Jake artifacts:** `HALT-AMBIGUOUS-VISION.md` or `HALT-ITER-3.md` written to `.planning/verification/runs/<phase>/<commit>/`. /gsd-execute-phase orchestrator detects + pauses with explicit Jake prompt.

## Criteria mandate (per D-17/D-18)

Every PLAN.md from stage-1.5c-verification-harness onward includes a `<criteria>` yaml block:

```yaml
criteria:
  mechanical: [...]
  dom: [...]
  visual: [...]
  behavioral: [...]
  semantic: [...]
```

**Template:** `.planning/templates/criteria-template.md`. **Enforcement:** `nightwork-plan-review` flags missing/vague criteria as REVISE. **Consumer:** harness Layer 1/2/3 + `nightwork-spec-checker` (per D-19).

**1.5c IA exemption (per Q6=B):** Plans 1/2/2-amend/3 on `phase/1.5-c-information-architecture` branch are exempt; criteria retrofit is a separate ~1-hour follow-up after harness merges to main.

**Friction-tax watchpoint (per D-18 / Plan-review watchpoint #5):** ~5 min/plan target. >20 criteria = plan should split. >50% PLAN_VAGUE_CRITERIA flags = template needs more default phrasings (extend, don't fail).

## Vercel preview URL discovery (per D-20/D-21 / watchpoint #6)

4-tier hierarchy in `src/lib/verification/vercel-discovery.ts`:

1. Explicit `--preview-url` arg
2. `vercel ls` CLI primary
3. Vercel REST API + `VERCEL_TOKEN` fallback
4. Deterministic pattern (`nightwork-platform-git-<slug>-jakeross838s-projects.vercel.app`) — unreliable for long branch names per AUTO-LOG empirical evidence
5. All paths failed → throw with actionable error

Wait-for-Ready: HEAD probe with exponential backoff up to 5 min cap.

## Calibration log (per D-13/D-14)

`.planning/calibration-log.md` is append-only; each phase gets one entry written by `nightwork-custodian` at post-ship sweep. JSON-front-matter (machine-readable) + markdown body (human-readable). Schema at `.planning/calibration-log-schema.md`.

`/np` orchestrator reads recent 3-5 entries when scoping next phase; adjusts parallelism / prompt density / iter budget per heuristics in schema reader rules.

## Agent coordination

**`gsd-research-standards`** (Plan 7) — runs at `/np` time BEFORE `gsd-planner`, BESIDE `gsd-phase-researcher`. Produces `.planning/research/<phase>-standards.md` with proposed Layer 2 rules. Caches by domain+subset+tenant with TTL.

**`gsd-fix-executor`** (Plan 7) — frontmatter-branched from `gsd-executor`. Receives ONE failure context, makes ONE focused commit, returns. Spawned by `/gsd-execute-phase` on failed-fixable. Never invokes the harness itself.

## CI integration

`.github/workflows/verify-phase.yml` (Plan 6) — first GH Action on main from this branch. Triggers on push to `phase/*` + workflow_dispatch. Posts PR comment + sets commit status. Concurrency: one run per branch.

## Reports

- **Per-commit:** `.planning/verification/runs/<phase>/<commit>/report.md` (gitignored per D-16)
- **Final:** `.planning/verification/reports/<phase>/final.md` (git-tracked per D-15)
- **Halt artifacts:** `.planning/verification/runs/<phase>/<commit>/HALT-*.md`

## Sequencing relative to /nightwork-qa

Per D-05: harness loop runs INSIDE /gsd-execute-phase BEFORE /nightwork-qa. /nightwork-qa continues unchanged as the final post-execute gate. Harness catches per-commit issues; /nightwork-qa catches phase-level integrity issues.

## Boundary vs existing agents

Per Q4=C:
- **`nightwork-smoke-tester`** (existing) — REPLACED. Mechanical-health-check logic absorbed into Layer 1 (Plan 2). Agent file deprecated.
- **`nightwork-end-to-end-test`** (existing) — KEPT. Workflow integrity walk through Drummond is complementary to harness per-route verification. Future date may retrofit to use harness vision-checks.

## Plan-review watchpoints (canonical anchors)

| # | Watchpoint | Anchor |
|---|---|---|
| 1 | Standards library schema designed for forward extensibility | Plan 1 (registry contract) + Plan 3 (schema + first rule) |
| 2 | Loop-with-executor state machine cleanly bounded | Plan 1 (state machine spec) + Plan 8b (implementation) |
| 3 | gsd-research-standards agent reusable across phases | Plan 7 (caching, TTL, search authority hierarchy) |
| 4 | Calibration-log integration with custodian produces useful lessons not noise | Plan 9 (entry schema + custodian writer) |
| 5 | gsd-planner mandate update enforces criteria sections without becoming a friction tax | Plan 8a (template + enforcement) |
| 6 | Vercel preview URL discovery robust | Plan 5 (4-tier discovery hierarchy) |
| 7 | Idempotency contract precise | Plan 1 (contract spec) + Plan 4 (cache implementation) + Plan 11 (rerun-no-op self-test) |

## Operational reference

**Run harness locally:**
```bash
npx tsx scripts/verify-phase.ts --phase stage-X-Y --preview-url https://...
```

**Run loop (with state machine + halt artifacts):**
```bash
npx tsx scripts/loop-with-harness.ts --phase stage-X-Y
```

**Run via `/nx`:** auto-invokes loop inside `/gsd-execute-phase` after each commit.

**Run via GitHub Actions:** push to `phase/*` → workflow fires automatically. Or `gh workflow run verify-phase.yml -f phase=stage-X-Y`.

**Skip layers (debugging):**
```bash
npx tsx scripts/verify-phase.ts --phase X --skip-layer 3
```

**Local mode (per D-04):**
```bash
npx tsx scripts/verify-phase.ts --phase X --mode local --base-url http://localhost:3000
```

## Extension points (forward work)

- **Layer 2 rule additions:** F1 + Wave 1.1+ phases via `gsd-research-standards` (NAHB / AIA / FL statutes / IRS / GAAP authority hierarchy)
- **Mobile Safari support:** `--browser` flag designed in (Plan 4 Layer 3); WebKit implementation deferred to Wave 1.1 (mobile-PM workflow)
- **Cross-phase research caching:** single-phase TTL this phase; cross-phase cache deferred (per Dependent-Soon §3 #4)
- **Org-monthly Anthropic cost cap:** per-run cap implemented; org-monthly deferred to F3
- **Auto-spawn `gsd-fix-executor` mode:** loop has the knob (`auto_spawn_fix_executor`); full implementation deferred to Wave 1.1+ for fully-autonomous CI runs

## Out-of-scope (not part of this pipeline)

- `/nightwork-end-to-end-test` workflow integrity walk — separate concern (per Q4=C)
- F3 narrowed-scope CI test gate (`npm test` runner + typecheck integration) — separate phase
- 1.5c IA criteria retrofit — separate ~1-hour follow-up post-harness-merge

## Tenant safety boundary (per stage-1.5c-verification-harness D-30)

**Per D-30 — verbatim:** "Verification harness operates on fixture-org-scoped data only. Real-tenant data is unreachable BY CONSTRUCTION at every layer (auth strategy, screenshot capture, criteria loading, vision API calls, report generation, idempotency cache, fix executor commits). No exception path exists for 'just this once' platform-admin verification — if a future need arises, it requires explicit D-30 amendment, not a code workaround."

**Implementation map (8 layers):**

| Layer | Tenant-safety mechanism |
|---|---|
| Auth strategy (Plan 5) | `auth-strategy.ts` `createHarnessSession` authenticates as `harness-fixture@nightwork.local` with membership in single fixture org `FIXTURE_ORG_ID`. RLS unchanged but session's org_id claim resolves only to fixture data. |
| Screenshot capture (Plan 4) | Path includes `org_id`: `runs/<phase>/<commit>/screenshots/<org_id>/<route>.png`. Per-org subdir means cross-tenant screenshots never co-mingled on disk. |
| Criteria loading (Plan 5) | `sanitizeCriterionText` blocklist (200-char cap; rejects "ignore prior", "system:", "override", JSON-shaped braces+backticks, nested newlines). Prompt injection is defense-in-depth. |
| Vision API calls (Plan 4) | Defender system prompt + strict JSON schema validation + suspicious-PASS detection (high-confidence PASS on criterion containing literal "PASS" → halt-for-Jake). |
| Report generation (Plan 5) | `report-writer.ts` `writeReport` (final tracked) sanitizes evidence — strips page URLs, screenshot paths, raw vision reasoning. Per-commit (gitignored) report keeps full evidence. |
| Idempotency cache (Plan 1) | `canonicalCriterionHash` includes `org_id`. Cached PASS verdict from tenant A cannot be reused for tenant B with same criterion text. Tenant-bounded by hash shape. |
| Fix executor commits (Plan 7) | `gsd-fix-executor` MUST NOT edit `_fixtures/`, `sanitize-drummond.ts`, RLS migrations, `getCurrentMembership` callsites, tenant-table migrations, `/api/_introspect/*` routes, `src/middleware.ts`, `src/lib/api/auth*`, other phases' planning artifacts. Pre-commit shell snippet enforces. |
| GH Actions artifact upload (Plan 6) | Retention 30→7 days; artifact path glob excludes `screenshots/` subtree. Raw bytes never reach git-tracked artifact. |

**Why "by construction" rather than "by enforcement":** A dropped RLS policy must not cause a leak. Tenant-blind primitives + fixture-org-scoped session + per-org_id-keyed cache + sanitized git-tracked artifacts work together so that even if any single safeguard fails, the others still hold. The harness has no platform-admin escape hatch and never will without explicit D-30 amendment.

**Cross-reference:** PHILOSOPHY.md §6.9 (Verification-first development pattern) — design verification leverages this same boundary. CP3/CP4 picks (future) require updating both design tokens AND Layer 3 vision criteria so the harness verifies the new lock at every commit.

## Report sanitization rules (per ITER-1 multi-tenant C5 + compliance WARNING-3)

Two report types with different sanitization profiles:

| Report | Path | Tracked? | Evidence sanitized? |
|---|---|---|---|
| Per-commit | `.planning/verification/runs/<phase>/<commit>/report.md` | gitignored | NO (full evidence: pageUrl=, screenshot=, full vision reasoning) |
| Final | `.planning/verification/reports/<phase>/final.md` | git-tracked | **YES** (only criterion ID + verdict + confidence + truncated evidence) |
| HALT artifacts (per ITER-1 W3) | `.planning/verification/runs/<phase>/<commit>/HALT-*.md` + `.planning/verification/reports/<phase>/halts/<sha>-<reason>.md` | mixed (runs/ gitignored, reports/halts/ tracked) | The `reports/halts/` copies inherit final.md sanitization rules. |

**Sanitization implementation (Plan 5 `report-writer.ts`):**

Strip from `final.md`:
- Full page URLs (`pageUrl=https://...`) — replace with `[route hash truncated]`
- Screenshot paths (`screenshot=runs/.../foo.png`) — replace with `[screenshot retained gitignored]`
- Raw vision reasoning text — replace with `[reasoning preserved in per-commit report]`

Keep in `final.md`:
- Criterion ID, verdict, confidence
- Layer (1/2/3) + category (mechanical/dom/visual/behavioral/semantic)
- Truncated evidence (≤80 chars; useful for diagnosis without leaking specifics)
- Cumulative cost + duration

**Why git-track final.md at all:** historical audit ("how many phases passed at first try?"), trend analysis (vision cost over time), executive reporting. The non-sanitized fields (pageUrl, screenshot, full reasoning) leak nothing useful for trend analysis but DO leak tenant data — strip them.

## Data retention table (per ITER-1 compliance WARNING-4)

| Artifact | Storage | Retention | Trigger for deletion |
|---|---|---|---|
| Per-commit report (`runs/<phase>/<commit>/report.md`) | repo gitignored dir | indefinite (until repo clone replaced) | manual cleanup or `git clean -fdx .planning/verification/runs/` |
| Screenshots (`runs/<phase>/<commit>/screenshots/<org_id>/`) | repo gitignored dir | indefinite | same as per-commit report |
| Idempotency cache (`runs/<phase>/<commit>/idempotency.json`) | repo gitignored dir | indefinite | same |
| HALT artifacts (`runs/<phase>/<commit>/HALT-*.md`) | repo gitignored dir | indefinite | same |
| HALT artifact retention copies (`reports/<phase>/halts/<sha>-<reason>.md`) | repo git-tracked | indefinite (in git history) | requires git history rewrite (avoid) |
| Final phase report (`reports/<phase>/final.md`) | repo git-tracked | indefinite | overwritten on next harness run for same phase |
| Final phase report versioned alias (per ITER-1 W4) | `reports/<phase>/final-<run-iso-date>.md` | indefinite | manual cleanup at retrospective time |
| GH Actions artifact (`verify-phase-<sha>.zip`) | GitHub | **7 days** (per ITER-1 C2 — was 30) | automatic GitHub expiration |
| Anthropic API request/response data | Anthropic servers | **zero retention per Anthropic API agreement** (zero-retention attestation in DPA — see WARNING-2 disclosure below) | not applicable |
| Sentry error events (per ITER-1 W1) | Sentry servers | per Sentry org plan (typically 30/90 days) | per Sentry data retention settings |
| Calibration log (`calibration-log.md`) | repo git-tracked | indefinite | append-only |

## SOC2 control map (per ITER-1 compliance WARNING-5)

| Control | Description | Implementation |
|---|---|---|
| **CC6.1** Logical and physical access controls | The harness session is fixture-org-scoped (auth-strategy.ts); `gsd-fix-executor` has tenant-context fence; `getCurrentMembership` enforces RLS; secrets via `${{ secrets.X }}` only. | Plans 5 (auth), 7 (fence), 6 (workflow secrets), CLAUDE.md (RLS rule) |
| **CC6.7** Data transmission and disposal | API key redaction (`redactApiKey`) in stderr / Sentry / report files; vision API uses TLS 1.2+; Anthropic zero-retention attestation (WARNING-2 below); GH artifact retention 7d (was 30). | Plans 4 + 5 (redaction), Plan 6 (artifact retention) |
| **CC7.2** System monitoring | Sentry tagging on harness exceptions; HALT artifacts retained in git for SOC2 traceability (W3); commit status set on every CI run; PR comment on every push. | Plans 4 + 5 (Sentry), Plan 8b (HALT retention), Plan 6 (CI status) |
| **PI1.1** System inputs are authorized, complete, accurate, and timely | Criteria sanitization (`sanitizeCriterionText`); strict vision API JSON schema validation; suspicious-PASS detection; criterion text 200-char cap; injection pattern blocklist. | Plans 4 + 5 (SEC-HIGH-2 amendments) |

Each control row should be confirmed by an external SOC2 auditor at the appropriate trust-services-criteria audit cycle. Nightwork is pre-SOC2 today; this map is preparatory documentation.

## Anthropic data-processor disclosure (per ITER-1 compliance WARNING-2)

**Anthropic acts as a data sub-processor for the verification harness.** Layer 3 vision calls send screenshots + criterion text to Anthropic Claude API.

**Data processed:**
- Screenshots of Vercel preview pages (per Plan 4 `runLayer3` Playwright capture).
- Criterion text (visual / semantic categories from PLAN files).
- Page URL (in user message body).

**Anthropic processing posture:**
- Per Anthropic's API Data Usage Policy: API inputs/outputs are NOT used for model training. Zero-retention attestation applies.
- Per Anthropic's DPA: data residency in US; encryption in transit + at rest; processed only for the duration of the API call.

**Per ITER-1 D-30 coverage:** the screenshots sent to Anthropic are FIXTURE-ORG-ONLY by construction (harness session authenticates as harness-fixture user; production tenant data unreachable). Even if Anthropic's processing were less strict, no real-tenant data could leak.

**Future (when Wave 1.1+ adds per-tenant verification):** before extending the harness to per-tenant preview verification, formal Anthropic DPA addendum required. Tenant must opt-in to per-tenant Layer 3 vision in their org_settings. F3 narrowed-scope CI test gate phase covers this.

## API key redaction (per ITER-1 compliance WARNING-1)

API keys (`sk-ant-*` Anthropic, `Bearer *` for various) MUST be redacted from any string that could end up in:
- stderr output (CI logs, local dev console)
- Sentry exception payloads
- Committed report files (`reports/<phase>/final.md`, halt artifacts)
- GitHub artifact zips (per Plan 6 retention)

**Implementation (Plans 4 + 5 inline):**

```typescript
function redactApiKey(message: string): string {
  return message
    .replace(/sk-ant-[A-Za-z0-9_\-]+/g, "[REDACTED:ANTHROPIC_KEY]")
    .replace(/Bearer\s+[A-Za-z0-9_\-\.=]+/g, "Bearer [REDACTED]");
}
```

Every error path that could surface a key wraps with `redactApiKey(message)` before stderr / Sentry / report write. The vision client also uses this when re-throwing parse errors.

## Final report versioning (per ITER-1 W4 enterprise-readiness)

**Problem:** `reports/<phase>/final.md` is overwritten on each harness run for the same phase. Historical comparison ("how did pass-rate change between commits A and B?") is lost.

**Amendment:** in addition to writing `final.md`, write a versioned alias `final-<run-iso-date>.md`. The unversioned `final.md` continues to be the latest snapshot; the versioned files preserve history.

**Implementation (Plan 5 `report-writer.ts`):**

```typescript
// Write final.md (overwrite, latest snapshot)
writeFileSync(path.join(dir, "final.md"), md, "utf-8");

// Per ITER-1 W4: also write versioned alias for historical comparison
const isoDate = new Date().toISOString().slice(0, 10);  // YYYY-MM-DD
writeFileSync(path.join(dir, `final-${isoDate}.md`), md, "utf-8");
```

Versioned files are git-tracked alongside final.md. Manual cleanup at retrospective time prevents unbounded growth.

## Vercel ls stdout grep fragility (per ITER-1 W6 enterprise-readiness)

**Problem:** Plan 5 `vercel-discovery.ts` `tryVercelCli` parses `vercel ls` stdout via line-by-line grep + URL regex. Vercel CLI output format changes between versions; the regex is fragile.

**Recommended future amendment (Wave 1.1+):** replace `vercel ls` parsing with `vercel inspect <branch> --json`. The `--json` flag provides structured output that's stable across CLI versions.

**Why deferred:** `vercel inspect` requires deployment ID (not branch name) — discovery still needs `vercel ls` to find the deployment ID first. Multi-step discovery deserves a focused refactor, not an inline patch.

**Mitigation today:** Plan 5 has 4-tier hierarchy with REST API as fallback (per D-20/D-21). If `vercel ls` parsing breaks, the harness falls through to REST API + deterministic pattern. Discovery is robust by layered defense, not by primary-path stability.

## Iter-1 plan-review amendments anchored here

Per Jake nwrp49 (2026-05-06) — applied targeted amendments to address iter-1 plan-review verdict (REVISE-PLAN — 11 CRITICAL/HIGH findings + 24 WARNINGs). Full enumeration in `.planning/phases/stage-1.5c-verification-harness/README.md` § "Iter-1 plan-review amendments".

**Cluster summary (cross-references for downstream readers):**

| Cluster | Findings | Affected plans | Doc location |
|---|---|---|---|
| 1: Tenant boundary by construction | C1-C6 + WARNING-3 | Plans 1, 3, 4, 5, 6, 7, 10 | D-30 in CONTEXT.md + this doc § Tenant safety boundary |
| 2: State machine + criteria loader | ARCH-CRIT-1/2/3 | Plans 1, 5, 8b, 11 | this doc § State machine + Plan 1 README |
| 3: Injection hardening | SEC-HIGH-1/2 | Plans 4, 5, 6 | this doc § Vision prompt injection + § GH Actions injection |
| Inline WARNINGs (~10) | W1, W2, W3, W5 enterprise-readiness; MEDIUM-2 + MEDIUM-3 security; etc. | Plans 4, 5, 8b, 6 | inline in each plan's amendment block |
| Plan 10 doc-pass (~7) | DPA, retention, SOC2, sanitization rules, redaction, final report versioning, vercel grep fragility | This Plan 10 (this doc) | sections above this one |
| Wave-1.1+ deferred (~7) | scalability/multi-tenant futures | deferred-items.md | `.planning/phases/stage-1.5c-verification-harness/deferred-items.md` |

**Iter-2 plan-review watchpoints (per Jake nwrp49 §5):**
1. C1 fix produces real auth strategy, not deferred-to-F1 placeholder ✓ (Plan 5 auth-strategy.ts ships)
2. ARCH-CRIT-1 fix verified by iter-3 hard-halt test case ✓ (Plan 11 self-test executes)
3. ARCH-CRIT-2 fix includes criteria-loader smoke test ✓ (Plan 1 scaffold + Plan 11 execution)
4. SEC-HIGH-1 fix uses env-var indirection ✓ (Plan 6 env: blocks on every step)
5. SEC-HIGH-2 fix includes prompt sanitization layer + assertion ✓ (Plan 5 sanitizeCriterionText + Plan 4 strict schema validation)
6. D-30 wording lands in CONTEXT.md, cross-referenced from PHILOSOPHY.md ✓ (commit 1)

## Source attribution

Spec: `.planning/expansions/stage-1.5c-verification-harness-EXPANDED-SCOPE.md` (APPROVED 2026-05-06).
Plans: `.planning/phases/stage-1.5c-verification-harness/01.5-c-verification-harness-{1..11}-*-PLAN.md`.
Decisions: D-01..D-30 in `.planning/phases/stage-1.5c-verification-harness/01.5-c-verification-harness-CONTEXT.md` (D-30 added 2026-05-06 per Jake nwrp49 + iter-1 plan-review).
Implementation: `src/lib/verification/`, `scripts/verify-phase.ts`, `scripts/loop-with-harness.ts`, `.github/workflows/verify-phase.yml`, `.claude/agents/gsd-research-standards.md`, `.claude/agents/gsd-fix-executor.md`, `.planning/templates/criteria-template.md`, `.planning/calibration-log.md`, `.planning/calibration-log-schema.md`, `supabase/migrations/00050_verification_harness_fixture_org.sql`.
```

**STANDING RULES:**
- Process discipline.
- No code edits in this task; doc-only.

**COMMIT:**
```
docs(stage-1.5c-vh): Plan 10 Task 1 — VERIFICATION-PIPELINE.md canonical doc

New canonical doc at .planning/architecture/VERIFICATION-PIPELINE.md
documenting the 3-layer pipeline + state machine + criteria mandate +
calibration log + agent coordination + CI integration + plan-review
watchpoints + extension points + out-of-scope guards.

Format precedent: .planning/architecture/WORKFLOW-INTELLIGENCE.md
(WI catalogue from 1.5c IA Plan 2 amendment).

Source attribution at end links every spec/plan/decision/file shipped
in this phase.
```
  </action>

  <verify>
    <automated>
test -f .planning/architecture/VERIFICATION-PIPELINE.md &&
grep -q "^# Verification pipeline" .planning/architecture/VERIFICATION-PIPELINE.md &&
grep -q "## Layer 1\|## Layer 2\|## Layer 3" .planning/architecture/VERIFICATION-PIPELINE.md &&
grep -q "Plan-review watchpoints" .planning/architecture/VERIFICATION-PIPELINE.md &&
grep -q "Operational reference" .planning/architecture/VERIFICATION-PIPELINE.md &&
grep -q "D-01\|D-22\|D-29" .planning/architecture/VERIFICATION-PIPELINE.md
    </automated>
  </verify>

  <done>
    - VERIFICATION-PIPELINE.md exists; documents pipeline overview, all 3 layers, state machine, criteria mandate, idempotency, calibration, agents, CI, watchpoints, operational reference, extension points, out-of-scope
    - Cross-references all 11 plans + agents + scripts + CI + tracking dirs
    - Source attribution at end
  </done>
</task>

<task type="auto">
  <name>Task 2: MASTER-PLAN.md + PHILOSOPHY.md + CLAUDE.md + CONTEXT.md cross-refs</name>
  <files>.planning/MASTER-PLAN.md, .planning/design/PHILOSOPHY.md, CLAUDE.md, .planning/phases/stage-1.5c-verification-harness/01.5-c-verification-harness-CONTEXT.md</files>

  <read_first>
    - .planning/MASTER-PLAN.md (sections §10 DECISIONS LOG + §12 NEXT PLANNED — full file)
    - .planning/design/PHILOSOPHY.md (just enough to find a sensible insertion section)
    - CLAUDE.md (find Testing Rule section for Q2=C update)
    - .planning/phases/stage-1.5c-verification-harness/01.5-c-verification-harness-CONTEXT.md
  </read_first>

  <action>
**Step A — `.planning/MASTER-PLAN.md` DECISIONS LOG (D-073..D-077):**

Append the following 5 entries to the §10 DECISIONS LOG table (after the last existing D-NNN entry; current last is D-039):

```markdown
| **D-073** | 2026-05-XX | Verification pipeline canonical infrastructure (3 layers + state machine + criteria mandate + idempotency + calibration log + loop-with-executor + 2 new agents + GH Action). Documented in `.planning/architecture/VERIFICATION-PIPELINE.md`. Replaces `nightwork-smoke-tester` (Layer 1 absorption per Q4=C); keeps `nightwork-end-to-end-test` as workflow integrity walk. | stage-1.5c-verification-harness shipped 2026-05-XX. Per Jake nwrp47 + nwrp48: per-commit verification on Vercel preview URLs catches bugs that slipped past 3 rounds of manual review (e.g., cost-code-missing-on-line-item in 1.5c IA). 6.5-8 day phase; full vision per Jake; MVP path explicitly rejected. |
| **D-074** | 2026-05-XX | Layer 2 standards scope: framework + ONE working rule (`conservation/money-line-items-sum`) ships this phase per Q1=A. Other domain rules (aia / accounting / lien-law / dates) populate as future phases need them via `gsd-research-standards` agent (NAHB / AIA / FL state statutes / IRS / GAAP authority hierarchy with caching + TTL). | Construction-domain depth (AIA G702/G703 structure, accounting identities, FL lien law, dates) is genuine F1+ territory. Folding into stage-1.5c would have added 1-2 days with marginal vs incremental population per phase need. |
| **D-075** | 2026-05-XX | Criteria mandate: every PLAN.md from stage-1.5c-verification-harness onward includes a `<criteria>` yaml block (5 categories: mechanical/dom/visual/behavioral/semantic). Enforced by `nightwork-plan-review` (BLOCKING on missing block, REVISE on vague entries). Consumed by harness Layer 1/2/3 + `nightwork-spec-checker` (per D-19). 1.5c IA Plans 1/2/2-amend/3 retrofit is separate ~1-hour follow-up per Q6=B. | Plan-review watchpoint #5 (friction-tax): template at `.planning/templates/criteria-template.md` + ~5 min/plan target + worked examples from this phase's PLANs 1-11 keep mandate enforcing structured thinking without bloating plan-write time. |
| **D-076** | 2026-05-XX | Calibration log at `.planning/calibration-log.md` — append-only phase retrospectives written by `nightwork-custodian` at post-ship sweep per Q7=A. JSON-front-matter (machine-readable, parsed by `/np` orchestrator at next-phase scope time per D-27 dynamic prompting) + markdown body (human-readable). Schema at `.planning/calibration-log-schema.md`. | Plan-review watchpoint #4 (useful lessons not noise): extractable-data-only rule prevents calibration entries from devolving into noisy free-text. Heuristic adjustments for orchestrator: parallelism / prompt density / iter budget / vision cost. |
| **D-077** | 2026-05-XX | Loop-with-executor: harness loop INSIDE `/gsd-execute-phase` BEFORE `/nightwork-qa` per Q3=A. Max 3 iter ADDITIVE per Q3.1 (does NOT consume halt-after budget). Halt-for-Jake on iter-3 OR Layer 3 confidence < 0.7 (per D-07). State machine bounded; HALT artifacts at `.planning/verification/runs/<phase>/<commit>/HALT-{ITER-3,AMBIGUOUS-VISION}.md`. New `gsd-fix-executor` agent (frontmatter-branched from gsd-executor) makes one focused commit per loop iteration. | Plan-review watchpoint #2 (state machine cleanly bounded): MAX_ITERATIONS=3 hard cap; pure-function transitions; no recursive calls. Plan-review watchpoint #7 (idempotency contract precise): commit_sha + criterion_hash key tested; rerun-on-same-commit really is no-op (Plan 11 self-test). |
```

**Step B — `.planning/MASTER-PLAN.md` §9 CURRENT POSITION update:**

Find §9 (around line 145-152). Append (or have nightwork-custodian update at post-ship — but this plan documents the change):

This plan does NOT update §9 directly (that's the custodian's job at post-ship). This task's responsibility is the DECISIONS LOG entries. The §9 update happens after Plan 11 self-test PASSes and the merge to main lands.

Add a note in §12 NEXT PLANNED WORK pointing to the harness:

Find §12 (around line 245). Insert near "Stage 1.5c — Real data + test infrastructure":

Replace the existing "Stage 1.5c — Real data + test infrastructure" bullet with:

```markdown
**Stage 1.5c — Verification harness + test infrastructure:**
- ✅ stage-1.5c-verification-harness shipped 2026-05-XX (per D-073..D-077). Canonical doc: `.planning/architecture/VERIFICATION-PIPELINE.md`. Replaces `nightwork-smoke-tester` (Layer 1 absorption); keeps `nightwork-end-to-end-test` as workflow integrity walk.
- 🔜 1.5c IA branch rebases main + criteria retrofit (~1 hour, per Q6=B).
- 🔜 Plan 3a-now spawns through harness (first real production use).
- 🔜 Wave 3 (Plans 4+5) ships through harness.
- 🔜 1.5c ships.
```

(The exact line text may differ; preserve adjacent context. Use Edit tool to insert the new block in place of the existing 1.5c line.)

**Step C — `.planning/design/PHILOSOPHY.md` addition:**

Find a sensible insertion point (likely a section about development workflow / cross-references). Add a new section near the bottom or in §0.3 Cross-references area:

```markdown
## X. Verification-first development pattern (per stage-1.5c-verification-harness D-073)

The Nightwork verification pipeline (`.planning/architecture/VERIFICATION-PIPELINE.md`) verifies design decisions made in this PHILOSOPHY document at every commit. The link is concrete:

- **Direction lock (CP2 = Site Office + Set B per D-037)** is verified mechanically by `.claude/hooks/nightwork-post-edit.sh` (forbidden-token regex catches palette drift) and visually by the harness Layer 3 (Claude vision against `<criteria>` blocks like `"Site Office signature visible: tracking-eyebrow 0.18em UPPERCASE on eyebrows"`).
- **Slate Set B palette tokens** are checked at hook level (no hardcoded hex outside `globals.css`/`tailwind.config.*`) AND at vision level ("Slate Set B palette only — no off-token hex").
- **Document Review pattern** (file preview LEFT + right-rail panel + audit timeline BELOW) is a vision-criterion: `"Document Review pattern present"`.

The implication for design changes: a new direction or palette pick (CP3, CP4) requires updating both the design tokens (mechanical) AND the Layer 3 vision criteria (semantic) so the harness verifies the new lock at every commit.

The PHILOSOPHY document remains the design intent + constraints; the harness is the enforcement mechanism. Design drift is impossible if both are kept in sync.
```

(The exact section number depends on existing structure; use Grep to find the right insertion point.)

**Step D — `CLAUDE.md` Testing Rule update (per Q2=C):**

Find the `## Testing Rule (MANDATORY)` section (around line 376). Per Q2=C — augment-with-deprecation-path. Append a note:

```markdown
## Testing Rule (MANDATORY)

(... existing content ...)

### Verification harness deprecation path (per stage-1.5c-verification-harness D-003 / Q2=C)

Beginning 2026-05-XX, the 3-layer verification harness (`.planning/architecture/VERIFICATION-PIPELINE.md`) is the canonical CI signal for visual + mechanical verification. The localhost-walk-with-Chrome-DevTools-MCP pattern above remains the inner-loop dev tool during weeks 1-3 of harness adoption (currently 1.5c IA + 1.5c ship + Wave 1.1 onset).

Re-evaluation: at end of Wave 3, decide whether the harness has stabilized enough to fully replace localhost walks for visual sign-off. Until then, hybrid posture:
- **Local dev iteration:** localhost + Chrome DevTools MCP (above rule). Cheap, fast.
- **Visual sign-off:** Vercel preview URL via harness. Authoritative; what production runtime sees.

For verification-eligible surfaces (every UI change post-stage-1.5c-verification-harness): the harness's Layer 3 vision against PLAN-file `<criteria>` is the canonical pass/fail. Localhost-walk continues as a sanity check during code-write but does not substitute for the harness verdict.

For non-UI changes (infra, types, agent files): localhost walks not applicable; harness Layer 1 + Layer 2 cover.
```

**Step E — `.planning/phases/stage-1.5c-verification-harness/01.5-c-verification-harness-CONTEXT.md` cross-refs:**

Add at the END of CONTEXT.md (do not modify existing content; append):

```markdown
## Forward documents (added by Plan 10 of this phase)

These documents land during this phase and become canonical references afterward:

- `.planning/architecture/VERIFICATION-PIPELINE.md` — canonical doc (Plan 10)
- `.planning/templates/criteria-template.md` — drop-in template + specificity rubric (Plan 8a)
- `.planning/calibration-log.md` + `.planning/calibration-log-schema.md` — append-only retrospectives (Plan 9)
- `.claude/agents/gsd-research-standards.md` — industry-standards research agent (Plan 7)
- `.claude/agents/gsd-fix-executor.md` — frontmatter-branched fix executor (Plan 7)
- `.github/workflows/verify-phase.yml` — first GH Action on main (Plan 6)
- `scripts/verify-phase.ts` + `scripts/loop-with-harness.ts` — harness binaries (Plans 5 + 8b)
- `src/lib/verification/` — module tree (Plans 1-5 + 8b)
- `.planning/verification/standards/` — Layer 2 rules library (Plan 1 scaffold + Plan 3 first rule)
- `.planning/verification/reports/` — git-tracked final reports per phase (Plan 1)

MASTER-PLAN.md DECISIONS LOG entries D-073..D-077 added per Plan 10 Task 2.
CLAUDE.md Testing Rule updated per Q2=C deprecation-path (Plan 10).
PHILOSOPHY.md verification-first development pattern note added (Plan 10).
```

**STANDING RULES:**
- Process discipline.
- Per D-29: no edits to design-system playground, fixtures, IA branch.
- The MASTER-PLAN.md §9 update is custodian's job at actual ship; this task only adds DECISIONS LOG + §12 update.

**COMMIT:**
```
docs(stage-1.5c-vh): Plan 10 Task 2 — MASTER-PLAN/PHILOSOPHY/CLAUDE/CONTEXT updates

MASTER-PLAN.md DECISIONS LOG D-073..D-077: pipeline canonical, Layer 2
scope (Q1=A), criteria mandate (D-17/D-18), calibration log (Q7=A),
loop-with-executor (Q3=A + Q3.1=ADDITIVE).

MASTER-PLAN.md §12 NEXT PLANNED: stage-1.5c-verification-harness shipped
+ pointer to next 1.5c IA rebase + retrofit + Plan 3a-now + Wave 3 sequence.

PHILOSOPHY.md verification-first pattern note: design lock enforced at
hook level + vision level; design drift impossible if both kept in sync.

CLAUDE.md Testing Rule: hybrid posture (localhost inner-loop, harness CI
signal); re-eval at end of Wave 3 (per Q2=C deprecation-path).

CONTEXT.md cross-refs: 11 forward documents enumerated.

Wave 6 complete. Plan 10 ready for halt-for-Jake before Wave 7 self-test.
```
  </action>

  <verify>
    <automated>
grep -q "D-073" .planning/MASTER-PLAN.md &&
grep -q "D-074" .planning/MASTER-PLAN.md &&
grep -q "D-075" .planning/MASTER-PLAN.md &&
grep -q "D-076" .planning/MASTER-PLAN.md &&
grep -q "D-077" .planning/MASTER-PLAN.md &&
grep -q "Verification harness\|verification harness\|Verification-first\|verification-first" .planning/design/PHILOSOPHY.md &&
grep -q "verification harness\|harness deprecation\|verification-eligible" CLAUDE.md &&
grep -q "Forward documents\|VERIFICATION-PIPELINE.md\|Plan 10" .planning/phases/stage-1.5c-verification-harness/01.5-c-verification-harness-CONTEXT.md
    </automated>
  </verify>

  <done>
    - MASTER-PLAN.md DECISIONS LOG has D-073..D-077 entries
    - MASTER-PLAN.md §12 NEXT PLANNED reflects stage-1.5c-verification-harness ship sequence
    - PHILOSOPHY.md has verification-first development pattern note
    - CLAUDE.md Testing Rule has Q2=C deprecation-path note
    - CONTEXT.md has Forward documents cross-ref section
    - Wave 6 halt signal surfaced for Jake
  </done>
</task>

</tasks>

<acceptance_criteria>

- [ ] **AC-1.5c-vh-10-1:** `.planning/architecture/VERIFICATION-PIPELINE.md` exists; ~700+ lines documenting pipeline / 3 layers / state machine / criteria / calibration / agents / CI / watchpoints / operational reference / extension points / out-of-scope
- [ ] **AC-1.5c-vh-10-2:** VERIFICATION-PIPELINE.md cross-references all 7 plan-review watchpoints with anchor plans
- [ ] **AC-1.5c-vh-10-3:** VERIFICATION-PIPELINE.md source attribution links every spec / plan / decision / file shipped in this phase
- [ ] **AC-1.5c-vh-10-4:** MASTER-PLAN.md DECISIONS LOG has D-073..D-077 entries (5 new decisions, dated, with rationale)
- [ ] **AC-1.5c-vh-10-5:** MASTER-PLAN.md §12 NEXT PLANNED WORK shows stage-1.5c-verification-harness ✅ + sequence to 1.5c IA rebase + Plan 3a-now + Wave 3
- [ ] **AC-1.5c-vh-10-6:** PHILOSOPHY.md has "Verification-first development pattern" section linking design lock (CP2) to harness Layer 3 vision criteria
- [ ] **AC-1.5c-vh-10-7:** CLAUDE.md Testing Rule has Q2=C deprecation-path note: hybrid posture during weeks 1-3; re-eval at Wave 3 end
- [ ] **AC-1.5c-vh-10-8:** CONTEXT.md has "Forward documents" cross-ref section listing 11+ documents authored by Plans 1-10
- [ ] **AC-1.5c-vh-10-9:** All hooks silent on every commit (T10a-T10d, post-edit drift, Drummond grep)
- [ ] **AC-1.5c-vh-10-10:** Wave 6 halt signal surfaced for Jake review (Wave 7 self-test next)
- [ ] **AC-1.5c-vh-10-11 (iter-1 multi-tenant C5 + compliance WARNING-3 + D-30):** VERIFICATION-PIPELINE.md contains "## Tenant safety boundary" section with 8-layer implementation map + "## Report sanitization rules" section with strip/keep field tables
- [ ] **AC-1.5c-vh-10-12 (iter-1 compliance WARNING-2):** VERIFICATION-PIPELINE.md contains "## Anthropic data-processor disclosure" section with zero-retention attestation reference
- [ ] **AC-1.5c-vh-10-13 (iter-1 compliance WARNING-4):** VERIFICATION-PIPELINE.md contains "## Data retention table" listing every artifact + storage + retention + deletion-trigger
- [ ] **AC-1.5c-vh-10-14 (iter-1 compliance WARNING-5):** VERIFICATION-PIPELINE.md contains "## SOC2 control map" section citing CC6.1, CC6.7, CC7.2, PI1.1 with implementation references
- [ ] **AC-1.5c-vh-10-15 (iter-1 compliance WARNING-1):** VERIFICATION-PIPELINE.md contains "## API key redaction" section + redactApiKey code reference
- [ ] **AC-1.5c-vh-10-16 (iter-1 enterprise-readiness W4):** VERIFICATION-PIPELINE.md contains "## Final report versioning" section documenting `final-<run-iso-date>.md` versioned aliases
- [ ] **AC-1.5c-vh-10-17 (iter-1 enterprise-readiness W6):** VERIFICATION-PIPELINE.md contains "## Vercel ls stdout grep fragility" section with vercel inspect future-amendment recommendation
- [ ] **AC-1.5c-vh-10-18 (iter-1 amendments coverage):** VERIFICATION-PIPELINE.md contains "## Iter-1 plan-review amendments anchored here" section enumerating all 6 clusters + 6 iter-2 watchpoints with ✓ status markers
- [ ] **AC-1.5c-vh-10-19 (iter-1 deferrals):** `.planning/phases/stage-1.5c-verification-harness/deferred-items.md` exists; lists 7 Wave-1.1+ deferred WARNINGs with rationale
- [ ] **AC-1.5c-vh-10-20 (iter-1 D-30 cross-refs):** VERIFICATION-PIPELINE.md "Source attribution" footer cites D-01..D-30 (was D-01..D-29)

</acceptance_criteria>

<criteria>

```yaml
criteria:
  mechanical:
    - "All hooks silent on every commit (T10a-T10d, post-edit drift, Drummond grep)"
    - "Privacy gate clean across new doc updates"
    - "All 5 modified files parse as valid markdown (code fence parity check)"
    - "VERIFICATION-PIPELINE.md cross-references all 7 plan-review watchpoints"
    - "VERIFICATION-PIPELINE.md contains 'Tenant safety boundary' section with 8-layer table (auth strategy, screenshot capture, criteria loading, vision API calls, report generation, idempotency cache, fix executor commits, GH Actions artifact upload) — iter-1 C5 + WARNING-3 + D-30"
    - "VERIFICATION-PIPELINE.md 'Data retention table' lists at least 11 artifact types with retention windows + deletion triggers (iter-1 WARNING-4)"
    - "VERIFICATION-PIPELINE.md 'SOC2 control map' cites all 4 controls (CC6.1, CC6.7, CC7.2, PI1.1) with implementation references (iter-1 WARNING-5)"
    - "VERIFICATION-PIPELINE.md 'Anthropic data-processor disclosure' includes zero-retention attestation language + per-D-30 fixture-only coverage note (iter-1 WARNING-2)"
    - "VERIFICATION-PIPELINE.md 'Iter-1 plan-review amendments anchored here' section: 6 cluster rows + 6 iter-2 watchpoints with ✓ markers"
    - "deferred-items.md exists with 7 Wave-1.1+ deferrals: GH Actions fetch-depth re-eval, Vercel REST 429 retry, Vercel project-ID assertion, per-tenant cost attribution, PR comment vision-reasoning leakage, Plan 11 manual-verification automation, actionlint optional-skip"
  dom:
    - "N/A — no UI surfaces in this plan (documentation only)"
  visual:
    - "N/A — no UI"
  behavioral:
    - "VERIFICATION-PIPELINE.md documents the 3 layers, state machine, criteria mandate, idempotency, calibration log, agents, CI, watchpoints, extension points, out-of-scope"
    - "MASTER-PLAN.md DECISIONS LOG D-073..D-077 entries are dated + cite source artifacts (PHASE-CONTEXT, watchpoints, etc.)"
    - "MASTER-PLAN.md §12 NEXT PLANNED replaces the prior 'Stage 1.5c — Real data + test infrastructure' line with the post-harness sequence"
    - "PHILOSOPHY.md cross-references VERIFICATION-PIPELINE.md as the design-lock enforcement mechanism"
    - "CLAUDE.md Testing Rule update preserves existing rule + appends Q2=C deprecation-path note"
    - "CONTEXT.md Forward documents section enumerates 11+ documents (VERIFICATION-PIPELINE / criteria-template / calibration-log + schema / 2 agents / GH Action / 2 scripts / src/lib/verification/ / standards/ / reports/)"
  semantic:
    - "VERIFICATION-PIPELINE.md design: canonical doc role established; future docs/skills/agents reference this rather than duplicating layer responsibilities"
    - "Cross-doc consistency: VERIFICATION-PIPELINE.md + MASTER-PLAN D-073..D-077 + PHILOSOPHY verification-first + CLAUDE.md Testing Rule + CONTEXT.md Forward documents all reference each other coherently"
```

</criteria>

<halt_conditions>

- **HARD HALT after Task 2 commits.** Wave 6 complete. Per D-25, halt for Jake review of canonical doc + MASTER-PLAN updates before Wave 7 (Plan 11 self-test) dispatches.
- Surface to Jake: 1 new canonical doc + 5 file updates. All 7 plan-review watchpoints anchored in VERIFICATION-PIPELINE.md.
- Soft halt if: hooks fire, doc cross-refs broken (link to non-existent file).

</halt_conditions>

<canonical_refs>

- All Wave 0-5 outputs (this plan documents what they built)
- `.planning/MASTER-PLAN.md` (DECISIONS LOG format precedent — D-001..D-039 existing entries)
- `.planning/architecture/WORKFLOW-INTELLIGENCE.md` (canonical-doc format precedent from 1.5c IA Plan 2 amendment)
- `.planning/design/PHILOSOPHY.md` (insertion point for verification-first pattern)
- `CLAUDE.md` (Testing Rule section to update per Q2=C)

</canonical_refs>
