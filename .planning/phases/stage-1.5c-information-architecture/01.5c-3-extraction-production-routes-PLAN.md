---
phase: stage-1.5c-information-architecture
plan: 3
plan-name: extraction-production-routes
type: execute
status: not-started
wave: 2
depends_on: [2]
autonomous: false
halt_after: true
threat_model_severity: medium
requirements: []
files_modified:
  - src/components/prototypes/DrawApprovalView.tsx
  - src/components/prototypes/DrawPrintView.tsx
  - src/components/prototypes/ScheduleView.tsx
  - src/components/prototypes/VendorListView.tsx
  - src/components/prototypes/VendorDetailView.tsx
  - src/components/prototypes/DocumentReviewView.tsx
  - src/components/prototypes/OwnerDashboardView.tsx
  - src/components/prototypes/OwnerDrawView.tsx
  - src/components/prototypes/MobileApprovalView.tsx
  - src/components/prototypes/ReconciliationView.tsx
  - src/app/financials/bills/[id]/page.tsx
  - src/app/financials/pay-apps/[id]/page.tsx
  - src/app/financials/pay-apps/[id]/print/page.tsx
  - src/app/financials/reconciliation/page.tsx
  - src/app/jobs/[id]/budget/page.tsx
  - src/app/jobs/[id]/schedule/page.tsx
  - src/app/people/vendors/page.tsx
  - src/app/people/vendors/[id]/page.tsx
  - src/app/jobs/[id]/documents/[documentId]/page.tsx
  - src/app/owner-portal/page.tsx
  - src/app/owner-portal/pay-apps/[id]/page.tsx
  - src/app/jobs/[id]/mobile-approval/page.tsx
  - src/app/design-system/prototypes/draws/[id]/page.tsx
  - src/app/design-system/prototypes/draws/[id]/print/page.tsx
  - src/app/design-system/prototypes/jobs/[id]/schedule/page.tsx
  - src/app/design-system/prototypes/vendors/page.tsx
  - src/app/design-system/prototypes/vendors/[id]/page.tsx
  - src/app/design-system/prototypes/documents/[id]/page.tsx
  - src/app/design-system/prototypes/owner-portal/page.tsx
  - src/app/design-system/prototypes/owner-portal/draws/[id]/page.tsx
  - src/app/design-system/prototypes/mobile-approval/page.tsx
  - src/app/design-system/prototypes/reconciliation/page.tsx
requires:
  - .planning/phases/stage-1.5c-information-architecture/CONTEXT.md
  - .planning/phases/stage-1.5c-information-architecture/PATTERNS.md
  - .planning/phases/stage-1.5c-information-architecture/01.5c-2-thin-wrapper-architect-PLAN.md
  - .planning/phases/stage-1.5c-information-architecture/01.5c-2-thin-wrapper-architect-SUMMARY.md (Plan 2 must SUMMARY first)
  - .planning/phases/stage-1.5c-information-architecture/AUDIT-LOG-STRATEGY.md
  - src/components/prototypes/InvoiceReviewView.tsx (Plan 2 exemplar — Document Review pattern)
  - src/components/prototypes/BudgetView.tsx (Plan 2 exemplar — multi-fixture aggregation pattern; iter-2 W-2)
  - src/lib/audit/action-labels.ts (Plan 2 helper)
  - src/app/design-system/prototypes/* (all 11 source files for extraction)
  - .planning/phases/stage-1.5b-prototype-gallery/findings.md (TD-20/25/26 line numbers)
  - src/components/job-tabs.tsx (existing 9-page consumer; iter-2 must-fix #2 — explicit removal)
provides:
  - 10 thin-wrapper View components extracted (props-only renders) — Plan 2 already shipped 2 (Invoice + Budget); this plan ships 10 more for 12 total
  - 12 production routes mounting Caldwell fixtures via thin wrappers wrapped in .design-system-scope class (per D-19 Decision A)
  - 11 portfolio prototype routes rewritten as thin wrappers (kept accessible under platform_admin gate)
  - Side-by-side visual verification of all 12 production-vs-portfolio pairs (D-07 CRITICAL halt criterion at 3 viewports per iter-2 W-3 method lock)
  - JobTabs inline mounts REMOVED from 9 existing /jobs/[id]/*/page.tsx files (iter-2 must-fix CRITICAL #2 — double-nav collision elimination)
  - getCurrentMembership() / org_id / requireMembership preservation verified per route (iter-2 mechanical #10 — grep diff verify)
  - Site Office scope wrap on production routes (per D-19 Decision A — iter-2 must-fix CRITICAL #1)
affects:
  - All 12 production routes for Bills + Pay Apps + Budget + Schedule + Vendors + Documents + Owner Portal + Mobile Approval + Reconciliation
  - 11 portfolio /design-system/prototypes/* routes (rewritten as thin wrappers; functionality preserved)
  - 9 existing /jobs/[id]/*/page.tsx files (JobTabs inline mounts removed — Plan 5's PerJobTabs in layout.tsx replaces them)
acceptance-criteria-target: 22 falsifiable items
estimated-duration: 3 days (was 2-3 days; +0.5 day for D-19 scope wrap + 9-page JobTabs removal + getCurrentMembership grep diffs)
must_haves:
  truths:
    - "10 NEW View components exist at src/components/prototypes/*View.tsx; each accepts data via props (no runtime fixture imports)"
    - "12 total Views (10 NEW + 2 from Plan 2: InvoiceReviewView + BudgetView)"
    - "Each NEW View applies TD-20 (NwButton bypass), TD-25 (Card padding=md), TD-26 (fontWeight: 500) structural fixes"
    - "12 production routes mount the View components: /financials/bills/[id], /financials/pay-apps/[id], /financials/pay-apps/[id]/print, /financials/reconciliation, /jobs/[id]/budget, /jobs/[id]/schedule, /people/vendors, /people/vendors/[id], /jobs/[id]/documents/[documentId], /owner-portal, /owner-portal/pay-apps/[id], /jobs/[id]/mobile-approval"
    - "Each production route's outer container wraps in .design-system-scope class (per D-19 Decision A) — extends Site Office direction-aware selectors from playground-only to production consumer"
    - "11 portfolio /design-system/prototypes/* routes rewritten as thin wrappers"
    - "Side-by-side D-07 visual verification: each production route renders identically to its portfolio prototype counterpart at 3 viewports per iter-2 W-3"
    - "iter-2 watchpoint #1 verified: visual diff before/after .design-system-scope wrap confirms 1px slate-tile left-stamp + JetBrains Mono UPPERCASE eyebrow + Space Grotesk Medium headline appear post-wrap on production routes"
    - "JobTabs inline mounts REMOVED from 9 existing /jobs/[id]/*/page.tsx files (iter-2 must-fix CRITICAL #2): page.tsx, draws/page.tsx, budget/page.tsx, invoices/page.tsx, change-orders/page.tsx, activity/page.tsx, lien-releases/page.tsx, internal-billings/page.tsx, purchase-orders/page.tsx — zero double-tab-bar rendering"
    - "Smoke test of all 9 /jobs/[id]/* legacy routes confirms: NO double tab bar (PerJobTabs from Plan 5's layout.tsx is the sole tab surface)"
    - "iter-2 mechanical #10 verified: getCurrentMembership / org_id / requireMembership preserved verbatim across re-mounted routes per route grep diff"
    - "Production routes inherit Site Office direction from .design-system-scope wrap (per D-19)"
    - "Portfolio routes inherit data-direction=C data-palette=B from /design-system/prototypes/layout.tsx (per 1.5b D-02)"
    - "Production routes are gated to authenticated org members (existing middleware posture); portfolio routes gated to platform_admin (existing posture per 1.5a)"
    - "T10c hook silent on all production routes (per Plan 2 architect finding — Option A confirmed)"
    - "Tenant-blind hook silent on all View components (no org_id / membership / vendor_id props)"
    - "Bouncy-easing hook silent on all 10 NEW Views"
    - "Build + typecheck + post-edit Forbidden tokens silent throughout"
    - "Privacy gate clean across all 22+ new files"
    - "Owner Portal at /owner-portal/* gated per existing middleware (D-08 posture clarified per iter-2 multi-tenant W-1: 1.5c is structural — Caldwell fixtures only; F1 wires real auth via either client_portal_access tokens OR owner_view role with RLS, NOT both — F1 commits to one path)"
    - "Mobile Approval at /jobs/[id]/mobile-approval renders inside iPhone viewport (393x852); 56px high-stakes Approve button"
    - "Print preview at /financials/pay-apps/[id]/print uses pure CSS @page + @media print (preserved from 1.5b)"
    - "Bills/Pay Apps terminology rendered in production routes (UI labels say Bill / Pay App, not Invoice / Draw)"
  artifacts:
    - path: "src/components/prototypes/DrawApprovalView.tsx"
      provides: "Pay App approval Document Review component (formerly Draw approval)"
      contains: "DrawApprovalViewProps"
    - path: "src/components/prototypes/DrawPrintView.tsx"
      provides: "Pay App print preview component (G702 cover + G703 detail)"
      contains: "DrawPrintViewProps"
    - path: "src/components/prototypes/ScheduleView.tsx"
      provides: "Schedule (Gantt) view component"
      contains: "ScheduleViewProps"
    - path: "src/components/prototypes/VendorListView.tsx"
      provides: "Vendor list-detail layout component (17 vendors)"
      contains: "VendorListViewProps"
    - path: "src/components/prototypes/VendorDetailView.tsx"
      provides: "Vendor detail Document Review component"
      contains: "VendorDetailViewProps"
    - path: "src/components/prototypes/DocumentReviewView.tsx"
      provides: "Generic Document Review component"
      contains: "DocumentReviewViewProps"
    - path: "src/components/prototypes/OwnerDashboardView.tsx"
      provides: "Homeowner-facing dashboard"
      contains: "OwnerDashboardViewProps"
    - path: "src/components/prototypes/OwnerDrawView.tsx"
      provides: "Owner-facing pay app approval"
      contains: "OwnerDrawViewProps"
    - path: "src/components/prototypes/MobileApprovalView.tsx"
      provides: "Mobile invoice/bill approval"
      contains: "MobileApprovalViewProps"
    - path: "src/components/prototypes/ReconciliationView.tsx"
      provides: "Reconciliation strawman component"
      contains: "ReconciliationViewProps"
    - path: "src/app/financials/bills/[id]/page.tsx"
      provides: "Production /financials/bills/[id] mounting InvoiceReviewView wrapped in .design-system-scope (D-19)"
      contains: "design-system-scope"
    - path: "src/app/financials/pay-apps/[id]/page.tsx"
      provides: "Production /financials/pay-apps/[id] mounting DrawApprovalView wrapped in .design-system-scope"
      contains: "design-system-scope"
    - path: "src/app/owner-portal/page.tsx"
      provides: "Production /owner-portal mounting OwnerDashboardView wrapped in .design-system-scope"
      contains: "design-system-scope"
  key_links:
    - from: "src/components/prototypes/*View.tsx (all 10 NEW)"
      to: "src/app/design-system/_fixtures/drummond/types.ts"
      via: "type-only import"
      pattern: "import type"
    - from: "src/components/prototypes/*View.tsx (all 10 NEW)"
      to: "src/components/nw/* (Card, Eyebrow, Money, DataRow, Badge, NwButton)"
      via: "named imports"
      pattern: "components/nw"
    - from: "src/app/financials/bills/[id]/page.tsx + 11 other production wrappers"
      to: "src/components/prototypes/*View.tsx + Caldwell fixtures"
      via: "thin wrapper pattern with .design-system-scope outer wrap (D-19)"
      pattern: "design-system-scope"
    - from: "9 existing /jobs/[id]/*/page.tsx"
      to: "no inline JobTabs (REMOVED per iter-2 must-fix CRITICAL #2)"
      via: "Plan 5's /jobs/[id]/layout.tsx PerJobTabs replaces"
      pattern: "JobTabs"
---

# Plan 3 — extraction-production-routes (1.5c iter-3 Wave 2)

## iter-3 amendments (per Jake nwrp46 / Q-iter2-1 = a + SEC-10)

**iter-2 wrap template was mechanically ineffective.** Plan-review iter-2 architect NEW-C-1 + design-pushback REVISE both flagged that `<div className="design-system-scope">` alone does NOT activate Site Office C rules because (a) `design-system.css` was not bundled into production routes (only loaded by `/design-system/*` layouts via side-effect import), and (b) the CSS rules are double-gated on `.design-system-scope[data-direction="C"]`. iter-3 corrections:

1. **Wrap template UPDATED** (already inline at the wrapper template below): now reads `<div data-direction="C" data-palette="B" className="design-system-scope">` mirroring `src/app/design-system/prototypes/layout.tsx:39`.
2. **Plan 1 imports design-system.css from root layout** (per D-23/D-057). Plan 3 wrap activates Site Office C rules only if Plan 1's CSS import has landed (it will — Plan 3 depends on Plan 2, which depends on Plan 1, so root layout import lands first).
3. **Watchpoint #1 verify EXTENDED** (per D-23 Plan 3 verify):
   - `grep -E "data-direction=\"C\"" src/app/<production-route>/page.tsx` returns >=1 (per template above)
   - `grep -cE "globals\.css|design-system\.css" src/app/layout.tsx` returns 2 (existing globals.css + new design-system.css). Note: `grep -c "design-system.css"` alone returns 1 (substring only matches new import).
   - Visual diff at 3 fixed viewports — production renders 1px slate-tile left-stamp on cards + UPPERCASE 0.18em JetBrains Mono eyebrow + Space Grotesk Medium 500 headline + 16px Site Office C compact card padding. Side-by-side with `/design-system/prototypes/<route>` should be IDENTICAL (modulo TD-fix-attributable differences).

**SEC-10 follow-up (per security iter-2 LOW finding + D-26):** Plan 3 verify block ADDS defensive grep:
```
grep -rE 'data-org-id|data-org-name|data-tenant' src/app/financials/ src/app/jobs/ src/app/owner-portal/ src/app/people/
```
Returns 0. Confirms production wrappers don't introduce DOM tenant-context attributes (.design-system-scope is a CSS scope class only).

---

<objective>
Refactor 9 remaining prototype components into props-only View components per Plan 2 exemplars (InvoiceReviewView for Document Review pattern + BudgetView for multi-fixture aggregation pattern), apply structural TD-20/25/26 fixes, mount 12 production routes as thin wrappers WRAPPED in `.design-system-scope` class (per D-19 Decision A), rewrite 11 portfolio prototype routes as thin wrappers, REMOVE inline JobTabs from 9 existing /jobs/[id]/*/page.tsx files (iter-2 must-fix CRITICAL #2), verify getCurrentMembership() preservation across re-mounted routes (iter-2 mechanical #10), and run side-by-side D-07 visual verification on all 12 pairs at 3 viewports.

Per nwrp44: this is the parallel implementers wave. Plan 2 produced TWO exemplars (InvoiceReviewView + BudgetView per iter-2 W-2). Plan 3 spawns 4-6 parallel agents to refactor the remaining 9 components + mount 12 production routes + verify all 12 pairs visually. Halt after Plan 3 so Jake can verify D-07 visual parity holds across all 12 pairs before Wave 3 spawns.

iter-2 differences from iter-1 (per Jake nwrp45 + plan-review iter-1 findings):

1. **iter-2 must-fix CRITICAL #1 — D-19 Decision A: .design-system-scope wrap on production routes.** Plan-review iter-1 design-pushback CRITICAL 1 found that Site Office direction-aware selectors at `src/app/design-system/design-system.css:84-153` are ALL scoped to `.design-system-scope`. Outside scope, `[data-slot="card"]` and `.nw-direction-headline` are inert. Production routes WITHOUT scope-wrap render WITHOUT 1px slate-tile left-stamp, WITHOUT direction-aware headlines, WITHOUT padding adjustments. D-07 acceptance (production renders identically to prototype) is broken by construction. Decision A: each production wrapper wraps its outer container in `.design-system-scope` className. Extends scope's meaning from "playground-only" to "Site Office consumer."

2. **iter-2 must-fix CRITICAL #2 — JobTabs removal from 9 existing pages.** iter-1 plan didn't address that 9 existing `/jobs/[id]/*/page.tsx` files import + mount `<JobTabs>` inline (verified via grep at plan time). Plan 5 adds `<PerJobTabs>` in `/jobs/[id]/layout.tsx`. WITHOUT removing inline JobTabs, every `/jobs/[id]/*` page would render BOTH tab bars stacked. Plan 3 explicitly removes the `<JobTabs>` import + render line per page (simpler than refactoring to consume layout). Affected: page.tsx, draws/page.tsx, budget/page.tsx, invoices/page.tsx, change-orders/page.tsx, activity/page.tsx, lien-releases/page.tsx, internal-billings/page.tsx, purchase-orders/page.tsx (9 total per plan-time grep). Goal: zero double-tab-bar rendering. Verify: smoke test all 9 routes confirming no double tabs.

3. **iter-2 mechanical #10 — getCurrentMembership preserve verbatim:** Plan 3's production wrappers mount Views with Caldwell fixtures, but for routes that ALREADY had real-backend production page.tsx (e.g., /jobs/[id]/budget if one exists), the new mount must REPLACE the existing — preserving any auth check (`getCurrentMembership()`, `org_id`, `requireMembership`) verbatim. Per-route grep diff verify: `diff <(grep -E 'getCurrentMembership|org_id|requireMembership' src/app/<source>/page.tsx | sort) <(grep -E ... src/app/<destination>/page.tsx | sort)` expects zero diff. Apply for the 7 Plan 3 routes that potentially had existing implementations.

4. **iter-2 watchpoint #1 — visual diff verify D-19:** Task 4 explicitly screenshots production route before/after .design-system-scope wrap. Expect 1px slate-tile left-stamp + JetBrains Mono UPPERCASE eyebrow + Space Grotesk Medium headline now appear on production after wrap.

5. **iter-2 architect W-3 method lock for D-07:** Plan 3 Task 4 uses Chrome DevTools at 3 fixed viewports (1920x1080, 1280x800, 393x852) + TD-fix-attributable checklist (acceptable differences vs unexpected divergence) per Plan 2 precedent.

6. **iter-2 architect W-5 — REAL vs PLACEHOLDER per route:** Production wrapper SUMMARY explicitly identifies whether the route was a placeholder destination (mounting prototype = thin wrapper for 1.5c per CONTEXT D-14 Q8=A) or had pre-existing logic (in which case the existing logic is BACKED UP for F1 reference and replaced by thin wrapper).

7. **iter-2 multi-tenant W-1 — Owner Portal posture clarified:** D-08's "owner_view role gate" claim is misleading. iter-2 plans clarify: 1.5c is structural — Owner Portal at `/owner-portal/*` mounts thin-wrapper components with Caldwell fixtures. NO real auth flow exercised in 1.5c. F1 wires real backend via either (a) extending `client_portal_access` token model (SHA-256 + service-role-API + anon-grant per migration 00074) OR (b) introducing `owner_view` member role with RLS. NOT both. Plan 7 ARCHITECTURE.md (or new section in CONTEXT.md) documents this constraint so F1 inherits.

The single highest-risk task in Stage 1.5c is the D-07 visual verification gate. Per CONTEXT D-07 + nwrp44 Amendment 2 + iter-2 watchpoint #1: divergence = CRITICAL halt criterion. This plan encodes that gate as Task 5.

Purpose:
- Mechanical extraction of 9 prototype components following Plan 2's 2 exemplars.
- 12 production routes wired with new IA paths from Plan 1, wrapped in .design-system-scope per D-19.
- Bills/Pay Apps terminology applied throughout.
- D-07 visual verification gate — production-vs-portfolio parity confirmed at 3 viewports.
- JobTabs inline mounts REMOVED per iter-2 must-fix #2.
- getCurrentMembership preservation per iter-2 mechanical #10.

Output:
- 10 NEW Views in src/components/prototypes/ (DrawApprovalView, DrawPrintView, ScheduleView, VendorListView, VendorDetailView, DocumentReviewView, OwnerDashboardView, OwnerDrawView, MobileApprovalView, ReconciliationView)
- 12 NEW production route page.tsx files mounting the Views as thin wrappers WITH .design-system-scope wrap
- 11 REWRITTEN portfolio /design-system/prototypes/* page.tsx files as thin wrappers
- 9 EXISTING /jobs/[id]/* files with JobTabs inline mount REMOVED
- D-07 visual verification report in plan SUMMARY at 3 viewports (any divergence = CRITICAL halt)
- HALT for Jake review of D-07 verification + integration smoke test
</objective>

<execution_context>
@C:/Users/Jake/nightwork-platform/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jake/nightwork-platform/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/stage-1.5c-information-architecture/CONTEXT.md
@.planning/phases/stage-1.5c-information-architecture/PATTERNS.md
@.planning/phases/stage-1.5c-information-architecture/01.5c-1-nav-today-redirects-PLAN.md
@.planning/phases/stage-1.5c-information-architecture/01.5c-2-thin-wrapper-architect-PLAN.md
@.planning/phases/stage-1.5c-information-architecture/AUDIT-LOG-STRATEGY.md
@.planning/expansions/stage-1.5c-information-architecture-EXPANDED-SCOPE.md
@.planning/phases/stage-1.5b-prototype-gallery/CONTEXT.md
@.planning/phases/stage-1.5b-prototype-gallery/findings.md
@.planning/phases/stage-1.5b-prototype-gallery/01.5-2-invoice-draw-vendors-PLAN.md
@.planning/phases/stage-1.5b-prototype-gallery/01.5-3-budget-documents-PLAN.md
@.planning/phases/stage-1.5b-prototype-gallery/01.5-4-owner-mobile-PLAN.md
@.planning/phases/stage-1.5b-prototype-gallery/01.5-5-schedule-gantt-PLAN.md
@.planning/phases/stage-1.5b-prototype-gallery/01.5-6-print-reconciliation-PLAN.md
@CLAUDE.md
@.planning/design/SYSTEM.md
@.planning/design/COMPONENTS.md
@.planning/design/PATTERNS.md
@.planning/design/CHOSEN-DIRECTION.md
@src/app/design-system/_fixtures/drummond/index.ts
@src/app/design-system/_fixtures/drummond/types.ts
@src/components/prototypes/InvoiceReviewView.tsx
@src/components/prototypes/BudgetView.tsx
@src/components/nw/Button.tsx
@src/components/nw/Card.tsx
@src/components/nw/Eyebrow.tsx
@src/components/nw/Money.tsx
@src/components/nw/DataRow.tsx
@src/components/nw/Badge.tsx
@src/middleware.ts
@src/app/design-system/design-system.css
@src/components/job-tabs.tsx
@src/app/jobs/[id]/page.tsx
@src/app/jobs/[id]/draws/page.tsx
@src/app/jobs/[id]/budget/page.tsx
@src/app/jobs/[id]/invoices/page.tsx
@src/app/jobs/[id]/change-orders/page.tsx
@src/app/jobs/[id]/activity/page.tsx
@src/app/jobs/[id]/lien-releases/page.tsx
@src/app/jobs/[id]/internal-billings/page.tsx
@src/app/jobs/[id]/purchase-orders/page.tsx

<interfaces>
The 9 remaining prototype source files (extraction sources):
- src/app/design-system/prototypes/draws/[id]/page.tsx (DrawApprovalView source)
- src/app/design-system/prototypes/draws/[id]/print/page.tsx (DrawPrintView source)
- src/app/design-system/prototypes/jobs/[id]/schedule/page.tsx (ScheduleView source)
- src/app/design-system/prototypes/vendors/page.tsx (VendorListView source)
- src/app/design-system/prototypes/vendors/[id]/page.tsx (VendorDetailView source)
- src/app/design-system/prototypes/documents/[id]/page.tsx (DocumentReviewView source)
- src/app/design-system/prototypes/owner-portal/page.tsx (OwnerDashboardView source)
- src/app/design-system/prototypes/owner-portal/draws/[id]/page.tsx (OwnerDrawView source)
- src/app/design-system/prototypes/mobile-approval/page.tsx (MobileApprovalView source)
- src/app/design-system/prototypes/reconciliation/page.tsx (ReconciliationView source)

(BudgetView already shipped in Plan 2 per iter-2 W-2; ScheduleView extracted in Plan 3 — slightly different from BudgetView's KPI strip + DataGrid since Schedule uses TanStack Table v8 with custom timeline cell renderer.)

Plan 2 exemplars (canonical extraction shapes — Plan 3 implementers MUST follow):
- src/components/prototypes/InvoiceReviewView.tsx (Document Review pattern)
- src/components/prototypes/BudgetView.tsx (multi-fixture aggregation pattern; iter-2 W-2)

Production route wrapper template (per Plan 2 + iter-2 D-19 .design-system-scope wrap):

```typescript
// src/app/<canonical-path>/page.tsx
import { notFound } from "next/navigation";
import { CALDWELL_X, CALDWELL_Y } from "@/app/design-system/_fixtures/drummond";
import <Name>View from "@/components/prototypes/<Name>View";

export default function <Name>Page({ params }: { params: { id: string } }) {
  const primary = CALDWELL_X.find((x) => x.id === params.id);
  if (!primary) return notFound();
  // ... resolve related data
  return (
    // iter-2 must-fix CRITICAL #1: .design-system-scope wrap (per D-19/D-23)
    // Extends Site Office direction-aware selectors from playground-only to production consumer.
    // iter-3 correction (per Jake nwrp46 / Q-iter2-1=a): wrap MUST mirror playground layout
    // (src/app/design-system/prototypes/layout.tsx:39) — both data-direction="C" + data-palette="B"
    // attributes are required to activate Site Office C rules. Plan 1 also imports
    // design-system.css from root layout.tsx so the CSS bundles into production.
    <div data-direction="C" data-palette="B" className="design-system-scope">
      <<Name>View
        primary={primary}
        related={...}
        breadcrumbRoot={{ href: "/financials/bills", label: "Bills" }}
      />
    </div>
  );
}
```

Per CONTEXT D-08 (Owner Portal — clarified per iter-2 multi-tenant W-1):
  Owner Portal at /owner-portal/* gated to authenticated org members per existing middleware. 1.5c is structural — Caldwell fixtures only. The role check is mechanical — middleware already differentiates owner from non-owner via useCurrentRole() / getCurrentMembership(). 1.5c does NOT modify middleware.
  
  F1 commits to ONE auth path (not both):
  - Path A: extend client_portal_access token model (SHA-256 + service-role-API + anon-grant per migration 00074)
  - Path B: introduce owner_view member role with RLS for owner-scoped reads
  
  Plan 7 ARCHITECTURE.md / OWNER-PORTAL-CUTOVER.md documents this constraint so F1 inherits it.

Per CONTEXT D-04 + Plan 1 mobile chrome:
  Mobile approval prototype runs at iPhone viewport (393x852). NwButton size="lg-touch" enforces 56px for high-stakes Approve button per SYSTEM.md §11.

Per CONTEXT D-01 (Bills/Pay Apps rename):
  All UI labels in extracted Views use Bill / Pay App terminology. Where source uses "Invoice approval" → "Bill approval"; "Draw approval" → "Pay App approval"; "Draw print" → "Pay App print"; etc. Component file names retain Draw* / Invoice* internally for code-search continuity.

NwButton API (verify at execute time per src/components/nw/Button.tsx).

Site Office tokens (per SYSTEM.md):
  - UPPERCASE 0.18em eyebrows
  - Space Grotesk Medium 500 headlines (NOT 600 — TD-26 fix)
  - JetBrains Mono 0.18em tracking
  - Card padding="md" (NOT "lg" — TD-25 fix)
  - 1px slate-tile left-stamp on cards (CSS rule, automatic — ACTIVE inside .design-system-scope per D-19 wrap)
  - 150ms ease-out motion (no bouncy)
  - NwButton (NOT raw button — TD-20 fix)
  - No hardcoded hex

JobTabs source (existing component):
  src/components/job-tabs.tsx — 9 existing /jobs/[id]/*/page.tsx files import + mount this inline.

  9 affected files (verified at plan time):
  - src/app/jobs/[id]/page.tsx (line 8 import + line 316 render `<JobTabs jobId={job.id} active="overview" />`)
  - src/app/jobs/[id]/draws/page.tsx (line 6 import + line 131 render `active="draws"`)
  - src/app/jobs/[id]/budget/page.tsx (line 6 import + line 710 render `active="budget"`)
  - src/app/jobs/[id]/invoices/page.tsx (line 6 import + line 181 render `active="invoices"`)
  - src/app/jobs/[id]/change-orders/page.tsx (line 6 import + line 199 render `active="change-orders"`)
  - src/app/jobs/[id]/activity/page.tsx (line 5 import + line 36 render `active="activity"`)
  - src/app/jobs/[id]/lien-releases/page.tsx (line 6 import + line 179 render `active="draws"`)
  - src/app/jobs/[id]/internal-billings/page.tsx (line 6 import + line 556 render `active="budget"`)
  - src/app/jobs/[id]/purchase-orders/page.tsx (line 6 import + line 194 render `active="budget"`)

  iter-2 must-fix CRITICAL #2 — Task 4 removes:
  (a) `import JobTabs from "@/components/job-tabs";` line at top of each
  (b) `<JobTabs jobId={...} active={...} />` render line in each
  
  PerJobTabs in Plan 5's /jobs/[id]/layout.tsx is the sole tab surface post-1.5c.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract 10 remaining prototype components to props-only Views (parallel implementers; Plan 2 exemplars precede)</name>
  <files>src/components/prototypes/DrawApprovalView.tsx, src/components/prototypes/DrawPrintView.tsx, src/components/prototypes/ScheduleView.tsx, src/components/prototypes/VendorListView.tsx, src/components/prototypes/VendorDetailView.tsx, src/components/prototypes/DocumentReviewView.tsx, src/components/prototypes/OwnerDashboardView.tsx, src/components/prototypes/OwnerDrawView.tsx, src/components/prototypes/MobileApprovalView.tsx, src/components/prototypes/ReconciliationView.tsx</files>

  <read_first>
    - src/components/prototypes/InvoiceReviewView.tsx (FULL file — Plan 2 exemplar — Document Review pattern)
    - src/components/prototypes/BudgetView.tsx (FULL file — Plan 2 exemplar — multi-fixture aggregation pattern; iter-2 W-2)
    - .planning/phases/stage-1.5c-information-architecture/01.5c-2-thin-wrapper-architect-PLAN.md (Tasks 2 + 3 — 2 extraction templates)
    - .planning/phases/stage-1.5c-information-architecture/AUDIT-LOG-STRATEGY.md (T10c hook posture from Plan 2)
    - .planning/phases/stage-1.5b-prototype-gallery/findings.md (TD-20/21/22/23/24/25/26/27/28 file references — only TD-20/25/26 in scope per CONTEXT D-06)
    - For each extraction, read the corresponding source file FULLY (10 source files listed in `<interfaces>`)
    - src/app/design-system/_fixtures/drummond/types.ts (all Caldwell types)
    - src/components/nw/Button.tsx (NwButton actual API for TD-20 fix)
    - src/components/nw/Card.tsx + Eyebrow.tsx + Money.tsx + DataRow.tsx + Badge.tsx (other primitives)
  </read_first>

  <action>
**Per nwrp44 standing rule "Independent work → parallel batches of 6-8 agents per wave":** Suitable for 4-6 parallel implementer agents.

**Suggested parallel split:**

Batch A (financial — Pay Apps gold-standard pattern):
- DrawApprovalView (Document Review pattern; analog to InvoiceReviewView)
- DrawPrintView (specialized print preview; @media print stylesheet)

Batch B (operational data-grid):
- ScheduleView (TanStack Table v8 + custom timeline cell renderer; multi-fixture aggregation pattern variant)

Batch C (vendors + documents):
- VendorListView (List+Detail; 17 vendors)
- VendorDetailView (Document Review pattern)
- DocumentReviewView (handles 3 document types via discriminator)

Batch D (owner-facing + mobile):
- OwnerDashboardView (simplified Pattern3Dashboard for non-builder)
- OwnerDrawView (Document Review with vendor-name transparency)
- MobileApprovalView (iPhone viewport; 56px high-stakes)

Batch E (reconciliation):
- ReconciliationView (4 candidates × 2 drift types)

**Per implementer — extraction recipe:**

1. Open source file in full. Inventory:
   - Fixture imports at top
   - Type imports
   - UI primitive imports
   - Internal lookups (.find(), .filter() against fixture arrays)
   - Raw button elements (TD-20 scope)
   - Card padding=lg usages (TD-25 scope)
   - fontWeight: 600 inline styles (TD-26 scope)
   - Hardcoded hex (privacy gate; expect 0)

2. Determine View props shape. What data does the component need?

3. Create src/components/prototypes/<Name>View.tsx following the Plan 2 exemplar template. Choose:
   - Document Review pattern (InvoiceReviewView shape) for: DrawApprovalView, VendorDetailView, DocumentReviewView, OwnerDrawView, MobileApprovalView
   - Multi-fixture aggregation pattern (BudgetView shape) for: ScheduleView, VendorListView, OwnerDashboardView, ReconciliationView
   - Specialized (print preview): DrawPrintView (preserves @media print stylesheet)

4. Verify View has zero runtime fixture imports. `grep -n "from \"@/app/design-system/_fixtures/drummond\"" <NewViewFile>.tsx` returns only `import type` lines.

5. Apply Bills/Pay Apps terminology in user-visible labels per CONTEXT D-01:
   - "Invoice" → "Bill" (UI labels)
   - "Draw" → "Pay App" (UI labels)
   - "Approve invoice" → "Approve bill"
   - Component file names + variable names retain Draw* / Invoice* internally per CONTEXT D-01 scope clarification.

6. Per-component nuances:

   - **DrawApprovalView (Pay App):** All UI labels say "Pay App". Breadcrumb says "Pay Apps". Status timeline reads "Pay App submitted".
   - **DrawPrintView (Pay App print):** G702/G703 layout preserved. Print stylesheet preserved. Header text "Pay Application #N" (already AIA-correct).
   - **ScheduleView:** Custom timeline cell renderer preserved (TanStack Table v8 base). Today-marker preserved. Milestones render as diamonds. Status colors via Site Office tokens.
   - **VendorListView:** 17 Caldwell vendors render in List+Detail. Long names preserved.
   - **VendorDetailView:** Document Review pattern. Audit timeline currently missing per TD-23 — DEFERRED per CONTEXT D-06.
   - **DocumentReviewView:** Type discriminator (plans / contracts / lien releases). All 4 Florida statute types render correctly for lien releases.
   - **OwnerDashboardView:** Simplified Pattern3Dashboard. Cost-plus open-book transparency language. NO builder jargon.
   - **OwnerDrawView:** Owner-facing Pay App approval. Vendor-name visible. Audit timeline currently missing per TD-24 — DEFERRED.
   - **MobileApprovalView:** iPhone viewport. 56px high-stakes Approve button (NwButton size="lg-touch"). Reject button currently uses rgba inline per TD-21 — DEFERRED.
   - **ReconciliationView:** 4 candidates × 2 drift types = 8 prototypes preserved. Section anchors enable scroll-to-section comparison.

**Token discipline (apply uniformly):**
- All colors: `var(--...)` (no hex except where TD-21 deferred — and ONLY in mobile-approval, NOT introduced fresh elsewhere)
- All fonts: `var(--font-space-grotesk)`, `var(--font-jetbrains-mono)`, `var(--font-inter)`
- Site Office signature: UPPERCASE eyebrows + 0.18em tracking + 150ms ease-out motion
- Tenant-blind: NO `org_id` / `membership` / `vendor_id` / similar tenant-keying props on Views
  </action>

  <verify>
    <automated>npm run build &amp;&amp; ls src/components/prototypes/*View.tsx | wc -l</automated>
    Expected: build exits 0; ls returns 12 (10 NEW + InvoiceReviewView + BudgetView from Plan 2).

    Per-component checks (run for each of 10 NEW Views):
    - Type-only fixture imports: `grep -E "^import type" src/components/prototypes/<Name>View.tsx` returns >=1
    - Runtime fixture imports: `grep -E "^import \{[^}]*\} from \"@/app/design-system/_fixtures/drummond\"" src/components/prototypes/<Name>View.tsx` returns 0
    - TD-20 fix: `grep -cE '<button[^>]*type="button"' src/components/prototypes/<Name>View.tsx` returns 0
    - TD-25 fix: `grep -cE 'padding="lg"' src/components/prototypes/<Name>View.tsx` returns 0
    - TD-26 fix: `grep -cE 'fontWeight:\s*600' src/components/prototypes/<Name>View.tsx` returns 0
    - Tenant-blind: `grep -E 'org_id|membership' src/components/prototypes/<Name>View.tsx` returns 0
    - No hex (except TD-21 deferred in MobileApprovalView): `grep -nE '#[0-9a-fA-F]{3,6}' src/components/prototypes/<Name>View.tsx | grep -v 'var(--' | wc -l` returns 0 (or 1 for MobileApprovalView)

    Privacy gate: `grep -rE 'Drummond|501 74th|...' src/components/prototypes/ | wc -l` returns 0.

    Hooks silent: post-edit hook should not reject any of the 10 new files.

    NwButton replacements visible (Chrome DevTools MCP):
    - DrawApprovalView header: Approve / Reject / Submit buttons render as NwButton
    - OwnerDrawView: Approve button uses NwButton
    - MobileApprovalView: Approve button uses NwButton size="lg-touch" (56px high-stakes)
  </verify>

  <done>
    - 10 NEW View components created
    - All Views are props-only (no runtime fixture imports)
    - TD-20 / TD-25 / TD-26 structural fixes applied uniformly
    - Bills/Pay Apps terminology in user-visible labels
    - Cosmetic TDs (TD-21/22/23/24/27/28) explicitly NOT addressed (deferred per CONTEXT D-06)
    - Build passes
    - Privacy gate clean
    - Hooks silent
  </done>
</task>

<task type="auto">
  <name>Task 2: Mount 12 production routes as thin wrappers WITH .design-system-scope wrap (per D-19 Decision A — iter-2 must-fix CRITICAL #1)</name>
  <files>src/app/financials/bills/[id]/page.tsx, src/app/financials/pay-apps/[id]/page.tsx, src/app/financials/pay-apps/[id]/print/page.tsx, src/app/financials/reconciliation/page.tsx, src/app/jobs/[id]/budget/page.tsx, src/app/jobs/[id]/schedule/page.tsx, src/app/people/vendors/page.tsx, src/app/people/vendors/[id]/page.tsx, src/app/jobs/[id]/documents/[documentId]/page.tsx, src/app/owner-portal/page.tsx, src/app/owner-portal/pay-apps/[id]/page.tsx, src/app/jobs/[id]/mobile-approval/page.tsx</files>

  <read_first>
    - src/app/design-system/prototypes/invoices/[id]/page.tsx (Plan 2 wrapper template)
    - src/components/prototypes/*View.tsx (all 12 Views from Plan 2 + Task 1)
    - src/middleware.ts (full file — auth + role gate posture)
    - .planning/phases/stage-1.5c-information-architecture/CONTEXT.md (D-08 Owner Portal posture clarified per iter-2 multi-tenant W-1; D-14 no real-backend wiring; D-19 .design-system-scope wrap)
    - For each route, read the EXISTING production route at the corresponding path (if exists) — for jobs/[id]/budget there's possibly an existing /jobs/[id]/budget; verify and decide whether to REPLACE or AUGMENT.
    - src/app/design-system/design-system.css (lines 84-153 — direction-aware selectors scoped to .design-system-scope; verify D-19 wrap propagates Site Office signatures correctly)
  </read_first>

  <action>
**Per CONTEXT D-14 (Q8=A): no real-backend wiring promotion in 1.5c. Production routes mount Caldwell fixtures.** F1 swaps fixtures for real data.

**Per iter-2 must-fix CRITICAL #1 + D-19 Decision A:** each production wrapper wraps its outer container in `.design-system-scope` className. Without this wrap, Site Office direction-aware CSS rules (1px slate-tile left-stamp + headline scaling + padding adjustments) are inert outside `.design-system-scope`.

**Per nwrp44: 4-6 parallel implementers can split the 12 production routes.**

**Wrapper template (Plan 2 exemplar + iter-2 D-19 wrap):**

```typescript
// src/app/<canonical-path>/page.tsx
import { notFound } from "next/navigation";
import {
  CALDWELL_X,
  CALDWELL_Y,
} from "@/app/design-system/_fixtures/drummond";
import <Name>View from "@/components/prototypes/<Name>View";

export default function <CanonicalName>Page({ params }: { params: { id: string } }) {
  const primary = CALDWELL_X.find((x) => x.id === params.id);
  if (!primary) return notFound();
  // ... resolve related data
  return (
    // iter-2 must-fix CRITICAL #1: .design-system-scope wrap (per D-19 Decision A)
    // Without this wrap, [data-slot="card"] + .nw-direction-headline are inert
    // outside the scope (verified via /design-system/design-system.css:84-153).
    <div className="design-system-scope">
      <<Name>View
        primary={primary}
        related={...}
        breadcrumbRoot={{ href: "/financials/bills", label: "Bills" }}
      />
    </div>
  );
}
```

**Per-route implementations:**

1. **src/app/financials/bills/[id]/page.tsx** — mounts InvoiceReviewView (Plan 2 exemplar) + .design-system-scope wrap. Breadcrumb root: `{ href: "/financials/bills", label: "Bills" }`.

2. **src/app/financials/pay-apps/[id]/page.tsx** — mounts DrawApprovalView + .design-system-scope wrap. Breadcrumb root: `{ href: "/financials/pay-apps", label: "Pay Apps" }`. Loads CALDWELL_DRAWS + CALDWELL_DRAW_LINE_ITEMS + CALDWELL_COST_CODES + CALDWELL_CHANGE_ORDERS + CALDWELL_JOBS.

3. **src/app/financials/pay-apps/[id]/print/page.tsx** — mounts DrawPrintView + .design-system-scope wrap. Same data as #2 + print stylesheet inheritance.

4. **src/app/financials/reconciliation/page.tsx** — mounts ReconciliationView + .design-system-scope wrap. Loads CALDWELL_RECONCILIATION_PAIRS. No `[id]` — single-page route.

5. **src/app/jobs/[id]/budget/page.tsx** — mounts BudgetView (Plan 2 exemplar) + .design-system-scope wrap.
   - **iter-2 architect W-5 + mechanical #10:** Existing `src/app/jobs/[id]/budget/page.tsx` ALREADY exists in production code (per JobTabs verified mounts at line 6+710). The new mount must REPLACE it (per Q8=A: production = Caldwell fixture in 1.5c; F1 swaps in real data). 
   - **BACKUP existing content** as a comment in the SUMMARY for F1 swap reference (specifically: any getCurrentMembership() / org_id / requireMembership calls).
   - **iter-2 mechanical #10 verify:** `diff <(grep -E 'getCurrentMembership|org_id|requireMembership' src/app/jobs/[id]/budget/page.tsx.bak | sort) <(grep -E 'getCurrentMembership|org_id|requireMembership' src/app/jobs/[id]/budget/page.tsx | sort)` — preserve verbatim. If existing page used auth checks, the new page MUST preserve them (or document why removal is safe — e.g., F1 wires real auth + Caldwell fixtures don't require auth scoping).
   - **iter-2 must-fix CRITICAL #2 — JobTabs removal:** the existing /jobs/[id]/budget/page.tsx imports + renders `<JobTabs>` at line 6 + 710. After replacing the page entirely with the Plan 3 mount, the JobTabs import is GONE (the new mount uses BudgetView wrapped in .design-system-scope; PerJobTabs from Plan 5's layout.tsx provides the tab surface). Document the JobTabs removal in SUMMARY.

6. **src/app/jobs/[id]/schedule/page.tsx** — mounts ScheduleView + .design-system-scope wrap. Loads CALDWELL_SCHEDULE_ITEMS + CALDWELL_VENDORS + CALDWELL_JOBS. Likely a NEW production route (no existing implementation; verify).

7. **src/app/people/vendors/page.tsx** — mounts VendorListView + .design-system-scope wrap. Loads CALDWELL_VENDORS + CALDWELL_INVOICES (for activity). Replaces existing /vendors route (which 301-redirects to here per Plan 1).

8. **src/app/people/vendors/[id]/page.tsx** — mounts VendorDetailView + .design-system-scope wrap. Loads CALDWELL_VENDORS + CALDWELL_INVOICES + CALDWELL_LIEN_RELEASES.

9. **src/app/jobs/[id]/documents/[documentId]/page.tsx** — mounts DocumentReviewView + .design-system-scope wrap. Loads CALDWELL_LIEN_RELEASES + CALDWELL_VENDORS + CALDWELL_INVOICES (for type discriminator).

10. **src/app/owner-portal/page.tsx** — mounts OwnerDashboardView + .design-system-scope wrap. Loads CALDWELL_DRAWS + CALDWELL_JOBS + CALDWELL_CHANGE_ORDERS.
    - **Per CONTEXT D-08 + iter-2 multi-tenant W-1 clarification:** owner role gated via existing middleware. 1.5c is structural. Caldwell fixtures only. F1 commits to ONE auth path (client_portal_access tokens OR owner_view role with RLS). Plan 7 documents this constraint.
    - 1.5c does NOT modify middleware.

11. **src/app/owner-portal/pay-apps/[id]/page.tsx** — mounts OwnerDrawView + .design-system-scope wrap. Loads CALDWELL_DRAW_LINE_ITEMS + CALDWELL_INVOICES + CALDWELL_VENDORS + CALDWELL_COST_CODES.

12. **src/app/jobs/[id]/mobile-approval/page.tsx** — mounts MobileApprovalView + .design-system-scope wrap. Loads CALDWELL_INVOICES + CALDWELL_VENDORS + CALDWELL_COST_CODES.

**Each wrapper:**
- Imports CALDWELL_* fixtures (legitimate temporary 1.5c stand-in per CONTEXT D-14 + Plan 2 architect's T10c finding)
- Calls notFound() on missing data
- WRAPS outer container in `<div className="design-system-scope">` per iter-2 must-fix #1 / D-19
- Passes resolved data via props to the View
- Sets breadcrumbRoot to canonical production path

**No business logic in wrappers.** All rendering is in the View. The wrapper is mechanical: lookup + .design-system-scope wrap + notFound() + props pass.

**iter-2 mechanical #10 — getCurrentMembership preserve verbatim:**

For each of these 7 routes that potentially have existing implementations:
- /jobs/[id]/budget (CONFIRMED exists — JobTabs at line 6+710)
- /jobs/[id]/schedule (verify — likely NEW)
- /people/vendors (verify — original at /vendors; current existence at /people/vendors unlikely)
- /people/vendors/[id] (verify)
- /jobs/[id]/documents/[documentId] (verify)
- /owner-portal (verify — likely NEW for owner role)
- /jobs/[id]/mobile-approval (verify)

Run `diff <(grep -E 'getCurrentMembership|org_id|requireMembership' <existing-page.tsx> | sort) <(grep -E 'getCurrentMembership|org_id|requireMembership' <new-page.tsx> | sort)` for each.

Expected behavior:
- For routes that previously had real-backend logic (auth checks present): new mount preserves the auth checks OR explicitly documents why removal is safe (e.g., F1 wires real auth at swap time + Caldwell fixtures don't require auth scoping; SUMMARY lists removed auth surfaces for F1 to re-add).
- For routes that were placeholders (no auth checks): new mount has no auth checks; SUMMARY notes "REAL vs PLACEHOLDER" status per iter-2 architect W-5.

Document each route's "REAL vs PLACEHOLDER" status in SUMMARY.

**iter-2 architect W-5 verification:** SUMMARY explicitly identifies for each Plan 3 production route:
- Was a placeholder before 1.5c → REAL (now Caldwell fixture mount; F1 wires real backend)
- Had pre-existing real-backend logic → SUMMARY backs up the existing logic for F1 reference
  </action>

  <verify>
    <automated>npm run build &amp;&amp; grep -c 'design-system-scope' src/app/financials/bills/[id]/page.tsx &amp;&amp; grep -c 'design-system-scope' src/app/financials/pay-apps/[id]/page.tsx &amp;&amp; grep -c 'design-system-scope' src/app/owner-portal/page.tsx</automated>
    Expected: build exits 0; grep returns >=1 for each (D-19 wrap present in all 12 production wrappers).

    Verification per route (12 routes):
    - View import: `grep -c 'from "@/components/prototypes/"' <route_file>.tsx` returns >=1
    - Fixture import: `grep -c 'from "@/app/design-system/_fixtures/drummond"' <route_file>.tsx` returns 1
    - notFound() call: `grep -c "notFound()" <route_file>.tsx` returns >=1 for routes with [id] params
    - .design-system-scope wrap: `grep -c 'design-system-scope' <route_file>.tsx` returns >=1 (iter-2 D-19)

    iter-2 mechanical #10 — getCurrentMembership grep diff (7 routes):
    For each route that potentially had existing implementation, run:
      `diff <(grep -E 'getCurrentMembership|org_id|requireMembership' src/app/<source>/page.tsx | sort) <(grep -E 'getCurrentMembership|org_id|requireMembership' src/app/<destination>/page.tsx | sort)`
    Expected: zero diff (auth checks preserved verbatim) OR documented reasoning in SUMMARY for any divergence.

    iter-2 architect W-5 — REAL vs PLACEHOLDER per route documented in SUMMARY.

    Manual smoke (Chrome DevTools MCP):
    - Visit each production route — renders the same content as the corresponding portfolio prototype path
    - /people/vendors lists 17 vendors
    - /financials/reconciliation renders 4 candidates × 2 drift types
    - /owner-portal renders the dashboard
    - .design-system-scope wrap visible in DOM inspection (the `<div class="design-system-scope">` outer wrapper)

    Privacy gate: `grep -rE 'Drummond|...' src/app/financials/ src/app/jobs/[id]/budget/ src/app/people/vendors/ src/app/owner-portal/ src/app/jobs/[id]/mobile-approval/ src/app/jobs/[id]/documents/ src/app/jobs/[id]/schedule/ | wc -l` returns 0.

    Hooks silent on all 12 wrappers.
  </verify>

  <done>
    - 12 production page.tsx files (each wrapped in .design-system-scope per D-19)
    - Each wrapper imports its View component
    - Each wrapper imports Caldwell fixtures
    - Each wrapper calls notFound() on missing fixture lookup
    - iter-2 mechanical #10: getCurrentMembership preservation verified per re-mounted route
    - iter-2 architect W-5: REAL vs PLACEHOLDER status documented per route
    - iter-2 multi-tenant W-1: Owner Portal posture clarification documented (Caldwell fixtures only; F1 commits to one auth path)
    - Build passes
    - Privacy gate clean
    - Hooks silent
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite 11 portfolio /design-system/prototypes/* routes as thin wrappers</name>
  <files>src/app/design-system/prototypes/draws/[id]/page.tsx, src/app/design-system/prototypes/draws/[id]/print/page.tsx, src/app/design-system/prototypes/jobs/[id]/schedule/page.tsx, src/app/design-system/prototypes/vendors/page.tsx, src/app/design-system/prototypes/vendors/[id]/page.tsx, src/app/design-system/prototypes/documents/[id]/page.tsx, src/app/design-system/prototypes/owner-portal/page.tsx, src/app/design-system/prototypes/owner-portal/draws/[id]/page.tsx, src/app/design-system/prototypes/mobile-approval/page.tsx, src/app/design-system/prototypes/reconciliation/page.tsx</files>

  <read_first>
    - src/app/design-system/prototypes/invoices/[id]/page.tsx (Plan 2 portfolio wrapper template)
    - src/app/design-system/prototypes/jobs/[id]/budget/page.tsx (Plan 2 portfolio wrapper template — iter-2 W-2)
    - src/app/design-system/prototypes/layout.tsx (locks data-direction=C data-palette=B per 1.5b D-02 — preserved verbatim; .design-system-scope ALREADY in scope here so portfolio wrappers don't need extra wrap)
    - .planning/phases/stage-1.5c-information-architecture/CONTEXT.md (D-03 — portfolio prototype paths stay accessible under platform_admin gate)
    - src/middleware.ts (verify platform_admin gate at /design-system/* per existing middleware semantics)
  </read_first>

  <action>
**Per CONTEXT D-03 + EXPANDED-SCOPE Part 7 B:** "The /design-system/prototypes/* routes from 1.5b stay accessible at their current paths under platform_admin gate. The PRODUCTION routes (without /design-system prefix) wire to the same components."

**Per Plan 2 exemplar:** /design-system/prototypes/invoices/[id]/page.tsx + /design-system/prototypes/jobs/[id]/budget/page.tsx are now thin wrappers. Plan 3 applies the same pattern to the remaining 10 portfolio routes.

**Note on .design-system-scope:** Portfolio wrappers don't need to add .design-system-scope className because /design-system/prototypes/layout.tsx (1.5b D-02) already provides scope context. (Verify this is correct at execute time — if layout.tsx doesn't wrap in scope, portfolio wrappers may need explicit .design-system-scope className too.)

**Portfolio wrapper template:**

```typescript
// src/app/design-system/prototypes/.../page.tsx
import { notFound } from "next/navigation";
import {
  CALDWELL_X,
  // ... same fixtures as production wrapper
} from "@/app/design-system/_fixtures/drummond";
import <Name>View from "@/components/prototypes/<Name>View";

export default function <Name>PrototypePage({ params }: { params: { id: string } }) {
  // Same fixture lookup as production wrapper
  const primary = CALDWELL_X.find((x) => x.id === params.id);
  if (!primary) return notFound();
  // ... resolve related data
  return (
    <<Name>View
      primary={primary}
      related={...}
      breadcrumbRoot={{ href: "/design-system/prototypes/", label: "Prototypes" }}
    />
  );
}
```

**Per-route rewrite plan (mechanical):**

1. /design-system/prototypes/draws/[id]/page.tsx → mounts DrawApprovalView with breadcrumbRoot to /design-system/prototypes/draws
2. /design-system/prototypes/draws/[id]/print/page.tsx → mounts DrawPrintView
3. /design-system/prototypes/jobs/[id]/schedule/page.tsx → mounts ScheduleView
4. /design-system/prototypes/vendors/page.tsx → mounts VendorListView
5. /design-system/prototypes/vendors/[id]/page.tsx → mounts VendorDetailView
6. /design-system/prototypes/documents/[id]/page.tsx → mounts DocumentReviewView
7. /design-system/prototypes/owner-portal/page.tsx → mounts OwnerDashboardView
8. /design-system/prototypes/owner-portal/draws/[id]/page.tsx → mounts OwnerDrawView
9. /design-system/prototypes/mobile-approval/page.tsx → mounts MobileApprovalView
10. /design-system/prototypes/reconciliation/page.tsx → mounts ReconciliationView

(/design-system/prototypes/invoices/[id]/page.tsx + /design-system/prototypes/jobs/[id]/budget/page.tsx already shipped in Plan 2.)

**Per CONTEXT D-03 + Plan 2 architect's T10c finding:**
- Portfolio wrappers REPLACE the original full prototype page.tsx files. Original rendering logic now in View component.
- Portfolio wrapper itself is now ~20 lines.
- Behavior MUST be identical to pre-refactor version (D-07 visual parity).
  </action>

  <verify>
    <automated>npm run build &amp;&amp; ls src/app/design-system/prototypes/draws/[id]/page.tsx src/app/design-system/prototypes/draws/[id]/print/page.tsx src/app/design-system/prototypes/jobs/[id]/schedule/page.tsx src/app/design-system/prototypes/vendors/page.tsx src/app/design-system/prototypes/vendors/[id]/page.tsx src/app/design-system/prototypes/documents/[id]/page.tsx src/app/design-system/prototypes/owner-portal/page.tsx src/app/design-system/prototypes/owner-portal/draws/[id]/page.tsx src/app/design-system/prototypes/mobile-approval/page.tsx src/app/design-system/prototypes/reconciliation/page.tsx 2>&amp;1 | grep -c 'page.tsx'</automated>
    Expected: build exits 0; ls returns 10 (10 portfolio wrappers in this task; +2 from Plan 2 = 12 total).

    Per-route check (each portfolio wrapper):
    - File size: each portfolio wrapper should be <50 LOC. `wc -l <file>` returns <50.
    - View import: `grep -c 'from "@/components/prototypes/"' <file>` returns >=1
    - breadcrumbRoot includes "/design-system/prototypes/": `grep -c 'design-system/prototypes' <file>` returns >=1

    T10c hook silent on all 10 portfolio wrappers (per Plan 2 finding).

    Manual smoke (Chrome DevTools MCP):
    - Visit each portfolio route (logged in as Jake/Andrew = platform_admin) — renders the same content as before refactor + same content as production routes
    - Side-by-side comparison: open production /financials/bills/inv-caldwell-001 in one tab + portfolio /design-system/prototypes/invoices/inv-caldwell-001 in another tab → render identically (same content, possibly different breadcrumb labels)
  </verify>

  <done>
    - 10 portfolio wrappers rewritten (12 total with Plan 2's 2)
    - All import their View from src/components/prototypes/
    - Same fixture lookup as production wrappers
    - breadcrumbRoot points to /design-system/prototypes/
    - File sizes drastically smaller (20-40 LOC each)
    - Build passes
    - Hooks silent
  </done>
</task>

<task type="auto">
  <name>Task 4: REMOVE inline JobTabs from 9 existing /jobs/[id]/*/page.tsx files (iter-2 must-fix CRITICAL #2 — double-nav collision elimination)</name>
  <files>src/app/jobs/[id]/page.tsx, src/app/jobs/[id]/draws/page.tsx, src/app/jobs/[id]/budget/page.tsx, src/app/jobs/[id]/invoices/page.tsx, src/app/jobs/[id]/change-orders/page.tsx, src/app/jobs/[id]/activity/page.tsx, src/app/jobs/[id]/lien-releases/page.tsx, src/app/jobs/[id]/internal-billings/page.tsx, src/app/jobs/[id]/purchase-orders/page.tsx</files>

  <read_first>
    - src/components/job-tabs.tsx (FULL file — verify what JobTabs does so we know what we're removing)
    - src/app/jobs/[id]/page.tsx (FULL file — line 8 import + line 316 render)
    - src/app/jobs/[id]/draws/page.tsx (FULL file — line 6 import + line 131 render)
    - src/app/jobs/[id]/budget/page.tsx (FULL file — line 6 import + line 710 render — note this file is REPLACED in Task 2 step 5; if Task 2 fully replaces, the JobTabs is gone implicitly; if Task 2 augments instead, Task 4 explicitly removes)
    - src/app/jobs/[id]/invoices/page.tsx (FULL file — line 6 import + line 181 render)
    - src/app/jobs/[id]/change-orders/page.tsx (FULL file — line 6 import + line 199 render)
    - src/app/jobs/[id]/activity/page.tsx (FULL file — line 5 import + line 36 render)
    - src/app/jobs/[id]/lien-releases/page.tsx (FULL file — line 6 import + line 179 render)
    - src/app/jobs/[id]/internal-billings/page.tsx (FULL file — line 6 import + line 556 render)
    - src/app/jobs/[id]/purchase-orders/page.tsx (FULL file — line 6 import + line 194 render)
    - .planning/phases/stage-1.5c-information-architecture/01.5c-5-per-job-tabs-PLAN.md (PerJobTabs in /jobs/[id]/layout.tsx replaces JobTabs)
  </read_first>

  <action>
**iter-2 must-fix CRITICAL #2 (per architect C-1):** 9 existing `/jobs/[id]/*/page.tsx` files import + mount `<JobTabs>` inline. Plan 5 adds `<PerJobTabs>` in `/jobs/[id]/layout.tsx`. WITHOUT removing inline JobTabs, every `/jobs/[id]/*` page would render BOTH tab bars stacked. Plan 3 explicitly removes the `<JobTabs>` import + render line per page.

**For each of 9 files:**

1. **Remove the `import JobTabs from "@/components/job-tabs";` line at the top.**
2. **Remove the `<JobTabs jobId={...} active={...} />` render line in the JSX body.**
3. **Verify the page still compiles** — JobTabs's role (active-tab indicator) is taken over by PerJobTabs from Plan 5's layout.tsx (read pathname + match against tab list).

**Special handling:**

- **/jobs/[id]/budget/page.tsx:** This file is REPLACED entirely in Task 2 Step 5 (mounts BudgetView wrapped in .design-system-scope). The JobTabs is gone implicitly because the entire page contents are replaced. Task 4 verifies post-Task-2 grep returns 0 for JobTabs in this file.

- **/jobs/[id]/draws/page.tsx:** This is the LEGACY job-scoped /draws path. Plan 1's redirect map adds `/draws/:id → /financials/pay-apps/:id` but the JOB-scoped `/jobs/[id]/draws/page.tsx` is NOT in the redirect map (it's a list of draws for THIS job, displayed inside the job). The new IA puts this content at `/jobs/[id]/pay-apps/page.tsx` (Plan 5 placeholder). The legacy path /jobs/[id]/draws may stay accessible for transition OR be redirected internally. For Plan 3, just REMOVE the JobTabs (not the whole page); migration to /jobs/[id]/pay-apps content is Plan 5's concern.

- **/jobs/[id]/invoices/page.tsx:** Same as above — legacy job-scoped invoices. New IA puts content at /jobs/[id]/bills (Plan 5). Plan 3 removes JobTabs only; full migration is Plan 5.

- **/jobs/[id]/lien-releases, internal-billings, purchase-orders/page.tsx:** Job-scoped routes in legacy structure. Plan 3 removes JobTabs only. These pages may stay or be migrated to More-dropdown items in Plan 5.

**For each file's edit:**

```typescript
// Before:
import JobTabs from "@/components/job-tabs";
// ... other imports ...

export default async function ...() {
  // ... function body ...
  return (
    <div>
      <JobTabs jobId={job.id} active="overview" />
      {/* ... rest of page ... */}
    </div>
  );
}

// After (lines removed):
// import JobTabs from "@/components/job-tabs";  ← REMOVED
// ... other imports ...

export default async function ...() {
  // ... function body ...
  return (
    <div>
      {/* JobTabs render REMOVED — PerJobTabs in /jobs/[id]/layout.tsx (Plan 5) replaces */}
      {/* ... rest of page ... */}
    </div>
  );
}
```

**Verify smoke test (post-edit + post-Plan-5):**

After Plan 3 + Plan 5 ship, navigate to each of 9 affected routes:
- /jobs/j-caldwell-1 (was page.tsx active="overview")
- /jobs/j-caldwell-1/draws (was draws/page.tsx active="draws")
- /jobs/j-caldwell-1/budget (was budget/page.tsx active="budget" — fully replaced in Task 2)
- /jobs/j-caldwell-1/invoices (was invoices/page.tsx active="invoices")
- /jobs/j-caldwell-1/change-orders (was change-orders/page.tsx active="change-orders")
- /jobs/j-caldwell-1/activity (was activity/page.tsx active="activity")
- /jobs/j-caldwell-1/lien-releases (was lien-releases/page.tsx active="draws")
- /jobs/j-caldwell-1/internal-billings (was internal-billings/page.tsx active="budget")
- /jobs/j-caldwell-1/purchase-orders (was purchase-orders/page.tsx active="budget")

**Confirm: ZERO double tab bars.** Only the PerJobTabs in /jobs/[id]/layout.tsx renders. (Plan 5 must be merged before this verify can complete; the smoke test happens post-Plan 5 at the integration checkpoint.)
  </action>

  <verify>
    <automated>grep -c "JobTabs" src/app/jobs/[id]/page.tsx src/app/jobs/[id]/draws/page.tsx src/app/jobs/[id]/invoices/page.tsx src/app/jobs/[id]/change-orders/page.tsx src/app/jobs/[id]/activity/page.tsx src/app/jobs/[id]/lien-releases/page.tsx src/app/jobs/[id]/internal-billings/page.tsx src/app/jobs/[id]/purchase-orders/page.tsx</automated>
    Expected: each grep returns 0 (JobTabs import + render lines removed). /jobs/[id]/budget/page.tsx is replaced in Task 2 — also returns 0.

    Verification per file:
    - `grep -c 'import JobTabs from' <file>` returns 0
    - `grep -c '<JobTabs' <file>` returns 0
    - npm run build passes (no missing-import errors)

    iter-2 must-fix #2 smoke test (post-Plan 5 integration):
    - For each of 9 routes, navigate in Chrome DevTools and verify NO double tab bar in DOM
    - PerJobTabs from /jobs/[id]/layout.tsx is the SOLE tab surface
    - Active tab styling correct per current pathname
  </verify>

  <done>
    - JobTabs imports + renders removed from 9 existing /jobs/[id]/*/page.tsx files
    - npm run build passes (no missing imports)
    - Smoke test (post-Plan 5) confirms zero double tab bars
    - PerJobTabs from /jobs/[id]/layout.tsx is the sole tab surface
  </done>
</task>

<task type="auto">
  <name>Task 5: Side-by-side D-07 visual verification of all 12 production-vs-portfolio route pairs at 3 viewports per iter-2 W-3 method lock + iter-2 watchpoint #1 (D-19 .design-system-scope wrap visual diff)</name>
  <files>(verification only — no file modifications; output is the visual report appended to plan SUMMARY)</files>

  <read_first>
    - .planning/phases/stage-1.5c-information-architecture/CONTEXT.md (D-07 CRITICAL halt criterion + D-19 Decision A; iter-2 watchpoint #1 visual diff)
    - .planning/expansions/stage-1.5c-information-architecture-EXPANDED-SCOPE.md (Amendment 2 — D-07 acceptance criterion; iter-2 amendments must-fix #1)
    - .planning/phases/stage-1.5c-information-architecture/01.5c-2-thin-wrapper-architect-PLAN.md (iter-2 architect W-3 — 3-viewport method lock)
    - All 12 production routes from Task 2
    - All 11 portfolio routes from Task 3 + 2 portfolio routes from Plan 2
  </read_first>

  <action>
**Per CONTEXT D-07 + nwrp44 Amendment 2 + iter-2 watchpoint #1 (THE CRITICAL HALT CRITERIA):**

- D-07: After D-053 thin-wrapper extraction completes, run side-by-side visual verification: production route renders identically to portfolio counterpart at 3 viewports per iter-2 W-3 method.
- iter-2 watchpoint #1: Visual diff before/after .design-system-scope wrap on production routes confirms 1px slate-tile left-stamp + JetBrains Mono UPPERCASE eyebrow + Space Grotesk Medium headline appear post-wrap.

**Verification method (iter-2 architect W-3 method lock):**

**Method: Manual visual walk + Chrome DevTools MCP screenshots at 3 fixed viewports.**

For each of 12 pairs:
1. Open production route in browser tab 1
2. Open portfolio route in browser tab 2 (logged in as Jake/Andrew = platform_admin)
3. Compare side-by-side at 3 fixed viewports:
   - 1920x1080 desktop
   - 1280x800 laptop
   - 393x852 iPhone (mobile-approval especially)
4. Diff observable elements (TD-fix-attributable checklist):
   - Layout proportions (hero grid 50/50; sidebar widths; card padding)
   - Typography (headline weights, eyebrow rendering, monospace for money)
   - Colors (background, text, accent, status badges)
   - Icons (presence, sizing, stroke widths)
   - Interactive elements (NwButton styling per TD-20 — minor visual change is EXPECTED + acceptable)
   - Audit timeline rendering
   - Status badges + state labels
   - File preview placeholder
   - **iter-2 watchpoint #1 — Site Office signatures on production:** 1px slate-tile left-stamp on cards, JetBrains Mono UPPERCASE eyebrow at 0.18em tracking, Space Grotesk Medium fontWeight: 500 headline, 150ms ease-out motion. These should now appear on production routes (post-D-19 wrap) — they did NOT appear pre-wrap (per design-pushback CRITICAL 1 finding).
5. Document findings:
   - "Pair N: identical except for [list of differences attributable to TD-20/25/26 fixes]" — ACCEPTABLE
   - "Pair N: differs in [list] — investigate" — CRITICAL HALT

**Expected acceptable differences (NOT divergence):**

- Breadcrumb labels: production says "Bills > [id]"; portfolio says "Prototypes > Invoices > [id]"
- Buttons: NwButton styling vs raw button — TD-20 fix; acceptable
- Card padding: 16px vs 24px — TD-25 fix; acceptable
- Headline weight: 500 vs 600 — TD-26 fix; acceptable
- Direction lock: portfolio inherits data-direction=C from /design-system/prototypes/layout.tsx; production now inherits from .design-system-scope wrap (per D-19) — should resolve to same Site Office tokens; if NOT, that IS divergence (the wrap isn't propagating properly)

**Unexpected = CRITICAL HALT divergence:**

- Layout shift (different column counts, different grid proportions, different spacing not attributable to TD-25 padding change)
- Color drift
- Missing content (a section that exists in portfolio but not production)
- Broken icons
- Console errors in either route
- Fonts not loading
- Site Office signature missing from production despite .design-system-scope wrap (means the wrap doesn't propagate; investigate the design-system.css selector specificity)
- iter-2 watchpoint #1: production WITHOUT slate-tile left-stamp + JetBrains Mono eyebrow + Space Grotesk headline — means D-19 didn't work; CRITICAL halt + investigate

**12 pairs to verify:**

| # | Production | Portfolio |
|---|------------|-----------|
| 1 | /financials/bills/inv-caldwell-001 | /design-system/prototypes/invoices/inv-caldwell-001 |
| 2 | /financials/pay-apps/d-caldwell-05 | /design-system/prototypes/draws/d-caldwell-05 |
| 3 | /financials/pay-apps/d-caldwell-05/print | /design-system/prototypes/draws/d-caldwell-05/print |
| 4 | /financials/reconciliation | /design-system/prototypes/reconciliation |
| 5 | /jobs/j-caldwell-1/budget | /design-system/prototypes/jobs/j-caldwell-1/budget |
| 6 | /jobs/j-caldwell-1/schedule | /design-system/prototypes/jobs/j-caldwell-1/schedule |
| 7 | /people/vendors | /design-system/prototypes/vendors |
| 8 | /people/vendors/v-caldwell-coastal-smart-systems | /design-system/prototypes/vendors/v-caldwell-coastal-smart-systems |
| 9 | /jobs/j-caldwell-1/documents/lr-caldwell-001 | /design-system/prototypes/documents/lr-caldwell-001 |
| 10 | /owner-portal | /design-system/prototypes/owner-portal |
| 11 | /owner-portal/pay-apps/d-caldwell-05 | /design-system/prototypes/owner-portal/draws/d-caldwell-05 |
| 12 | /jobs/j-caldwell-1/mobile-approval | /design-system/prototypes/mobile-approval |

**iter-2 watchpoint #1 specific verification (D-19 .design-system-scope wrap):**

Sub-task: TEMPORARILY remove the .design-system-scope wrap from ONE production route (e.g., /financials/bills/inv-caldwell-001), screenshot at 3 viewports. Then re-add wrap, screenshot at 3 viewports. Diff:

- Pre-wrap: cards have NO 1px slate-tile left-stamp; eyebrows in default font (NOT JetBrains Mono UPPERCASE); headlines in default weight
- Post-wrap: cards HAVE 1px slate-tile left-stamp; eyebrows in JetBrains Mono UPPERCASE 0.18em; headlines in Space Grotesk Medium 500

If post-wrap state matches the pre-wrap state (no Site Office signature), the wrap isn't propagating — investigate design-system.css selector specificity. (Restore the wrap before continuing; this is a temporary verify-only test.)

**Output:**

For each pair, produce a verification report appended to plan SUMMARY:

```
## D-07 Visual Verification Report (iter-2 W-3 method: 3 viewports)

### Pair 1: /financials/bills/inv-caldwell-001 ↔ /design-system/prototypes/invoices/inv-caldwell-001
**Viewport: 1920x1080 desktop**
- Status: IDENTICAL (or DIVERGENT)
- Differences observed: [list]
- Differences attributable to TD-20/25/26 fixes: [list — ACCEPTABLE]
- Site Office signatures present (post-D-19 wrap on production): YES / NO
- Divergence requiring investigation: [list — CRITICAL HALT if any]

**Viewport: 1280x800 laptop**
- (same fields)

**Viewport: 393x852 iPhone**
- (same fields)

(repeat for all 12 pairs at 3 viewports = 36 verification cells)

### iter-2 watchpoint #1 — D-19 wrap visual diff
- Test route: /financials/bills/inv-caldwell-001
- Pre-wrap (temporary remove): [state observed at 3 viewports]
- Post-wrap (restored): [state observed at 3 viewports]
- 1px slate-tile left-stamp present post-wrap: YES / NO
- JetBrains Mono UPPERCASE eyebrow present post-wrap: YES / NO
- Space Grotesk Medium headline present post-wrap: YES / NO
- D-19 effective: YES / NO
- If NO: CRITICAL halt + investigate design-system.css selector specificity

### Summary
- Total pair-viewport cells verified: 36 (12 pairs × 3 viewports)
- Cells IDENTICAL (or differing only by acceptable TD-fix attribution): X
- Cells DIVERGENT (CRITICAL): Y
- iter-2 watchpoint #1 status: PASS / FAIL
- If Y > 0 OR watchpoint #1 = FAIL: HALT EXTRACTION + investigate before proceeding to Wave 3
```

**Halt rule:** If ANY pair-viewport cell shows unexpected divergence OR iter-2 watchpoint #1 fails, halt and investigate. Do NOT mark Plan 3 complete. Surface to Jake.
  </action>

  <verify>
    <automated>test -f .planning/phases/stage-1.5c-information-architecture/01.5c-3-extraction-production-routes-SUMMARY.md &amp;&amp; grep -c 'D-07 Visual Verification' .planning/phases/stage-1.5c-information-architecture/01.5c-3-extraction-production-routes-SUMMARY.md &amp;&amp; grep -c 'Pair 1' .planning/phases/stage-1.5c-information-architecture/01.5c-3-extraction-production-routes-SUMMARY.md &amp;&amp; grep -c 'watchpoint #1' .planning/phases/stage-1.5c-information-architecture/01.5c-3-extraction-production-routes-SUMMARY.md</automated>
    Expected: SUMMARY.md exists; grep returns >=1 for "D-07 Visual Verification"; >=1 for "Pair 1"; >=1 for "watchpoint #1".

    Manual halt check:
    - Read SUMMARY.md "D-07 Visual Verification Report" section
    - Confirm 12 pairs × 3 viewports = 36 verification cells with status per cell
    - Count DIVERGENT cells — if >0, plan is NOT complete; halt and investigate
    - Confirm iter-2 watchpoint #1 PASS

    Per nwrp44: "Treat divergence as a CRITICAL finding, not polish."
  </verify>

  <done>
    - 12 production-vs-portfolio pairs verified side-by-side at 3 viewports per iter-2 W-3 method
    - 36 verification cells documented in SUMMARY
    - iter-2 watchpoint #1 verified (D-19 .design-system-scope wrap propagates Site Office signatures)
    - 0 DIVERGENT cells (or, if any, halt + investigation underway)
    - Acceptable TD-fix-attributable differences explicitly documented
    - SUMMARY hands off to Jake review
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production wrapper → View component | Wrapper imports fixtures + passes via props; View has no direct fixture access |
| Production route → middleware auth | Existing middleware enforces auth + role gate; new routes inherit |
| Owner Portal → role gate | /owner-portal/* gated per existing middleware; iter-2 multi-tenant W-1 clarification: 1.5c is structural Caldwell-fixture-only; F1 commits to ONE auth path (client_portal_access tokens OR owner_view role with RLS) — not both |
| Portfolio route → platform_admin gate | /design-system/prototypes/* gated to platform_admin per existing middleware (1.5a) |
| iter-2 D-19 .design-system-scope wrap | Outer container className extends Site Office direction-aware CSS rules from playground-only to production consumer |
| Existing /jobs/[id]/*/page.tsx files (9 files) | JobTabs inline mounts removed per iter-2 must-fix #2; PerJobTabs from Plan 5's layout.tsx replaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.5c-3-01 | I (Information disclosure) | 12 production routes rendering Caldwell fixtures | accept | All Caldwell fixture data already-sanitized per 1.5b SUBSTITUTION-MAP.md. No real Drummond identifiers. F1 swaps fixtures for real data + audit-log writes. Privacy gate verified clean. |
| T-1.5c-3-02 | T (Tampering) | View components props contract | mitigate | Type-only imports for Caldwell types ensure compile-time safety. Wrapper passes mismatched props would fail TypeScript. Build gate enforces. |
| T-1.5c-3-03 | E (Elevation of privilege) | /owner-portal/* role gate | mitigate | Per D-08 + iter-2 multi-tenant W-1 clarification: 1.5c is structural; Caldwell fixtures only; no real auth flow. F1 commits to ONE auth path (not both). Plan 7 documents the constraint. |
| T-1.5c-3-04 | I (Information disclosure) | Owner Portal data exposure | mitigate | Owner Portal renders Caldwell fixtures. Real-data wiring (F1) MUST filter by client_id / homeowner relationship. 1.5c does NOT create new exposure surface beyond what 1.5b prototype already had. |
| T-1.5c-3-05 | S (Spoofing) | Production wrapper fixture imports | accept | Fixture imports are static const arrays at build time. No runtime mutation. Spoofing requires code deploy (gated by Vercel auth). |
| T-1.5c-3-06 | R (Repudiation) | Production read-only routes | accept | All production routes are read-only in 1.5c (no mutations per Q8=A). Existing middleware logs every authenticated request via Sentry tags. |
| T-1.5c-3-07 | D (Denial of service) | 12 production routes | accept | Static fixture data; no database queries. Rendering performance is bounded. F1 wires real DB queries with proper indexing. |
| T-1.5c-3-08 | E (Elevation of privilege) | NwButton replacing raw button | accept | Same as Plan 2: NwButton is tenant-blind primitive; no-op in prototype + wrapper. F1 wires actions to authenticated API routes. |
| T-1.5c-3-09 | I (Information disclosure) | iter-2 mechanical #10 — getCurrentMembership preservation | mitigate | Per-route grep diff verify confirms auth checks preserved verbatim across re-mounted routes. SUMMARY documents any divergence with reasoning (typically: F1 wires real auth at swap time + Caldwell fixtures don't require auth scoping). |
</threat_model>

<verification>
After all 5 tasks complete:

1. 10 NEW View components exist at src/components/prototypes/ + Plan 2's InvoiceReviewView + BudgetView = 12 total Views
2. 12 production routes mount Views as thin wrappers + render with Caldwell fixtures + WRAPPED in .design-system-scope per D-19 / iter-2 must-fix CRITICAL #1
3. 11 portfolio /design-system/prototypes/* routes are thin wrappers + still accessible under platform_admin gate
4. JobTabs inline mounts REMOVED from 9 existing /jobs/[id]/*/page.tsx files per iter-2 must-fix CRITICAL #2 — zero double tab bar rendering
5. iter-2 mechanical #10: getCurrentMembership / org_id / requireMembership preserved verbatim across re-mounted routes (or removal documented)
6. iter-2 architect W-5: REAL vs PLACEHOLDER status documented per route in SUMMARY
7. iter-2 multi-tenant W-1: Owner Portal posture clarification documented (1.5c structural; F1 commits to one auth path)
8. D-07 visual verification at 3 viewports: ALL 12 pairs render IDENTICALLY (or differences are TD-fix-attributable + ACCEPTABLE) — 36 verification cells
9. iter-2 watchpoint #1: D-19 .design-system-scope wrap visual diff confirms Site Office signatures appear on production post-wrap
10. npm run build passes
11. typecheck clean
12. Hooks silent: T10c (production routes don't trigger; portfolio routes don't trigger fixture-side gate per Plan 2 architect finding); tenant-blind silent on Views; bouncy-easing silent; post-edit Forbidden tokens silent
13. Privacy gate clean: 32-token denylist returns 0 hits across all 22+ new + modified files

**Halt for Jake review:** halt_after: true. Jake reviews:
- D-07 verification report (12 pairs × 3 viewports = 36 cells; all IDENTICAL or acceptable-TD-fix-only)
- iter-2 watchpoint #1: D-19 wrap effective; production routes show Site Office signatures
- Spot-check 2-3 production routes on desktop + iPhone Safari
- Confirm Wave 2 ready for Wave 3 spawn
</verification>

<success_criteria>
- 10 NEW Views + 12 production wrappers (with .design-system-scope wrap per D-19) + 11 rewritten portfolio wrappers
- 9 existing /jobs/[id]/*/page.tsx files: JobTabs inline mounts removed (iter-2 must-fix CRITICAL #2)
- TD-20 / TD-25 / TD-26 structural fixes applied uniformly
- Bills/Pay Apps terminology in all UI labels
- D-07 visual verification at 3 viewports per iter-2 W-3: 36 cells IDENTICAL or acceptable-TD-fix-only
- iter-2 watchpoint #1 verified: D-19 wrap propagates Site Office signatures to production
- iter-2 mechanical #10: getCurrentMembership preservation per route
- iter-2 architect W-5: REAL vs PLACEHOLDER per route documented
- iter-2 multi-tenant W-1: Owner Portal F1 auth path constraint documented for Plan 7
- All 22 must_haves truths verified
- Build + typecheck + hooks + privacy gate clean
- Halt review with Jake passes
</success_criteria>

## Acceptance criteria

- [ ] AC-1.5c-3-01: 10 NEW View components exist at src/components/prototypes/*View.tsx (12 total with Plan 2's InvoiceReviewView + BudgetView)
- [ ] AC-1.5c-3-02: All 10 NEW Views import Caldwell types via `import type` only (no runtime fixture imports)
- [ ] AC-1.5c-3-03: All 10 NEW Views apply TD-20 fix (zero raw button type for action elements; NwButton replaces)
- [ ] AC-1.5c-3-04: All 10 NEW Views apply TD-25 fix (zero Card padding="lg" usages)
- [ ] AC-1.5c-3-05: All 10 NEW Views apply TD-26 fix (zero fontWeight: 600 inline styles for headlines)
- [ ] AC-1.5c-3-06: All Views are tenant-blind (zero org_id / membership / vendor_id props)
- [ ] AC-1.5c-3-07: 12 production page.tsx files exist at the canonical paths
- [ ] AC-1.5c-3-08: Each production wrapper imports Caldwell fixtures + its View (thin wrapper pattern)
- [ ] AC-1.5c-3-09: Each production wrapper calls notFound() on missing fixture lookup
- [ ] AC-1.5c-3-10: Each production wrapper outer container WRAPPED in `<div className="design-system-scope">` per iter-2 must-fix CRITICAL #1 / D-19
- [ ] AC-1.5c-3-11: 11 portfolio /design-system/prototypes/* page.tsx files rewritten as thin wrappers (file size <50 LOC each)
- [ ] AC-1.5c-3-12: Bills/Pay Apps terminology applied in user-visible labels
- [ ] AC-1.5c-3-13: Owner Portal at /owner-portal/* mounted with .design-system-scope wrap; iter-2 multi-tenant W-1 clarification documented (1.5c structural; F1 commits to one auth path)
- [ ] AC-1.5c-3-14: Mobile Approval at /jobs/[id]/mobile-approval renders inside iPhone viewport (393x852); 56px Approve button (NwButton size="lg-touch")
- [ ] AC-1.5c-3-15: Print preview at /financials/pay-apps/[id]/print preserves @media print stylesheet from 1.5b
- [ ] AC-1.5c-3-16: D-07 side-by-side visual verification: 12 pairs × 3 viewports = 36 cells IDENTICAL or differences are TD-fix-attributable (ACCEPTABLE) — 0 DIVERGENT cells
- [ ] AC-1.5c-3-17: iter-2 watchpoint #1 verified — D-19 .design-system-scope wrap visual diff confirms 1px slate-tile left-stamp + JetBrains Mono UPPERCASE eyebrow + Space Grotesk Medium headline appear on production post-wrap
- [ ] AC-1.5c-3-18: iter-2 must-fix CRITICAL #2 — JobTabs inline mounts REMOVED from 9 existing /jobs/[id]/*/page.tsx files; smoke test confirms zero double tab bar rendering
- [ ] AC-1.5c-3-19: iter-2 mechanical #10 — getCurrentMembership / org_id / requireMembership preserved verbatim across re-mounted routes per route grep diff (or divergence documented)
- [ ] AC-1.5c-3-20: iter-2 architect W-5 — REAL vs PLACEHOLDER status documented per Plan 3 production route in SUMMARY
- [ ] AC-1.5c-3-21: Build + typecheck + post-edit Forbidden tokens + tenant-blind + bouncy-easing hooks silent throughout
- [ ] AC-1.5c-3-22: Privacy gate clean (32-token denylist) across all 22+ new + modified files
- [ ] AC-1.5c-3-23: iter-3 D-26 / SEC-10 — defensive grep for tenant-context attributes on production wrappers returns 0: `grep -rE 'data-org-id|data-org-name|data-tenant' src/app/financials/ src/app/jobs/ src/app/owner-portal/ src/app/people/` returns 0. Confirms `.design-system-scope` wrap (CSS scope class only) + `data-direction="C" data-palette="B"` (presentation attributes only) don't introduce DOM tenant-context attributes.

## Nyquist coverage

| Acceptance criterion | Verified by |
|---|---|
| AC-1.5c-3-01 | Task 1 verify (ls src/components/prototypes/*View.tsx returns 12) |
| AC-1.5c-3-02 | Task 1 verify (per-component grep "import type" + grep absence of runtime fixture imports) |
| AC-1.5c-3-03 | Task 1 verify (per-component grep button type returns 0) |
| AC-1.5c-3-04 | Task 1 verify (per-component grep padding="lg" returns 0) |
| AC-1.5c-3-05 | Task 1 verify (per-component grep fontWeight: 600 returns 0) |
| AC-1.5c-3-06 | Task 1 verify (per-component grep org_id|membership|vendor_id returns 0) |
| AC-1.5c-3-07 | Task 2 verify (ls of all 12 production page.tsx) |
| AC-1.5c-3-08 | Task 2 verify (per-route grep View import + fixture import) |
| AC-1.5c-3-09 | Task 2 verify (per-route grep "notFound()" returns >=1) |
| AC-1.5c-3-10 | Task 2 verify (per-route grep "design-system-scope" returns >=1) |
| AC-1.5c-3-11 | Task 3 verify (per-route wc -l <50 + grep View import) |
| AC-1.5c-3-12 | Task 1 verify + Task 5 visual check (UI labels say Bill / Pay App in all extracted Views) |
| AC-1.5c-3-13 | Task 2 verify (manual smoke at /owner-portal; iter-2 multi-tenant W-1 documented in SUMMARY) |
| AC-1.5c-3-14 | Task 5 visual check (mobile-approval pair rendered at 393x852 viewport) |
| AC-1.5c-3-15 | Task 5 visual check (print pair rendered + window.print() preview) |
| AC-1.5c-3-16 | Task 5 (CRITICAL halt criterion — 12-pair × 3-viewport verification report in SUMMARY) |
| AC-1.5c-3-17 | Task 5 sub-task (iter-2 watchpoint #1 — pre/post .design-system-scope wrap diff at 3 viewports) |
| AC-1.5c-3-18 | Task 4 verify (grep JobTabs returns 0 in 9 files; smoke test post-Plan 5 integration confirms no double tabs) |
| AC-1.5c-3-19 | Task 2 verify (per-route grep diff for getCurrentMembership; SUMMARY documents per-route status) |
| AC-1.5c-3-20 | Task 2 SUMMARY (REAL vs PLACEHOLDER per route) |
| AC-1.5c-3-21 | Plan-level verification (npm run build + typecheck + manual hook silent check) |
| AC-1.5c-3-22 | Plan-level verification (privacy gate grep across all modified files) |

## Decisions inherited (from CONTEXT.md)

- D-01 (= MASTER-PLAN D-051): Bills/Pay Apps terminology in all UI labels
- D-03 (= MASTER-PLAN D-053): Thin-wrapper extraction — 11 prototype components refactored to props-only Views
- D-06 (= MASTER-PLAN D-056): Fix only structural TDs in 1.5c (TD-20/25/26)
- D-07: D-07 visual verification — divergence = CRITICAL halt criterion
- D-08: Owner Portal at /owner-portal/* same auth + role gate per existing middleware (clarified per iter-2 multi-tenant W-1 — 1.5c structural; F1 commits to ONE auth path)
- D-14 (Q8=A): No real-backend wiring promotion in 1.5c
- AUDIT-LOG-STRATEGY.md from Plan 2: Strategy 1-prime; entity_type unchanged in 1.5c
- **D-19 (= MASTER-PLAN D-057) — iter-2:** Site Office .design-system-scope wrapping for production routes (Decision A); each production wrapper outer container wraps in className="design-system-scope"

## Halt conditions

- Halt at end of plan (halt_after: true) for Jake D-07 visual verification review at 3 viewports + integration smoke + iter-2 watchpoint #1 verification
- Halt during execution if:
  - **D-07 verification flags ANY DIVERGENT cell** at any of 12 pairs × 3 viewports
  - **iter-2 watchpoint #1 fails** — D-19 .design-system-scope wrap doesn't propagate Site Office signatures (means design-system.css selector specificity is wrong; investigate)
  - Build / typecheck breaks at any point during 9-component extraction
  - T10c hook rejects production wrapper despite Plan 2 architect's Option A finding
  - NwButton replacement breaks visual parity in unexpected way
  - Existing /jobs/[id]/budget production route conflicts with new mount in unexpected way (e.g., real-backend code paths the existing route had that 1.5c can't preserve — surface decision to Jake)
  - Privacy gate flags any new file
  - **iter-2 must-fix CRITICAL #2 smoke fails** — double tab bar still visible after Plan 3 + Plan 5 ship; investigate
  - **iter-2 mechanical #10 grep diff** shows getCurrentMembership removal without documented reasoning — halt + reconcile
  - Existing middleware semantics for /owner-portal/* are surprising — log to Wave 1.1-Lite, do NOT modify middleware in 1.5c

## Output

After completion, create `.planning/phases/stage-1.5c-information-architecture/01.5c-3-extraction-production-routes-SUMMARY.md` per `.claude/get-shit-done/templates/summary.md`. Document:
- Files created / modified (10 NEW Views + 12 production wrappers + 11 portfolio wrappers + 9 JobTabs-removed files + any backed-up existing routes)
- Acceptance criteria status (22 items checked)
- D-07 Visual Verification Report (12 pairs × 3 viewports = 36 cells with status per cell — see Task 5 action template)
- iter-2 watchpoint #1 verification (D-19 .design-system-scope wrap visual diff results)
- iter-2 must-fix CRITICAL #2 — JobTabs removal smoke test results (post-Plan 5 integration)
- iter-2 mechanical #10 — getCurrentMembership grep diff results per route
- iter-2 architect W-5 — REAL vs PLACEHOLDER status per Plan 3 production route
- iter-2 multi-tenant W-1 — Owner Portal posture constraint for F1 (Path A or Path B; Plan 7 ARCHITECTURE.md inheritance)
- TD-20 / TD-25 / TD-26 fix counts per component
- Hook silence + build / typecheck / privacy gate pass
- Halt-review checklist for Jake (D-07 status; iter-2 watchpoint #1; spot-check 2-3 routes on desktop + iPhone Safari)
- Any backed-up existing route content (preserved as comments in SUMMARY for F1 swap reference)
</content>
</invoke>

<criteria>

```yaml
criteria:
  mechanical:
    - "npm run build clean (0 errors)"
    - "npx tsc --noEmit returns 0 errors in src/"
    - "All hooks silent on every commit (T10a-T10d, post-edit drift, Drummond grep)"
    - "Privacy gate clean across new files"
    - "10 View components exist at src/components/prototypes/ (DrawApproval, DrawPrint, Schedule, VendorList, VendorDetail, DocumentReview, OwnerDashboard, OwnerDraw, MobileApproval, Reconciliation)"
    - "All 10 View components are props-only (no Supabase imports inside any file at src/components/prototypes/*.tsx)"
    - "All 8 /jobs/[id]/* page files (page, draws, invoices, change-orders, activity, lien-releases, internal-billings, purchase-orders) contain 0 inline JobTabs JSX hits"
    - "SEC-10 defensive grep clean: 0 hits for 'data-org-id|data-org-name|data-tenant' across 12 production routes"
  dom:
    - 'Page /design-system/prototypes/draws/d-caldwell-05 contains element ".design-system-scope" wrapping the prototype'
    - 'Page /design-system/prototypes/vendors contains text matching a vendor name from the Caldwell fixture set'
  visual:
    - "Page /design-system/prototypes/draws/d-caldwell-05: draw approval surface visible with line-item table + KPI strip + approval action region (Document Review pattern extended)"
    - "Page /design-system/prototypes/reconciliation: reconciliation surface visible with diff/match indicators between paired entities (mock-up rendered, not 404)"
    - "Page /design-system/prototypes/vendors: vendor list rendered as table or card grid with at least 3 vendor rows visible"
  behavioral:
    - "Every src/app/design-system/prototypes/**/page.tsx file is a thin wrapper (< 100 LOC) importing a corresponding View from src/components/prototypes/"
    - "src/app/jobs/[id]/layout.tsx contains the sole PerJobTabs surface; no /jobs/[id]/* page.tsx imports JobTabs directly"
    - "Production route wrapping pattern: 12 routes wrap their View in <div data-direction=\"C\" data-palette=\"B\" className=\"design-system-scope\">"
  semantic:
    - "Page /design-system/prototypes/vendors/v-caldwell-coastal-smart-systems: vendor detail surface visible with name, contact info, invoice/payment history sections"
    - "Page /design-system/prototypes/mobile-approval: mobile-optimized approval flow visible (single-column layout, large touch targets ≥44px, primary action button prominent)"
```

</criteria>
