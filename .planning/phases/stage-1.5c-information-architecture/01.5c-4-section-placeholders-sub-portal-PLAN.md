---
phase: stage-1.5c-information-architecture
plan: 4
plan-name: section-placeholders-sub-portal
type: execute
status: not-started
wave: 3
depends_on: [1, 3]
autonomous: true
halt_after: false
threat_model_severity: low
requirements: []
files_modified:
  - src/app/pipeline/page.tsx
  - src/app/pipeline/leads/page.tsx
  - src/app/pipeline/estimates/page.tsx
  - src/app/pipeline/bids-out/page.tsx
  - src/app/pipeline/proposals/page.tsx
  - src/app/pipeline/contracts/page.tsx
  - src/app/financials/page.tsx
  - src/app/financials/bills/page.tsx
  - src/app/financials/bills/queue/page.tsx
  - src/app/financials/bills/qa/page.tsx
  - src/app/financials/payments/page.tsx
  - src/app/financials/lien-releases/page.tsx
  - src/app/financials/pay-apps/page.tsx
  - src/app/financials/purchase-orders/page.tsx
  - src/app/financials/change-orders/page.tsx
  - src/app/financials/aging/page.tsx
  - src/app/price-intel/page.tsx
  - src/app/price-intel/cost-lookup/page.tsx
  - src/app/price-intel/selections-catalog/page.tsx
  - src/app/price-intel/material-orders/page.tsx
  - src/app/price-intel/bid-comparison/page.tsx
  - src/app/price-intel/anomaly-review/page.tsx
  - src/app/price-intel/vendor-performance/page.tsx
  - src/app/price-intel/verification/page.tsx
  - src/app/price-intel/cost-database/page.tsx
  - src/app/people/page.tsx
  - src/app/people/clients/page.tsx
  - src/app/people/team/page.tsx
  - src/app/people/org-chart/page.tsx
  - src/app/sub-portal/page.tsx
  - src/app/sub-portal/magic/[token]/page.tsx
  - src/app/sub-portal/public/[token]/page.tsx
  - src/app/company/page.tsx
  - src/app/company/p-l/page.tsx
  - src/app/company/cash-flow/page.tsx
  - src/app/company/expenses/page.tsx
  - src/app/company/time-tracking/page.tsx
  - src/app/company/compliance/page.tsx
  - src/app/company/fleet-equipment/page.tsx
  - src/app/company/insurance/page.tsx
  - src/app/company/permits-licensing/page.tsx
  - src/app/company/tax/page.tsx
  - src/app/company/capacity-planning/page.tsx
  - src/app/company/documents/page.tsx
  - src/app/reports/page.tsx
  - src/app/reports/saved/page.tsx
  - src/app/reports/ai-builder/page.tsx
  - src/app/reports/custom-builder/page.tsx
  - src/app/reports/scheduled/page.tsx
  - src/app/reports/templates/page.tsx
requires:
  - .planning/phases/stage-1.5c-information-architecture/CONTEXT.md
  - .planning/phases/stage-1.5c-information-architecture/PATTERNS.md
  - .planning/phases/stage-1.5c-information-architecture/01.5c-1-nav-today-redirects-PLAN.md (Wave 1 — NwPlaceholderCard hoisted here per iter-2 mechanical #1)
  - .planning/phases/stage-1.5c-information-architecture/01.5c-3-extraction-production-routes-PLAN.md (Wave 2)
  - src/components/nw/NwPlaceholderCard.tsx (Plan 1 hoisted per iter-2 mechanical #1)
  - src/app/design-system/prototypes/page.tsx (existing 10-Card index pattern)
provides:
  - Pipeline section (6 routes): page + leads + estimates + bids-out + proposals + contracts (all PLACEHOLDERS)
  - Financials section overview restructure + 9 sub-routes — REAL-LOGIC vs PLACEHOLDER per route documented per iter-2 architect W-5
  - Price Intel section (9 routes) — REAL-LOGIC (3) + PLACEHOLDERS (6) per iter-2 W-5
  - People section (4 routes): page + clients + team + org-chart (all PLACEHOLDERS; vendors handled in Plan 3)
  - Sub Portal 3 stubs: /sub-portal + /sub-portal/magic/[token] + /sub-portal/public/[token] — with explicit F3 compliance contract + param-reflection grep verify per iter-2 mechanical #5
  - Company section (12 routes) — REAL-LOGIC (1: /company/overview from Plan 1) + PLACEHOLDERS (11)
  - Reports section (6 routes) — all PLACEHOLDERS
affects:
  - All 8 nav sections have a primary route + sub-routes (paired with Plan 5 per-job tabs + Plan 6 admin restructure)
  - Plan 1's 32-path redirect map destinations all resolve
  - iter-2 mechanical #10 + W-5: REAL-LOGIC re-mounts (Half A) preserve getCurrentMembership() verbatim per route grep diff
acceptance-criteria-target: 16 falsifiable items (was 12; +3 for iter-2 must-fix #5 Sub Portal stubs + +1 for Wave 3 mid-checkpoint smoke per planner LOW-2/LOW-4)
estimated-duration: 1.25 days (was 1 day; +0.25 day for Sub Portal F3 compliance contracts + getCurrentMembership grep diffs + Wave 3 mid-checkpoint smoke)
must_haves:
  truths:
    - "NwPlaceholderCard imported from src/components/nw/NwPlaceholderCard.tsx (HOISTED to Plan 1 per iter-2 mechanical #1; this plan consumes only)"
    - "Pipeline section has 6 routes: /pipeline (overview) + /pipeline/leads + /pipeline/estimates + /pipeline/bids-out + /pipeline/proposals + /pipeline/contracts — all PLACEHOLDERS"
    - "Financials overview at /financials renders Card grid linking to Bills + Pay Apps + Purchase Orders + Change Orders + Lien Releases + Payments + Aging"
    - "Financials REAL-LOGIC mounts (7 routes) preserve getCurrentMembership() verbatim per iter-2 mechanical #10 grep diff: /financials/bills (← /invoices), /financials/bills/queue, /financials/bills/qa, /financials/payments, /financials/lien-releases, /financials/pay-apps, /financials/aging"
    - "Financials PLACEHOLDER routes (2): /financials/purchase-orders + /financials/change-orders use NwPlaceholderCard"
    - "Price Intel section has 9 routes — 3 REAL-LOGIC (cost-lookup, verification, /price-intel itself if existing /cost-intelligence has substantial logic) + 6 PLACEHOLDERS — getCurrentMembership() preserved on REAL-LOGIC re-mounts"
    - "People section has 4 placeholder routes + /people/vendors lives from Plan 3 (already mounted)"
    - "Sub Portal has 3 distinct routes: /sub-portal + /sub-portal/magic/[token] + /sub-portal/public/[token] (per CONTEXT D-02 / Q3=B)"
    - "Each Sub Portal stub has copy describing its specific F3 future-state with EXPLICIT compliance contract per iter-2 mechanical #5 (token expiry, scope binding, audit-log-on-use, cookie-vs-querystring posture, JWT-with-org-claim REJECTED by-construction, mirror client_portal_access SHA-256 + service-role-API pattern)"
    - "Sub Portal stubs param-reflection grep verify per iter-2 mechanical #5: `grep -E 'params\\.token' src/app/sub-portal/magic/[token]/page.tsx src/app/sub-portal/public/[token]/page.tsx` returns 0 (no token reflection into HTML)"
    - "AC-1.5c-4-07 verification per iter-2 mechanical #9: 3 explicit grep tokens, one per Sub Portal stub"
    - "Company section has 12 routes; /company/overview is from Plan 1 (Cash Flow + Getting Started); rest are placeholders for F4 / Wave 2 / Wave 3"
    - "Reports section has 6 placeholder routes (Wave 3 / AI report builder context)"
    - "All placeholder routes use Site Office tokens (UPPERCASE eyebrows + Space Grotesk Medium headlines + JetBrains Mono Wave badge)"
    - "Each placeholder is ~30 LOC (per CONTEXT D-13 spec)"
    - "Build + typecheck + hooks silent across ~50 placeholder files; privacy gate clean"
    - "Wave 3 mid-checkpoint smoke (iter-2 planner LOW-2 / LOW-4): at end of Plan 4 (before Plan 5 + 6 spawn) navigate to all 8 nav sections + spot-check 5 random per-section sub-routes; document any 404s"
  artifacts:
    - path: "src/app/pipeline/page.tsx"
      provides: "Pipeline section overview with Card grid linking to Leads / Estimates / Bids out / Proposals / Contracts"
      exports: ["default"]
    - path: "src/app/sub-portal/page.tsx"
      provides: "Sub Portal authenticated stub describing F3 future-state with explicit compliance contract"
      exports: ["default"]
      contains: "Subcontractor self-service"
    - path: "src/app/sub-portal/magic/[token]/page.tsx"
      provides: "Sub Portal magic link stub describing F3 magic-link auth pattern with explicit compliance contract"
      exports: ["default"]
      contains: "magic link"
    - path: "src/app/sub-portal/public/[token]/page.tsx"
      provides: "Sub Portal public read-only link stub describing F3 architects + inspectors pattern with explicit compliance contract"
      exports: ["default"]
      contains: "public read-only"
    - path: "src/app/financials/page.tsx"
      provides: "Financials section overview restructure (Card grid linking to Bills + Pay Apps + ...)"
      exports: ["default"]
    - path: "src/app/price-intel/page.tsx"
      provides: "Price Intel section overview"
      exports: ["default"]
    - path: "src/app/people/page.tsx"
      provides: "People section overview"
      exports: ["default"]
    - path: "src/app/company/page.tsx"
      provides: "Company section overview"
      exports: ["default"]
    - path: "src/app/reports/page.tsx"
      provides: "Reports section overview (Wave 3 AI Report Builder context)"
      exports: ["default"]
  key_links:
    - from: "All placeholder page.tsx files"
      to: "src/components/nw/NwPlaceholderCard.tsx (Plan 1 hoisted)"
      via: "named import"
      pattern: "import NwPlaceholderCard"
    - from: "src/app/financials/bills/page.tsx + queue + qa"
      to: "src/app/invoices/page.tsx + src/app/invoices/queue/page.tsx + src/app/invoices/qa/page.tsx (existing — reuse logic)"
      via: "rename / re-mount pattern (Plan 1's redirect handles legacy paths)"
      pattern: "(label|terminology) Bills"
    - from: "Sub Portal 3 stubs"
      to: "Caldwell fixtures + future F3 token validation infrastructure"
      via: "stub copy describes F3 compliance contract; mirror client_portal_access SHA-256 pattern (migration 00074)"
      pattern: "client_portal_access"
---

# Plan 4 — section-placeholders-sub-portal (1.5c iter-2 Wave 3)

<objective>
Build out the section-overview routes and sub-section placeholders for 6 of the 8 top-nav sections (Pipeline, Financials, Price Intel, People, Company, Reports), plus the 3 Sub Portal stubs with explicit F3 compliance contracts. Plan 1 mounted Today + Company/Overview + NwPlaceholderCard primitive (HOISTED per iter-2 mechanical #1); Plan 3 mounted the 12 production routes; Plan 5 handles the per-job sub-nav; Plan 6 handles Admin section. This plan ships the breadth — every nav section + every Wave-N future-state has a coherent placeholder.

Per CONTEXT D-13 (Q11=C): full per-job tab placeholders for all More-dropdown items + section-level placeholders so the IA can be evaluated end-to-end. Per CONTEXT D-02 (Q3=B): 3 Sub Portal stubs (auth + magic + public), each describing its specific F3 future-state.

This is parallel-safe with Plan 5 (per-job tabs) + Plan 6 (admin) — different file sets. Within this plan, the ~50 placeholder routes are all independent of each other; 4-6 parallel implementer agents can split.

iter-2 differences from iter-1:

1. **iter-2 mechanical #1 — NwPlaceholderCard HOISTED to Plan 1:** This plan no longer creates the primitive. Plan 1 ships it (renamed from PlaceholderCard to NwPlaceholderCard per PROPAGATION-RULES.md §4b). Plan 4 imports + consumes only. Removes the Wave 3 race condition where Plans 4/5/6 declared `requires PlaceholderCard` without `depends_on Plan 4 Task 1`.

2. **iter-2 mechanical #5 — Sub Portal token stub fixes:**
   - **(a) Param-reflection grep verify:** `grep -E "params\.token" src/app/sub-portal/magic/[token]/page.tsx src/app/sub-portal/public/[token]/page.tsx` returns 0 (no token reflection into HTML; security HIGH SEC-3).
   - **(b) F3 compliance contract in stub copy** (compliance W-2 + multi-tenant W-3 + iter-2 must-fix CRITICAL — but this is XR-5 aggregated WARNING per plan-review iter-1, escalated per synthesis rule but Jake assigned to iter-2 mechanical #5). Stub copy explicitly states:
     - Token expiry posture
     - Scope binding (single job / single document)
     - Audit-log-on-use (every magic link follow logs to activity_log)
     - Cookie-vs-querystring posture (querystring-only for magic; cookie-based session for authenticated /sub-portal)
     - JWT-with-org-claim REJECTED by-construction (vulnerability surface; not used)
     - Mirrors client_portal_access SHA-256 + service-role-API + anon-grant pattern per migration 00074

3. **iter-2 mechanical #9 — AC-1.5c-4-07 verify with 3 explicit grep tokens:**
   - `grep -c 'Subcontractor self-service' src/app/sub-portal/page.tsx` returns >=1
   - `grep -c 'magic link' src/app/sub-portal/magic/[token]/page.tsx` returns >=1
   - `grep -c 'public read-only' src/app/sub-portal/public/[token]/page.tsx` returns >=1

4. **iter-2 architect W-5 — REAL vs PLACEHOLDER per renamed route:** Plan 4 documents per route in SUMMARY whether it's a REAL-LOGIC re-mount (preserving getCurrentMembership() per iter-2 mechanical #10) or a fresh PLACEHOLDER. Affects the 7 REAL-LOGIC mounts in Half A (Bills/Pay Apps/Lien-Releases/Aging/Price-Intel reroutes).

5. **iter-2 mechanical #10 — REAL-LOGIC re-mounts preserve getCurrentMembership() verbatim:** For each REAL-LOGIC mount in Half A, run `diff <(grep -E 'getCurrentMembership|org_id|requireMembership' <source>/page.tsx | sort) <(grep -E ... <destination>/page.tsx | sort)` — expects zero diff. Apply for 7 routes:
   - /financials/bills (from /invoices)
   - /financials/bills/queue (from /invoices/queue)
   - /financials/bills/qa (from /invoices/qa)
   - /financials/payments (from /invoices/payments)
   - /financials/lien-releases (from /invoices/liens)
   - /financials/pay-apps (from /draws)
   - /financials/aging (from /financials/aging-report)
   - (Also Price Intel re-mounts: /price-intel/cost-lookup from /cost-intelligence/lookup; /price-intel/verification from /cost-intelligence/verification)

6. **iter-2 planner LOW-2 / LOW-4 — Wave 3 mid-checkpoint smoke:** At end of Plan 4 (before Plans 5 + 6 spawn), navigate to all 8 nav sections + spot-check 5 random per-section sub-routes; document any 404s in SUMMARY. Catches integration regressions early.

7. **iter-2 architect W-5 — /financials/aging real-backend disposition:** /financials/aging is a REAL-LOGIC re-mount of /financials/aging-report (existing functionality renamed). Plan 4 SUMMARY explicitly states REAL-LOGIC.

Purpose:
- Every nav section is reachable + has coherent landing page (no 404s on nav clicks)
- Every Wave-N future-state is documented as a placeholder route
- Sub Portal 3 routes named correctly with explicit F3 compliance contracts so F3 doesn't refactor URL paths AND token threat model is stable
- Site Office direction applied uniformly to ~50 placeholder pages (token discipline)
- REAL-LOGIC re-mounts preserve auth-checks verbatim

Output:
- ~50 placeholder + section-overview page.tsx files across Pipeline, Financials, Price Intel, People, Company, Reports
- 3 Sub Portal stubs at /sub-portal, /sub-portal/magic/[token], /sub-portal/public/[token] — with explicit F3 compliance contracts per iter-2 mechanical #5
</objective>

<execution_context>
@C:/Users/Jake/nightwork-platform/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jake/nightwork-platform/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/stage-1.5c-information-architecture/CONTEXT.md
@.planning/phases/stage-1.5c-information-architecture/PATTERNS.md
@.planning/phases/stage-1.5c-information-architecture/01.5c-1-nav-today-redirects-PLAN.md
@.planning/phases/stage-1.5c-information-architecture/01.5c-3-extraction-production-routes-PLAN.md
@.planning/expansions/stage-1.5c-information-architecture-EXPANDED-SCOPE.md
@CLAUDE.md
@.planning/design/SYSTEM.md
@.planning/design/COMPONENTS.md
@.planning/design/PATTERNS.md
@src/app/design-system/prototypes/page.tsx
@src/components/nw/Card.tsx
@src/components/nw/Eyebrow.tsx
@src/components/nw/Badge.tsx
@src/components/nw/NwPlaceholderCard.tsx
@src/app/financials/page.tsx
@src/app/invoices/page.tsx
@src/app/invoices/queue/page.tsx
@src/app/invoices/qa/page.tsx
@src/app/invoices/payments/page.tsx
@src/app/invoices/liens/page.tsx
@src/app/draws/page.tsx
@src/app/financials/aging-report/page.tsx
@src/app/cost-intelligence/page.tsx
@src/app/cost-intelligence/lookup/page.tsx
@src/app/cost-intelligence/verification/page.tsx
@src/app/proposals/review/[extraction_id]/page.tsx

<interfaces>
NwPlaceholderCard primitive (HOISTED to Plan 1 per iter-2 mechanical #1; this plan imports only):

  // src/components/nw/NwPlaceholderCard.tsx (Plan 1 already created)
  import NwPlaceholderCard from "@/components/nw/NwPlaceholderCard";

  <NwPlaceholderCard
    eyebrow="Section · Subsection"
    headline="Feature name"
    body="2-3 sentence description"
    wave="F4"
  />

Section overview pattern (analog: src/app/design-system/prototypes/page.tsx):
  - Header band: Eyebrow + Section headline + brief copy
  - Card grid: each card links to a sub-route with eyebrow + headline + 1-line description
  - Site Office signature throughout

Section-overview routes that mount real existing logic (NOT placeholders) per iter-2 architect W-5:
  - /financials/bills → mount existing /invoices/page.tsx logic with Bill terminology (REAL-LOGIC)
  - /financials/bills/queue → mount existing /invoices/queue/page.tsx logic (REAL-LOGIC)
  - /financials/bills/qa → mount existing /invoices/qa/page.tsx logic (REAL-LOGIC)
  - /financials/payments → mount existing /invoices/payments/page.tsx logic (REAL-LOGIC)
  - /financials/lien-releases → mount existing /invoices/liens/page.tsx logic (REAL-LOGIC)
  - /financials/pay-apps → mount existing /draws/page.tsx logic with Pay App terminology (REAL-LOGIC)
  - /financials/aging → mount existing /financials/aging-report/page.tsx logic (REAL-LOGIC; iter-2 architect W-5 explicit)
  - /price-intel/page.tsx → mount existing /cost-intelligence/page.tsx logic OR section overview Card grid (executor decides — depends on existing complexity)
  - /price-intel/cost-lookup → mount existing /cost-intelligence/lookup/page.tsx logic (REAL-LOGIC)
  - /price-intel/verification → mount existing /cost-intelligence/verification/page.tsx logic (REAL-LOGIC)

  These REAL-LOGIC routes (NOT placeholders) re-render existing logic at the new canonical paths. Plan 1 redirect map handles legacy paths → 308 to here. iter-2 mechanical #10 verifies getCurrentMembership() preservation.

Section-overview routes that are placeholders (no existing logic):
  - /pipeline + sub-routes — F4 / Wave 4 (CRM pre-construction)
  - /financials/purchase-orders + /financials/change-orders — placeholders for F1
  - /price-intel/selections-catalog + material-orders + bid-comparison + anomaly-review + vendor-performance + cost-database — F5
  - /people + clients + team + org-chart — placeholders for F1 / F2
  - /sub-portal + magic + public — F3
  - /company + sub-routes — F4 / Wave 2 / Wave 3 / Wave 4
  - /reports + sub-routes — Wave 3

Sub Portal F3 compliance contract (iter-2 mechanical #5):

  Per migration 00074 — client_portal_access table uses:
  - SHA-256-hashed token storage (not raw)
  - Service-role-API for token validation (not RLS)
  - Anon-grant for read access at the API layer
  - Single-job scope binding via foreign key
  - Audit-log writes on every link follow

  Sub Portal F3 wires the same pattern. JWT-with-org-claim is REJECTED — by-construction vulnerability surface (org_id leakage on stolen token; cross-tenant exposure if token validation fails).

  Stub copy in 1.5c MUST describe:
  1. Token expiry (e.g., "30-day expiry from email send; configurable per email type")
  2. Scope binding (single job / single document; no cross-job exposure)
  3. Audit-log-on-use (every magic link follow logs entity_type='client_portal_access' / action='accessed' to activity_log)
  4. Cookie-vs-querystring (magic links use querystring; authenticated /sub-portal uses cookie session)
  5. JWT-with-org-claim REJECTED (vulnerability surface; we do NOT include org_id in the token)
  6. Mirror client_portal_access pattern (SHA-256 hash + service-role-API + anon-grant + audit-log)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pipeline section (6 placeholder routes — all use NwPlaceholderCard from Plan 1)</name>
  <files>src/app/pipeline/page.tsx, src/app/pipeline/leads/page.tsx, src/app/pipeline/estimates/page.tsx, src/app/pipeline/bids-out/page.tsx, src/app/pipeline/proposals/page.tsx, src/app/pipeline/contracts/page.tsx</files>

  <read_first>
    - src/app/design-system/prototypes/page.tsx (10-Card index analog for section overview)
    - src/components/nw/Card.tsx + Eyebrow.tsx + Badge.tsx (primitive contracts)
    - src/components/nw/NwPlaceholderCard.tsx (Plan 1 hoisted)
    - .planning/design/SYSTEM.md §13 Forbidden tokens
    - .planning/phases/stage-1.5c-information-architecture/PATTERNS.md (Pipeline mappings)
    - .planning/expansions/stage-1.5c-information-architecture-EXPANDED-SCOPE.md Part 3 PIPELINE definition
    - src/app/proposals/review/[extraction_id]/page.tsx (existing — Plan 1 redirects /proposals/review/[extraction_id] to /pipeline/proposals/review/[extraction_id]; Plan 4 just creates /pipeline/proposals/page.tsx as placeholder overview)
  </read_first>

  <action>
**iter-2 mechanical #1 — NO PlaceholderCard creation here.** Plan 1 hoisted NwPlaceholderCard. This task imports + consumes.

**Step A — Create Pipeline section overview at /pipeline/page.tsx.**

Section overview pattern: header + Card grid linking to 5 sub-routes.

```typescript
// src/app/pipeline/page.tsx
//
// Pipeline section overview — pre-construction lifecycle per Principle 8.
// Profile-aware (custom_builder shows full Pipeline; remodeler hides — F2 wires).
// 1.5c: same nav for all profiles; F2 adds profile-based hiding.

import Link from "next/link";
import Card from "@/components/nw/Card";
import Eyebrow from "@/components/nw/Eyebrow";
import Badge from "@/components/nw/Badge";

const PIPELINE_SECTIONS = [
  { href: "/pipeline/leads", label: "Leads", desc: "CRM intake — track potential clients", wave: "Wave 4" as const },
  { href: "/pipeline/estimates", label: "Estimates", desc: "Price Intel powered estimates", wave: "Wave 4" as const },
  { href: "/pipeline/bids-out", label: "Bids out", desc: "Sub bids in flight", wave: "Wave 4" as const },
  { href: "/pipeline/proposals", label: "Proposals", desc: "E-sign proposals to clients", wave: "Wave 4" as const },
  { href: "/pipeline/contracts", label: "Contracts", desc: "Owner contracts + sub master agreements", wave: "Wave 4" as const },
];

export default function PipelineOverviewPage() {
  return (
    <div className="px-6 py-8 max-w-[1200px] mx-auto">
      <div className="mb-6">
        <Eyebrow tone="muted" className="mb-2">Pipeline</Eyebrow>
        <h1 className="text-[28px] mb-1" style={{ fontFamily: "var(--font-space-grotesk)", fontWeight: 500, color: "var(--text-primary)" }}>
          Pre-construction
        </h1>
        <p className="text-[14px]" style={{ color: "var(--text-secondary)" }}>
          From lead intake through signed contract. Powered by Price Intel for accurate estimates.
        </p>
      </div>
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-5">
        {PIPELINE_SECTIONS.map((s) => (
          <Link key={s.href} href={s.href} className="block">
            <Card padding="md">
              <Eyebrow tone="default" className="mb-2">{s.label}</Eyebrow>
              <p className="text-[13px] mb-3" style={{ color: "var(--text-secondary)" }}>{s.desc}</p>
              <Badge variant="neutral">Coming {s.wave}</Badge>
            </Card>
          </Link>
        ))}
      </div>
    </div>
  );
}
```

**Step B — Create 5 Pipeline sub-route placeholders using NwPlaceholderCard.**

```typescript
// src/app/pipeline/leads/page.tsx
import NwPlaceholderCard from "@/components/nw/NwPlaceholderCard";
export default function LeadsPage() {
  return (
    <div className="px-6 py-8 max-w-[800px] mx-auto">
      <NwPlaceholderCard
        eyebrow="Pipeline · Leads"
        headline="CRM lead intake"
        body="Track potential clients from first contact through estimate-ready. Source attribution, pipeline stage, last-touch tracking, conversion rate metrics."
        wave="Wave 4"
      />
    </div>
  );
}
```

Apply same pattern to:
- /pipeline/estimates/page.tsx — "Price Intel powered estimates" — Wave 4
- /pipeline/bids-out/page.tsx — "Sub bids in flight" — Wave 4
- /pipeline/proposals/page.tsx — "E-sign proposals to clients" — Wave 4
- /pipeline/contracts/page.tsx — "Owner contracts + sub master agreements" — Wave 4

Per CONTEXT D-13: each ~30 LOC. Token discipline.
  </action>

  <verify>
    <automated>npm run build &amp;&amp; ls src/app/pipeline/*/page.tsx | wc -l &amp;&amp; ls src/app/pipeline/page.tsx &amp;&amp; grep -c 'NwPlaceholderCard' src/app/pipeline/leads/page.tsx</automated>
    Expected: build exits 0; ls returns 5 sub-routes; pipeline overview exists; NwPlaceholderCard imported per sub-route.

    Hex check: `grep -nE '#[0-9a-fA-F]{3,6}' src/components/nw/NwPlaceholderCard.tsx src/app/pipeline/**/page.tsx | grep -v 'var(--' | wc -l` returns 0.

    Tenant-blind check: `grep -E 'org_id|membership|vendor_id' src/app/pipeline/**/page.tsx` returns 0.

    Visual: visit /pipeline + each sub-route — renders NwPlaceholderCard with Site Office tokens.
  </verify>

  <done>
    - Pipeline section: 1 overview + 5 sub-route placeholders (6 files)
    - All sub-routes consume NwPlaceholderCard from Plan 1
    - Build passes
    - Privacy gate clean
    - Hooks silent
  </done>
</task>

<task type="auto">
  <name>Task 2: Financials restructure + 9 sub-routes (Bills + Pay Apps + Lien Releases + Payments + Aging + POs + COs) + Price Intel section (9 routes) — REAL-LOGIC mounts preserve getCurrentMembership() (iter-2 mechanical #10)</name>
  <files>src/app/financials/page.tsx, src/app/financials/bills/page.tsx, src/app/financials/bills/queue/page.tsx, src/app/financials/bills/qa/page.tsx, src/app/financials/payments/page.tsx, src/app/financials/lien-releases/page.tsx, src/app/financials/pay-apps/page.tsx, src/app/financials/purchase-orders/page.tsx, src/app/financials/change-orders/page.tsx, src/app/financials/aging/page.tsx, src/app/price-intel/page.tsx, src/app/price-intel/cost-lookup/page.tsx, src/app/price-intel/selections-catalog/page.tsx, src/app/price-intel/material-orders/page.tsx, src/app/price-intel/bid-comparison/page.tsx, src/app/price-intel/anomaly-review/page.tsx, src/app/price-intel/vendor-performance/page.tsx, src/app/price-intel/verification/page.tsx, src/app/price-intel/cost-database/page.tsx</files>

  <read_first>
    - src/app/financials/page.tsx (current — restructure to be section overview)
    - src/app/invoices/page.tsx + queue + qa + payments + liens (existing logic; mount at new /financials/bills + sub-paths)
    - src/app/draws/page.tsx (existing logic; mount at /financials/pay-apps)
    - src/app/financials/aging-report/page.tsx (existing; rename to /financials/aging)
    - src/app/cost-intelligence/page.tsx (existing; rename to /price-intel)
    - src/app/cost-intelligence/lookup/page.tsx (existing; rename to /price-intel/cost-lookup)
    - src/app/cost-intelligence/verification/page.tsx (existing; rename to /price-intel/verification)
    - src/components/nw/NwPlaceholderCard.tsx (Plan 1 hoisted)
    - .planning/phases/stage-1.5c-information-architecture/PATTERNS.md (Financials + Price Intel mappings)
  </read_first>

  <action>
**Two halves to this task — REAL-LOGIC mounts (Half A) vs PLACEHOLDER mounts (Half B):**

**Half A — REAL-LOGIC mounts (existing pages re-mounted at canonical paths with Bills/Pay Apps terminology + iter-2 mechanical #10 getCurrentMembership preservation):**

For each, COPY the rendering logic from the existing source and rename to canonical path. Apply Bills/Pay Apps label rename in user-visible UI:

1. **`/financials/bills/page.tsx`** — copy from `src/app/invoices/page.tsx`. Rename "Invoice" → "Bill" / "Invoices" → "Bills" in UI labels. Component file imports + state names retain `invoice*` per CONTEXT D-01 scope clarification (UI labels visible; code identifiers not).
   - **iter-2 mechanical #10 verify:** `diff <(grep -E 'getCurrentMembership|org_id|requireMembership' src/app/invoices/page.tsx | sort) <(grep -E ... src/app/financials/bills/page.tsx | sort)` returns zero diff. Document in SUMMARY: REAL-LOGIC re-mount.

2. **`/financials/bills/queue/page.tsx`** — copy from `src/app/invoices/queue/page.tsx`. Same rename + iter-2 mechanical #10 verify.

3. **`/financials/bills/qa/page.tsx`** — copy from `src/app/invoices/qa/page.tsx`. Same rename + iter-2 mechanical #10 verify.

4. **`/financials/payments/page.tsx`** — copy from `src/app/invoices/payments/page.tsx`. Section path changed; logic unchanged. + iter-2 mechanical #10 verify.

5. **`/financials/lien-releases/page.tsx`** — copy from `src/app/invoices/liens/page.tsx`. Rename "Liens" → "Lien Releases" in UI labels. + iter-2 mechanical #10 verify.

6. **`/financials/pay-apps/page.tsx`** — copy from `src/app/draws/page.tsx`. Rename "Draw" → "Pay App" / "Draws" → "Pay Apps" in UI labels. + iter-2 mechanical #10 verify.

7. **`/financials/aging/page.tsx`** — copy from `src/app/financials/aging-report/page.tsx`. UI labels: "Aging" stays (iter-2 architect W-5: this is REAL-LOGIC re-mount; SUMMARY explicit). + iter-2 mechanical #10 verify.

**For each REAL-LOGIC mount:**
- Read source fully
- Create new file at canonical path
- Apply terminology rename in user-visible labels (not in variable names / state shape)
- Verify hooks silent (the existing pages already pass hooks; the rename should preserve)
- iter-2 mechanical #10: grep diff getCurrentMembership / org_id / requireMembership — expects zero diff
- Note: Plan 1's redirect map handles legacy paths /invoices/* + /draws/* + /financials/aging-report

**Half B — PLACEHOLDER mounts (no existing logic):**

8. **`/financials/page.tsx`** — RESTRUCTURE existing page to be a section overview Card grid linking to all 7 sub-routes (Bills, Pay Apps, Purchase Orders, Change Orders, Lien Releases, Payments, Aging). Use Pipeline section pattern (Task 1).
   - **iter-2 architect W-5:** This is a RESTRUCTURE of existing /financials/page.tsx. Verify if existing has substantive logic; if so, preserve it in a comment block for F1 reference. SUMMARY documents.

9. **`/financials/purchase-orders/page.tsx`** — placeholder. Body: "Org-wide PO list. Job-scoped POs already exist at /jobs/[id]/purchase-orders. F1 unifies." Wave: F1.

10. **`/financials/change-orders/page.tsx`** — placeholder. Body: "Org-wide change order list. Job-scoped COs at /jobs/[id]/change-orders. F1 unifies." Wave: F1.

**Price Intel section (9 routes):**

11. **`/price-intel/page.tsx`** — copy + rename from `/cost-intelligence/page.tsx`. UI labels: "Cost Intelligence" → "Price Intel". OR if existing /cost-intelligence/page.tsx has heavy code, refactor to also be a section overview Card grid linking to all 8 sub-routes.
    - **iter-2 architect W-5:** SUMMARY documents whether REAL-LOGIC re-mount OR section overview restructure.

12. **`/price-intel/cost-lookup/page.tsx`** — copy + rename from `/cost-intelligence/lookup/page.tsx`. + iter-2 mechanical #10 verify.

13. **`/price-intel/verification/page.tsx`** — copy + rename from `/cost-intelligence/verification/page.tsx`. + iter-2 mechanical #10 verify.

14. **`/price-intel/cost-database/page.tsx`** — placeholder. Body: "Cost database — historical costs across vendors / cost codes / jobs / time. F5 wires this." Wave: F5.

15. **`/price-intel/selections-catalog/page.tsx`** — placeholder. Body: "Selections catalog as a VIEW of Price Intel filtered by product entities. Picked → spec → ordered → delivered → installed → warranty lifecycle." Wave: F5.

16. **`/price-intel/material-orders/page.tsx`** — placeholder. Body: "Material orders — vendor material orders linked to jobs + cost codes." Wave: F5.

17. **`/price-intel/bid-comparison/page.tsx`** — placeholder. Body: "Bid comparison engine — sub bid normalization + delta analysis." Wave: F5.

18. **`/price-intel/anomaly-review/page.tsx`** — placeholder. Body: "Anomaly review — bills flagged by Price Intel for unusual cost vs historical." Wave: F5.

19. **`/price-intel/vendor-performance/page.tsx`** — placeholder. Body: "Vendor performance scoring — on-time + on-budget + dispute rate." Wave: F5.

**Hook silence requirement:** all 18 files must trigger zero hook rejections.
  </action>

  <verify>
    <automated>npm run build &amp;&amp; ls src/app/financials/bills/page.tsx src/app/financials/bills/queue/page.tsx src/app/financials/bills/qa/page.tsx src/app/financials/payments/page.tsx src/app/financials/lien-releases/page.tsx src/app/financials/pay-apps/page.tsx src/app/financials/aging/page.tsx src/app/financials/purchase-orders/page.tsx src/app/financials/change-orders/page.tsx src/app/financials/page.tsx src/app/price-intel/page.tsx src/app/price-intel/cost-lookup/page.tsx src/app/price-intel/verification/page.tsx src/app/price-intel/cost-database/page.tsx src/app/price-intel/selections-catalog/page.tsx src/app/price-intel/material-orders/page.tsx src/app/price-intel/bid-comparison/page.tsx src/app/price-intel/anomaly-review/page.tsx src/app/price-intel/vendor-performance/page.tsx 2>&amp;1 | grep -c 'page.tsx'</automated>
    Expected: build exits 0; ls returns 19.

    Bills/Pay Apps terminology check:
    - `grep -c 'Bills' src/app/financials/bills/page.tsx` returns >=1
    - `grep -c 'Pay Apps' src/app/financials/pay-apps/page.tsx` returns >=1
    - `grep -c 'Lien Releases' src/app/financials/lien-releases/page.tsx` returns >=1
    - `grep -c 'Price Intel' src/app/price-intel/page.tsx` returns >=1

    iter-2 mechanical #10 — REAL-LOGIC getCurrentMembership preservation per route (7 Financials + 2 Price Intel = 9 total):
    For each REAL-LOGIC re-mount, run grep diff:
    - `diff <(grep -E 'getCurrentMembership|org_id|requireMembership' src/app/invoices/page.tsx | sort) <(grep -E 'getCurrentMembership|org_id|requireMembership' src/app/financials/bills/page.tsx | sort)` — zero diff
    - (repeat for queue, qa, payments, liens, draws, aging-report, cost-intelligence/lookup, cost-intelligence/verification)
    SUMMARY documents per-route status (zero diff = preserved verbatim; OR documented divergence reasoning).

    Hex check: across all 19 new files, 0 hardcoded hex outside CSS-var fallbacks.

    Visual smoke: visit each route — renders without errors.
  </verify>

  <done>
    - 7 REAL-LOGIC routes mounted at canonical paths with terminology rename
    - 12 PLACEHOLDER routes with NwPlaceholderCard
    - Financials + Price Intel section overviews link to sub-routes
    - iter-2 mechanical #10: getCurrentMembership preservation verified per re-mounted route (9 routes)
    - iter-2 architect W-5: REAL vs PLACEHOLDER status per route documented in SUMMARY
    - Build passes
    - Privacy gate clean + hooks silent
  </done>
</task>

<task type="auto">
  <name>Task 3: People section + Sub Portal 3 stubs (with explicit F3 compliance contracts per iter-2 mechanical #5) + Company section + Reports section (~30 placeholders)</name>
  <files>src/app/people/page.tsx, src/app/people/clients/page.tsx, src/app/people/team/page.tsx, src/app/people/org-chart/page.tsx, src/app/sub-portal/page.tsx, src/app/sub-portal/magic/[token]/page.tsx, src/app/sub-portal/public/[token]/page.tsx, src/app/company/page.tsx, src/app/company/p-l/page.tsx, src/app/company/cash-flow/page.tsx, src/app/company/expenses/page.tsx, src/app/company/time-tracking/page.tsx, src/app/company/compliance/page.tsx, src/app/company/fleet-equipment/page.tsx, src/app/company/insurance/page.tsx, src/app/company/permits-licensing/page.tsx, src/app/company/tax/page.tsx, src/app/company/capacity-planning/page.tsx, src/app/company/documents/page.tsx, src/app/reports/page.tsx, src/app/reports/saved/page.tsx, src/app/reports/ai-builder/page.tsx, src/app/reports/custom-builder/page.tsx, src/app/reports/scheduled/page.tsx, src/app/reports/templates/page.tsx</files>

  <read_first>
    - src/components/nw/NwPlaceholderCard.tsx (Plan 1 hoisted)
    - src/app/pipeline/page.tsx (Task 1 — section overview pattern)
    - .planning/phases/stage-1.5c-information-architecture/PATTERNS.md (People + Sub Portal + Company + Reports mappings)
    - .planning/expansions/stage-1.5c-information-architecture-EXPANDED-SCOPE.md Part 3 (PEOPLE / COMPANY / REPORTS definitions)
    - .planning/expansions/stage-1.5c-information-architecture-EXPANDED-SCOPE.md Part 5 (Sub Portal 3-stub structure)
    - **Migration 00074 (client_portal_access table)** — verify SHA-256 + service-role-API + anon-grant pattern that Sub Portal F3 mirrors per iter-2 mechanical #5
  </read_first>

  <action>
**Per nwrp44 standing rule:** parallel-safe — 25 files independent. 4-6 parallel implementer agents can split.

**People section (4 routes):**

1. **`/people/page.tsx`** — section overview. Card grid linking to: /people/vendors (LIVE — Plan 3) + /people/clients + /people/team + /people/org-chart + /sub-portal. Use Pipeline section pattern.

2. **`/people/clients/page.tsx`** — placeholder. Body: "Client directory + relationship history. Current + past clients + leads from Pipeline. F1 schema; Wave 1.1-Lite goes live with Ross Built data." Wave: F1.

3. **`/people/team/page.tsx`** — placeholder. Body: "Internal team — employees with roles, comp, performance. F2 expands org_members to 15-role permission matrix." Wave: F2.

4. **`/people/org-chart/page.tsx`** — placeholder. Body: "Visual org chart driving reporting + approval routing + notification routing + permission inheritance." Wave: F2.

**Sub Portal 3 stubs (per CONTEXT D-02 / Q3=B + iter-2 mechanical #5 explicit F3 compliance contracts):**

5. **`/sub-portal/page.tsx`** — authenticated sub portal stub.

```typescript
// src/app/sub-portal/page.tsx
//
// Sub Portal AUTHENTICATED stub per CONTEXT D-02 / Q3=B.
// F3 wires real authentication. iter-2 mechanical #5: explicit compliance contract.

import NwPlaceholderCard from "@/components/nw/NwPlaceholderCard";

export default function SubPortalAuthPage() {
  return (
    <div className="px-6 py-8 max-w-[800px] mx-auto">
      <NwPlaceholderCard
        eyebrow="Sub Portal · Authenticated"
        headline="Subcontractor self-service portal"
        body="Subcontractors with full accounts manage Bids / POs / Bills / Schedule / Daily Logs / RFIs / Documents / Profile. Authenticated via existing Supabase auth + role gate ('sub' or future 'sub_foreman' / 'sub_bookkeeper'). F3 wires the role engine + sub-side dashboards. Compliance contract: standard org_members RLS posture (org-scoped reads); cookie-based session; audit-log writes on every mutation. JWT-with-org-claim REJECTED by-construction (vulnerability surface)."
        wave="F3"
      />
    </div>
  );
}
```

6. **`/sub-portal/magic/[token]/page.tsx`** — magic link stub with explicit F3 compliance contract.

```typescript
// src/app/sub-portal/magic/[token]/page.tsx
//
// Sub Portal MAGIC LINK stub per CONTEXT D-02 / Q3=B + iter-2 mechanical #5.
// F3 wires real token validation infrastructure.
//
// IMPORTANT: this stub does NOT validate or display the [token] param.
// iter-2 mechanical #5 (a) — param-reflection grep verify:
//   `grep -E "params\.token" src/app/sub-portal/magic/[token]/page.tsx` returns 0
// We accept the [token] route segment so the URL pattern is named correctly
// for F3 emails, but we do NOT reflect the token into HTML (XSS surface).

import NwPlaceholderCard from "@/components/nw/NwPlaceholderCard";

export default function SubPortalMagicLinkPage() {
  return (
    <div className="px-6 py-8 max-w-[800px] mx-auto">
      <NwPlaceholderCard
        eyebrow="Sub Portal · Magic link"
        headline="Email-driven magic link auth"
        body={[
          "Subcontractor clicks 'Respond to RFI' / 'Sign PO' / 'Submit Bill' email link → temporary session via signed token → respond.",
          "F3 compliance contract:",
          "(a) Token expiry: 30-day default from email send (configurable per email type).",
          "(b) Scope binding: single job + single document; no cross-job exposure.",
          "(c) Audit-log-on-use: every magic link follow logs entity_type='client_portal_access' / action='accessed' to activity_log (mirror migration 00074 pattern).",
          "(d) Cookie-vs-querystring: magic links use querystring; verified via service-role-API endpoint; querystring is single-use (rotated on first follow).",
          "(e) JWT-with-org-claim REJECTED by-construction (vulnerability surface; we do NOT include org_id in the token — instead, server-side resolves scope from token hash).",
          "(f) Mirrors client_portal_access pattern (migration 00074): SHA-256 hash + service-role-API + anon-grant + audit-log."
        ].join(" ")}
        wave="F3"
      />
    </div>
  );
}
```

7. **`/sub-portal/public/[token]/page.tsx`** — public read-only link stub with explicit F3 compliance contract.

```typescript
// src/app/sub-portal/public/[token]/page.tsx
//
// Sub Portal PUBLIC READ-ONLY stub per CONTEXT D-02 / Q3=B + iter-2 mechanical #5.
// F3 wires real public-link infrastructure.
//
// IMPORTANT: this stub does NOT validate or display the [token] param.
// iter-2 mechanical #5 (a) — param-reflection grep verify:
//   `grep -E "params\.token" src/app/sub-portal/public/[token]/page.tsx` returns 0

import NwPlaceholderCard from "@/components/nw/NwPlaceholderCard";

export default function SubPortalPublicLinkPage() {
  return (
    <div className="px-6 py-8 max-w-[800px] mx-auto">
      <NwPlaceholderCard
        eyebrow="Sub Portal · Public read-only"
        headline="Architects, inspectors, external observers"
        body={[
          "Public read-only access: architects, inspectors, external observers receive a unique URL granting view-only access to specific job documents (plans, schedules, RFIs, photos). NO write/edit access.",
          "F3 compliance contract:",
          "(a) Token expiry: configurable per share (default 90 days).",
          "(b) Scope binding: single job + view-scope filter (e.g., 'plans only', 'schedule only').",
          "(c) Audit-log-on-use: every public link follow logs entity_type='client_portal_access' / action='accessed' (cross-link with the magic-link audit pattern).",
          "(d) Cookie-vs-querystring: public links use querystring (no session); each request validates token + view-scope.",
          "(e) JWT-with-org-claim REJECTED by-construction (vulnerability surface; we do NOT include org_id in the token — server-side resolves scope from token hash).",
          "(f) Mirrors client_portal_access pattern (migration 00074): SHA-256 hash + service-role-API + anon-grant + audit-log."
        ].join(" ")}
        wave="F3"
      />
    </div>
  );
}
```

Per iter-2 mechanical #5 (a): the route accepts a `[token]` param so URL pattern is named correctly for F3 emails, but the page does NOT use `params.token` anywhere — verify via grep. F3 will validate the token at the route handler level (server-side; not in the page component).

**Company section (12 routes):**

8. **`/company/page.tsx`** — section overview. Card grid linking to all sub-routes. /company/overview is from Plan 1 (Cash Flow + Getting Started — link to it as one of the cards).

9-19. **Company sub-routes** — all NwPlaceholderCard placeholders:
- `/company/p-l/page.tsx` — Wave: F4
- `/company/cash-flow/page.tsx` — Wave: F4
- `/company/expenses/page.tsx` — Wave: F4
- `/company/time-tracking/page.tsx` — Wave: F4
- `/company/compliance/page.tsx` — Wave: Wave 2
- `/company/fleet-equipment/page.tsx` — Wave: F4
- `/company/insurance/page.tsx` — Wave: Wave 2
- `/company/permits-licensing/page.tsx` — Wave: Wave 2
- `/company/tax/page.tsx` — Wave: Wave 4
- `/company/capacity-planning/page.tsx` — Wave: Wave 3
- `/company/documents/page.tsx` — Wave: Wave 2

**Reports section (6 routes):**

20. **`/reports/page.tsx`** — section overview describing Wave 3 AI Report Builder.

21-25. **Reports sub-routes** — all NwPlaceholderCard placeholders, all Wave 3.

**Each placeholder ~30 LOC.** Pattern (mirror Task 1's leads/page.tsx):

```typescript
import NwPlaceholderCard from "@/components/nw/NwPlaceholderCard";
export default function <Name>Page() {
  return (
    <div className="px-6 py-8 max-w-[800px] mx-auto">
      <NwPlaceholderCard
        eyebrow="Section · Subsection"
        headline="Headline"
        body="Body description"
        wave="WaveValue"
      />
    </div>
  );
}
```

**Token discipline:** Site Office throughout. No hardcoded hex.
  </action>

  <verify>
    <automated>npm run build &amp;&amp; ls src/app/people/*/page.tsx src/app/sub-portal/page.tsx src/app/sub-portal/magic/[token]/page.tsx src/app/sub-portal/public/[token]/page.tsx src/app/company/*/page.tsx src/app/reports/*/page.tsx 2>&amp;1 | grep -c 'page.tsx'</automated>
    Expected: build exits 0; ls returns >=25.

    Hex check: across all 25 new files, 0 hardcoded hex outside CSS-var fallbacks.

    Tenant-blind: 25 page.tsx files have no org_id / membership / vendor_id props.

    Sub Portal 3 stubs verified:
    - `/sub-portal/page.tsx` exists
    - `/sub-portal/magic/[token]/page.tsx` exists with [token] param
    - `/sub-portal/public/[token]/page.tsx` exists with [token] param

    iter-2 mechanical #9 — AC-1.5c-4-07 verify with 3 explicit grep tokens:
    - `grep -c 'Subcontractor self-service' src/app/sub-portal/page.tsx` returns >=1
    - `grep -c 'magic link' src/app/sub-portal/magic/[token]/page.tsx` returns >=1
    - `grep -c 'public read-only' src/app/sub-portal/public/[token]/page.tsx` returns >=1

    iter-2 mechanical #5 (a) — param-reflection grep verify:
    `grep -E "params\.token" src/app/sub-portal/magic/[token]/page.tsx src/app/sub-portal/public/[token]/page.tsx` returns 0 (no token reflection into HTML — XSS prevention).

    iter-2 mechanical #5 (b) — F3 compliance contract grep verify:
    - `grep -c 'Token expiry\|expires' src/app/sub-portal/magic/[token]/page.tsx src/app/sub-portal/public/[token]/page.tsx` returns >=2 (one per stub)
    - `grep -c 'JWT-with-org-claim REJECTED' src/app/sub-portal/magic/[token]/page.tsx src/app/sub-portal/public/[token]/page.tsx` returns >=2 (one per stub)
    - `grep -c 'client_portal_access\|migration 00074\|SHA-256' src/app/sub-portal/magic/[token]/page.tsx src/app/sub-portal/public/[token]/page.tsx` returns >=2

    Visual smoke: visit each route — NwPlaceholderCard renders.
  </verify>

  <done>
    - People section: 4 routes (overview + 3 placeholders; vendors lives from Plan 3)
    - Sub Portal: 3 stubs at distinct routes per D-02
    - Each Sub Portal stub has explicit F3 compliance contract per iter-2 mechanical #5
    - Sub Portal stubs do NOT reflect params.token into HTML
    - AC-1.5c-4-07 grep tokens (iter-2 mechanical #9) verified per stub
    - Company section: 12 routes (overview + 11 placeholders; /company/overview lives from Plan 1)
    - Reports section: 6 routes (overview + 5 placeholders)
    - All ~30 LOC per Spec D-13
    - All consume NwPlaceholderCard from Plan 1 (iter-2 mechanical #1)
    - Build passes
    - Privacy gate clean
    - Hooks silent
  </done>
</task>

<task type="auto">
  <name>Task 4: Wave 3 mid-checkpoint smoke (iter-2 planner LOW-2 + LOW-4)</name>
  <files>(verification only — output is smoke report appended to plan SUMMARY)</files>

  <read_first>
    - All Plan 4 page.tsx files just created
    - Plan 1's nav-bar.tsx (8-section nav structure)
    - Plan 3's 12 production routes
  </read_first>

  <action>
**iter-2 planner LOW-2 + LOW-4 — Wave 3 mid-checkpoint smoke test.**

Per plan-review iter-1 LOW findings: Wave 3 (Day 4.25-7.25) is a 3-day stretch with no integration checkpoint between Plans 4-6. Real-data smoke under-encoded mid-phase. iter-2 adds an end-of-Plan-4 smoke step before Plans 5 + 6 spawn.

**Smoke procedure:**

1. `npm run dev`

2. **Visit all 8 nav sections** (clicking each top-nav item):
   - / → /today (Plan 1)
   - /pipeline → renders section overview Card grid (Plan 4)
   - /jobs → existing list page (no change)
   - /financials → restructured section overview (Plan 4)
   - /price-intel → renders (Plan 4)
   - /people → renders (Plan 4)
   - /company → renders (Plan 4)
   - /reports → renders (Plan 4)

3. **Spot-check 5 random per-section sub-routes** (sample):
   - /pipeline/leads (Plan 4 placeholder)
   - /financials/bills (Plan 4 REAL-LOGIC mount)
   - /price-intel/cost-database (Plan 4 placeholder)
   - /sub-portal/magic/abc123 (Plan 4 stub; verify [token] param accepts but renders generic stub)
   - /company/p-l (Plan 4 placeholder)

4. **Spot-check 3 of Plan 3's production routes:**
   - /financials/bills/inv-caldwell-001 (Plan 3 — Document Review)
   - /jobs/j-caldwell-1/budget (Plan 3 — Budget multi-fixture)
   - /people/vendors (Plan 3 — Vendor list)

5. **Document any 404s** in SUMMARY:
   - /jobs/j-caldwell-1/[any per-job sub-tab from Plan 5] → expected 404 at this checkpoint (Plan 5 not yet spawned)
   - /admin/[any sub-route from Plan 6] → expected 404 (Plan 6 not yet spawned)
   - /platform-admin → expected 404 (Plan 6 not yet spawned)

6. **Build + typecheck + hooks + privacy gate clean across all 50+ Plan 4 new files.**

**Output in SUMMARY:**

```
## Wave 3 Mid-Checkpoint Smoke (iter-2 planner LOW-2 / LOW-4)

### 8 nav sections all reachable
- /today: PASS (Plan 1)
- /pipeline: PASS (Plan 4 — overview)
- /jobs: PASS (existing)
- /financials: PASS (Plan 4 — restructured overview)
- /price-intel: PASS (Plan 4 — REAL-LOGIC mount)
- /people: PASS (Plan 4 — overview)
- /company: PASS (Plan 4 — overview; /company/overview from Plan 1 has Cash Flow)
- /reports: PASS (Plan 4 — overview)

### 5 sub-route spot-checks
- /pipeline/leads: PASS
- /financials/bills: PASS (REAL-LOGIC mount per iter-2 W-5)
- /price-intel/cost-database: PASS
- /sub-portal/magic/abc123: PASS (stub renders; [token] param accepts; no reflection)
- /company/p-l: PASS

### 3 Plan 3 production route spot-checks
- /financials/bills/inv-caldwell-001: PASS (with .design-system-scope wrap per D-19)
- /jobs/j-caldwell-1/budget: PASS
- /people/vendors: PASS

### Expected 404s (Plan 5 + Plan 6 pending)
- /jobs/j-caldwell-1/selections: 404 (Plan 5)
- /admin/users: 404 (Plan 6)
- /platform-admin: 404 (Plan 6)

### Issues observed: 0 (or list)

### Wave 3 ready for Plans 5 + 6 spawn: YES
```

If any unexpected 404 OR console errors observed, halt + investigate before Wave 3 continues.
  </action>

  <verify>
    <automated>test -f .planning/phases/stage-1.5c-information-architecture/01.5c-4-section-placeholders-sub-portal-SUMMARY.md &amp;&amp; grep -c 'Wave 3 Mid-Checkpoint Smoke' .planning/phases/stage-1.5c-information-architecture/01.5c-4-section-placeholders-sub-portal-SUMMARY.md</automated>
    Expected: SUMMARY exists; grep returns >=1 for Wave 3 Mid-Checkpoint Smoke section.

    Manual halt check: SUMMARY documents PASS for all 8 nav sections + 5 sub-route spot-checks + 3 Plan 3 production route spot-checks; expected 404s explicitly listed; issues observed counted; Wave 3 spawn readiness confirmed.
  </verify>

  <done>
    - Wave 3 mid-checkpoint smoke executed
    - SUMMARY documents results (per iter-2 planner LOW-2 / LOW-4)
    - Wave 3 ready for Plans 5 + 6 spawn (or halt if issues observed)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Section overview pages → sub-routes | Pure render-only navigation; no auth surfaces |
| Sub Portal 3 stubs → token params | Stubs accept [token] param; do NOT validate or reflect (F3 wires); current behavior renders generic stub regardless of token value |
| REAL-LOGIC re-mounts → original /invoices /draws /financials /cost-intelligence logic | Same RLS, same auth, same audit-log writes; only UI labels change; iter-2 mechanical #10 verifies preservation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.5c-4-01 | I (Information disclosure) | Sub Portal magic / public stubs accept [token] in URL | mitigate | Stubs do NOT validate or reflect token into HTML (iter-2 mechanical #5 (a)). Generic placeholder content renders for any token value. F3 implements actual token validation + scope filtering. iter-2 mechanical #5 (b) F3 compliance contract documented in stub copy ensures F3 implementation will follow client_portal_access pattern. No information leak in 1.5c. |
| T-1.5c-4-02 | E (Elevation of privilege) | Sub Portal authenticated stub | accept | Sub Portal authenticated route gates on existing middleware. 1.5c stub does not provide actual sub-role gating; F3 wires the role engine. Placeholder content does not include sensitive data. |
| T-1.5c-4-03 | T (Tampering) | Real-logic mounts (Bills/Pay Apps/Lien-Releases/Aging/Price-Intel/etc.) | mitigate | Mount preserves existing logic verbatim; only UI labels change. iter-2 mechanical #10 grep diff verifies getCurrentMembership / org_id / requireMembership preservation. Existing logic's auth + RLS + audit-log posture preserved. |
| T-1.5c-4-04 | R (Repudiation) | ~50 placeholder pages | accept | Read-only; no audit-log writes. Existing middleware Sentry tagging applies. |
| T-1.5c-4-05 | E (Elevation of privilege) | iter-2 mechanical #5 (b) F3 compliance contract — JWT-with-org-claim rejection | mitigate | Stub copy explicitly REJECTS JWT-with-org-claim by-construction. F3 implementer reading stub copy understands: do NOT include org_id in token; server-side resolves scope from token hash. Vulnerability surface (org_id leakage on stolen token; cross-tenant exposure) prevented at design time. |
</threat_model>

<verification>
After all 4 tasks complete:

1. NwPlaceholderCard imported (NOT created) — Plan 1 hoisted per iter-2 mechanical #1
2. Pipeline section: 6 routes (1 overview + 5 placeholders)
3. Financials section: 9 sub-routes (7 REAL-LOGIC + 2 placeholders) + 1 restructured overview; getCurrentMembership preservation verified per route
4. Price Intel section: 9 routes (3 REAL-LOGIC + 6 placeholders); getCurrentMembership preservation verified
5. People section: 4 routes (1 overview + 3 placeholders; vendors from Plan 3)
6. Sub Portal: 3 stubs at distinct paths with explicit F3 compliance contracts (iter-2 mechanical #5)
7. Company section: 12 routes (1 overview + 11 placeholders; /company/overview from Plan 1)
8. Reports section: 6 routes
9. iter-2 architect W-5: REAL vs PLACEHOLDER status documented per route
10. iter-2 mechanical #9: AC-1.5c-4-07 verified with 3 explicit grep tokens (one per Sub Portal stub)
11. iter-2 planner LOW-2 / LOW-4: Wave 3 mid-checkpoint smoke executed
12. Build + typecheck + hooks silent
13. Privacy gate clean across all ~50 new files
14. All 32-path redirects from Plan 1 resolve (Plan 4 covers most destinations; Plan 5 + 6 cover the rest)

No halt — this plan ships and Wave 3 continues with Plan 5 + 6 (parallel-safe). Plan 4 Task 4 mid-checkpoint smoke confirms readiness.
</verification>

<success_criteria>
- ~50 page.tsx files created
- All 8 nav sections have a primary route + sub-routes
- Sub Portal 3 stubs at distinct paths with F3 compliance contracts
- iter-2 mechanical #1 (NwPlaceholderCard hoist) honored — Plan 4 imports only
- iter-2 mechanical #5 (Sub Portal token stubs) verified
- iter-2 mechanical #9 (AC-1.5c-4-07 grep tokens) verified
- iter-2 mechanical #10 (REAL-LOGIC re-mounts preserve getCurrentMembership) verified
- iter-2 architect W-5 (REAL vs PLACEHOLDER per route) documented
- iter-2 planner LOW-2 / LOW-4 (Wave 3 mid-checkpoint smoke) completed
- All 16 must_haves truths verified
- Build + typecheck + hooks + privacy gate clean
</success_criteria>

## Acceptance criteria

- [ ] AC-1.5c-4-01: NwPlaceholderCard imported from src/components/nw/NwPlaceholderCard.tsx (HOISTED to Plan 1; this plan does NOT create it)
- [ ] AC-1.5c-4-02: Pipeline section: 6 routes exist (overview + 5 placeholders)
- [ ] AC-1.5c-4-03: Financials section: 9 sub-route page.tsx + 1 restructured overview exist; UI labels say Bills / Pay Apps / Lien Releases (not Invoices / Draws / Liens)
- [ ] AC-1.5c-4-04: Price Intel section: 9 routes exist; UI labels say Price Intel (not Cost Intelligence)
- [ ] AC-1.5c-4-05: People section: 4 routes exist (overview + 3 placeholders); /people/vendors handled in Plan 3
- [ ] AC-1.5c-4-06: Sub Portal: 3 distinct routes /sub-portal + /sub-portal/magic/[token] + /sub-portal/public/[token]
- [ ] AC-1.5c-4-07: Sub Portal stubs each describe their specific F3 future-state per iter-2 mechanical #5; 3 explicit grep tokens (one per stub) verified per iter-2 mechanical #9
- [ ] AC-1.5c-4-08: Sub Portal magic + public stubs do NOT reflect params.token into HTML per iter-2 mechanical #5 (a) — grep returns 0
- [ ] AC-1.5c-4-09: Sub Portal magic + public stubs include explicit F3 compliance contract per iter-2 mechanical #5 (b) — token expiry + scope binding + audit-log-on-use + cookie-vs-querystring + JWT-with-org-claim REJECTED + mirror client_portal_access pattern
- [ ] AC-1.5c-4-10: Company section: 12 routes exist; /company/overview from Plan 1 (not duplicated)
- [ ] AC-1.5c-4-11: Reports section: 6 routes (overview + 5 placeholders) all reference Wave 3 AI Report Builder
- [ ] AC-1.5c-4-12: Each placeholder is ~30 LOC; NwPlaceholderCard pattern uniformly applied
- [ ] AC-1.5c-4-13: All ~50 routes apply Site Office tokens (UPPERCASE eyebrows, Space Grotesk Medium headlines, no hardcoded hex)
- [ ] AC-1.5c-4-14: iter-2 mechanical #10 — REAL-LOGIC re-mounts preserve getCurrentMembership() / org_id / requireMembership verbatim per grep diff (9 routes: 7 Financials REAL-LOGIC + 2 Price Intel REAL-LOGIC)
- [ ] AC-1.5c-4-15: iter-2 architect W-5 — REAL vs PLACEHOLDER status documented per route in SUMMARY
- [ ] AC-1.5c-4-16: iter-2 planner LOW-2 / LOW-4 — Wave 3 mid-checkpoint smoke executed at end of Plan 4; results documented; Wave 3 readiness confirmed before Plans 5 + 6 spawn

## Nyquist coverage

| Acceptance criterion | Verified by |
|---|---|
| AC-1.5c-4-01 | Task 1 verify (grep "import NwPlaceholderCard from") |
| AC-1.5c-4-02 | Task 1 verify (ls Pipeline routes returns 6) |
| AC-1.5c-4-03 | Task 2 verify (ls Financials + grep "Bills" / "Pay Apps") |
| AC-1.5c-4-04 | Task 2 verify (ls Price Intel + grep "Price Intel") |
| AC-1.5c-4-05 | Task 3 verify (ls People routes returns 4) |
| AC-1.5c-4-06 | Task 3 verify (ls Sub Portal 3 distinct paths) |
| AC-1.5c-4-07 | Task 3 verify (3 explicit grep tokens per iter-2 mechanical #9) |
| AC-1.5c-4-08 | Task 3 verify (grep "params.token" in magic/public stubs returns 0 per iter-2 mechanical #5 (a)) |
| AC-1.5c-4-09 | Task 3 verify (grep "Token expiry" / "JWT-with-org-claim REJECTED" / "client_portal_access" per stub) |
| AC-1.5c-4-10 | Task 3 verify (ls Company routes returns 12) |
| AC-1.5c-4-11 | Task 3 verify (ls Reports routes returns 6 + grep "Wave 3") |
| AC-1.5c-4-12 | Plan-level (wc -l on each placeholder ~30 LOC) |
| AC-1.5c-4-13 | Plan-level (hex check across all 50 files) |
| AC-1.5c-4-14 | Task 2 verify (per-route grep diff for getCurrentMembership / org_id / requireMembership; SUMMARY documents per-route status) |
| AC-1.5c-4-15 | Task 2 + Task 3 SUMMARY (REAL vs PLACEHOLDER per route) |
| AC-1.5c-4-16 | Task 4 (Wave 3 mid-checkpoint smoke documented in SUMMARY) |

## Decisions inherited (from CONTEXT.md)

- D-02 (= MASTER-PLAN D-052): Sub Portal 3 distinct stubs at /sub-portal + /sub-portal/magic/[token] + /sub-portal/public/[token]
- D-13: Multi-modal ingestion structure-only — placeholder copy describes future ingestion patterns
- D-14 (Q8=A): No real-backend wiring promotion in 1.5c — placeholders describe future-state
- D-01 (= MASTER-PLAN D-051): Bills/Pay Apps terminology in UI labels at all real-logic mounts

## Halt conditions

- No halt_after — Wave 3 continues with Plan 5 + 6 (parallel-safe) after Plan 4 mid-checkpoint smoke
- Halt during execution if:
  - Real-logic mount of /financials/bills (from /invoices/page.tsx) breaks existing tests / build
  - Hook rejection on any of ~50 new files
  - Privacy gate fail on any new file
  - **iter-2 mechanical #10** — getCurrentMembership grep diff shows divergence in any REAL-LOGIC re-mount without documented reasoning; halt + reconcile
  - **iter-2 mechanical #5 (a)** — params.token grep returns >0 in Sub Portal stubs; halt + remove reflection
  - **iter-2 planner LOW-2 / LOW-4** — Wave 3 mid-checkpoint smoke surfaces unexpected 404 or console errors; halt + investigate

## Output

After completion, create `.planning/phases/stage-1.5c-information-architecture/01.5c-4-section-placeholders-sub-portal-SUMMARY.md` per `.claude/get-shit-done/templates/summary.md`. Document:
- Files created (list of ~50)
- Acceptance criteria status (16 items)
- iter-2 mechanical #1: NwPlaceholderCard imported (NOT created); confirmed Plan 1 hoist
- iter-2 mechanical #5 (a) + (b): Sub Portal stubs param-reflection grep + F3 compliance contract verified
- iter-2 mechanical #9: AC-1.5c-4-07 3 explicit grep tokens verified
- iter-2 mechanical #10: REAL-LOGIC re-mount getCurrentMembership preservation per route grep diff results
- iter-2 architect W-5: REAL vs PLACEHOLDER status per route
- iter-2 planner LOW-2 / LOW-4: Wave 3 mid-checkpoint smoke results (8 nav sections + 5 sub-routes + 3 Plan 3 production routes; expected 404s; issues count; Wave 3 spawn readiness)
- Hook silence + build / privacy gate pass
</content>
</invoke>

<criteria>

```yaml
criteria:
  mechanical:
    - "npm run build clean (0 errors)"
    - "npx tsc --noEmit returns 0 errors in src/"
    - "All hooks silent on every commit (T10a-T10d, post-edit drift, Drummond grep)"
    - "Privacy gate clean across new section + sub-portal files"
    - "NwPlaceholderCard imported (not redefined) across Plan 4 placeholder routes — grep 'function NwPlaceholderCard' returns 0 hits in those subtrees"
    - "Sub Portal token-reflection grep clean: grep -E 'params.token' src/app/sub-portal/magic/[token]/page.tsx src/app/sub-portal/public/[token]/page.tsx returns 0"
    - "AC-1.5c-4-07 grep: 'Subcontractor self-service' >=1 in sub-portal/page.tsx; 'magic link' >=1 in sub-portal/magic/[token]/page.tsx; 'public read-only' >=1 in sub-portal/public/[token]/page.tsx"
  dom:
    - 'Page /pipeline contains text "Pipeline"'
    - 'Page /financials contains text "Financials"'
    - 'Page /price-intel contains text "Price"'
    - 'Page /people contains text "People"'
    - 'Page /sub-portal contains text "Subcontractor"'
  visual:
    - "Page /pipeline: section overview with placeholder cards rendered using Site Office aesthetic (stone-blue accents, no purple/pink hues)"
    - "Page /financials: section overview visible — Bills / Pay Apps / Lien Releases / Payments / Aging / POs / COs surfaces or links represented"
    - "Page /people/vendors: vendor list table or card grid with at least 3 vendor entries visible (real Caldwell fixture data via Plan 3 mount)"
  behavioral:
    - "Sub Portal F3 compliance contract text visible: token expiry posture + audit-log-on-use + JWT-with-org-claim REJECTED policy documented in stub copy"
    - "REAL-LOGIC re-mounts preserve getCurrentMembership() posture: /financials/bills + /financials/pay-apps + /financials/lien-releases match original sources via grep diff"
  semantic:
    - "Page /sub-portal: subcontractor self-service entry point clearly labeled; no leaked sensitive data (e.g., no real-tenant org names visible)"
```

</criteria>
