---
phase: stage-1.5c-information-architecture
plan: 3
plan-name: extraction-production-routes
type: execute
status: not-started
wave: 2
depends_on: [2]
autonomous: false
halt_after: true
threat_model_severity: medium
requirements: []
files_modified:
  - src/components/prototypes/DrawApprovalView.tsx
  - src/components/prototypes/DrawPrintView.tsx
  - src/components/prototypes/BudgetView.tsx
  - src/components/prototypes/ScheduleView.tsx
  - src/components/prototypes/VendorListView.tsx
  - src/components/prototypes/VendorDetailView.tsx
  - src/components/prototypes/DocumentReviewView.tsx
  - src/components/prototypes/OwnerDashboardView.tsx
  - src/components/prototypes/OwnerDrawView.tsx
  - src/components/prototypes/MobileApprovalView.tsx
  - src/components/prototypes/ReconciliationView.tsx
  - src/app/financials/bills/[id]/page.tsx
  - src/app/financials/pay-apps/[id]/page.tsx
  - src/app/financials/pay-apps/[id]/print/page.tsx
  - src/app/financials/reconciliation/page.tsx
  - src/app/jobs/[id]/budget/page.tsx
  - src/app/jobs/[id]/schedule/page.tsx
  - src/app/people/vendors/page.tsx
  - src/app/people/vendors/[id]/page.tsx
  - src/app/jobs/[id]/documents/[documentId]/page.tsx
  - src/app/owner-portal/page.tsx
  - src/app/owner-portal/pay-apps/[id]/page.tsx
  - src/app/jobs/[id]/mobile-approval/page.tsx
  - src/app/design-system/prototypes/draws/[id]/page.tsx
  - src/app/design-system/prototypes/draws/[id]/print/page.tsx
  - src/app/design-system/prototypes/jobs/[id]/budget/page.tsx
  - src/app/design-system/prototypes/jobs/[id]/schedule/page.tsx
  - src/app/design-system/prototypes/vendors/page.tsx
  - src/app/design-system/prototypes/vendors/[id]/page.tsx
  - src/app/design-system/prototypes/documents/[id]/page.tsx
  - src/app/design-system/prototypes/owner-portal/page.tsx
  - src/app/design-system/prototypes/owner-portal/draws/[id]/page.tsx
  - src/app/design-system/prototypes/mobile-approval/page.tsx
  - src/app/design-system/prototypes/reconciliation/page.tsx
requires:
  - .planning/phases/stage-1.5c-information-architecture/CONTEXT.md
  - .planning/phases/stage-1.5c-information-architecture/PATTERNS.md
  - .planning/phases/stage-1.5c-information-architecture/01.5c-2-thin-wrapper-architect-PLAN.md
  - .planning/phases/stage-1.5c-information-architecture/01.5c-2-thin-wrapper-architect-SUMMARY.md (Plan 2 must SUMMARY first)
  - .planning/phases/stage-1.5c-information-architecture/AUDIT-LOG-STRATEGY.md
  - src/components/prototypes/InvoiceReviewView.tsx (Plan 2 exemplar)
  - src/app/design-system/prototypes/* (all 11 source files for extraction)
  - .planning/phases/stage-1.5b-prototype-gallery/findings.md (TD-20/25/26 line numbers)
provides:
  - 11 thin-wrapper View components extracted (props-only renders)
  - 11 production routes mounting Caldwell fixtures via thin wrappers
  - 11 portfolio prototype routes rewritten as thin wrappers (kept accessible under platform_admin gate)
  - Side-by-side visual verification of all 11 production-vs-prototype pairs (D-07 CRITICAL halt criterion)
affects:
  - All 11 production routes for Bills + Pay Apps + Budget + Schedule + Vendors + Documents + Owner Portal + Mobile Approval + Reconciliation
  - 11 portfolio /design-system/prototypes/* routes (rewritten as thin wrappers; functionality preserved)
acceptance-criteria-target: 17 falsifiable items
estimated-duration: 2-3 days
must_haves:
  truths:
    - "11 View components exist at src/components/prototypes/*View.tsx; each accepts data via props (no runtime fixture imports)"
    - "Each View applies TD-20 (NwButton bypass), TD-25 (Card padding=md), TD-26 (fontWeight: 500) structural fixes"
    - "11 production routes mount the View components: /financials/bills/[id], /financials/pay-apps/[id], /financials/pay-apps/[id]/print, /financials/reconciliation, /jobs/[id]/budget, /jobs/[id]/schedule, /people/vendors, /people/vendors/[id], /jobs/[id]/documents/[documentId], /owner-portal, /owner-portal/pay-apps/[id], /jobs/[id]/mobile-approval"
    - "11 portfolio /design-system/prototypes/* routes rewritten as thin wrappers; all still render correctly under platform_admin gate"
    - "Side-by-side D-07 visual verification: each production route renders identically to its portfolio prototype counterpart (HALT CRITICAL on divergence)"
    - "Production routes inherit Site Office direction from root chain (no manual data-direction attribute needed)"
    - "Portfolio routes inherit data-direction=C data-palette=B from /design-system/prototypes/layout.tsx (per 1.5b D-02)"
    - "Production routes are gated to authenticated org members (existing middleware posture); portfolio routes gated to platform_admin (existing posture per 1.5a)"
    - "T10c hook silent on all production routes (per Plan 2 architect finding — Option A confirmed)"
    - "Tenant-blind hook silent on all View components (no org_id / membership / vendor_id props)"
    - "Bouncy-easing hook silent on all 11 Views"
    - "Build + typecheck + post-edit Forbidden tokens silent throughout"
    - "Privacy gate clean across all 22+ new files"
    - "Owner Portal at /owner-portal/* gated to owner role per existing middleware (D-08 — same auth, role-gated)"
    - "Mobile Approval at /jobs/[id]/mobile-approval renders inside iPhone viewport (393x852); 56px high-stakes Approve button"
    - "Print preview at /financials/pay-apps/[id]/print uses pure CSS @page + @media print (preserved from 1.5b)"
    - "Bills/Pay Apps terminology rendered in production routes (UI labels say Bill / Pay App, not Invoice / Draw)"
  artifacts:
    - path: "src/components/prototypes/DrawApprovalView.tsx"
      provides: "Pay App approval Document Review component (formerly Draw approval)"
      contains: "DrawApprovalViewProps"
    - path: "src/components/prototypes/DrawPrintView.tsx"
      provides: "Pay App print preview component (G702 cover + G703 detail)"
      contains: "DrawPrintViewProps"
    - path: "src/components/prototypes/BudgetView.tsx"
      provides: "Budget view component (KPI strip + DataGrid; computed fields via R.2 recalculate-don't-increment)"
      contains: "BudgetViewProps"
    - path: "src/components/prototypes/ScheduleView.tsx"
      provides: "Schedule (Gantt) view component (TanStack Table v8 + custom timeline cell renderer)"
      contains: "ScheduleViewProps"
    - path: "src/components/prototypes/VendorListView.tsx"
      provides: "Vendor list-detail layout component (17 vendors)"
      contains: "VendorListViewProps"
    - path: "src/components/prototypes/VendorDetailView.tsx"
      provides: "Vendor detail Document Review component"
      contains: "VendorDetailViewProps"
    - path: "src/components/prototypes/DocumentReviewView.tsx"
      provides: "Generic Document Review component (handles plans, contracts, lien releases via type discriminator)"
      contains: "DocumentReviewViewProps"
    - path: "src/components/prototypes/OwnerDashboardView.tsx"
      provides: "Homeowner-facing dashboard (simplified Pattern3Dashboard)"
      contains: "OwnerDashboardViewProps"
    - path: "src/components/prototypes/OwnerDrawView.tsx"
      provides: "Owner-facing pay app approval (Document Review with vendor-name transparency)"
      contains: "OwnerDrawViewProps"
    - path: "src/components/prototypes/MobileApprovalView.tsx"
      provides: "Mobile invoice/bill approval (iPhone viewport; 56px high-stakes Approve)"
      contains: "MobileApprovalViewProps"
    - path: "src/components/prototypes/ReconciliationView.tsx"
      provides: "Reconciliation strawman component (4 candidates × 2 drift types)"
      contains: "ReconciliationViewProps"
    - path: "src/app/financials/bills/[id]/page.tsx"
      provides: "Production /financials/bills/[id] mounting InvoiceReviewView (Plan 2 exemplar)"
      contains: "InvoiceReviewView"
    - path: "src/app/financials/pay-apps/[id]/page.tsx"
      provides: "Production /financials/pay-apps/[id] mounting DrawApprovalView"
      contains: "DrawApprovalView"
    - path: "src/app/owner-portal/page.tsx"
      provides: "Production /owner-portal mounting OwnerDashboardView (gated to owner role)"
      contains: "OwnerDashboardView"
  key_links:
    - from: "src/components/prototypes/*View.tsx (all 11)"
      to: "src/app/design-system/_fixtures/drummond/types.ts"
      via: "type-only import: import type { ... } from"
      pattern: "import type"
    - from: "src/components/prototypes/*View.tsx (all 11)"
      to: "src/components/nw/* (Card, Eyebrow, Money, DataRow, Badge, NwButton)"
      via: "named imports"
      pattern: "components/nw"
    - from: "src/app/financials/bills/[id]/page.tsx"
      to: "src/components/prototypes/InvoiceReviewView.tsx + Caldwell fixtures"
      via: "thin wrapper pattern (Plan 2 exemplar)"
      pattern: "InvoiceReviewView"
    - from: "src/app/financials/pay-apps/[id]/page.tsx"
      to: "src/components/prototypes/DrawApprovalView.tsx + CALDWELL_DRAWS + CALDWELL_DRAW_LINE_ITEMS + CALDWELL_COST_CODES + CALDWELL_CHANGE_ORDERS"
      via: "thin wrapper pattern"
      pattern: "DrawApprovalView"
    - from: "src/app/design-system/prototypes/draws/[id]/page.tsx (REWRITE)"
      to: "src/components/prototypes/DrawApprovalView.tsx"
      via: "thin wrapper pattern (portfolio path; same View, same data)"
      pattern: "DrawApprovalView"
    - from: "src/app/owner-portal/page.tsx"
      to: "src/components/prototypes/OwnerDashboardView.tsx + middleware role gate"
      via: "thin wrapper + auth check"
      pattern: "OwnerDashboardView"
---

<objective>
Refactor 10 remaining prototype components into props-only View components per Plan 2 exemplar (InvoiceReviewView), apply structural TD-20/25/26 fixes, mount 11 production routes as thin wrappers, rewrite 11 portfolio prototype routes as thin wrappers (preserving the under-platform_admin-gate access), and run side-by-side D-07 visual verification on all 11 pairs.

Per nwrp44: this is the parallel implementers wave. Plan 2 produced ONE exemplar (InvoiceReviewView). Plan 3 spawns 4-6 parallel agents to refactor the remaining 10 components + mount 11 production routes + verify all 11 pairs visually. **Halt after Plan 3** so Jake can verify D-07 visual parity holds across all 11 production-vs-portfolio pairs before Wave 3 spawns.

The single highest-risk task in Stage 1.5c is the D-07 visual verification gate. Per CONTEXT D-07 + nwrp44 Amendment 2: "Treat divergence as a CRITICAL finding, not polish. Halt extraction and investigate before proceeding to other items." This plan encodes that gate as Task 4 — explicit halt-on-divergence.

Purpose:
- Mechanical extraction of 10 prototype components following the Plan 2 exemplar.
- 11 production routes wired with the new IA paths from Plan 1.
- Bills/Pay Apps terminology applied throughout (UI labels, breadcrumb labels, component names).
- D-07 visual verification gate — production-vs-portfolio parity confirmed before Wave 3 spawns.

Output:
- 10 NEW Views in src/components/prototypes/ (DrawApprovalView, DrawPrintView, BudgetView, ScheduleView, VendorListView, VendorDetailView, DocumentReviewView, OwnerDashboardView, OwnerDrawView, MobileApprovalView, ReconciliationView)
- 11 NEW production route page.tsx files mounting the Views as thin wrappers
- 11 REWRITTEN portfolio /design-system/prototypes/* page.tsx files as thin wrappers
- D-07 visual verification report in plan SUMMARY (any divergence = CRITICAL halt)
- HALT for Jake review of D-07 verification + integration smoke test
</objective>

<execution_context>
@C:/Users/Jake/nightwork-platform/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jake/nightwork-platform/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/stage-1.5c-information-architecture/CONTEXT.md
@.planning/phases/stage-1.5c-information-architecture/PATTERNS.md
@.planning/phases/stage-1.5c-information-architecture/01.5c-1-nav-today-redirects-PLAN.md
@.planning/phases/stage-1.5c-information-architecture/01.5c-2-thin-wrapper-architect-PLAN.md
@.planning/phases/stage-1.5c-information-architecture/AUDIT-LOG-STRATEGY.md
@.planning/expansions/stage-1.5c-information-architecture-EXPANDED-SCOPE.md
@.planning/phases/stage-1.5b-prototype-gallery/CONTEXT.md
@.planning/phases/stage-1.5b-prototype-gallery/findings.md
@.planning/phases/stage-1.5b-prototype-gallery/01.5-2-invoice-draw-vendors-PLAN.md
@.planning/phases/stage-1.5b-prototype-gallery/01.5-3-budget-documents-PLAN.md
@.planning/phases/stage-1.5b-prototype-gallery/01.5-4-owner-mobile-PLAN.md
@.planning/phases/stage-1.5b-prototype-gallery/01.5-5-schedule-gantt-PLAN.md
@.planning/phases/stage-1.5b-prototype-gallery/01.5-6-print-reconciliation-PLAN.md
@CLAUDE.md
@.planning/design/SYSTEM.md
@.planning/design/COMPONENTS.md
@.planning/design/PATTERNS.md
@.planning/design/CHOSEN-DIRECTION.md
@src/app/design-system/_fixtures/drummond/index.ts
@src/app/design-system/_fixtures/drummond/types.ts
@src/components/prototypes/InvoiceReviewView.tsx
@src/components/nw/Button.tsx
@src/components/nw/Card.tsx
@src/components/nw/Eyebrow.tsx
@src/components/nw/Money.tsx
@src/components/nw/DataRow.tsx
@src/components/nw/Badge.tsx
@src/middleware.ts

<interfaces>
The 10 remaining prototype source files (extraction sources):
- src/app/design-system/prototypes/draws/[id]/page.tsx (DrawApprovalView source)
- src/app/design-system/prototypes/draws/[id]/print/page.tsx (DrawPrintView source)
- src/app/design-system/prototypes/jobs/[id]/budget/page.tsx (BudgetView source)
- src/app/design-system/prototypes/jobs/[id]/schedule/page.tsx (ScheduleView source)
- src/app/design-system/prototypes/vendors/page.tsx (VendorListView source)
- src/app/design-system/prototypes/vendors/[id]/page.tsx (VendorDetailView source)
- src/app/design-system/prototypes/documents/[id]/page.tsx (DocumentReviewView source)
- src/app/design-system/prototypes/owner-portal/page.tsx (OwnerDashboardView source)
- src/app/design-system/prototypes/owner-portal/draws/[id]/page.tsx (OwnerDrawView source)
- src/app/design-system/prototypes/mobile-approval/page.tsx (MobileApprovalView source)
- src/app/design-system/prototypes/reconciliation/page.tsx (ReconciliationView source)

Plan 2 exemplar (canonical extraction shape — Plan 3 implementers MUST follow):
- src/components/prototypes/InvoiceReviewView.tsx
- src/app/design-system/prototypes/invoices/[id]/page.tsx (refactored thin wrapper)

Component contract template (per Plan 2):

  // src/components/prototypes/<Name>View.tsx
  "use client";

  import type {
    Caldwell<Entity1>,
    Caldwell<Entity2>,
    // ... type-only imports
  } from "@/app/design-system/_fixtures/drummond/types";

  import Card from "@/components/nw/Card";
  import Eyebrow from "@/components/nw/Eyebrow";
  // ... other primitives
  import NwButton from "@/components/nw/Button";  // TD-20 fix

  export interface <Name>ViewProps {
    // All data via props; no fixture lookups internally
    primary: Caldwell<Entity1>;
    related?: { /* lookup-resolved data */ };
    breadcrumbRoot?: { href: string; label: string };
  }

  export default function <Name>View(props: <Name>ViewProps) {
    // Render logic from source, with TD-20/25/26 fixes applied
  }

Production route wrapper template (per Plan 2):

  // src/app/<canonical-path>/page.tsx
  import { notFound } from "next/navigation";
  import { CALDWELL_X, CALDWELL_Y } from "@/app/design-system/_fixtures/drummond";
  import <Name>View from "@/components/prototypes/<Name>View";

  export default function <Name>Page({ params }: { params: { id: string } }) {
    // Fixture lookup + props pass to View
    const primary = CALDWELL_X.find((x) => x.id === params.id);
    if (!primary) return notFound();
    // ... resolve related data
    return <<Name>View primary={primary} related={...} breadcrumbRoot={{ href: "/financials/bills", label: "Bills" }} />;
  }

Portfolio route wrapper template (per Plan 2):

  // src/app/design-system/prototypes/.../page.tsx
  // Same as production wrapper but breadcrumbRoot points to /design-system/prototypes/
  // and remains under platform_admin gate via existing middleware.

Per CONTEXT D-08 (Owner Portal):
  Owner Portal at /owner-portal/* gated to owner role via existing middleware. The role check is mechanical — middleware already differentiates owner from non-owner via useCurrentRole() / getCurrentMembership(). 1.5c does NOT modify middleware; just mounts the View at the canonical /owner-portal path.

Per CONTEXT D-04 + Plan 1 mobile chrome:
  Mobile approval prototype runs at iPhone viewport (393x852). NwButton size="lg-touch" enforces 56px for high-stakes Approve button per SYSTEM.md §11.

Per CONTEXT D-01 (Bills/Pay Apps rename):
  All UI labels in extracted Views use Bill / Pay App terminology. Where source uses "Invoice approval" → "Bill approval"; "Draw approval" → "Pay App approval"; "Draw print" → "Pay App print"; etc. Component file names retain Draw* / Invoice* internally for code-search continuity (renaming components themselves is excessive churn for 1.5c — UI labels are user-visible, component names are not).

NwButton API (verify at execute time per src/components/nw/Button.tsx):
  Per Plan 2 architect finding (verified): the actual NwButton API is the source of truth — Plan 3 implementers read src/components/nw/Button.tsx fully and apply consistent NwButton replacement.

Site Office tokens (per SYSTEM.md):
  - UPPERCASE 0.18em eyebrows
  - Space Grotesk Medium 500 headlines (NOT 600 — TD-26 fix)
  - JetBrains Mono 0.18em tracking
  - Card padding="md" (NOT "lg" — TD-25 fix)
  - 1px slate-tile left-stamp on cards (CSS rule, automatic)
  - 150ms ease-out motion (no bouncy)
  - NwButton (NOT raw <button> — TD-20 fix)
  - No hardcoded hex
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract 10 remaining prototype components to props-only Views (parallel implementers)</name>
  <files>src/components/prototypes/DrawApprovalView.tsx, src/components/prototypes/DrawPrintView.tsx, src/components/prototypes/BudgetView.tsx, src/components/prototypes/ScheduleView.tsx, src/components/prototypes/VendorListView.tsx, src/components/prototypes/VendorDetailView.tsx, src/components/prototypes/DocumentReviewView.tsx, src/components/prototypes/OwnerDashboardView.tsx, src/components/prototypes/OwnerDrawView.tsx, src/components/prototypes/MobileApprovalView.tsx, src/components/prototypes/ReconciliationView.tsx</files>

  <read_first>
    - src/components/prototypes/InvoiceReviewView.tsx (FULL file — Plan 2 exemplar; the canonical extraction shape)
    - .planning/phases/stage-1.5c-information-architecture/01.5c-2-thin-wrapper-architect-PLAN.md (Task 2 — extraction template)
    - .planning/phases/stage-1.5c-information-architecture/AUDIT-LOG-STRATEGY.md (T10c hook posture finding from Plan 2)
    - .planning/phases/stage-1.5b-prototype-gallery/findings.md (TD-20/21/22/23/24/25/26/27/28 file references — only TD-20/25/26 in scope per CONTEXT D-06)
    - For each extraction, read the corresponding source file FULLY:
      * src/app/design-system/prototypes/draws/[id]/page.tsx (DrawApprovalView)
      * src/app/design-system/prototypes/draws/[id]/print/page.tsx (DrawPrintView)
      * src/app/design-system/prototypes/jobs/[id]/budget/page.tsx (BudgetView)
      * src/app/design-system/prototypes/jobs/[id]/schedule/page.tsx (ScheduleView)
      * src/app/design-system/prototypes/vendors/page.tsx (VendorListView)
      * src/app/design-system/prototypes/vendors/[id]/page.tsx (VendorDetailView)
      * src/app/design-system/prototypes/documents/[id]/page.tsx (DocumentReviewView)
      * src/app/design-system/prototypes/owner-portal/page.tsx (OwnerDashboardView)
      * src/app/design-system/prototypes/owner-portal/draws/[id]/page.tsx (OwnerDrawView)
      * src/app/design-system/prototypes/mobile-approval/page.tsx (MobileApprovalView)
      * src/app/design-system/prototypes/reconciliation/page.tsx (ReconciliationView)
    - src/app/design-system/_fixtures/drummond/types.ts (all Caldwell types)
    - src/components/nw/Button.tsx (NwButton actual API for TD-20 fix)
    - src/components/nw/Card.tsx + Eyebrow.tsx + Money.tsx + DataRow.tsx + Badge.tsx (other primitives)
  </read_first>

  <action>
**Per nwrp44 standing rule "Independent work → parallel batches of 6-8 agents per wave":** This task is suitable for 4-6 parallel implementer agents, each handling 1-3 components. The Plan 2 exemplar provides the shape; each implementer applies it mechanically.

**Suggested parallel split (executor's call — adjust based on actual agent batch size):**

Batch A (financial — Bills/Pay Apps gold-standard pattern):
- DrawApprovalView (analog to InvoiceReviewView; Document Review pattern)
- DrawPrintView (specialized print preview; @media print stylesheet)

Batch B (operational data-grid):
- BudgetView (KPI strip + DataGrid with TanStack Table v8; R.2 recalculate-don't-increment for computed fields)
- ScheduleView (TanStack Table v8 + custom timeline cell renderer)

Batch C (vendors + documents):
- VendorListView (List+Detail; 17 vendors)
- VendorDetailView (Document Review pattern)
- DocumentReviewView (handles 3 document types via discriminator)

Batch D (owner-facing + mobile):
- OwnerDashboardView (simplified Pattern3Dashboard for non-builder)
- OwnerDrawView (Document Review with vendor-name transparency)
- MobileApprovalView (iPhone viewport; 56px high-stakes)

Batch E (reconciliation):
- ReconciliationView (4 candidates × 2 drift types)

**Per implementer — extraction recipe (apply to each component):**

1. **Open the source file in full.** Read every line. Inventory:
   - Fixture imports at top
   - Type imports
   - UI primitive imports (Card, Eyebrow, Money, DataRow, Badge, etc.)
   - Internal lookups (.find(), .filter() calls against fixture arrays)
   - Raw <button> elements (count for TD-20 scope)
   - Card padding=lg usages (count for TD-25 scope)
   - fontWeight: 600 inline styles (count for TD-26 scope)
   - Hardcoded hex (privacy gate concern; expect 0)

2. **Determine the View props shape.** What data does the component need? E.g., DrawApprovalView needs:
   - `draw: CaldwellDraw` (the primary entity)
   - `lineItems: CaldwellDrawLineItem[]` (G703 rows)
   - `costCodes: CaldwellCostCode[]` (resolve cost_code_id)
   - `changeOrders: CaldwellChangeOrder[]` (PCCO log)
   - `breadcrumbRoot?: { href: string; label: string }` (production vs portfolio)

3. **Create src/components/prototypes/<Name>View.tsx** following the InvoiceReviewView template:
   - "use client" if needed
   - Type-only imports for Caldwell types (`import type { ... }`)
   - Runtime imports for nw/* primitives + Heroicons + Link
   - Export `<Name>ViewProps` interface
   - Export default `<Name>View` function
   - Move all rendering JSX into this component
   - Replace internal fixture lookups with prop reads
   - Apply TD-20: any raw `<button type="...">` for action elements becomes `<NwButton variant="..." size="...">`
   - Apply TD-25: any `<Card padding="lg">` becomes `<Card padding="md">`
   - Apply TD-26: any `style={{ fontWeight: 600 }}` becomes `style={{ fontWeight: 500 }}`
   - Preserve EVERYTHING else verbatim (this is the D-07 visual parity contract)

4. **Verify the View has zero runtime fixture imports.** `grep -n "from \"@/app/design-system/_fixtures/drummond\"" <NewViewFile>.tsx` should return only `import type` lines (or zero lines if types live elsewhere).

5. **Apply Bills/Pay Apps terminology in user-visible labels** per CONTEXT D-01:
   - "Invoice" → "Bill" (UI labels, breadcrumb labels, headlines)
   - "Draw" → "Pay App" (UI labels, breadcrumb labels, headlines)
   - "Approve invoice" → "Approve bill"
   - "Draw approval" → "Pay App approval"
   - "G702/G703" terminology stays (industry-standard AIA reference)
   - Component file names + variable names retain Draw* / Invoice* internally per CONTEXT D-01 scope clarification (UI labels are user-visible; code identifiers are not — full code rename is excessive churn for 1.5c)

6. **Per-component nuances:**

   - **DrawApprovalView (Pay App):** All UI labels say "Pay App" not "Draw". Breadcrumb says "Pay Apps". Status timeline reads "Pay App submitted" not "Draw submitted". Audit-log action strings not yet wired (this is a read-only View; AUDIT-LOG-STRATEGY.md applies to future writes).
   - **DrawPrintView (Pay App print):** G702/G703 layout preserved. Print stylesheet preserved. Header text says "Pay Application #N" (already AIA-correct). Breadcrumb says "Pay Apps" / "Print preview".
   - **BudgetView:** R.2 recalculate-don't-increment preserved. Computed fields (previous_applications, this_period, total_to_date, percent_complete, balance_to_finish) derived on render from invoices prop.
   - **ScheduleView:** Custom timeline cell renderer preserved (TanStack Table v8 base). Today-marker preserved. Milestones render as diamonds. Status colors via Site Office tokens.
   - **VendorListView:** 17 Caldwell vendors render in List+Detail. Long names stress test (Bay Region Carpentry Inc, Coastal Smart Systems LLC) — preserve from source.
   - **VendorDetailView:** Document Review pattern. Audit timeline currently missing per TD-23 — DEFERRED per CONTEXT D-06; do NOT add audit timeline in 1.5c.
   - **DocumentReviewView:** Type discriminator (plans / contracts / lien releases). All 4 Florida statute types render correctly for lien releases.
   - **OwnerDashboardView:** Simplified Pattern3Dashboard. Cost-plus open-book transparency language. NO builder jargon.
   - **OwnerDrawView:** Owner-facing Pay App approval. Vendor-name visible. Audit timeline currently missing per TD-24 — DEFERRED per CONTEXT D-06; do NOT add audit timeline in 1.5c.
   - **MobileApprovalView:** iPhone viewport. 56px high-stakes Approve button (NwButton size="lg-touch"). Reject button currently uses rgba inline per TD-21 — DEFERRED per CONTEXT D-06; do NOT replace rgba with var(--...) in 1.5c.
   - **ReconciliationView:** 4 candidates × 2 drift types = 8 prototypes preserved. Section anchors enable scroll-to-section comparison.

**Token discipline (apply to ALL 10 Views uniformly):**
- All colors: `var(--...)` (no hex except where explicitly approved like TD-21 deferred — and that's mobile-approval, NOT introduced fresh elsewhere)
- All fonts: `var(--font-space-grotesk)`, `var(--font-jetbrains-mono)`, `var(--font-inter)`
- Site Office signature: UPPERCASE eyebrows + 0.18em tracking + 1px slate-tile left-stamp on cards (automatic via CSS direction selectors) + 150ms ease-out motion
- Tenant-blind: NO `org_id` / `membership` / `vendor_id` / similar tenant-keying props on Views (these are pure presentation primitives)
  </action>

  <verify>
    <automated>npm run build && ls src/components/prototypes/*View.tsx | wc -l</automated>
    Expected: build exits 0; ls returns 12 (11 NEW + 1 InvoiceReviewView from Plan 2).

    Per-component checks (run for each of 10 NEW Views):
    - Type-only fixture imports: `grep -E "^import type" src/components/prototypes/<Name>View.tsx` returns >=1 (Caldwell types)
    - Runtime fixture imports: `grep -E "^import \{[^}]*\} from \"@/app/design-system/_fixtures/drummond\"" src/components/prototypes/<Name>View.tsx` returns 0 (no runtime fixture imports)
    - TD-20 fix: `grep -cE '<button[^>]*type="button"' src/components/prototypes/<Name>View.tsx` returns 0
    - TD-25 fix: `grep -cE 'padding="lg"' src/components/prototypes/<Name>View.tsx` returns 0
    - TD-26 fix: `grep -cE 'fontWeight:\s*600' src/components/prototypes/<Name>View.tsx` returns 0
    - Tenant-blind check: `grep -E 'org_id|membership' src/components/prototypes/<Name>View.tsx` returns 0
    - No hardcoded hex (except TD-21 deferred in MobileApprovalView): `grep -nE '#[0-9a-fA-F]{3,6}' src/components/prototypes/<Name>View.tsx | grep -v 'var(--' | wc -l` returns 0 (or 1 for MobileApprovalView's TD-21-deferred rgba)

    Privacy gate: `grep -rE 'Drummond|501 74th|...' src/components/prototypes/ | wc -l` returns 0 (full denylist).

    Hooks silent: post-edit hook should not reject any of the 10 new files.

    NwButton replacements visible (executor uses Chrome DevTools MCP):
    - DrawApprovalView header: Approve / Reject / Submit buttons render as NwButton (subtle styling difference vs raw button — borders, focus ring, hover state per Site Office contract)
    - OwnerDrawView (where applicable): Approve button uses NwButton
    - MobileApprovalView: Approve button uses NwButton size="lg-touch" (56px high-stakes)
  </verify>

  <done>
    - 10 View components created at src/components/prototypes/*View.tsx
    - All Views are props-only (no runtime fixture imports)
    - TD-20 / TD-25 / TD-26 structural fixes applied uniformly
    - Bills/Pay Apps terminology applied in user-visible labels (component file names retain Draw* / Invoice*)
    - Cosmetic TDs (TD-21/22/23/24/27/28) explicitly NOT addressed (deferred per CONTEXT D-06)
    - Build passes
    - Privacy gate clean
    - Hooks silent
  </done>
</task>

<task type="auto">
  <name>Task 2: Mount 11 production routes as thin wrappers</name>
  <files>src/app/financials/bills/[id]/page.tsx, src/app/financials/pay-apps/[id]/page.tsx, src/app/financials/pay-apps/[id]/print/page.tsx, src/app/financials/reconciliation/page.tsx, src/app/jobs/[id]/budget/page.tsx, src/app/jobs/[id]/schedule/page.tsx, src/app/people/vendors/page.tsx, src/app/people/vendors/[id]/page.tsx, src/app/jobs/[id]/documents/[documentId]/page.tsx, src/app/owner-portal/page.tsx, src/app/owner-portal/pay-apps/[id]/page.tsx, src/app/jobs/[id]/mobile-approval/page.tsx</files>

  <read_first>
    - src/app/design-system/prototypes/invoices/[id]/page.tsx (Plan 2 wrapper template)
    - src/components/prototypes/*View.tsx (all 12 Views from Plan 2 + Task 1 — wrapper props shape)
    - src/middleware.ts (full file — auth + role gate posture; verify owner role check for /owner-portal mount)
    - .planning/phases/stage-1.5c-information-architecture/CONTEXT.md (D-08 Owner Portal posture; D-14 no real-backend wiring)
    - For each route, read the EXISTING production route at the corresponding path (if exists) — for jobs/[id]/budget there's possibly an existing /jobs/[id]/budget; verify and decide whether to REPLACE or AUGMENT.
  </read_first>

  <action>
**Per CONTEXT D-14 (Q8=A): no real-backend wiring promotion in 1.5c. Production routes mount Caldwell fixtures.** F1 swaps fixtures for real data.

**Per nwrp44: 4-6 parallel implementers can split the 12 production routes (1 already done in Plan 2 = /design-system/prototypes/invoices/[id]; 11 NEW + 1 wrapper rewrite remaining).**

**Wrapper template (Plan 2 exemplar):**

```typescript
// src/app/<canonical-path>/page.tsx
import { notFound } from "next/navigation";
import {
  CALDWELL_X,
  CALDWELL_Y,
  // ...
} from "@/app/design-system/_fixtures/drummond";
import <Name>View from "@/components/prototypes/<Name>View";

export default function <CanonicalName>Page({ params }: { params: { id: string } }) {
  const primary = CALDWELL_X.find((x) => x.id === params.id);
  if (!primary) return notFound();
  // ... resolve related data
  return (
    <<Name>View
      primary={primary}
      related={...}
      breadcrumbRoot={{ href: "/financials/bills", label: "Bills" }}
    />
  );
}
```

**Per-route implementations:**

1. **`src/app/financials/bills/[id]/page.tsx`** — mounts InvoiceReviewView (Plan 2 exemplar). Breadcrumb root: `{ href: "/financials/bills", label: "Bills" }`.

2. **`src/app/financials/pay-apps/[id]/page.tsx`** — mounts DrawApprovalView. Breadcrumb root: `{ href: "/financials/pay-apps", label: "Pay Apps" }`. Loads CALDWELL_DRAWS + CALDWELL_DRAW_LINE_ITEMS + CALDWELL_COST_CODES + CALDWELL_CHANGE_ORDERS + CALDWELL_JOBS.

3. **`src/app/financials/pay-apps/[id]/print/page.tsx`** — mounts DrawPrintView. Same data as #2 + print stylesheet inheritance.

4. **`src/app/financials/reconciliation/page.tsx`** — mounts ReconciliationView. Loads CALDWELL_RECONCILIATION_PAIRS. No `[id]` — single-page route.

5. **`src/app/jobs/[id]/budget/page.tsx`** — mounts BudgetView. Loads CALDWELL_BUDGET_LINES + CALDWELL_INVOICES + CALDWELL_COST_CODES + CALDWELL_JOBS + CALDWELL_CHANGE_ORDERS + CALDWELL_DRAWS. Breadcrumb root: `{ href: "/jobs", label: "Jobs" }` → "Budget".
   - **CRITICAL:** if `src/app/jobs/[id]/budget/page.tsx` ALREADY exists in production code (a real-backend version), the new mount must REPLACE it (per Q8=A: production = Caldwell fixture in 1.5c; F1 swaps in real data). Verify if it exists first; if it does, BACKUP its content as a comment in the SUMMARY for F1 swap reference, then replace.

6. **`src/app/jobs/[id]/schedule/page.tsx`** — mounts ScheduleView. Loads CALDWELL_SCHEDULE_ITEMS + CALDWELL_VENDORS + CALDWELL_JOBS.

7. **`src/app/people/vendors/page.tsx`** — mounts VendorListView. Loads CALDWELL_VENDORS + CALDWELL_INVOICES (for activity).
   - Replaces existing `/vendors` route (which 301-redirects to here per Plan 1's `next.config.mjs` extension).

8. **`src/app/people/vendors/[id]/page.tsx`** — mounts VendorDetailView. Loads CALDWELL_VENDORS + CALDWELL_INVOICES + CALDWELL_LIEN_RELEASES.

9. **`src/app/jobs/[id]/documents/[documentId]/page.tsx`** — mounts DocumentReviewView. Loads CALDWELL_LIEN_RELEASES + CALDWELL_VENDORS + CALDWELL_INVOICES (for type discriminator).

10. **`src/app/owner-portal/page.tsx`** — mounts OwnerDashboardView. Loads CALDWELL_DRAWS + CALDWELL_JOBS + CALDWELL_CHANGE_ORDERS.
    - **Per CONTEXT D-08:** owner role gated via existing middleware. 1.5c does NOT modify middleware. The page assumes the middleware filters for `owner` role; if the middleware allows non-owner users to reach this route, that's a F2 finding — log to Wave 1.1-Lite.
    - Per existing middleware `useCurrentRole()` posture, the production /owner-portal route inherits the same auth + role-gate as any authenticated route. The role-specific filtering (only owner sees Owner Portal nav item) is enforced by Plan 1's nav ACCESS map.

11. **`src/app/owner-portal/pay-apps/[id]/page.tsx`** — mounts OwnerDrawView. Loads CALDWELL_DRAW_LINE_ITEMS + CALDWELL_INVOICES + CALDWELL_VENDORS + CALDWELL_COST_CODES.

12. **`src/app/jobs/[id]/mobile-approval/page.tsx`** — mounts MobileApprovalView. Loads CALDWELL_INVOICES + CALDWELL_VENDORS + CALDWELL_COST_CODES.
    - Existing /design-system/prototypes/mobile-approval may not have a `[id]` segment — verify. If the prototype is single-page (no per-invoice path), the production mount may also be single-page or accept `?invoice=` query param. Plan 3 implementer decides based on source structure.

**Each wrapper:**
- Imports CALDWELL_* fixtures (this is the legitimate temporary 1.5c stand-in per CONTEXT D-14 + Plan 2 architect's T10c finding)
- Calls notFound() on missing data
- Passes resolved data via props to the View
- Sets breadcrumbRoot to canonical production path

**No business logic in wrappers.** All rendering is in the View. The wrapper is mechanical: lookup + notFound() + props pass.

**Per CLAUDE.md "All business logic in server-side API routes or Supabase functions. Frontend is display + forms only."** — these wrappers are display-only (no mutation). F1 wires real-data + audit-log writes for any mutation surfaces.
  </action>

  <verify>
    <automated>npm run build && ls src/app/financials/bills/[id]/page.tsx src/app/financials/pay-apps/[id]/page.tsx src/app/financials/pay-apps/[id]/print/page.tsx src/app/financials/reconciliation/page.tsx src/app/jobs/[id]/budget/page.tsx src/app/jobs/[id]/schedule/page.tsx src/app/people/vendors/page.tsx src/app/people/vendors/[id]/page.tsx src/app/jobs/[id]/documents/[documentId]/page.tsx src/app/owner-portal/page.tsx src/app/owner-portal/pay-apps/[id]/page.tsx src/app/jobs/[id]/mobile-approval/page.tsx 2>&1 | grep -c 'page.tsx'</automated>
    Expected: build exits 0; ls returns 12 (all 12 production page.tsx files present).

    Per-route checks:
    - View import: `grep -c "from \"@/components/prototypes/\"" <route_file>.tsx` returns >=1 for each route (each wrapper imports its View)
    - Fixture import: `grep -c "from \"@/app/design-system/_fixtures/drummond\"" <route_file>.tsx` returns 1 for each route
    - notFound() call: `grep -c "notFound()" <route_file>.tsx` returns >=1 for routes with `[id]` params

    Manual smoke (executor uses Chrome DevTools MCP):
    - Visit each production route with a known fixture ID (e.g., /financials/bills/inv-caldwell-001, /financials/pay-apps/d-caldwell-05) — renders the same content as the corresponding portfolio prototype path
    - /people/vendors lists 17 vendors
    - /financials/reconciliation renders 4 candidates × 2 drift types
    - /owner-portal renders the dashboard (logged in as owner role; if not owner, middleware should redirect — verify if 403/404/redirect happens)

    Privacy gate: `grep -rE 'Drummond|...' src/app/financials/ src/app/jobs/[id]/budget/ src/app/people/vendors/ src/app/owner-portal/ src/app/jobs/[id]/mobile-approval/ src/app/jobs/[id]/documents/ src/app/jobs/[id]/schedule/ | wc -l` returns 0.

    Hooks silent on all 12 wrappers.
  </verify>

  <done>
    - 12 production page.tsx files created (11 NEW + Plan 2's invoice wrapper still in /design-system/prototypes/)
    - Each wrapper imports its View component from src/components/prototypes/
    - Each wrapper imports Caldwell fixtures (legitimate temporary 1.5c stand-in)
    - Each wrapper calls notFound() on missing fixture lookup
    - Build passes
    - Privacy gate clean
    - Hooks silent
    - Manual smoke confirms each route renders content
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite 11 portfolio /design-system/prototypes/* routes as thin wrappers</name>
  <files>src/app/design-system/prototypes/draws/[id]/page.tsx, src/app/design-system/prototypes/draws/[id]/print/page.tsx, src/app/design-system/prototypes/jobs/[id]/budget/page.tsx, src/app/design-system/prototypes/jobs/[id]/schedule/page.tsx, src/app/design-system/prototypes/vendors/page.tsx, src/app/design-system/prototypes/vendors/[id]/page.tsx, src/app/design-system/prototypes/documents/[id]/page.tsx, src/app/design-system/prototypes/owner-portal/page.tsx, src/app/design-system/prototypes/owner-portal/draws/[id]/page.tsx, src/app/design-system/prototypes/mobile-approval/page.tsx, src/app/design-system/prototypes/reconciliation/page.tsx</files>

  <read_first>
    - src/app/design-system/prototypes/invoices/[id]/page.tsx (Plan 2 portfolio wrapper template)
    - src/app/design-system/prototypes/layout.tsx (locks data-direction=C data-palette=B per 1.5b D-02 — preserved verbatim)
    - .planning/phases/stage-1.5c-information-architecture/CONTEXT.md (D-03 — portfolio prototype paths stay accessible under platform_admin gate)
    - src/middleware.ts (verify platform_admin gate at /design-system/* per existing middleware semantics)
  </read_first>

  <action>
**Per CONTEXT D-03 + EXPANDED-SCOPE Part 7 B:** "The /design-system/prototypes/* routes from 1.5b stay accessible at their current paths under platform_admin gate (for portfolio/demo). The PRODUCTION routes (without /design-system prefix) wire to the same components."

**Per Plan 2 exemplar:** /design-system/prototypes/invoices/[id]/page.tsx is now a thin wrapper. Plan 3 applies the same pattern to the remaining 11 portfolio routes.

**Portfolio wrapper template:**

```typescript
// src/app/design-system/prototypes/.../page.tsx
import { notFound } from "next/navigation";
import {
  CALDWELL_X,
  // ... same fixtures as production wrapper
} from "@/app/design-system/_fixtures/drummond";
import <Name>View from "@/components/prototypes/<Name>View";

export default function <Name>PrototypePage({ params }: { params: { id: string } }) {
  // Same fixture lookup as production wrapper
  const primary = CALDWELL_X.find((x) => x.id === params.id);
  if (!primary) return notFound();
  // ... resolve related data
  return (
    <<Name>View
      primary={primary}
      related={...}
      breadcrumbRoot={{ href: "/design-system/prototypes/", label: "Prototypes" }}
    />
  );
}
```

**Critical: portfolio wrappers preserve the prototype path semantics.**
- Same fixture lookup as production
- breadcrumbRoot points to /design-system/prototypes/ (NOT /financials/bills or wherever production goes)
- Inherits data-direction=C data-palette=B from /design-system/prototypes/layout.tsx (per 1.5b D-02)

**Per-route rewrite plan (mechanical — apply template to each):**

1. /design-system/prototypes/draws/[id]/page.tsx → mounts DrawApprovalView with breadcrumbRoot to /design-system/prototypes/draws
2. /design-system/prototypes/draws/[id]/print/page.tsx → mounts DrawPrintView
3. /design-system/prototypes/jobs/[id]/budget/page.tsx → mounts BudgetView
4. /design-system/prototypes/jobs/[id]/schedule/page.tsx → mounts ScheduleView
5. /design-system/prototypes/vendors/page.tsx → mounts VendorListView
6. /design-system/prototypes/vendors/[id]/page.tsx → mounts VendorDetailView
7. /design-system/prototypes/documents/[id]/page.tsx → mounts DocumentReviewView
8. /design-system/prototypes/owner-portal/page.tsx → mounts OwnerDashboardView
9. /design-system/prototypes/owner-portal/draws/[id]/page.tsx → mounts OwnerDrawView
10. /design-system/prototypes/mobile-approval/page.tsx → mounts MobileApprovalView
11. /design-system/prototypes/reconciliation/page.tsx → mounts ReconciliationView

**Per CONTEXT D-03 + Plan 2 architect's T10c finding:**
- Portfolio wrappers REPLACE the original full prototype page.tsx files. The original rendering logic is now in the View component.
- The portfolio wrapper itself is now ~20 lines (lookup + props pass) — drastically smaller than the original.
- Behavior MUST be identical to the pre-refactor version (D-07 visual parity).

**Note on the existing portfolio /design-system/prototypes/draws/[id]/page.tsx:** the source file currently renders the full Draw Approval JSX. After Task 1 extracts DrawApprovalView, this file is rewritten to be a 20-line thin wrapper. The pre-refactor rendering logic moves to DrawApprovalView verbatim (with TD-20/25/26 fixes applied).

**Same applies to all 11 routes.** Read each source file fully BEFORE writing the wrapper to ensure the View component captured the rendering logic correctly. If the View is missing something the source had, fix the View (Task 1) before completing this task — do NOT inline the missing logic in the wrapper.
  </action>

  <verify>
    <automated>npm run build && ls src/app/design-system/prototypes/draws/[id]/page.tsx src/app/design-system/prototypes/draws/[id]/print/page.tsx src/app/design-system/prototypes/jobs/[id]/budget/page.tsx src/app/design-system/prototypes/jobs/[id]/schedule/page.tsx src/app/design-system/prototypes/vendors/page.tsx src/app/design-system/prototypes/vendors/[id]/page.tsx src/app/design-system/prototypes/documents/[id]/page.tsx src/app/design-system/prototypes/owner-portal/page.tsx src/app/design-system/prototypes/owner-portal/draws/[id]/page.tsx src/app/design-system/prototypes/mobile-approval/page.tsx src/app/design-system/prototypes/reconciliation/page.tsx 2>&1 | grep -c 'page.tsx'</automated>
    Expected: build exits 0; ls returns 11.

    Per-route check (each portfolio wrapper):
    - File size: each portfolio wrapper should be <50 LOC (vs original ~200-500 LOC per file). `wc -l <file>` returns <50 for each.
    - View import: `grep -c "from \"@/components/prototypes/\"" <file>` returns >=1
    - breadcrumbRoot includes "/design-system/prototypes/" path: `grep -c "design-system/prototypes" <file>` returns >=1

    T10c hook silent on all 11 portfolio wrappers (they import fixtures, but they're inside /design-system/prototypes/ which is the legitimate portfolio path — T10c doesn't gate fixture imports there per Plan 2 finding).

    Manual smoke (executor uses Chrome DevTools MCP):
    - Visit each portfolio route (logged in as Jake/Andrew = platform_admin) — renders the same content as before refactor + same content as production routes
    - Side-by-side comparison: open production /financials/bills/inv-caldwell-001 in one tab + portfolio /design-system/prototypes/invoices/inv-caldwell-001 in another tab → render identically (same content, possibly different breadcrumb labels)
  </verify>

  <done>
    - 11 portfolio wrappers rewritten
    - All 11 import their corresponding View from src/components/prototypes/
    - Same fixture lookup as production wrappers
    - breadcrumbRoot points to /design-system/prototypes/
    - File sizes drastically smaller (20-40 LOC each)
    - Build passes
    - Hooks silent
    - Visual smoke shows portfolio routes still render correctly
  </done>
</task>

<task type="auto">
  <name>Task 4: Side-by-side D-07 visual verification of all 11 production-vs-portfolio route pairs (CRITICAL halt criterion)</name>
  <files>(verification only — no file modifications; output is the visual report appended to plan SUMMARY)</files>

  <read_first>
    - .planning/phases/stage-1.5c-information-architecture/CONTEXT.md (D-07 CRITICAL halt criterion definition)
    - .planning/expansions/stage-1.5c-information-architecture-EXPANDED-SCOPE.md (Amendment 2 — D-07 acceptance criterion)
    - All 11 production routes from Task 2
    - All 11 portfolio routes from Task 3
  </read_first>

  <action>
**Per CONTEXT D-07 + nwrp44 Amendment 2 (THE CRITICAL HALT CRITERION):**

"After D-053 thin-wrapper extraction completes, run side-by-side visual verification: production route at /financials/bills/[id] MUST render identically to /design-system/prototypes/invoices/[id] (same for all 11 extracted route pairs). Method: screenshot or DOM-diff at minimum, manual visual walk preferred. **Treat divergence as a CRITICAL finding, not polish.** Halt extraction and investigate before proceeding to other items."

**Verification method (in order of preference):**

**Method A — Manual visual walk (preferred).**

For each of 11 pairs:
1. Open production route in browser tab 1
2. Open portfolio route in browser tab 2 (logged in as Jake/Andrew = platform_admin)
3. Compare side-by-side at common viewport sizes: 1920x1080 desktop, 1280x800 laptop, 393x852 iPhone (mobile-approval especially)
4. Diff observable elements:
   - Layout proportions (hero grid 50/50; sidebar widths; card padding)
   - Typography (headline weights, eyebrow rendering, monospace for money)
   - Colors (background, text, accent, status badges)
   - Icons (presence, sizing, stroke widths)
   - Interactive elements (buttons render with NwButton styling per TD-20 — minor visual change is EXPECTED + acceptable; document explicitly)
   - Audit timeline rendering
   - Status badges + state labels
   - File preview placeholder
5. Document findings:
   - "Pair N: identical except for [list of differences attributable to TD-20/25/26 fixes]" — ACCEPTABLE
   - "Pair N: differs in [list] — investigate" — CRITICAL HALT

**Method B — Screenshot diff.**

If Chrome DevTools MCP supports screenshot capture:
1. For each pair, take a screenshot of production + portfolio at 1920x1080
2. Visually compare screenshots
3. Document differences
4. Save screenshots to .planning/phases/stage-1.5c-information-architecture/screenshots/ (not committed; review-only)

**Method C — Headless DOM diff (least preferred).**

Run a headless playwright script that:
1. Visits both production + portfolio for each pair
2. Captures `document.body.innerHTML` (after hydration)
3. Diffs with whitespace-collapse
4. Reports differences

This method is least preferred because:
- Requires playwright setup (which is already a dev-dep, but adds tooling overhead)
- DOM-diff misses CSS-only differences (e.g., a different color)
- Manual visual walk catches more nuanced issues

**Expected acceptable differences (NOT divergence):**

Document these explicitly so the verifier doesn't false-flag:
- Breadcrumb labels: production says "Bills > [id]"; portfolio says "Prototypes > Invoices > [id]"
- Buttons: NwButton styling vs raw <button> — slight visual difference (TD-20 fix); acceptable
- Card padding: 16px vs 24px in original — minor visual proportion shift (TD-25 fix); acceptable
- Headline weight: 500 vs 600 in original — slightly thinner letterforms (TD-26 fix); acceptable
- Direction lock: portfolio inherits data-direction=C from /design-system/prototypes/layout.tsx; production inherits from root chain — should resolve to same Site Office tokens; if NOT, that IS divergence (root chain is broken)

**Unexpected = CRITICAL HALT divergence:**
- Layout shift (different column counts, different grid proportions, different spacing not attributable to TD-25 padding change)
- Color drift (e.g., background changes from White Sand to white)
- Missing content (a section that exists in portfolio but not production)
- Broken icons (Heroicon paths different)
- Console errors in either route
- Fonts not loading
- Any rendering that doesn't match the gold-standard PATTERNS.md (Document Review = file preview LEFT + right-rail RIGHT + audit timeline BELOW; List+Detail; Pattern3Dashboard simplified for owner; etc.)

**11 pairs to verify:**

| # | Production | Portfolio |
|---|------------|-----------|
| 1 | /financials/bills/inv-caldwell-001 | /design-system/prototypes/invoices/inv-caldwell-001 |
| 2 | /financials/pay-apps/d-caldwell-05 | /design-system/prototypes/draws/d-caldwell-05 |
| 3 | /financials/pay-apps/d-caldwell-05/print | /design-system/prototypes/draws/d-caldwell-05/print |
| 4 | /financials/reconciliation | /design-system/prototypes/reconciliation |
| 5 | /jobs/j-caldwell-1/budget | /design-system/prototypes/jobs/j-caldwell-1/budget |
| 6 | /jobs/j-caldwell-1/schedule | /design-system/prototypes/jobs/j-caldwell-1/schedule |
| 7 | /people/vendors | /design-system/prototypes/vendors |
| 8 | /people/vendors/v-caldwell-coastal-smart-systems | /design-system/prototypes/vendors/v-caldwell-coastal-smart-systems |
| 9 | /jobs/j-caldwell-1/documents/lr-caldwell-001 | /design-system/prototypes/documents/lr-caldwell-001 |
| 10 | /owner-portal | /design-system/prototypes/owner-portal |
| 11 | /owner-portal/pay-apps/d-caldwell-05 | /design-system/prototypes/owner-portal/draws/d-caldwell-05 |
| 12 | /jobs/j-caldwell-1/mobile-approval (or /jobs/j-caldwell-1/mobile-approval?invoice=inv-caldwell-001) | /design-system/prototypes/mobile-approval |

(12 pairs, not 11 — InvoiceReviewView from Plan 2 is the 12th pair; verify it remains valid here too.)

**Output:**

For each pair, produce a verification report appended to plan SUMMARY:

```
## D-07 Visual Verification Report

### Pair 1: /financials/bills/inv-caldwell-001 ↔ /design-system/prototypes/invoices/inv-caldwell-001
- Status: IDENTICAL (or DIVERGENT)
- Differences observed: [list]
- Differences attributable to TD-20/25/26 fixes: [list — ACCEPTABLE]
- Divergence requiring investigation: [list — CRITICAL HALT if any]

### Pair 2: /financials/pay-apps/d-caldwell-05 ↔ /design-system/prototypes/draws/d-caldwell-05
- ...

(repeat for all 12 pairs)

### Summary
- Total pairs verified: 12
- Pairs IDENTICAL (or differing only by acceptable TD-fix attribution): X
- Pairs DIVERGENT (CRITICAL): Y
- If Y > 0: HALT EXTRACTION + investigate before proceeding to Wave 3
```

**Halt rule:** If ANY pair shows unexpected divergence, halt and investigate. Do NOT mark Plan 3 complete. Surface to Jake.
  </action>

  <verify>
    <automated>test -f .planning/phases/stage-1.5c-information-architecture/01.5c-3-extraction-production-routes-SUMMARY.md && grep -c 'D-07 Visual Verification' .planning/phases/stage-1.5c-information-architecture/01.5c-3-extraction-production-routes-SUMMARY.md && grep -c 'Pair 1' .planning/phases/stage-1.5c-information-architecture/01.5c-3-extraction-production-routes-SUMMARY.md</automated>
    Expected: SUMMARY.md exists; grep returns >=1 for "D-07 Visual Verification"; >=1 for "Pair 1".

    Manual halt check:
    - Read SUMMARY.md "D-07 Visual Verification Report" section
    - Confirm 12 pairs each have a status line
    - Count DIVERGENT pairs — if >0, this plan is NOT complete; halt and investigate

    Per nwrp44: "Treat divergence as a CRITICAL finding, not polish." If Plan 3's verifier flags any pair as DIVERGENT, halt — do NOT proceed to Wave 3.
  </verify>

  <done>
    - 12 production-vs-portfolio pairs verified side-by-side
    - Verification report appended to plan SUMMARY with status per pair
    - 0 DIVERGENT pairs (or, if any, halt + investigation underway)
    - Acceptable TD-fix-attributable differences explicitly documented
    - SUMMARY hands off to Jake review
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production wrapper → View component | Wrapper imports fixtures + passes via props; View has no direct fixture access |
| Production route → middleware auth | Existing middleware enforces auth + role gate; new routes inherit |
| Owner Portal → role gate | /owner-portal/* gated to owner role per existing middleware (D-08) |
| Portfolio route → platform_admin gate | /design-system/prototypes/* gated to platform_admin per existing middleware (1.5a) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.5c-3-01 | I (Information disclosure) | 12 production routes rendering Caldwell fixtures | accept | All Caldwell fixture data is already-sanitized per 1.5b SUBSTITUTION-MAP.md. No real Drummond identifiers in production routes. F1 swaps fixtures for real data + audit-log writes; 1.5c is structural-only. Privacy gate (32-token denylist) verified clean across all wrapper files. |
| T-1.5c-3-02 | T (Tampering) | View components props contract | mitigate | Type-only imports for Caldwell types ensure compile-time safety. Wrapper passes mismatched props would fail TypeScript. Build gate enforces. |
| T-1.5c-3-03 | E (Elevation of privilege) | /owner-portal/* role gate | mitigate | Per D-08, owner role gate via existing middleware (no change in 1.5c). Production /owner-portal mount inherits the same gate. If middleware allows non-owner users to reach /owner-portal, that's a separate finding from this plan — log to Wave 1.1-Lite. Plan 3 verifies the route renders correctly when accessed AS owner role; non-owner access behavior is the existing middleware's responsibility. |
| T-1.5c-3-04 | I (Information disclosure) | Owner Portal data exposure | mitigate | Owner Portal renders Caldwell fixtures filtered to a single owner (1 Caldwell job). Real-data wiring (F1) MUST filter by client_id / homeowner relationship. 1.5c does NOT create new exposure surface beyond what 1.5b prototype already had under platform_admin gate. |
| T-1.5c-3-05 | S (Spoofing) | Production wrapper fixture imports | accept | Fixture imports are static const arrays at build time. No runtime mutation. Spoofing requires code deploy (gated by Vercel auth). |
| T-1.5c-3-06 | R (Repudiation) | Production read-only routes | accept | All production routes are read-only in 1.5c (no mutations per Q8=A). Existing middleware logs every authenticated request via Sentry tags (user_id, org_id, platform_admin). No new audit-log surfaces introduced. |
| T-1.5c-3-07 | D (Denial of service) | 12 production routes | accept | Static fixture data; no database queries. Rendering performance is bounded by fixture array sizes (max ~30 schedule items, ~25 budget lines, etc.). No N+1 query risk. F1 wires real DB queries with proper indexing per CLAUDE.md "Every aggregation needs proper indexes." |
| T-1.5c-3-08 | E (Elevation of privilege) | NwButton replacing raw <button> | accept | Same as Plan 2 T-1.5c-2-03: NwButton is tenant-blind primitive; "Push to QB" / "Approve" actions are no-op in prototype + wrapper. F1 wires actions to authenticated API routes with proper authorization. |
</threat_model>

<verification>
After all 4 tasks complete:

1. 11 NEW View components exist at src/components/prototypes/ + Plan 2's InvoiceReviewView = 12 total Views
2. 12 production routes mount Views as thin wrappers + render with Caldwell fixtures
3. 11 portfolio /design-system/prototypes/* routes are thin wrappers + still accessible under platform_admin gate
4. D-07 visual verification: ALL 12 pairs render IDENTICALLY (or differences are TD-fix-attributable + ACCEPTABLE)
5. npm run build passes
6. typecheck clean (npx tsc --noEmit)
7. Hooks silent: T10c (production routes don't trigger; portfolio routes don't trigger fixture-side gate per architect finding); tenant-blind silent on Views; bouncy-easing silent; post-edit Forbidden tokens silent
8. Privacy gate clean: 32-token denylist returns 0 hits across all 22+ new + modified files

**Halt for Jake review:** halt_after: true. Jake reviews:
- D-07 verification report (all 12 pairs IDENTICAL or acceptable-TD-fix-only)
- Spot-check 2-3 production routes on desktop + iPhone Safari
- Confirm Wave 2 ready for Wave 3 spawn
</verification>

<success_criteria>
- 11 NEW Views + 12 production wrappers + 11 rewritten portfolio wrappers
- TD-20 / TD-25 / TD-26 structural fixes applied uniformly
- Bills/Pay Apps terminology applied in all UI labels
- D-07 visual verification: 12 pairs IDENTICAL or acceptable-TD-fix-only differences
- All 17 must_haves truths verified
- Build + typecheck + hooks + privacy gate clean
- Halt review with Jake passes
</success_criteria>

## Acceptance criteria

- [ ] AC-1.5c-3-01: 11 View components exist at src/components/prototypes/*View.tsx (12 total with Plan 2's InvoiceReviewView)
- [ ] AC-1.5c-3-02: All 11 NEW Views import Caldwell types via `import type` only (no runtime fixture imports)
- [ ] AC-1.5c-3-03: All 11 NEW Views apply TD-20 fix (zero raw <button type=...> for action elements; NwButton replaces)
- [ ] AC-1.5c-3-04: All 11 NEW Views apply TD-25 fix (zero Card padding="lg" usages)
- [ ] AC-1.5c-3-05: All 11 NEW Views apply TD-26 fix (zero fontWeight: 600 inline styles for headlines)
- [ ] AC-1.5c-3-06: All Views are tenant-blind (zero org_id / membership / vendor_id props)
- [ ] AC-1.5c-3-07: 12 production page.tsx files exist at the canonical paths (per files_modified list)
- [ ] AC-1.5c-3-08: Each production wrapper imports Caldwell fixtures + its View (thin wrapper pattern)
- [ ] AC-1.5c-3-09: Each production wrapper calls notFound() on missing fixture lookup
- [ ] AC-1.5c-3-10: 11 portfolio /design-system/prototypes/* page.tsx files rewritten as thin wrappers (file size <50 LOC each)
- [ ] AC-1.5c-3-11: Bills/Pay Apps terminology applied in user-visible labels (UI text, breadcrumb labels, headlines say Bill / Pay App)
- [ ] AC-1.5c-3-12: Owner Portal at /owner-portal/* mounted; role gate inherited from existing middleware
- [ ] AC-1.5c-3-13: Mobile Approval at /jobs/[id]/mobile-approval renders inside iPhone viewport (393x852); 56px Approve button (NwButton size="lg-touch")
- [ ] AC-1.5c-3-14: Print preview at /financials/pay-apps/[id]/print preserves @media print stylesheet from 1.5b
- [ ] AC-1.5c-3-15: D-07 side-by-side visual verification: 12 pairs IDENTICAL or differences are TD-fix-attributable (ACCEPTABLE) — 0 DIVERGENT pairs
- [ ] AC-1.5c-3-16: Build + typecheck + post-edit Forbidden tokens + tenant-blind + bouncy-easing hooks silent throughout
- [ ] AC-1.5c-3-17: Privacy gate clean (32-token denylist) across all 22+ new + modified files

## Nyquist coverage

| Acceptance criterion | Verified by |
|---|---|
| AC-1.5c-3-01 | Task 1 verify (ls src/components/prototypes/*View.tsx returns 12) |
| AC-1.5c-3-02 | Task 1 verify (per-component grep "import type" + grep absence of runtime fixture imports) |
| AC-1.5c-3-03 | Task 1 verify (per-component grep "<button[^>]*type=" returns 0) |
| AC-1.5c-3-04 | Task 1 verify (per-component grep "padding=\"lg\"" returns 0) |
| AC-1.5c-3-05 | Task 1 verify (per-component grep "fontWeight: 600" returns 0) |
| AC-1.5c-3-06 | Task 1 verify (per-component grep "org_id|membership|vendor_id" returns 0) |
| AC-1.5c-3-07 | Task 2 verify (ls of all 12 production page.tsx) |
| AC-1.5c-3-08 | Task 2 verify (per-route grep View import + fixture import) |
| AC-1.5c-3-09 | Task 2 verify (per-route grep "notFound()" returns >=1) |
| AC-1.5c-3-10 | Task 3 verify (per-route wc -l <50 + grep View import) |
| AC-1.5c-3-11 | Task 1 verify + Task 4 visual check (UI labels say Bill / Pay App in all extracted Views; Task 4 verifies in browser) |
| AC-1.5c-3-12 | Task 2 verify (manual smoke at /owner-portal as owner role; non-owner role test) |
| AC-1.5c-3-13 | Task 4 visual check (mobile-approval pair rendered at 393x852 viewport) |
| AC-1.5c-3-14 | Task 4 visual check (print pair rendered + window.print() preview) |
| AC-1.5c-3-15 | Task 4 (CRITICAL halt criterion — 12-pair verification report in SUMMARY) |
| AC-1.5c-3-16 | Plan-level verification (npm run build + typecheck + manual hook silent check) |
| AC-1.5c-3-17 | Plan-level verification (privacy gate grep across all modified files) |

## Decisions inherited (from CONTEXT.md)

- D-01 (= MASTER-PLAN D-051): Bills/Pay Apps terminology in all UI labels
- D-03 (= MASTER-PLAN D-053): Thin-wrapper extraction — 11 prototype components refactored to props-only Views
- D-06 (= MASTER-PLAN D-056): Fix only structural TDs in 1.5c (TD-20/25/26); cosmetic TDs deferred to Wave 1.1-Lite
- D-07: D-07 visual verification — divergence = CRITICAL halt criterion
- D-08: Owner Portal at /owner-portal/* same auth + role gate per existing middleware
- D-14 (Q8=A): No real-backend wiring promotion in 1.5c — production routes mount Caldwell fixtures
- AUDIT-LOG-STRATEGY.md from Plan 2: action string convention for future writes (not directly applied in 1.5c since Views are read-only, but architectural posture documented)

## Halt conditions

- Halt at end of plan (halt_after: true) for Jake D-07 visual verification review + integration smoke
- Halt during execution if:
  - **D-07 verification flags ANY DIVERGENT pair** (CRITICAL halt criterion per CONTEXT D-07 + nwrp44 Amendment 2). Investigate before proceeding.
  - Build / typecheck breaks at any point during 11-component extraction (incremental green per nwrp44 standing rule)
  - T10c hook rejects production wrapper despite Plan 2 architect's Option A finding — surface to Jake
  - NwButton replacement breaks visual parity in unexpected way (e.g., the existing primitive doesn't support the icon prop pattern Plan 2 assumed)
  - Existing /jobs/[id]/budget production route conflicts with new mount (the existing route may have real-backend code paths that 1.5c's mount must REPLACE — surface decision to Jake)
  - Privacy gate flags any new file
  - Existing middleware semantics for /owner-portal/* are surprising (e.g., the role gate doesn't actually filter as expected) — log to Wave 1.1-Lite, do NOT modify middleware in 1.5c

## Output

After completion, create `.planning/phases/stage-1.5c-information-architecture/01.5c-3-extraction-production-routes-SUMMARY.md` per `.claude/get-shit-done/templates/summary.md`. Document:
- Files created / modified (lists of 11 Views + 12 production wrappers + 11 portfolio wrappers + any backed-up existing routes)
- Acceptance criteria status (17 items checked)
- D-07 Visual Verification Report (12 pairs with status per pair — see Task 4 action template)
- TD-20 / TD-25 / TD-26 fix counts per component (e.g., "InvoiceReviewView: 2 raw button → NwButton, 0 padding=lg, 0 fontWeight: 600" → "DrawApprovalView: 3 → NwButton, 1 padding=lg → md, 1 fontWeight: 600 → 500")
- Hook silence + build / typecheck / privacy gate pass
- Halt-review checklist for Jake (D-07 status; spot-check 2-3 routes on desktop + iPhone Safari)
- Any backed-up existing route content (preserved as comments in SUMMARY for F1 swap reference)
