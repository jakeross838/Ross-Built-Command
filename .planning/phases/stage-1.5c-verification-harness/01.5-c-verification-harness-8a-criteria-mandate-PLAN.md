---
phase: stage-1.5c-verification-harness
plan_id: 08a
plan_name: criteria-mandate
type: execute
wave: 5
depends_on: [07]
autonomous: true
halt_after: false  # Wave 5 halt at end of Plan 8b
files_modified:
  - .claude/agents/gsd-planner.md
  - .claude/agents/nightwork-spec-checker.md
  - .claude/skills/nightwork-plan-review/SKILL.md  # path may vary; resolve before edit
  - .planning/templates/criteria-template.md
estimated_days: 0.5
plan_review_watchpoints: [5]
requirements: []
must_haves:
  truths:
    - "gsd-planner.md updated to require <criteria> yaml block in every PLAN.md per D-17/D-18"
    - "nightwork-plan-review enforces criteria presence + specificity (flags missing/vague as REVISE)"
    - "nightwork-spec-checker reads structured <criteria> block in addition to existing prose acceptance criteria per D-19"
    - "Criteria template at .planning/templates/criteria-template.md provides good defaults so plans don't invent format (per D-18 — not a friction tax)"
    - "Template documents the 5 categories (mechanical/dom/visual/behavioral/semantic) with example phrasings appropriate to each"
    - "Plan-review watchpoint #5 anchored: enforces structured thinking without bloating plan-writing time"
  artifacts:
    - path: ".claude/agents/gsd-planner.md"
      provides: "Updated planner prompt — requires <criteria> block; references template"
      contains: "criteria:"
    - path: ".claude/agents/nightwork-spec-checker.md"
      provides: "Updated spec-checker — reads structured <criteria> + existing prose"
      contains: "structured criteria"
    - path: ".claude/skills/nightwork-plan-review/SKILL.md (or wherever the skill prompt lives)"
      provides: "Updated plan-review prompt — enforces criteria presence + specificity"
      contains: "criteria"
    - path: ".planning/templates/criteria-template.md"
      provides: "Canonical template + 10+ example criteria across 5 categories"
      contains: "mechanical:|dom:|visual:|behavioral:|semantic:"
  key_links:
    - from: ".claude/agents/gsd-planner.md"
      to: ".planning/templates/criteria-template.md"
      via: "reference link in planner prompt"
      pattern: "criteria-template"
    - from: ".claude/agents/nightwork-spec-checker.md"
      to: "PLAN.md <criteria> block"
      via: "yaml parse during spec-check"
      pattern: "<criteria>"
---

<objective>
Wave 5 (parallel with Plan 8b) — protocol updates that establish the criteria mandate going forward. Every PLAN.md from this phase forward must include a structured `<criteria>` yaml block per D-17/D-18. This phase's PLANs (1-11) ARE the reference template — they all ship with criteria sections.

Plan-review watchpoint #5: must enforce structured thinking without becoming a friction tax. The criteria template (good defaults) is the load-bearing concern.

Three touchpoints:
1. `gsd-planner.md` — requires the section, references the template
2. `nightwork-plan-review` — enforces presence + specificity
3. `nightwork-spec-checker.md` — consumes structured criteria during /nightwork-qa

Plus the template document itself.

Q6=B explicitly excludes 1.5c IA Plans 1/2/2-amend/3 retrofit (~1-hour separate follow-up after harness merges to main).

Output:
- 3 modified agent/skill files
- 1 new template document
- No halt (Wave 5 halt is owned by Plan 8b)
</objective>

<execution_context>
@C:/Users/Jake/nightwork-platform/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jake/nightwork-platform/.claude/get-shit-done/templates/summary.md
</execution_context>

<canonical_refs>
@.planning/STATE.md
@.planning/phases/stage-1.5c-verification-harness/01.5-c-verification-harness-CONTEXT.md
@.planning/expansions/stage-1.5c-verification-harness-EXPANDED-SCOPE.md
@CLAUDE.md
@.planning/lessons.md

<!-- Existing planner / plan-review / spec-checker prompts to update -->
@.claude/agents/gsd-planner.md
@.claude/agents/nightwork-spec-checker.md
<!-- nightwork-plan-review may be a skill; resolve path before edit -->

<!-- Reference: this phase's PLANs (1-7) all have <criteria> blocks; they
     are the canonical worked examples this template anchors on. -->
@.planning/phases/stage-1.5c-verification-harness/01.5-c-verification-harness-1-foundation-PLAN.md
@.planning/phases/stage-1.5c-verification-harness/01.5-c-verification-harness-2-layer-1-static-checks-PLAN.md
@.planning/phases/stage-1.5c-verification-harness/01.5-c-verification-harness-3-layer-2-standards-framework-PLAN.md
@.planning/phases/stage-1.5c-verification-harness/01.5-c-verification-harness-4-layer-3-vision-PLAN.md
@.planning/phases/stage-1.5c-verification-harness/01.5-c-verification-harness-5-orchestrator-and-vercel-discovery-PLAN.md
</canonical_refs>

<tasks>

<task type="auto">
  <name>Task 1: Criteria template + spec-checker update</name>
  <files>.planning/templates/criteria-template.md, .claude/agents/nightwork-spec-checker.md</files>

  <read_first>
    - .claude/agents/nightwork-spec-checker.md (FULL FILE — current spec-checker prompt to update)
    - .planning/phases/stage-1.5c-verification-harness/01.5-c-verification-harness-1-foundation-PLAN.md (worked example of <criteria> block — Plan 1 of THIS phase)
    - .planning/phases/stage-1.5c-verification-harness/01.5-c-verification-harness-3-layer-2-standards-framework-PLAN.md (another worked example)
    - .planning/phases/stage-1.5c-verification-harness/01.5-c-verification-harness-CONTEXT.md (D-17/D-18/D-19; <specifics> Vision API criteria contract)
    - .gitignore (verify .planning/templates/ tracked or add carve-out)
  </read_first>

  <action>
**Step A — `.planning/templates/criteria-template.md`:**

Verify gitignore handling: `.planning/templates/` is not in current carve-outs. Add to gitignore (after expansions/ block):

```
# templates/ — canonical author templates (criteria, plan, etc.). Synced via
# git so /np pulls templates from any PC. Per D-17/D-18.
!/.planning/templates/
!/.planning/templates/**
```

Then write the template:

```markdown
# Criteria template

**For:** every PLAN.md from stage-1.5c-verification-harness onward.
**Mandate:** D-17/D-18 in `.planning/phases/stage-1.5c-verification-harness/01.5-c-verification-harness-CONTEXT.md`.
**Watchpoint #5:** enforce structured thinking without becoming a friction tax. Use the defaults below; only invent phrasings when the defaults don't fit.

## Why structured criteria

The verification harness (Plans 1-11 of stage-1.5c-verification-harness) verifies plans against falsifiable criteria, in 5 categories:

- `mechanical` → Layer 1 (static checks: build/typecheck/hooks/privacy)
- `dom` → Layer 1 (Playwright DOM assertions across 3 viewports)
- `behavioral` → Layer 2 (industry-standards rules: math identities, state machine completeness)
- `visual` → Layer 3 (Claude vision: design tokens, palette, typography, layout)
- `semantic` → Layer 3 (Claude vision: meaning-level checks like "cost code visible on each line item")

PLAN <criteria> blocks fuel the harness. Vague criteria = vision call ambiguity = halt-for-Jake. Specific criteria = clean PASS or actionable FAIL.

## Drop-in template (copy + edit per plan)

> **Route convention (per nwrp65 FIX 7):** `dom` / `visual` / `semantic` criteria MUST start with an **explicit route** in the form `Page /<exact-path>: …`. The previously-shown `<route>` placeholder pattern is **deprecated** because the verification harness's Layer 3 runner extracts the path literally — `Page <route>: …` causes Playwright to navigate to `/<route>`, which redirects to login on Vercel-protected previews and renders the wrong page. Substitute the actual route the criterion is meant to verify (e.g. `/design-system/patterns` for Document Review pattern, `/financials/invoices/[id]` for cost-code visibility on a real invoice).
>
> If a criterion describes a UI surface that doesn't have a stable rendered page yet, point it at the closest design-system showcase (e.g. `/design-system/patterns`) where the pattern renders against fixture data — or move the criterion to `behavioral:` if it's really about implementation invariants.
>
> **Backward compat:** criteria authored without an explicit route still work — Layer 3 falls back to the preview-URL root. But you'll get FAILs whenever the marketing/login page doesn't show what the criterion describes. Always prefer explicit routes.

```yaml
criteria:
  mechanical:
    - "npm run build clean (0 warnings, 0 errors)"
    - "npx tsc --noEmit returns 0 errors in src/"
    - "All hooks silent on every commit (T10a-T10d, post-edit drift, Drummond grep)"
    - "Privacy gate clean across new files"
  dom:
    - 'Page /financials/invoices/[id] contains element "th[scope=col]:has-text(\"Cost Code\")"'
    - 'Page /dashboard contains text "Active jobs"'
  visual:
    - "Page /design-system/philosophy: Site Office signature visible (tracking-eyebrow 0.18em UPPERCASE on eyebrows)"
    - "Page /design-system/palette: Slate Set B palette only (#5B8699 stone-blue accents; no off-token hex)"
  behavioral:
    - "<function-name>(<input>) returns <expected-output>"
    - "<API endpoint> POST <body> transitions <entity>.status to <expected-status>"
    - "<state machine> on event <X> from state <A> moves to state <B>"
  semantic:
    - "Page /design-system/patterns: cost code is visible on each invoice line item (no blank cells)"
    - "Page /design-system/patterns: Document Review pattern present (file preview LEFT, right-rail panel, audit timeline BELOW)"
```

## When a category doesn't apply

Use `N/A — <one-line reason>` rather than omitting the category. The criteria-loader skips N/A entries silently:

```yaml
criteria:
  mechanical:
    - "npm run build clean"
  dom:
    - "N/A — no UI surfaces in this plan (foundation module + JSON schema only)"
  visual:
    - "N/A — no UI surfaces"
  behavioral:
    - "<test>"
  semantic:
    - "N/A — no Layer 3 vision criteria for this plan"
```

## Specificity rubric (for plan-review enforcement)

A criterion is **specific** if a different Claude instance can run the verification without asking clarifying questions.

| Vague | Specific |
|---|---|
| "Page works" | "Page /dashboard returns HTTP 200" |
| "Component renders" | "Page /financials/bills/[id] contains element 'th[scope=col]:has-text(\"Cost Code\")'" |
| "Auth flow good" | "POST /api/auth/login with valid credentials returns 200 + session cookie; transitions session to active" |
| "Site Office vibe" | "Page /design-system uses tracking-eyebrow 0.18em UPPERCASE on all eyebrow elements" |
| "Cost codes visible" | "Page /financials/bills/[id]: every row in the line items table has a non-empty cost-code cell" |

`nightwork-plan-review` flags vague criteria (left column patterns) as REVISE. Authoring tip: if you find yourself writing "good", "correct", "works", "renders", you're vague — restate as a falsifiable assertion.

## Common authoring errors (post-mortem from stage-1.5c-verification-harness self-test)

The harness's first end-to-end run (#25515440465 on `a476a03`, 2026-05-07) surfaced **9 misauthored Layer 3 ACs across Plans 2/4/6/7/8b/9** — each describing something that vision API cannot evaluate from a UI screenshot. The vision model identified them all as "non-UI/documentation criterion" or "internal design unrelated to UI" with confidence ≤ 0.5. Pattern, with examples:

| Anti-pattern | Example | Why it's wrong | Correct home |
|---|---|---|---|
| **Documentation/README claims** | "Layer 3 README documents the idempotency contract…" (AC-04-60) | README content isn't on the rendered web page | `behavioral:` (file-content grep) or N/A |
| **CI workflow design** | "Workflow design: PR comment includes both run link and artifact link…" (AC-06-77) | CI artifacts aren't on the rendered web page | `behavioral:` (workflow YAML grep) or N/A |
| **Agent design / architecture** | "Agent design: gsd-fix-executor scope-narrowed…" (AC-07-92) | Agent prompts aren't on the rendered web page | `behavioral:` (frontmatter / fence grep) or N/A |
| **Code architecture / state machines** | "Loop design: state machine cleanly bounded (MAX_ITERATIONS=3 hard cap; no recursive calls; pure-function transitions)" (AC-08b-126) | Code structure isn't on the rendered web page | `behavioral:` (code structure grep) or N/A |
| **Schema / data-format rules** | "Schema design: extractable-data-only rule (no speculation) prevents calibration entries from devolving into noisy free-text" (AC-09-140) | Schema rules aren't on the rendered web page | `behavioral:` (schema parse) or N/A |
| **Source-file implementation claims** | "src/lib/verification/layer1/dom-assertions.ts uses chromium.launch() and 3 viewports (1920/1280/393)" (AC-02-20) | Source files aren't on the rendered web page | `behavioral:` (grep) or N/A |

**The rule:** `visual:` and `semantic:` criteria MUST describe something visible on a rendered web page when the harness navigates to a route via Playwright + chromium. If the criterion is about code, agents, READMEs, schemas, CI workflows, or "design intent" — it belongs in `behavioral:` (Layer 2 standards rule with a registry entry) or `mechanical:` (Layer 1 grep-style check) or as `N/A — <reason>` if it's tautologically verified by integration.

Cleanups landed in: nwrp60 FIX 3 (AC-02-20), nwrp64 FIX 6 (8 ACs across Plans 4/6/7/8b/9). The pattern: convert misauthored visual:/semantic: blocks to a single `N/A — <reason>` entry citing the run that surfaced the FAIL and explaining where the criterion's truth actually lives.

## Worked examples in stage-1.5c-verification-harness

This phase's PLAN files 1-11 are the canonical worked examples. Reference for shape:

- **Plan 1** (foundation): mechanical-heavy, dom/visual/semantic = N/A
- **Plan 3** (Layer 2 framework): behavioral-heavy with one mechanical
- **Plan 5** (orchestrator): all 5 categories represented; semantic anchors the rerun-no-op invariant
- **Plan 8a** (this plan): all 5 categories represented; semantic anchors the design

## Friction tax check (Plan-review watchpoint #5)

The criteria mandate must NOT cost more than ~5 minutes per plan to author. If a plan-author finds themselves spending 30+ minutes on the criteria block:

1. Are they over-specifying? Most criteria can be 1 line.
2. Is the plan too large? Each plan should have 5-15 criteria total. >20 = plan should split.
3. Is the template missing a category for this plan's scope? Surface as plan-review iter-1 feedback to extend the template.

The template above ships with the most-common phrasings. Reuse > invent.
```

**Step B — Update `.claude/agents/nightwork-spec-checker.md`:**

Read the full file. Find where it lists "Method" steps (around line 17-28 per the snippet earlier). Add a step that consumes the structured `<criteria>` yaml block:

Use Edit to insert a step into the existing Method section. Specifically, after the existing step 1 ("List acceptance criteria from SPEC.md..."), add:

```markdown
1a. **Read structured `<criteria>` block from PLAN.md** (per D-19 in `.planning/phases/stage-1.5c-verification-harness/01.5-c-verification-harness-CONTEXT.md`).

   PLAN files from stage-1.5c-verification-harness onward include a `<criteria>` yaml block under 5 categories: mechanical / dom / visual / behavioral / semantic. Parse this block and add each entry to the criteria checklist.

   Map categories to verification layer for cross-referencing the verification harness report:
   - `mechanical` + `dom` → Layer 1
   - `behavioral` → Layer 2
   - `visual` + `semantic` → Layer 3

   For each structured criterion, look up the corresponding result in `.planning/verification/reports/<phase>/final.md` (per D-15: harness final report is git-tracked).

   **If `<criteria>` block missing from a PLAN.md authored after stage-1.5c-verification-harness ships:** mark the spec-check as BLOCKING with finding "PLAN.md missing structured `<criteria>` block (D-17/D-18 mandate). Run /np to retrofit OR halt for Jake to author."

   **N/A entries are valid** — skip them (per criteria-loader convention).
```

Update the existing Method step list accordingly. Preserve all existing prose; insert step 1a inline.

Also add a new section near the end:

```markdown
## Structured criteria mapping (per D-19)

When the harness has run for a phase, cross-reference the harness verdict with the structured criteria:

| `<criteria>` category | Layer | Harness verdict source |
|---|---|---|
| `mechanical`, `dom` | 1 | `.planning/verification/reports/<phase>/final.md` Layer 1 table |
| `behavioral` | 2 | `.planning/verification/reports/<phase>/final.md` Layer 2 table |
| `visual`, `semantic` | 3 | `.planning/verification/reports/<phase>/final.md` Layer 3 table |

Spec-check verdict relationship:
- All structured criteria PASS in harness + all prose acceptance criteria covered → spec-check PASS
- Any structured criterion FAIL → spec-check NEEDS WORK
- Any structured criterion ambiguous (Layer 3 confidence < 0.7) → spec-check BLOCKING (halt-for-Jake)
- `<criteria>` block missing → spec-check BLOCKING (D-17/D-18 mandate)
```

**STANDING RULES:**
- Process discipline reinforced.
- No code edits to src/scripts; this plan only updates agent prompts + templates.

**COMMIT:**
```
feat(stage-1.5c-vh): Plan 8a Task 1 — criteria template + spec-checker structured-criteria consumer

.planning/templates/criteria-template.md: drop-in template + specificity
rubric + worked-example references + friction-tax check (D-18 watchpoint #5).

nightwork-spec-checker.md: adds Method step 1a — read structured <criteria>
yaml block + cross-reference harness final.md verdicts. BLOCKING if PLAN
post-harness-ship lacks <criteria> block (D-17/D-18 mandate).
```
  </action>

  <verify>
    <automated>
test -f .planning/templates/criteria-template.md &&
grep -q "Drop-in template" .planning/templates/criteria-template.md &&
grep -q "mechanical:" .planning/templates/criteria-template.md &&
grep -q "Specificity rubric" .planning/templates/criteria-template.md &&
grep -q "structured criteria" .claude/agents/nightwork-spec-checker.md &&
grep -q "D-17\|D-18\|D-19" .claude/agents/nightwork-spec-checker.md &&
grep -q "templates" .gitignore || echo "templates/ carve-out may be missing — verify"
    </automated>
  </verify>

  <done>
    - Template exists with template block + rubric + friction-tax + worked examples
    - Spec-checker updated to consume structured criteria + cross-ref harness verdict
    - .gitignore permits templates/
  </done>
</task>

<task type="auto">
  <name>Task 2: gsd-planner mandate update + plan-review enforcement</name>
  <files>.claude/agents/gsd-planner.md, .claude/skills/nightwork-plan-review/SKILL.md</files>

  <read_first>
    - .claude/agents/gsd-planner.md (FULL FILE — current planner prompt to update)
    - Search for nightwork-plan-review skill: `ls .claude/skills/nightwork-plan-review/` and `ls .claude/agents/ | grep nightwork-plan-review`
    - .planning/templates/criteria-template.md (just created)
    - .planning/phases/stage-1.5c-verification-harness/01.5-c-verification-harness-CONTEXT.md (D-17/D-18)
  </read_first>

  <action>
**Step A — Update `.claude/agents/gsd-planner.md`:**

Read the full planner prompt. Find the section that describes the PLAN.md format (likely a section header like "PLAN format" or "PLAN structure" or "Output format"). The planner prompt is large — use Grep to find the right insertion point:

```bash
grep -n "criteria\|PLAN.md\|Output format\|<criteria>\|acceptance" .claude/agents/gsd-planner.md
```

Add a new section near the existing PLAN format documentation:

```markdown
## Criteria mandate (per stage-1.5c-verification-harness D-17/D-18)

Every PLAN.md from stage-1.5c-verification-harness onward MUST include a `<criteria>` yaml block. The block is consumed by:

1. `nightwork-plan-review` (this orchestrator's downstream gate) — flags missing/vague criteria as REVISE
2. The verification harness `scripts/verify-phase.ts` — verifies each criterion mechanically (Layer 1), against industry standards (Layer 2), or via Claude vision (Layer 3)
3. `nightwork-spec-checker` (during `/nightwork-qa`) — cross-references harness verdict with structured criteria

### Format

```yaml
criteria:
  mechanical:
    - "<falsifiable assertion>"
  dom:
    - "<falsifiable DOM assertion>"
  visual:
    - "<falsifiable visual assertion>"
  behavioral:
    - "<falsifiable behavioral assertion>"
  semantic:
    - "<falsifiable semantic assertion>"
```

Categories map to verification layers:
- `mechanical` + `dom` → Layer 1 (static + DOM)
- `behavioral` → Layer 2 (industry-standards)
- `visual` + `semantic` → Layer 3 (Claude vision)

### When a category doesn't apply

Use `N/A — <one-line reason>` rather than omitting:

```yaml
criteria:
  mechanical:
    - "npm run build clean"
  dom:
    - "N/A — no UI surfaces in this plan (foundation module only)"
```

### Specificity is non-negotiable

Vague criteria = vision call ambiguity = halt-for-Jake. Use falsifiable assertions only.

| Vague (REVISE) | Specific (PASS plan-review) |
|---|---|
| "Page works" | "Page /dashboard returns HTTP 200" |
| "Auth good" | "POST /api/auth/login with valid creds returns 200 + session cookie" |

### Drop-in template

Reference `.planning/templates/criteria-template.md` for the canonical drop-in template + 10+ example phrasings + worked examples from stage-1.5c-verification-harness Plans 1-11.

### Friction-tax check

The mandate must NOT cost more than ~5 minutes per plan to author. If a plan has >20 criteria, the plan is too large — split it. If the template doesn't fit a plan's category, surface to plan-review iter-1 to extend the template.
```

**Step B — Plan-review enforcement:**

Find the nightwork-plan-review skill or agent file. Search:

```bash
find .claude -name "nightwork-plan-review*" -type d 2>/dev/null
find .claude -name "nightwork-plan-review*" -type f 2>/dev/null
```

Likely path: `.claude/skills/nightwork-plan-review/SKILL.md` or `.claude/commands/nightwork-plan-review.md`. Read whichever exists.

Add a new check to the plan-review reviewer aggregation:

```markdown
## Criteria mandate enforcement (per stage-1.5c-verification-harness D-17/D-18)

Every PLAN.md must include a `<criteria>` yaml block. Plan-review aggregates this check across all reviewers (architect, planner, enterprise-readiness, multi-tenant-architect, scalability, compliance, security, design-pushback) and surfaces as a discrete finding.

### Check

For each PLAN.md in the phase:
1. Confirm `<criteria>` ... `</criteria>` block present (regex match the opening + closing tags).
2. Confirm yaml block inside contains all 5 categories (mechanical, dom, visual, behavioral, semantic) — even if some are explicit `N/A`.
3. Confirm at least one entry per applicable category (non-N/A).
4. Specificity check: scan entries for vague patterns ("works", "good", "correct", "renders", "looks right" without specific selectors / state values / commands). Flag as REVISE.

### Verdict integration

- Missing block → BLOCKING finding (`PLAN_MISSING_CRITERIA_BLOCK`)
- Missing category → REVISE finding (`PLAN_MISSING_CATEGORY:<name>`)
- Vague entries (>2 detected) → REVISE finding (`PLAN_VAGUE_CRITERIA`)
- All 5 categories with specific entries → no finding

### 1.5c IA retrofit exclusion (per Q6=B)

Plans authored before stage-1.5c-verification-harness ships (specifically, 1.5c IA Plans 1/2/2-amend/3 on `phase/1.5-c-information-architecture` branch) are exempt from this check. The retrofit is a separate ~1-hour follow-up after the harness merges to main.

To detect: check the PLAN's frontmatter `phase:` field. If `stage-1.5c-information-architecture` AND no `<criteria>` block, mark `EXEMPT — Q6=B retrofit pending`.

### Friction-tax watchpoint (per D-18 / plan-review watchpoint #5)

If plan-review iter-1 finds itself flagging >50% of new PLAN files as `PLAN_VAGUE_CRITERIA`, the template is failing — friction tax is real. Surface as a meta-finding: "criteria-template needs better default phrasings for <plan domain>" — extend the template in a follow-up rather than failing every plan.
```

**STANDING RULES:**
- Process discipline reinforced.
- Per D-29: don't modify 1.5c IA branch's PLAN files; the retrofit happens separately after harness merges to main.

**COMMIT:**
```
feat(stage-1.5c-vh): Plan 8a Task 2 — gsd-planner mandate + plan-review enforcement

gsd-planner.md: criteria mandate section requires <criteria> block in every
PLAN.md going forward. References .planning/templates/criteria-template.md
+ specificity rubric + friction-tax watchpoint.

nightwork-plan-review: criteria-block presence + 5-category coverage +
specificity scan. BLOCKING on missing block; REVISE on missing category /
vague entries. 1.5c IA exemption per Q6=B.

Plan-review watchpoint #5 anchored.
```
  </action>

  <verify>
    <automated>
grep -q "Criteria mandate\|<criteria>" .claude/agents/gsd-planner.md &&
grep -q "criteria-template" .claude/agents/gsd-planner.md &&
PLAN_REVIEW_PATH=$(find .claude/skills .claude/agents .claude/commands -name "nightwork-plan-review*" -type f 2>/dev/null | head -1) &&
test -n "$PLAN_REVIEW_PATH" &&
grep -q "Criteria mandate enforcement\|criteria.*block\|<criteria>" "$PLAN_REVIEW_PATH" &&
grep -q "Q6=B\|1.5c IA exemption\|EXEMPT" "$PLAN_REVIEW_PATH"
    </automated>
  </verify>

  <done>
    - gsd-planner.md updated with criteria mandate section
    - nightwork-plan-review (skill OR agent OR command) updated with enforcement check
    - 1.5c IA exemption documented (Q6=B)
    - Plan-review watchpoint #5 anchored
  </done>
</task>

</tasks>

<acceptance_criteria>

- [ ] **AC-1.5c-vh-8a-1:** `.planning/templates/criteria-template.md` exists with drop-in template + 5 categories + 10+ example phrasings
- [ ] **AC-1.5c-vh-8a-2:** Template documents N/A convention for non-applicable categories
- [ ] **AC-1.5c-vh-8a-3:** Template includes specificity rubric (vague vs specific table)
- [ ] **AC-1.5c-vh-8a-4:** Template references stage-1.5c-verification-harness PLAN files 1-11 as worked examples
- [ ] **AC-1.5c-vh-8a-5:** Template includes friction-tax check (~5min/plan target; >20 criteria = split plan)
- [ ] **AC-1.5c-vh-8a-6:** `.gitignore` permits `.planning/templates/` (carve-out present)
- [ ] **AC-1.5c-vh-8a-7:** `.claude/agents/nightwork-spec-checker.md` updated with Method step 1a (read structured criteria) + structured-criteria-mapping section
- [ ] **AC-1.5c-vh-8a-8:** Spec-checker maps mechanical/dom→Layer 1, behavioral→Layer 2, visual/semantic→Layer 3
- [ ] **AC-1.5c-vh-8a-9:** Spec-checker BLOCKING when post-harness-ship PLAN missing `<criteria>` block
- [ ] **AC-1.5c-vh-8a-10:** `.claude/agents/gsd-planner.md` includes "Criteria mandate" section
- [ ] **AC-1.5c-vh-8a-11:** Planner prompt references `.planning/templates/criteria-template.md`
- [ ] **AC-1.5c-vh-8a-12:** nightwork-plan-review (path resolved) includes criteria-block enforcement check (BLOCKING / REVISE rules)
- [ ] **AC-1.5c-vh-8a-13:** Plan-review documents 1.5c IA exemption per Q6=B
- [ ] **AC-1.5c-vh-8a-14:** Plan-review documents friction-tax meta-finding (>50% PLAN_VAGUE_CRITERIA → extend template, not fail plans)

</acceptance_criteria>

<criteria>

```yaml
criteria:
  mechanical:
    - "All hooks silent on every commit"
    - "Privacy gate clean across new template + agent updates"
    - ".planning/templates/criteria-template.md is valid markdown (renders without syntax errors via grep -c '^```' = even number — code fence parity)"
    - ".gitignore permits /.planning/templates/ (grep returns matching carve-out line)"
  dom:
    - "N/A — no UI surfaces in this plan (template + agent prompt updates only)"
  visual:
    - "N/A — no UI"
  behavioral:
    - "Criteria template documents 5 categories (mechanical/dom/visual/behavioral/semantic)"
    - "Criteria template's specificity rubric has at least 5 vague-vs-specific examples"
    - "gsd-planner.md 'Criteria mandate' section explicitly references stage-1.5c-verification-harness D-17/D-18"
    - "nightwork-spec-checker.md Method section includes step 1a reading structured <criteria> block"
    - "nightwork-plan-review enforcement section flags vague entries (matching 'works|good|correct|renders|looks right' patterns) as REVISE"
    - "nightwork-plan-review enforcement section exempts 1.5c IA Plans 1/2/2-amend/3 (Q6=B retrofit pending)"
  semantic:
    - "N/A — these ACs described template-doc design + spec-checker design (friction-tax anchoring / cross-reference logic), neither of which is vision-evaluable from a UI screenshot. Same authoring anti-pattern as nwrp64 FIX 6 cleanup. Template + spec-checker correctness is verified by behavioral: ACs above (template content checks, spec-checker prompt grep) and by integration. Cleanup landed in nwrp65 FIX 7."
```

</criteria>

<halt_conditions>

- **No halt within this plan.** Wave 5 halt is owned by Plan 8b. When both Plan 8a + 8b complete, the orchestrator halts for Jake review.
- Soft halt if: spec-checker / planner / plan-review files not found at expected paths (resolve before edit), gitignore mistake.

</halt_conditions>

<canonical_refs>

- `.planning/phases/stage-1.5c-verification-harness/01.5-c-verification-harness-CONTEXT.md` (D-17/D-18/D-19; <specifics> Vision API criteria contract)
- `.planning/expansions/stage-1.5c-verification-harness-EXPANDED-SCOPE.md` §6 Q6=B + §7 item 7 + §8.5 watchpoint #5
- `.claude/agents/gsd-planner.md` (current planner prompt — full file)
- `.claude/agents/nightwork-spec-checker.md` (current spec-checker prompt — full file)
- `.planning/phases/stage-1.5c-verification-harness/01.5-c-verification-harness-1-foundation-PLAN.md` etc. (worked examples for the template)

</canonical_refs>
