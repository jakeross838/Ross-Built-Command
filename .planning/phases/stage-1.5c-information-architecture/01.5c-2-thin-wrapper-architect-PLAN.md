---
phase: stage-1.5c-information-architecture
plan: 2
plan-name: thin-wrapper-architect
type: execute
status: not-started
wave: 2
depends_on: [1]
autonomous: false
halt_after: true
threat_model_severity: low
requirements: []
files_modified:
  - src/components/prototypes/InvoiceReviewView.tsx
  - src/app/design-system/prototypes/invoices/[id]/page.tsx
  - .planning/phases/stage-1.5c-information-architecture/AUDIT-LOG-STRATEGY.md
  - .claude/hooks/nightwork-post-edit.sh
requires:
  - .planning/phases/stage-1.5c-information-architecture/CONTEXT.md
  - .planning/phases/stage-1.5c-information-architecture/PATTERNS.md
  - .planning/phases/stage-1.5b-prototype-gallery/01.5-2-invoice-draw-vendors-PLAN.md
  - src/app/design-system/prototypes/invoices/[id]/page.tsx (current — extraction source)
  - src/app/design-system/_fixtures/drummond/index.ts
  - .claude/hooks/nightwork-post-edit.sh (T10c lines 194-230)
provides:
  - InvoiceReviewView component (canonical thin-wrapper exemplar)
  - AUDIT-LOG-STRATEGY.md (audit-log action string strategy decision document for Bills/Pay Apps rename)
  - T10c hook posture verification + adjustment if needed (architectural finding for Plan 3)
  - Refactored /design-system/prototypes/invoices/[id]/page.tsx as the first thin-wrapper precedent
affects:
  - Plan 3 (10 remaining extractions follow this exemplar)
  - Future post-1.5c writes to audit_log (action strings change to bill_*; UI surfaces both)
  - Hook semantics if T10c needs adjustment for production-route fixture imports
acceptance-criteria-target: 9 falsifiable items
estimated-duration: 0.75 day
must_haves:
  truths:
    - "InvoiceReviewView component exists at src/components/prototypes/InvoiceReviewView.tsx accepting (invoice, vendor, job, costCode) props"
    - "InvoiceReviewView imports NO fixtures (no @/app/design-system/_fixtures/* runtime imports — type-only imports OK)"
    - "InvoiceReviewView applies TD-20 fix (Reject + Push to QB header buttons use NwButton, not raw <button>)"
    - "InvoiceReviewView applies TD-25 fix (Card padding=md not lg for any cards using lg in original)"
    - "InvoiceReviewView applies TD-26 fix (Space Grotesk Medium fontWeight: 500, not 600 for headlines)"
    - "/design-system/prototypes/invoices/[id]/page.tsx becomes a thin wrapper passing CALDWELL_INVOICES + CALDWELL_VENDORS + CALDWELL_JOBS + CALDWELL_COST_CODES via props"
    - "AUDIT-LOG-STRATEGY.md documents the action string convention: historical invoice_* preserved as-is; new writes use bill_* / pay_app_*; UI reads both as one"
    - "T10c hook semantics verified for production-route fixture imports — either confirmed safe-as-is OR hook adjusted for the new boundary (architectural finding documented)"
    - "Side-by-side visual verification: /design-system/prototypes/invoices/inv-caldwell-001 renders identically to its pre-refactor version (D-07 precedent — caught regressions early on the architect surface, not at Plan 3 scale)"
  artifacts:
    - path: "src/components/prototypes/InvoiceReviewView.tsx"
      provides: "Pure props-only Document Review component for invoice/bill review — both /financials/bills/[id] and /design-system/prototypes/invoices/[id] mount this"
      contains: "InvoiceReviewViewProps"
    - path: "src/app/design-system/prototypes/invoices/[id]/page.tsx"
      provides: "Thin wrapper for /design-system/prototypes/invoices/[id] — fixture lookup + InvoiceReviewView mount"
      contains: "InvoiceReviewView"
    - path: ".planning/phases/stage-1.5c-information-architecture/AUDIT-LOG-STRATEGY.md"
      provides: "Decision document: audit_log action string strategy for Bills/Pay Apps rename"
      exports: ["Strategy"]
    - path: ".claude/hooks/nightwork-post-edit.sh"
      provides: "Updated T10c semantics if production-route boundary needs adjustment (else unchanged)"
      contains: "T10c"
  key_links:
    - from: "src/components/prototypes/InvoiceReviewView.tsx"
      to: "src/app/design-system/_fixtures/drummond/types.ts"
      via: "type-only import"
      pattern: "import type"
    - from: "src/components/prototypes/InvoiceReviewView.tsx"
      to: "src/components/nw/* primitives"
      via: "named imports (Card, Eyebrow, Money, DataRow, Badge, NwButton)"
      pattern: "components/nw"
    - from: "src/app/design-system/prototypes/invoices/[id]/page.tsx"
      to: "InvoiceReviewView + Caldwell fixtures"
      via: "wrapper pattern — fixture lookup + props pass"
      pattern: "InvoiceReviewView"
    - from: ".planning/phases/stage-1.5c-information-architecture/AUDIT-LOG-STRATEGY.md"
      to: "any post-1.5c code that writes audit_log rows for invoice/bill/draw/pay-app actions"
      via: "documented convention readable by future plan-phase agents + execute agents"
      pattern: "Strategy"
---

<objective>
Architect-first task: produce the canonical thin-wrapper extraction exemplar (InvoiceReviewView), decide the audit_log action-string strategy for the Bills/Pay Apps rename, and verify T10c hook posture for production-route fixture imports. **Halt after Plan 2** so Jake reviews the architect's decisions before Plan 3 spawns 4-6 parallel implementers refactoring the remaining 10 prototype components.

Per CONTEXT D-03 + nwrp44 standing rule "Coupled work (thin-wrapper extraction architecture, audit-log string strategy, redirect map) → single architect first, then parallel implementers" — this plan is the architect; Plan 3 is the implementers.

The risk this plan mitigates is R1 in EXPANDED-SCOPE §8: "Q4=A (thin-wrapper extraction) takes longer than estimated; pushes budget past 7 days." If the architect surfaces extraction wrinkles (e.g., T10c hook needs adjustment, NwButton replacement requires component contract changes, audit-log strategy reveals downstream complications) on ONE component first, Plan 3's 10-component refactor goes faster + cleaner. The architect's exemplar IS the template Plan 3 follows.

Purpose:
- One canonical extraction precedent. Plan 3 implementers refactor against this exemplar.
- Lock the audit_log action string strategy. Future writes are deterministic.
- Verify T10c hook posture. Production routes mounting Caldwell fixtures may need a hook adjustment OR may already be safe.
- Catch D-07 visual divergence early. Side-by-side verify ONE component (the gold-standard Document Review pattern) before Plan 3 attempts all 11.

Output:
- src/components/prototypes/InvoiceReviewView.tsx — pure props-only component (the exemplar)
- Refactored src/app/design-system/prototypes/invoices/[id]/page.tsx — thin wrapper passing fixtures via props
- .planning/phases/stage-1.5c-information-architecture/AUDIT-LOG-STRATEGY.md — strategy doc for action string convention
- .claude/hooks/nightwork-post-edit.sh — adjusted ONLY IF T10c hook semantics need updating; else unchanged
- HALT for Jake review: Architectural approach + AUDIT-LOG-STRATEGY.md + T10c hook posture decision before parallel implementers run
</objective>

<execution_context>
@C:/Users/Jake/nightwork-platform/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jake/nightwork-platform/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/stage-1.5c-information-architecture/CONTEXT.md
@.planning/phases/stage-1.5c-information-architecture/PATTERNS.md
@.planning/phases/stage-1.5c-information-architecture/01.5c-1-nav-today-redirects-PLAN.md
@.planning/expansions/stage-1.5c-information-architecture-EXPANDED-SCOPE.md
@.planning/phases/stage-1.5b-prototype-gallery/CONTEXT.md
@.planning/phases/stage-1.5b-prototype-gallery/findings.md
@.planning/phases/stage-1.5b-prototype-gallery/01.5-2-invoice-draw-vendors-PLAN.md
@CLAUDE.md
@.planning/design/SYSTEM.md
@.planning/design/COMPONENTS.md
@.planning/design/PATTERNS.md
@.planning/design/CHOSEN-DIRECTION.md
@src/app/design-system/_fixtures/drummond/index.ts
@src/app/design-system/_fixtures/drummond/types.ts
@src/app/design-system/prototypes/invoices/[id]/page.tsx
@src/components/nw/Button.tsx
@src/components/nw/Card.tsx
@src/components/nw/Eyebrow.tsx
@src/components/nw/Money.tsx
@src/components/nw/DataRow.tsx
@src/components/nw/Badge.tsx
@.claude/hooks/nightwork-post-edit.sh

<interfaces>
The current /design-system/prototypes/invoices/[id]/page.tsx (extraction source) is documented in 01.5-2-invoice-draw-vendors-PLAN.md Task 1 code skeleton (around lines 200-450 of that plan file).

The component currently:
- Imports CALDWELL_INVOICES + CALDWELL_VENDORS + CALDWELL_JOBS + CALDWELL_COST_CODES directly
- Looks up `inv = CALDWELL_INVOICES.find((i) => i.id === params.id)` internally
- Has 2 raw <button> elements (Reject + Push to QB) — TD-20 violation
- Uses Card primitives (verify padding values for TD-25 violation count at execute time)
- Has h1 with `fontWeight: 500` per the original spec — verify; if 600, TD-26 violation
- No hardcoded hex per existing T10c-clean discipline — verify
- Renders the Document Review pattern: hero grid 50/50 (file preview LEFT + right-rail RIGHT) + audit timeline below

Component contract for the extracted view (props-only):

  // src/components/prototypes/InvoiceReviewView.tsx
  import type {
    CaldwellInvoice,
    CaldwellInvoiceConfidenceDetails,
    CaldwellInvoiceStatus,
  } from "@/app/design-system/_fixtures/drummond/types";

  export interface InvoiceReviewViewProps {
    invoice: CaldwellInvoice;
    vendor: { id: string; name: string };
    job: { id: string; name: string };
    costCode: { id: string; code: string; description: string } | null;
  }

  export default function InvoiceReviewView(props: InvoiceReviewViewProps): JSX.Element;

NwButton primitive (src/components/nw/Button.tsx) — verify exact API at execute time. Per .planning/design/COMPONENTS.md the contract supports variant + size + className + onClick + children:

  import NwButton from "@/components/nw/Button";
  <NwButton variant="primary" | "secondary" | "danger" size="sm" | "md" | "lg-touch">

T10c hook semantics (.claude/hooks/nightwork-post-edit.sh:194-230):
  Current rule: blocks @/lib/(supabase|org|auth) imports from files inside src/app/design-system/*. Designed for sample-data isolation in the playground.

  Open question: does T10c also block (or should it block) _fixtures/drummond/* imports from production routes (i.e., outside src/app/design-system/*)?

  Decision options for the architect:
  - Option A: T10c stays unchanged. Production routes import fixtures freely (the wrapper boundary makes this acceptable; F1 will swap fixtures for real data). Document the convention.
  - Option B: Add a new hook rule (T10e) that warns (not blocks) when production routes import _fixtures/drummond — to track the temporary debt.
  - Option C: Strict — block production routes from importing _fixtures/drummond entirely; thin wrappers must mount placeholder data or fixture-injected via a separate provider.

  Recommended: Option A — document the convention; do not extend hook. The architectural separation is preserved by the props-only View components (which import zero fixtures at runtime). The wrapper layer is a permitted temporary stand-in. F1 swaps the wrapper's data source from CALDWELL_INVOICES to real Supabase queries; the View component does not change.

Site Office tokens (per SYSTEM.md):
  - UPPERCASE 0.18em eyebrows (Eyebrow primitive)
  - Space Grotesk Medium 500 headlines (NEVER 600 — TD-26 fix)
  - JetBrains Mono 0.18em tracking for monospace
  - Card padding: md (16px) NOT lg (24px) — TD-25 fix
  - 1px slate-tile left-stamp on cards (CSS rule, automatic)
  - 150ms ease-out motion (no bouncy)
  - NwButton primitive (NEVER raw <button> for action elements — TD-20 fix)
  - No hardcoded hex (only var(--...) references)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Architect decisions — AUDIT-LOG-STRATEGY.md + T10c hook posture verification</name>
  <files>.planning/phases/stage-1.5c-information-architecture/AUDIT-LOG-STRATEGY.md, .claude/hooks/nightwork-post-edit.sh (only if hook adjustment needed)</files>

  <read_first>
    - .planning/phases/stage-1.5c-information-architecture/CONTEXT.md (full file — D-01 Bills/Pay Apps rename + R2 risk in EXPANDED-SCOPE)
    - .planning/expansions/stage-1.5c-information-architecture-EXPANDED-SCOPE.md (Risk R2 — audit-log action-string drift)
    - .claude/hooks/nightwork-post-edit.sh (FULL file — T10a-T10d rules; understand current sample-data isolation semantics; T10c specifically at lines 194-230)
    - src/app/design-system/prototypes/invoices/[id]/page.tsx (current — verify whether it imports anything that would trigger T10c)
    - CLAUDE.md (Architecture Rules — soft-delete + status_history + recalculate-don't-increment; audit_log convention)
    - .planning/architecture/CURRENT-STATE.md (audit_log table description)
    - Search for existing audit_log writes in the codebase: grep -rE 'audit_log|action_log' src/lib/ src/app/api/ (look for action string conventions)
    - Search for existing invoice_/draw_ action strings in the codebase
  </read_first>

  <action>
**Step A — Audit existing audit_log action string conventions.**

Run greps to surface:
- Where audit_log rows are written from (any API routes, any server actions, any service modules)
- What action strings are currently used (invoice_received, invoice_approved, invoice_pushed_to_qb, draw_submitted, etc.)
- Are there parallel "type" + "action" columns or just an "action" string?
- Are downstream consumers of audit_log filter by action prefix (e.g., action LIKE 'invoice_%') or by other column?

Document findings inline — list every audit_log write site found + its action string format.

**Step B — Decide audit_log action string strategy.**

Per EXPANDED-SCOPE §8 R2 (the open architectural question for Plan 2):

Three plausible strategies:

**Strategy 1 — Append new vocabulary (RECOMMENDED).**
- Historical audit_log rows with invoice_* action strings stay AS-IS. No data migration.
- New writes (post-1.5c) use bill_* action strings (and pay_app_* for pay-apps).
- UI components reading audit_log filter on action prefix; updated filter accepts both: action LIKE 'invoice_%' OR action LIKE 'bill_%' (rendered as the same surface — "Bill" UI label).
- Component-level translation table: a small helper module (src/lib/audit/action-labels.ts) maps both legacy and new action strings to user-facing labels.
- F1 may consolidate via migration when the knowledge graph schema lands; until then, dual-vocabulary is acceptable.

**Strategy 2 — Migrate everything atomically.**
- Write a Supabase migration that updates every historical audit_log row to use new action strings.
- All callers immediately use new action strings.
- Single vocabulary post-1.5c.
- Risk: migration touches every audit_log row; performance + audit-trail-mutation concerns.

**Strategy 3 — Translate at write time.**
- Application code accepts bill_* from new callers but writes invoice_* to the database (legacy vocabulary preserved at storage layer).
- UI reads database vocabulary; new code never sees invoice_*.
- Risk: long-term debt — F1 / F6 still has to migrate eventually + new developers see double vocabulary in code.

**Recommendation: Strategy 1.** Per CONTEXT R2 standing decision direction. Document rationale + complete plan in AUDIT-LOG-STRATEGY.md.

**Step C — Create AUDIT-LOG-STRATEGY.md.**

Document:
- The 3 strategies considered + the chosen one (Strategy 1 unless architect surfaces compelling reason for 2 or 3)
- Where new code writes bill_* / pay_app_* (list the API routes — extraction source = grep results from Step A)
- The action-labels helper module shape (for Plan 3 to wire up; this is NOT 1.5c implementation work — it is a future-phase deliverable that 1.5c plans + documents but does not ship code for unless trivially small)
- F1 / F6 migration path (when the knowledge graph schema redesigns audit logging, the dual-vocabulary collapses to single)
- The CLAUDE.md note to add in Plan 7's documentation update: "Bills = vendor invoices. Audit-log action strings post-1.5c = bill_*; historical = invoice_*; UI surfaces both as Bill uniformly via src/lib/audit/action-labels.ts."

If the existing codebase has zero audit_log write sites for invoice / draw events, document that finding — Strategy 1 still applies but the immediate impact is even lower (no new code in 1.5c writes audit_log; just establishing the convention for F1+).

**Step D — Verify T10c hook posture for production-route fixture imports.**

Read .claude/hooks/nightwork-post-edit.sh lines 194-230 carefully. Identify:
- The exact regex T10c uses
- Whether it blocks edits OR rejects commits OR both
- Which file paths trigger T10c (only files inside src/app/design-system/*? or wider?)

Test current semantics in a sandbox (do NOT commit):
1. Create a temp file at src/app/financials/_test/page.tsx (PRODUCTION path) that imports CALDWELL_INVOICES from @/app/design-system/_fixtures/drummond.
2. Run the post-edit hook manually on the temp file path.
3. Observe: does the hook reject the edit? Or print a warning? Or allow silently?
4. Delete the temp file.

Based on observed behavior, decide:
- If T10c is silent for production-route fixture imports → confirmed safe per Option A. NO hook change needed.
- If T10c rejects production-route fixture imports → decide between (a) extending the hook to recognize the legitimate temporary 1.5c boundary, or (b) routing fixture imports through a sanctioned wrapper module. Surface the choice to Jake at Plan 2 halt.

**Step E — Document hook posture finding in AUDIT-LOG-STRATEGY.md or a sibling section.**

Either:
- "T10c is silent for production-route fixture imports per current semantics. Plan 3 implementers can proceed without hook adjustments." (most likely outcome — T10c regex is src/app/design-system/*-scoped)
- "T10c blocks production-route fixture imports. Hook adjustment required: [describe the exact hook diff]." (less likely; if so, halt-block until Jake approves the hook change)

If a hook change IS required, edit .claude/hooks/nightwork-post-edit.sh to permit the boundary. Make the smallest change possible. Add a comment in the hook citing CONTEXT D-03 + this plan's filename.
  </action>

  <verify>
    <automated>test -f .planning/phases/stage-1.5c-information-architecture/AUDIT-LOG-STRATEGY.md && grep -c 'Strategy 1' .planning/phases/stage-1.5c-information-architecture/AUDIT-LOG-STRATEGY.md && grep -c 'T10c' .planning/phases/stage-1.5c-information-architecture/AUDIT-LOG-STRATEGY.md</automated>
    Expected: file exists; grep returns >=1 for both terms.

    Hook posture observation: write the exact hook behavior on production-route fixture imports as observed in Step D (silent / warning / rejection) into AUDIT-LOG-STRATEGY.md. Plan 3 implementers MUST read this finding.

    If hook was adjusted: `git diff .claude/hooks/nightwork-post-edit.sh` shows the diff; the diff is minimal (preferably just a regex update or a path-exclusion); contains a comment citing CONTEXT D-03.
  </verify>

  <done>
    - AUDIT-LOG-STRATEGY.md created with Strategy 1 documented (or alternative + rationale)
    - Codebase audit complete (where audit_log is written, what action strings exist)
    - T10c hook posture verified — either confirmed safe-as-is OR adjusted with documented diff
    - Plan 3 implementers have unambiguous guidance on (a) action-string convention to use in any new code that writes audit_log + (b) whether production-route fixture imports are safe
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor InvoiceReviewView to props-only + apply TD-20/25/26 fixes + rewrite prototype page as thin wrapper</name>
  <files>src/components/prototypes/InvoiceReviewView.tsx, src/app/design-system/prototypes/invoices/[id]/page.tsx</files>

  <read_first>
    - src/app/design-system/prototypes/invoices/[id]/page.tsx (FULL file — extraction source; ~250 lines per 1.5b plan)
    - .planning/phases/stage-1.5b-prototype-gallery/01.5-2-invoice-draw-vendors-PLAN.md Task 1 code skeleton (lines 200-450)
    - src/app/design-system/_fixtures/drummond/types.ts (CaldwellInvoice + sub-types)
    - src/components/nw/Button.tsx (NwButton API for TD-20 fix)
    - src/components/nw/Card.tsx (Card API; check padding="lg" usages for TD-25 fix)
    - src/components/nw/Eyebrow.tsx + Money.tsx + DataRow.tsx + Badge.tsx (other primitives)
    - .planning/design/COMPONENTS.md (NwButton + Card + Eyebrow contracts; verify TD-20/25/26 fix patterns)
    - .planning/design/SYSTEM.md (§13 Forbidden — Card padding=lg + fontWeight: 600; §11 touch targets — 44px standard, 56px high-stakes)
    - .planning/phases/stage-1.5b-prototype-gallery/findings.md (TD-20 + TD-25 + TD-26 findings — exact line numbers in source files)
    - AUDIT-LOG-STRATEGY.md (just-created from Task 1 — reference the T10c hook posture finding)
  </read_first>

  <action>
**Step A — Read the source file in full.**

Open src/app/design-system/prototypes/invoices/[id]/page.tsx. Inventory:
- All fixture imports at top (CALDWELL_INVOICES, CALDWELL_VENDORS, CALDWELL_JOBS, CALDWELL_COST_CODES)
- All type imports (CaldwellInvoice, CaldwellInvoiceStatus, CaldwellInvoiceConfidenceDetails)
- All UI primitive imports (Card, Eyebrow, Money, DataRow, Badge)
- The internal lookup (CALDWELL_INVOICES.find((i) => i.id === params.id) and chained vendor / job / costCode lookups)
- All raw <button> elements (count for TD-20 fix scope)
- All Card padding=lg usages (count for TD-25 fix scope)
- All fontWeight: 600 inline styles (count for TD-26 fix scope)
- Any hardcoded hex (privacy-gate concern; expect 0 per existing T10c-clean code — verify)

**Step B — Create src/components/prototypes/InvoiceReviewView.tsx.**

The component is a pure props-only render of the existing JSX. Critical changes from the source:
1. Move all fixture-lookup logic OUT (to the wrapper in Step C). The view component receives `invoice`, `vendor`, `job`, `costCode` directly as props.
2. Type-only imports for Caldwell types (no runtime fixture imports).
3. Replace raw <button> elements with NwButton (TD-20 fix).
4. Replace Card padding=lg with padding=md (TD-25 fix).
5. Replace fontWeight: 600 with fontWeight: 500 (TD-26 fix).
6. Preserve everything else verbatim.

```typescript
// src/components/prototypes/InvoiceReviewView.tsx
//
// Tenant-blind extracted view. Accepts ALL data via props (no fixture imports).
// Both /financials/bills/[id] (production) and /design-system/prototypes/invoices/[id]
// (portfolio) mount this with the same data shape.
//
// Per CONTEXT D-03 — refactored from src/app/design-system/prototypes/invoices/[id]/page.tsx
// for T10c-clean separation.
//
// Per CONTEXT D-06 — applies TD-20 (NwButton bypass), TD-25 (Card padding="lg"
// forbidden), TD-26 (fontWeight: 600 violation) structural fixes.
//
// Per .planning/phases/stage-1.5c-information-architecture/AUDIT-LOG-STRATEGY.md —
// audit_log action strings post-1.5c use bill_* / pay_app_* per Strategy 1.
// Currently this view does NOT write audit_log (read-only render); the strategy
// applies to future writes.

"use client";

import Link from "next/link";
import {
  PaperClipIcon,
  CheckBadgeIcon,
  XMarkIcon,
  ExclamationTriangleIcon,
} from "@heroicons/react/24/outline";

import type {
  CaldwellInvoice,
  CaldwellInvoiceConfidenceDetails,
  CaldwellInvoiceStatus,
} from "@/app/design-system/_fixtures/drummond/types";

import Card from "@/components/nw/Card";
import Eyebrow from "@/components/nw/Eyebrow";
import Money from "@/components/nw/Money";
import DataRow from "@/components/nw/DataRow";
import Badge from "@/components/nw/Badge";
import NwButton from "@/components/nw/Button";  // TD-20 fix

export interface InvoiceReviewViewProps {
  invoice: CaldwellInvoice;
  vendor: { id: string; name: string };
  job: { id: string; name: string };
  costCode: { id: string; code: string; description: string } | null;
  // Optional breadcrumb override for production-route shorter labels:
  breadcrumbRoot?: { href: string; label: string };  // default: { href: "/design-system/prototypes/", label: "Prototypes" }
}

const STATUS_BADGE: Record<CaldwellInvoiceStatus, { variant: "neutral" | "accent" | "success" | "warn" | "danger" | "info"; label: string }> = {
  // (verbatim from source — preserved for visual parity per D-07)
  received: { variant: "neutral", label: "RECEIVED" },
  ai_processed: { variant: "warn", label: "AI PROCESSED" },
  pm_review: { variant: "warn", label: "PM REVIEW" },
  pm_approved: { variant: "success", label: "PM APPROVED" },
  pm_held: { variant: "warn", label: "PM HELD" },
  pm_denied: { variant: "danger", label: "PM DENIED" },
  qa_review: { variant: "info", label: "QA REVIEW" },
  qa_approved: { variant: "success", label: "QA APPROVED" },
  qa_kicked_back: { variant: "danger", label: "QA KICKED BACK" },
  pushed_to_qb: { variant: "success", label: "PUSHED TO QB" },
  in_draw: { variant: "info", label: "IN DRAW" },
  paid: { variant: "success", label: "PAID" },
};

function confidenceTone(score: number): "success" | "warn" | "danger" {
  if (score >= 0.85) return "success";
  if (score >= 0.7) return "warn";
  return "danger";
}

function buildTimeline(inv: CaldwellInvoice) {
  // (verbatim from source — preserves the faked client-side timeline pattern)
  // ...
}

function ConfidenceGrid({ details }: { details: CaldwellInvoiceConfidenceDetails }) {
  // (verbatim from source)
  // ...
}

export default function InvoiceReviewView({
  invoice,
  vendor,
  job,
  costCode,
  breadcrumbRoot = { href: "/design-system/prototypes/", label: "Prototypes" },
}: InvoiceReviewViewProps) {
  const status = STATUS_BADGE[invoice.status];
  const timeline = buildTimeline(invoice);

  return (
    <div className="px-6 py-8 max-w-[1600px] mx-auto">
      {/* Header band — breadcrumb + invoice meta + status + actions */}
      <div className="mb-6 flex items-start justify-between flex-wrap gap-4">
        <div>
          <div className="flex items-center gap-2 text-[12px] mb-2" style={{ color: "var(--text-tertiary)" }}>
            <Link href={breadcrumbRoot.href} className="hover:underline">{breadcrumbRoot.label}</Link>
            <span>/</span>
            <Link href="/financials/bills" className="hover:underline">Bills</Link>
            <span>/</span>
            <span style={{ fontFamily: "var(--font-jetbrains-mono)" }}>{invoice.invoice_number ?? invoice.id}</span>
          </div>
          <h1
            className="text-[24px] mb-1"
            style={{
              fontFamily: "var(--font-space-grotesk)",
              fontWeight: 500,  // TD-26 fix: was 600 in source
              color: "var(--text-primary)",
            }}
          >
            {vendor.name}
          </h1>
          <div className="flex items-center gap-3">
            <Money cents={invoice.total_amount} size="lg" variant="emphasized" />
            <Badge variant={status.variant}>{status.label}</Badge>
            <span className="text-[11px]" style={{ fontFamily: "var(--font-jetbrains-mono)", color: "var(--text-tertiary)" }}>
              {invoice.invoice_type.replaceAll("_", " ").toUpperCase()}
            </span>
          </div>
        </div>
        <div className="flex items-center gap-2">
          {/* TD-20 fix: NwButton instead of raw <button> */}
          <NwButton variant="secondary" size="md" icon={<XMarkIcon className="w-4 h-4" strokeWidth={1.5} />}>
            Reject
          </NwButton>
          <NwButton variant="primary" size="md" icon={<CheckBadgeIcon className="w-4 h-4" strokeWidth={1.5} />}>
            Push to QB
          </NwButton>
        </div>
      </div>

      {/* Hero grid 50/50 — file preview LEFT + right-rail RIGHT (verbatim from source per D-07 visual parity) */}
      {/* (rest of body preserved verbatim from source — except Card padding=lg → md per TD-25 fix) */}
      {/* ... */}
    </div>
  );
}
```

**IMPORTANT — verify NwButton actual API at execute time.** Read src/components/nw/Button.tsx fully. The example above uses an `icon` prop assumption — if the actual NwButton API uses `<NwButton><Icon />text</NwButton>` instead, adjust accordingly. Per .planning/design/COMPONENTS.md the actual contract is the source of truth.

**Step C — Rewrite src/app/design-system/prototypes/invoices/[id]/page.tsx as a thin wrapper.**

```typescript
// src/app/design-system/prototypes/invoices/[id]/page.tsx
//
// Thin wrapper: fixture lookup + InvoiceReviewView mount. Per CONTEXT D-03.
//
// The View component is at src/components/prototypes/InvoiceReviewView.tsx
// and accepts data via props (T10c-clean separation). This wrapper handles
// the fixture-import + per-id lookup; the View handles rendering only.
//
// Production /financials/bills/[id] mounts the same View with the same fixture
// lookup (1.5c temporary stand-in; F1 swaps in real Supabase queries).

import { notFound } from "next/navigation";
import {
  CALDWELL_INVOICES,
  CALDWELL_VENDORS,
  CALDWELL_JOBS,
  CALDWELL_COST_CODES,
} from "@/app/design-system/_fixtures/drummond";
import InvoiceReviewView from "@/components/prototypes/InvoiceReviewView";

export default function InvoicePrototypePage({ params }: { params: { id: string } }) {
  const inv = CALDWELL_INVOICES.find((i) => i.id === params.id);
  if (!inv) return notFound();

  const vendor = CALDWELL_VENDORS.find((v) => v.id === inv.vendor_id) ?? null;
  const job = CALDWELL_JOBS.find((j) => j.id === inv.job_id) ?? null;
  const costCode = inv.cost_code_id
    ? (CALDWELL_COST_CODES.find((c) => c.id === inv.cost_code_id) ?? null)
    : null;

  if (!vendor || !job) return notFound();

  return (
    <InvoiceReviewView
      invoice={inv}
      vendor={vendor}
      job={job}
      costCode={costCode}
      breadcrumbRoot={{ href: "/design-system/prototypes/", label: "Prototypes" }}
    />
  );
}
```

**Step D — Side-by-side visual verification (D-07 precedent).**

Run `npm run dev`. Visit:
- /design-system/prototypes/invoices/inv-caldwell-001 (the refactored portfolio path)
- (No production /financials/bills/inv-caldwell-001 yet — Plan 3 ships that.)

Compare against pre-refactor screenshots if available (or visual memory of the 1.5b prototype). The refactored portfolio path MUST render identically except for:
- Reject + Push to QB buttons now use NwButton styling (subtle TD-20 fix — visual difference is minimal but check button shape, hover state, focus ring)
- Card padding may differ slightly if any padding=lg was changed to padding=md (TD-25 fix — visual difference is minimal; the card content fits with same proportions)
- Headline weight may differ slightly if fontWeight: 600 was changed to 500 (TD-26 fix — Space Grotesk Medium reads slightly thinner)

Document ALL observed differences in this Plan's SUMMARY. Per D-07: divergence = CRITICAL. If divergence is unexpected (e.g., layout shift, different colors, broken icons), HALT and investigate before proceeding to Plan 3.
  </action>

  <verify>
    <automated>npm run build && grep -c 'InvoiceReviewView' src/components/prototypes/InvoiceReviewView.tsx && grep -c 'InvoiceReviewView' src/app/design-system/prototypes/invoices/[id]/page.tsx && grep -c 'NwButton' src/components/prototypes/InvoiceReviewView.tsx && grep -c 'fontWeight: 500' src/components/prototypes/InvoiceReviewView.tsx</automated>
    Expected: build exits 0; greps return >=1 each.

    Fixture-import check: `grep -E "from \"@/app/design-system/_fixtures/drummond\"" src/components/prototypes/InvoiceReviewView.tsx` returns 0 matches (View imports types only via `import type`, NOT runtime).

    Type-only import check: `grep -E "^import type" src/components/prototypes/InvoiceReviewView.tsx` returns >=1 (CaldwellInvoice + types).

    Wrapper fixture-import check: `grep -E "from \"@/app/design-system/_fixtures/drummond\"" src/app/design-system/prototypes/invoices/[id]/page.tsx` returns 1 (the wrapper imports CALDWELL_INVOICES + CALDWELL_VENDORS + CALDWELL_JOBS + CALDWELL_COST_CODES).

    TD-20 fix check: `grep -cE '<button[^>]*type="button"' src/components/prototypes/InvoiceReviewView.tsx` returns 0 (NO raw <button> for action elements; NwButton replaces).

    TD-25 fix check: `grep -cE 'padding="lg"' src/components/prototypes/InvoiceReviewView.tsx` returns 0.

    TD-26 fix check: `grep -cE 'fontWeight:\s*600' src/components/prototypes/InvoiceReviewView.tsx` returns 0.

    Hex check: `grep -nE '#[0-9a-fA-F]{3,6}' src/components/prototypes/InvoiceReviewView.tsx src/app/design-system/prototypes/invoices/[id]/page.tsx | grep -v '^#' | grep -v 'var(--' | wc -l` returns 0.

    Privacy gate: `grep -rE 'Drummond|501 74th|Holmes Beach|...' src/components/prototypes/InvoiceReviewView.tsx src/app/design-system/prototypes/invoices/[id]/page.tsx | wc -l` returns 0 (full denylist per Plan 1).

    Manual visual check (executor uses Chrome DevTools MCP):
    - Visit `http://localhost:3000/design-system/prototypes/invoices/inv-caldwell-001` (or first invoice ID — whatever it is in CALDWELL_INVOICES)
    - Renders without errors. Document Review pattern visible: hero grid 50/50 (file preview LEFT + right-rail RIGHT) + audit timeline below.
    - All 4 invoice statuses cycle (paid, pm_review, qa_review, ai_processed) — visit different invoice IDs.
    - Reject + Push to QB buttons: visible, hover state working, focus ring on tab.
    - Headline weight visually consistent with Site Office (Space Grotesk Medium, NOT bold).
    - No console errors.
  </verify>

  <done>
    - InvoiceReviewView component exists and is purely props-only
    - InvoiceReviewView imports types only (no runtime fixture imports)
    - TD-20 / TD-25 / TD-26 fixes applied to InvoiceReviewView
    - /design-system/prototypes/invoices/[id]/page.tsx is a thin wrapper passing fixtures via props
    - Side-by-side visual verification completed; differences documented (or "no observable differences" stated explicitly)
    - npm run build passes
    - No hardcoded hex
    - Privacy gate clean
    - Hooks silent
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture data → View component | Pure read-only props; no mutation surface |
| Wrapper → View component | Wrapper imports fixtures; View imports nothing at runtime; clear separation per CONTEXT D-03 |
| AUDIT-LOG-STRATEGY.md → future writes | Documentation-only artifact; no runtime threat surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.5c-2-01 | I (Information disclosure) | InvoiceReviewView re-rendering Caldwell data | accept | All data is already-sanitized Caldwell fixture data per 1.5b SUBSTITUTION-MAP.md. No real Drummond identifiers in props. The View has zero direct access to any auth or user data. |
| T-1.5c-2-02 | T (Tampering) | T10c hook semantics | mitigate | If hook adjusted, the diff is minimal + commented + tied to CONTEXT D-03. Original hook semantics for /design-system/* sample-data isolation preserved. |
| T-1.5c-2-03 | E (Elevation of privilege) | NwButton replacing raw <button> for QB push action | accept | NwButton is a tenant-blind primitive (no org_id / membership props). The "Push to QB" action is currently no-op in the prototype (no API call) — F1+ wires it to a real endpoint with proper authorization. Plan 2 only changes the rendering primitive, not the auth surface. |
| T-1.5c-2-04 | R (Repudiation) | Audit-log action string strategy | mitigate | Strategy 1 preserves all historical audit-log rows verbatim (no migration). New writes use bill_* vocabulary. Both old and new are queryable for forensic analysis. F1 / F6 may consolidate via migration; preserved historical action strings are never deleted. |
| T-1.5c-2-05 | S (Spoofing) | Wrapper imports of Caldwell fixtures | accept | Wrapper is a server-rendered component path; fixture data is immutable static const arrays. No spoofing surface. F1 swaps to authenticated Supabase queries with RLS — Plan 2 architecture supports this swap mechanically. |
</threat_model>

<verification>
After Task 1 + Task 2 complete:

1. AUDIT-LOG-STRATEGY.md exists with documented strategy + T10c hook posture finding.
2. T10c hook semantics verified — either silent for production-route fixture imports (Option A; recommended) or adjusted with documented rationale.
3. InvoiceReviewView is purely props-only; no runtime fixture imports.
4. Refactored portfolio prototype page renders identically to pre-refactor (D-07 visual parity).
5. TD-20 / TD-25 / TD-26 structural fixes applied.
6. npm run build passes.
7. Hooks silent.
8. Privacy gate clean.

**Halt for Jake review:** halt_after: true. Jake reviews:
- AUDIT-LOG-STRATEGY.md (Strategy 1 confirmed; T10c hook posture decision approved)
- InvoiceReviewView component shape (does it look right as the exemplar Plan 3 follows?)
- Refactored portfolio prototype side-by-side visual (does it match the pre-refactor render?)

If Jake approves → Plan 3 spawns. If Jake wants changes (e.g., Strategy 2 instead of 1, hook adjustment differs from architect's call) → revise this plan + re-halt.
</verification>

<success_criteria>
- AUDIT-LOG-STRATEGY.md captures the action-string strategy + T10c hook posture finding
- InvoiceReviewView exists as canonical exemplar (props-only, TD-20/25/26 fixes applied)
- Refactored portfolio prototype renders identically to pre-refactor (D-07 visual parity caught early)
- All 9 must_haves truths verified
- Build + hooks + privacy gate clean
- Halt review with Jake passes
</success_criteria>

## Acceptance criteria

- [ ] AC-1.5c-2-01: InvoiceReviewView component file exists at src/components/prototypes/InvoiceReviewView.tsx
- [ ] AC-1.5c-2-02: InvoiceReviewView imports CaldwellInvoice + sub-types via `import type` only (no runtime fixture imports)
- [ ] AC-1.5c-2-03: InvoiceReviewView replaces all raw <button> action elements with NwButton (TD-20 fix)
- [ ] AC-1.5c-2-04: InvoiceReviewView uses Card padding="md" (NOT "lg") for any cards that originally used "lg" (TD-25 fix)
- [ ] AC-1.5c-2-05: InvoiceReviewView uses fontWeight: 500 (NOT 600) for all headlines (TD-26 fix)
- [ ] AC-1.5c-2-06: /design-system/prototypes/invoices/[id]/page.tsx is a thin wrapper that does fixture lookup + mounts InvoiceReviewView with props
- [ ] AC-1.5c-2-07: AUDIT-LOG-STRATEGY.md exists at .planning/phases/stage-1.5c-information-architecture/AUDIT-LOG-STRATEGY.md with documented Strategy 1 (or chosen alternative + rationale)
- [ ] AC-1.5c-2-08: T10c hook posture verified — either confirmed silent for production-route fixture imports OR hook adjusted with minimal documented diff
- [ ] AC-1.5c-2-09: Side-by-side visual verification of refactored portfolio path completed; differences documented or "no observable differences" stated

## Nyquist coverage

| Acceptance criterion | Verified by |
|---|---|
| AC-1.5c-2-01 | Task 2 verify (file exists test) |
| AC-1.5c-2-02 | Task 2 verify (grep "import type" + grep absence of runtime fixture imports) |
| AC-1.5c-2-03 | Task 2 verify (grep "<button[^>]*type=" returns 0; grep "NwButton" returns >=1) |
| AC-1.5c-2-04 | Task 2 verify (grep "padding=\"lg\"" returns 0 in InvoiceReviewView.tsx) |
| AC-1.5c-2-05 | Task 2 verify (grep "fontWeight: 600" returns 0; grep "fontWeight: 500" returns >=1) |
| AC-1.5c-2-06 | Task 2 verify (grep "InvoiceReviewView" in /design-system/prototypes/invoices/[id]/page.tsx returns >=1) |
| AC-1.5c-2-07 | Task 1 verify (test -f AUDIT-LOG-STRATEGY.md && grep "Strategy 1") |
| AC-1.5c-2-08 | Task 1 verify (manual hook test in a sandbox temp file; finding documented in AUDIT-LOG-STRATEGY.md) |
| AC-1.5c-2-09 | Task 2 verify (manual Chrome DevTools visual check; differences listed in plan SUMMARY) |

## Decisions inherited (from CONTEXT.md)

- D-03 (= MASTER-PLAN D-053): Thin-wrapper extraction — refactor 11 prototype components for prop-drilled fixture data, T10c-clean separation. Plan 2 is the architect; Plan 3 is the implementers.
- D-06 (= MASTER-PLAN D-056): Per nwrp44 Amendment 1 — fix only structural TDs in 1.5c during D-053 thin-wrapper extraction: TD-20 (NwButton bypass), TD-25 (Card padding="lg"), TD-26 (fontWeight: 600).
- D-07: Side-by-side visual verification after thin-wrapper extraction — divergence = CRITICAL halt criterion. Plan 2 verifies on the architect's exemplar (Invoice Review); Plan 3 verifies all 11.
- R2 (EXPANDED-SCOPE §8): Audit-log action-string drift — historical action strings preserved as-is; new writes use new strings; UI labels read both as same surface. Documented in AUDIT-LOG-STRATEGY.md.

## Halt conditions

- Halt at end of plan (halt_after: true) for Jake review of AUDIT-LOG-STRATEGY.md + InvoiceReviewView exemplar + side-by-side visual verification
- Halt during execution if:
  - T10c hook actually rejects production-route fixture imports — surface to Jake before adjusting the hook
  - InvoiceReviewView refactor surfaces an architectural blocker (e.g., the existing component is so tightly coupled with `params.id` that the prop-drill split requires re-architecting the parent layout)
  - Side-by-side visual verification shows ANY unexpected divergence (e.g., layout shift, color drift, broken icons, missing audit timeline) — the entire D-07 mitigation rests on catching divergence early
  - NwButton primitive's actual API conflicts with the assumed shape in `<interfaces>` block — surface and reconcile before proceeding
  - Hooks reject any modified file (post-edit Forbidden tokens, tenant-blind, bouncy-easing)

## Output

After completion, create `.planning/phases/stage-1.5c-information-architecture/01.5c-2-thin-wrapper-architect-SUMMARY.md` per `.claude/get-shit-done/templates/summary.md`. Document:
- Files created / modified
- Acceptance criteria status (9 items checked)
- AUDIT-LOG-STRATEGY.md decisions (Strategy chosen + rationale + T10c hook posture)
- Hook silence + build / typecheck / privacy gate pass
- Side-by-side visual verification finding (any differences observed; D-07 status)
- Plan 3 implementer guidance (refer to InvoiceReviewView as the exemplar; refer to AUDIT-LOG-STRATEGY.md for action-string convention)
- Halt-review checklist for Jake
