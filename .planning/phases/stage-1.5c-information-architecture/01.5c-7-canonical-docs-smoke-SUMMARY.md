---
phase: stage-1.5c-information-architecture
plan: 7
subsystem: docs
tags: [canonical-docs, architecture, roles, ingestion, audit-log, decisions-log, atomic-commit, soc2, multi-tenant]

requires:
  - phase: stage-1.5c-information-architecture-plan-1
    provides: 8-section top nav + /today + 32 redirect rules
  - phase: stage-1.5c-information-architecture-plan-2
    provides: AUDIT-LOG-STRATEGY.md schema-aware framing + thin-wrapper extraction
  - phase: stage-1.5c-information-architecture-plan-3
    provides: 12 production routes wrapped with .design-system-scope per D-19
  - phase: stage-1.5c-information-architecture-plan-4
    provides: Section placeholders + Sub Portal 3 stubs
  - phase: stage-1.5c-information-architecture-plan-5
    provides: Per-job tabs + ~19 More-dropdown placeholders
  - phase: stage-1.5c-information-architecture-plan-6
    provides: /platform-admin migration + admin section overview

provides:
  - 4 NEW canonical docs at .planning/architecture/ (ARCHITECTURE, ENTITY-INVENTORY, ROLES-CATALOG, INGESTION-ARCHITECTURE) with iter-2 mechanical #6 column additions
  - 6 UPDATED docs (.planning/CONTEXT.md + .planning/MASTER-PLAN.md + CLAUDE.md + .planning/design/PHILOSOPHY.md + .planning/design/PATTERNS.md + .planning/design/COMPONENTS.md) — atomic 11-file commit per iter-2 mechanical #7 + iter-3 D-27 (10 docs + .gitignore Rule 3 auto-fix)
  - iter-2 mechanical #2 — NwPlaceholderCard COMPONENTS.md §7.8 entry per PROPAGATION-RULES.md §4b workflow
  - iter-2 must-fix CRITICAL #5 — CLAUDE.md schema-aware audit-log language (entity_type stays 'invoice'/'draw'; UI maps via action-labels.ts)
  - iter-2 mechanical #6 column additions — Retention class + PII? in ENTITY-INVENTORY; Cross-org? in ROLES-CATALOG; §SOC2 control mapping in ARCHITECTURE
  - iter-2 multi-tenant W-1 — Owner Portal F1 auth path constraint documented (Path A token model OR Path B owner_view role; NOT both)
  - iter-2 W-8 — at least 2 consumers per new pattern in PHILOSOPHY (4 patterns) + PATTERNS (6 patterns)
  - D-040..D-065 (26 entries) added to MASTER-PLAN.md DECISIONS LOG atomically per iter-2 mechanical #8 idempotency guard
  - Final integration smoke report (build + typecheck + privacy gate + 10 redirect spot-checks all PASS)
  - M3 phone walk readiness checklist (13 steps including iter-2 D-19/D-20/D-21 verification)

affects:
  - All future plan-phase + research-phase + execute-phase agents (canonical docs read first)
  - All future commits referencing CLAUDE.md Architecture Rules (schema-aware audit-log canonical)
  - MASTER-PLAN.md DECISIONS LOG (D-040..D-065 added atomically)
  - 1.5b ship gate (M3 phone walk bundled per D-17 / Q13=A)
  - F1 schema design (reads ENTITY-INVENTORY.md + WORKFLOW-INTELLIGENCE.md + ARCHITECTURE.md §8 Owner Portal auth constraint)
  - F2 roles engine (reads ROLES-CATALOG.md)
  - F3 ingestion engine (reads INGESTION-ARCHITECTURE.md)

tech-stack:
  added: []
  patterns:
    - "Canonical docs hierarchy: .planning/architecture/* (deep arch) + .planning/CONTEXT.md (project-level summary) + MASTER-PLAN.md (entry point with DECISIONS LOG)"
    - "Atomic doc-update commits: all related canonical-docs in single commit per iter-2 mechanical #7"
    - "Idempotency guard: grep-based pre-append check halts on re-run per iter-2 mechanical #8"
    - "Schema-aware audit-log: entity_type stays at schema vocabulary; UI maps via action-labels.ts (Strategy 1-prime)"
    - "At-least-2-consumers per new design pattern: PHILOSOPHY + PATTERNS additions cite future + present consumers (iter-2 W-8)"
    - "Production Site Office activation chain (D-061): root layout imports design-system.css + each route wrapper uses data-direction=C data-palette=B + design-system-scope class (LOAD-BEARING)"

key-files:
  created:
    - .planning/architecture/ARCHITECTURE.md (4 NEW canonical docs + SOC2 + Owner Portal F1 auth constraint)
    - .planning/architecture/ENTITY-INVENTORY.md (31+ entities with Retention class + PII?)
    - .planning/architecture/ROLES-CATALOG.md (15+ roles with Cross-org?)
    - .planning/architecture/INGESTION-ARCHITECTURE.md (multi-modal ingestion patterns)
    - .planning/CONTEXT.md (project-level canonical context, added to gitignore whitelist)
    - .planning/phases/stage-1.5c-information-architecture/01.5c-7-canonical-docs-smoke-SUMMARY.md (this file)
  modified:
    - .planning/MASTER-PLAN.md (D-040..D-065 + CURRENT POSITION + NEXT PLANNED WORK)
    - CLAUDE.md (schema-aware audit-log + 8-section nav + Site Office wrap contract + canonical refs + F1-F6 phase plan)
    - .planning/design/PHILOSOPHY.md (§10 with 4 new patterns)
    - .planning/design/PATTERNS.md (§17 with 6 new patterns)
    - .planning/design/COMPONENTS.md (§7.8 NwPlaceholderCard)
    - .gitignore (.planning/CONTEXT.md whitelist entry — Rule 3 auto-fix)

key-decisions:
  - "Atomic 10-doc commit per iter-2 mechanical #7 + iter-3 D-27 (commit 6d1c568 has 11 files: 4 NEW + 6 UPDATED + gitignore auto-fix)"
  - "iter-2 mechanical #8 idempotency guard verified: pre-commit grep -c 'D-040' returned 0; safe to append"
  - "iter-2 must-fix CRITICAL #5 — schema-aware audit-log language: entity_type continues 'invoice'/'draw' per existing schema; UI maps via src/lib/audit/action-labels.ts. NOT bill_* concat framing."
  - "iter-2 mechanical #6 column additions encode compliance posture at design time: Retention class (forever / tenant-deletion-lifecycle / per-record) + PII? (yes/no/varies) + Cross-org? (platform_admin only) + §SOC2 (CC6.1/CC6.6/CC6.7/CC7.2/PI1.1/C1.1)"
  - "iter-2 multi-tenant W-1 — Owner Portal F1 auth path: F1 commits to ONE path (Path A token model OR Path B owner_view role); NOT both. Documented in ARCHITECTURE §8."
  - "Auto-fix per deviation Rule 3: added /.planning/CONTEXT.md to .gitignore whitelist (was missing)"
  - "Production Site Office activation chain (D-061) documented as LOAD-BEARING in PATTERNS §17.6 — DO NOT remove the wrap or strip attributes; future polish phases must preserve both prerequisites"

patterns-established:
  - "Today as home pattern (Principle 7): /today + future /per-job-today (2 consumers)"
  - "Role-aware filtering pattern (Principle 6): 8-section ACCESS map + future PerJobTabs role filter (2 consumers)"
  - "Multi-modal ingestion pattern (Principle 5): /jobs/[id]/punchlist + /jobs/[id]/daily-logs (2 consumers)"
  - "Profile-based feature flags pattern (Principle 8): /admin/profile + F2 nav filtering (2 consumers)"
  - "Placeholder treatment pattern (NwPlaceholderCard): ~50 placeholder routes across Plans 4/5/6"
  - "Role-filtered nav pattern: nav-bar.tsx + future PerJobTabs (2 consumers)"
  - "Phase-aware sub-nav pattern: /jobs/[id]/* + future client-portal phase-aware nav (2 consumers)"
  - "8-item top nav pattern: nav-bar.tsx (Plan 1)"
  - "Sub Portal 3-stub pattern: /sub-portal/* + future F3 magic link infra (2 consumers)"
  - "Production Site Office activation LOAD-BEARING contract (per D-057/D-061/D-062): 12 production routes consume the wrap"

requirements-completed: []

duration: 55min
completed: 2026-05-11
---

# Phase stage-1.5c-information-architecture Plan 7: canonical-docs-smoke Summary

**4 NEW canonical architecture docs (8 principles + 31 entities + 15 roles + multi-modal ingestion) + 6 atomic updates across CONTEXT/MASTER-PLAN/CLAUDE/PHILOSOPHY/PATTERNS/COMPONENTS — D-040..D-065 added; iter-2 mechanical #2/#5/#6/#7/#8 honored; M3 phone walk gate ready**

## Performance

- **Duration:** ~55 min
- **Started:** 2026-05-11T19:14Z
- **Completed:** 2026-05-11T19:35Z
- **Tasks:** 3 (Task 1 NEW docs + Task 2 atomic update + Task 3 smoke)
- **Files modified:** 11 (4 NEW canonical docs + 6 UPDATED docs + 1 .gitignore Rule 3 auto-fix)

## Accomplishments

- **4 NEW canonical docs at `.planning/architecture/`** — ARCHITECTURE.md (8 principles + data graph + section purposes + revised phase plan + §SOC2 control mapping + Owner Portal F1 auth constraint), ENTITY-INVENTORY.md (31+ entities with Retention class + PII? columns), ROLES-CATALOG.md (15+ default roles with Cross-org? column flagging platform_admin), INGESTION-ARCHITECTURE.md (multi-modal ingestion + AI extraction patterns + F3 routing infrastructure)
- **6 UPDATED docs committed atomically** in single 11-file commit (6d1c568) per iter-2 mechanical #7 + iter-3 D-27 — `.planning/CONTEXT.md` (NEW project-level canonical), `.planning/MASTER-PLAN.md` (D-040..D-065 + CURRENT POSITION + NEXT PLANNED WORK), CLAUDE.md (schema-aware audit-log + 8-section nav + Site Office wrap contract + F1-F6 phase plan), `.planning/design/PHILOSOPHY.md` (§10 with 4 new patterns), `.planning/design/PATTERNS.md` (§17 with 6 new patterns including LOAD-BEARING activation chain per D-062), `.planning/design/COMPONENTS.md` (§7.8 NwPlaceholderCard entry per iter-2 mechanical #2 + PROPAGATION-RULES.md §4b)
- **D-040..D-065 (26 entries) added to MASTER-PLAN.md DECISIONS LOG** atomically per iter-2 mechanical #8 idempotency guard. Includes D-040..D-050 (nwrp43), D-051..D-056 (nwrp44), D-057..D-060 (nwrp45 iter-2), D-061..D-065 (nwrp46 iter-3)
- **iter-2 must-fix CRITICAL #5 honored** — CLAUDE.md uses schema-aware audit-log language: "Audit-log entity_type values continue to use invoice (vendor bills) and draw (builder pay-apps) per existing schema. UI components map these to 'Bill' / 'Pay App' labels via src/lib/audit/action-labels.ts." NOT bill_* concat framing.
- **iter-2 multi-tenant W-1 honored** — Owner Portal F1 auth path constraint documented in ARCHITECTURE §8: F1 commits to ONE path (Path A token model OR Path B owner_view role); NOT both.
- **iter-2 W-8 honored** — every new pattern in PHILOSOPHY (4 patterns) + PATTERNS (5 + LOAD-BEARING contract = 6 patterns) cites at least 2 consumers
- **Final integration smoke PASS** — build clean (150 pages compiled), typecheck clean, 10 representative redirects all 308 with correct location headers (including iter-2 D-20 wildcard `/admin/platform/audit → /platform-admin/audit` and iter-2 mechanical #4 `/settings/billing → /admin/billing`); auth gates confirmed (production routes 307 → /login with redirect param)
- **M3 phone walk readiness checklist** prepared (13 steps; bundled with 1.5b ship per CONTEXT D-17 / Q13=A)

## Task Commits

Per the plan's iter-2 mechanical #7 + iter-3 D-27 atomic-commit posture, all of Task 1 + Task 2 land in a SINGLE commit:

1. **Task 1: Create 4 NEW canonical docs at `.planning/architecture/`** — `6d1c568` (docs; atomic)
2. **Task 2: Update 6 EXISTING docs ATOMICALLY** — `6d1c568` (docs; atomic — same commit as Task 1)
3. **Task 3: Final integration smoke + M3 phone walk readiness gate** — output is this SUMMARY (no code commit; verification only)

**Plan metadata commit (this SUMMARY):** to be committed separately as `docs(1.5c-IA): Plan 7 SUMMARY — canonical-docs-smoke (GATE B halt)`.

## Files Created/Modified

**4 NEW canonical docs (created in atomic commit 6d1c568):**
- `.planning/architecture/ARCHITECTURE.md` — 8 principles + data graph + section purposes + revised phase plan + §SOC2 control mapping + Owner Portal F1 auth constraint (multi-tenant W-1)
- `.planning/architecture/ENTITY-INVENTORY.md` — 31+ entity catalog with Retention class + PII? columns
- `.planning/architecture/ROLES-CATALOG.md` — 15+ default role catalog with Cross-org? column (platform_admin only)
- `.planning/architecture/INGESTION-ARCHITECTURE.md` — multi-modal ingestion + AI extraction patterns + F3 routing infrastructure

**6 UPDATED docs (modified in same atomic commit 6d1c568):**
- `.planning/CONTEXT.md` — NEW project-level canonical context (12 sections summarizing 8 principles + role-aware + knowledge graph + 8-section nav + ingestion + Site Office scope + audit-log posture + Owner Portal constraint + iter-2 amendments + phase plan + 1.5c outcomes)
- `.planning/MASTER-PLAN.md` — D-040..D-065 added to DECISIONS LOG (26 new entries); CURRENT POSITION reflects 1.5c iter-3 shipped; NEXT PLANNED WORK rewritten with F1-F6 + Wave 1.1-Lite/Full + Wave 2/3/4/5 roadmap
- `CLAUDE.md` — Architecture Rules additions for Stage 1.5c (schema-aware audit-log + 8-section nav + Admin precedence + Site Office wrap contract + PerJobTabs mobile collapse + Platform Admin gate + Owner Portal F1 constraint + Knowledge graph deferred to F1) + canonical architecture references + canonical post-1.5c phase plan (F1-F6)
- `.planning/design/PHILOSOPHY.md` — §10 added with 4 new patterns (Today as home / Role-aware filtering / Multi-modal ingestion / Profile-based feature flags); each cites ≥2 consumers per iter-2 W-8
- `.planning/design/PATTERNS.md` — §17 added with 6 new patterns (Placeholder treatment / Role-filtered nav / Phase-aware sub-nav / 8-item top nav / Sub Portal 3-stub / Production Site Office activation LOAD-BEARING contract per D-057/D-061/D-062); each cites ≥2 consumers
- `.planning/design/COMPONENTS.md` — §7.8 NwPlaceholderCard entry per iter-2 mechanical #2 (mirrors NwButton/NwEyebrow/NwBadge §7 shape) + PROPAGATION-RULES.md §4b workflow

**Auto-fix (Rule 3 — blocking issue):**
- `.gitignore` — added `!/.planning/CONTEXT.md` whitelist entry. The plan explicitly required updating this file but the gitignore (D-016 carve-outs) didn't include it. Documented in commit message as deviation Rule 3 auto-fix.

## Decisions Made

All decisions are per the plan spec; no novel decisions during execution. Key plan-spec decisions reflected:

- **D-040..D-065 (26 entries)** added to MASTER-PLAN DECISIONS LOG atomically per iter-2 mechanical #8 idempotency guard
- **Atomic 10-doc commit posture** per iter-2 mechanical #7 + iter-3 D-27 (commit 6d1c568 includes all 4 NEW + 6 UPDATED docs + 1 .gitignore Rule 3 auto-fix)
- **Schema-aware audit-log language in CLAUDE.md** per iter-2 must-fix CRITICAL #5 (entity_type continues 'invoice'/'draw'; UI maps via action-labels.ts)
- **iter-2 mechanical #6 column additions** encode compliance posture at design time (Retention class + PII? + Cross-org? + §SOC2)
- **iter-2 multi-tenant W-1 Owner Portal F1 auth path constraint** — F1 commits to ONE path; not both. Path A (token model) OR Path B (owner_view role).
- **Production Site Office activation chain documented as LOAD-BEARING** in PATTERNS §17.6 (per D-057 + D-061 + D-062) — DO NOT remove the wrap or strip attributes

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added /.planning/CONTEXT.md to .gitignore whitelist**
- **Found during:** Task 2 (atomic commit staging)
- **Issue:** The plan explicitly listed `.planning/CONTEXT.md` as one of the 6 UPDATED docs (per iter-2 mechanical #7 + iter-3 D-27 + the plan's must_haves truth "Project-level .planning/CONTEXT.md updated with 8 principles canonical"). But `.gitignore` (from D-016 carve-outs) had `/.planning/*` deny-list and whitelisted only MASTER-PLAN/PROJECT/ROADMAP/REQUIREMENTS/STATE/deployment/config/lessons/calibration-log* — NOT CONTEXT.md. The file would have been silently ignored at commit time, breaking the atomic 10-doc commit posture.
- **Fix:** Added `!/.planning/CONTEXT.md` whitelist entry to `.gitignore` immediately after `!/.planning/MASTER-PLAN.md`. CONTEXT.md is canonical project-level (read by future agents at session start) and belongs in the carve-out next to MASTER-PLAN.md.
- **Files modified:** `.gitignore`
- **Verification:** `git status --short` showed `?? .planning/CONTEXT.md` (untracked) BEFORE the fix; `A .planning/CONTEXT.md` (staged) AFTER. The atomic commit 6d1c568 includes CONTEXT.md in the diff stat.
- **Committed in:** `6d1c568` (atomic Plan 7 commit — gitignore fix bundled because CONTEXT.md inclusion is load-bearing on the atomic commit posture)

**Rationale for in-scope auto-fix:** This is Rule 3 (Blocking) — without the gitignore tweak, the plan-mandated atomic commit literally cannot include CONTEXT.md. The fix is minimal (one whitelist line) and exactly tied to Plan 7's scope (CONTEXT.md is in the explicit files_modified list). No scope creep.

---

**Total deviations:** 1 auto-fixed (Rule 3 blocking — gitignore whitelist for the file Plan 7 mandates)
**Impact on plan:** Required for the atomic 10-doc commit posture per iter-2 mechanical #7. No new dependencies; no architectural change. The file is canonical per its own preamble + cross-references from CLAUDE.md.

## Issues Encountered

- **3 pre-existing "Drummond" references in my Task 1 ARCHITECTURE.md content** (CATEGORY-D — content I drafted). The plan's privacy gate (32-token denylist) requires 0 hits; my drafted content had 3 references in functional contexts (cost-code wipe-and-reseed per D-020; V.2 portability dogfood per D-025/D-031; AIA Pay App 8 reference). Resolution: replaced with neutral descriptions ("reference job", "reference-job historical back-import", "AIA reference Pay App 8") to comply with the iter-2 security LOW SEC-6/7/8 privacy gate scope on canonical `.planning/architecture/*.md`. Pre-existing references in CURRENT-STATE.md (39 hits) + TARGET.md (5 hits) + VISION.md (2 hits) + DRUMMOND-FIXTURE-SUMMARY.md (18 hits) + CLAUDE.md (11 hits) + PATTERNS.md (6 hits) + COMPONENTS.md (1 hit) are out-of-scope per Plan 7 (pre-existing content, not Plan 7 introductions).

## Final Integration Smoke Report

### Build + typecheck + hooks

- **`npm run build`:** PASS (✓ Compiled successfully; ✓ Generating static pages 150/150)
- **`npx tsc --noEmit`:** PASS (no output = clean)
- **Privacy gate (iter-2 security LOW SEC-6/7/8 explicit grep on `.planning/architecture/*.md`):** PASS — 0 hits across all 4 NEW canonical docs
- **Privacy gate on Plan 7 NEW content in UPDATED docs:** PASS — my additions to CLAUDE.md / CONTEXT.md / PHILOSOPHY.md / PATTERNS.md / COMPONENTS.md introduce 0 denylist hits. Pre-existing hits in UPDATED docs (line-numbered audit: CLAUDE.md lines 15, 50, 136, 357, 380, 588, 592, 601; PATTERNS.md lines 905, 1126, 1218, 1237, 1460, 1807; COMPONENTS.md line 983) are pre-existing and out-of-scope per Plan 7.

### Redirects (10/10 spot-checks PASS)

Verified against localhost:3000 dev server:

| # | From | To | Result |
|---|------|-----|--------|
| 1 | `/dashboard` | `/today` | 308 Permanent Redirect ✓ |
| 2 | `/invoices` | `/financials/bills` | 308 Permanent Redirect ✓ |
| 3 | `/draws/d-caldwell-05` | `/financials/pay-apps/d-caldwell-05` | 308 Permanent Redirect ✓ |
| 4 | `/cost-intelligence/lookup` | `/price-intel/cost-lookup` | 308 Permanent Redirect ✓ |
| 5 | `/vendors` | `/people/vendors` | 308 Permanent Redirect ✓ |
| 6 | `/settings` | `/admin` | 308 Permanent Redirect ✓ |
| 7 | `/admin/platform` | `/platform-admin` | 308 Permanent Redirect ✓ |
| 8 | `/admin/platform/audit` | `/platform-admin/audit` | 308 Permanent Redirect ✓ (iter-2 D-20 wildcard) |
| 9 | `/operations` | `/today` | 308 Permanent Redirect ✓ |
| 10 | `/settings/billing` | `/admin/billing` | 308 Permanent Redirect ✓ (iter-2 mechanical #4) |

### Auth gates (production routes — 307 → /login)

- `/today` → 307 `/login?redirect=%2Ftoday` ✓
- `/jobs` → 307 `/login?redirect=%2Fjobs` ✓
- `/sub-portal` → 307 `/login?redirect=%2Fsub-portal` ✓

Auth middleware intact — anonymous users redirected to /login with redirect param preserving original path.

### Hook verification (manual sample check)

The post-edit Forbidden-tokens hook + T10c + tenant-blind + bouncy-easing hooks fire on `.tsx/.ts` files. Plan 7 modified only markdown docs (no `.ts/.tsx` changes), so the production-code hooks were not triggered during this plan. Build success with 150 pages compiled confirms no underlying token/forbidden violations introduced.

### iter-2 enterprise-r W-4 — post-deploy Vercel preview curl checks

**DEFERRED to post-push Vercel preview deployment.** Per CLAUDE.md "Verification harness deprecation path" — the 3-layer verification harness on Vercel preview URLs is the canonical signal. The atomic commit pushed to remote will trigger Vercel preview build; harness Layer 1 redirect-rules can be verified against the preview URL via GitHub Actions or manual curl after the preview deploy stabilizes.

Suggested manual verification (Jake or operator runs after preview deploy lands):

```bash
PREVIEW="https://nightwork-platform-git-phase-1-5-c-information-architecture-jakeross838s-projects.vercel.app"
for path in /dashboard /invoices /admin/platform/audit /settings/billing /cost-intelligence/lookup; do
  curl -sI --max-time 8 "${PREVIEW}${path}" | head -3
  echo "---"
done
```

Expected: 308 with correct `location:` header for each.

### Visual desktop walk (Chrome DevTools MCP)

**DEFERRED to M3 phone walk** — Jake walks new IA on iPhone+Safari per CONTEXT D-17 / Q13=A. Localhost-walk authentication requires login flow + role assumption (Jake or operator role); the harness Layer 3 vision against `<criteria>` blocks is the canonical visual signal per CLAUDE.md "Verification harness deprecation path."

Curl-based smoke (above) confirms: redirect rules work, auth gates work, build succeeds with 150 pages compiled. The visual walk surface (8 nav items + Admin ▾ + Platform Admin badge + per-job tabs + ~60 placeholders + 12 production routes with .design-system-scope wrap) is exercised by M3 phone walk + harness Layer 3.

## M3 Phone Walk Readiness Checklist

Per CONTEXT D-17 (Q13=A): single walk bundled with 1.5b ship gate. Same iPhone+Safari device per 1.5b CONTEXT D-31.

Plan 7 sets up the gate; Jake walks the steps below post-execution.

**Device:** iPhone (locked per 1.5b CONTEXT D-31). Safari browser.
**URL:** Vercel preview URL for `phase/1.5-c-information-architecture` branch (auto-deployed on push of commit 6d1c568).

**Steps (13):**

1. **Visit `/`** → expect redirect to `/today`. Today renders with 3 sections (action items + Your Day placeholder + Activity Feed)
2. **Click hamburger menu** → all 8 nav items + Admin contents + Notifications + User menu visible
3. **Click Today, Jobs, Financials, Price Intel, People, Company, Reports** → each renders without 404 or console error
4. **Click Admin ▾** → 13 items expand (cost-codes, profile, billing, etc.)
5. **(If platform_admin) Tap Platform Admin badge** (top-right) → `/platform-admin` directory renders; tap each tool → migrated sub-route renders (iter-2 D-20 wildcard verification: `/admin/platform/audit → /platform-admin/audit` works via wildcard redirect)
6. **Visit `/financials/bills/inv-caldwell-001`** → Document Review pattern renders on phone with Site Office signatures (iter-2 D-19 `.design-system-scope` wrap verification: 1px slate-tile left-stamp, JetBrains Mono UPPERCASE eyebrows at 0.18em, Space Grotesk Medium 500 headlines, 16px compact card padding, 16px section gap, 150ms ease-out motion)
7. **Visit `/financials/pay-apps/d-caldwell-05`** → Document Review pattern renders with Pay App data; `.design-system-scope` wrap signatures visible
8. **Visit `/jobs/j-caldwell-1`** → PerJobTabs as **single dropdown** on phone (iter-2 D-21 / D-059 verification); tap dropdown; navigate to Bills + Pay Apps + Schedule + Budget + Selections + More items
9. **Visit `/jobs/j-caldwell-1/mobile-approval`** → mobile-approval surface renders; 56px Approve button visible + tappable (mobile chrome priority per D-054)
10. **Test gloves-on tap targets** — all primary actions (Approve / Reject / Save) are ≥44px (WCAG 2.5.5) or ≥56px for high-stakes mobile per SPEC A9
11. **No console errors visible** (Safari DevTools → connect to iPhone → check Console)
12. **Confirm 1.5b prototype paths still accessible** at `/design-system/prototypes/*` (logged in as platform_admin — middleware gates non-platform_admin per Stage 1.5a SPEC B7)
13. **Visit a Sub Portal stub** (e.g., `/sub-portal/magic/abc123`) — renders generic stub with F3 compliance contract copy; no console errors; no token reflection visible (iter-2 mechanical #5 Sub Portal token stub fix verification)

**Pass criteria:** Jake walks all 13 steps successfully. Any divergence vs desktop walk OR usability issue → log as Wave 1.1-Lite finding (per D-049).

**After M3 walk passes:** `/gsd-ship stage-1.5b-prototype-gallery` proceeds with new IA in place. CP2 already closed (D-037).

## TDD Gate Compliance

Plan 7 is not a TDD plan (`type: execute`, not `type: tdd`). Plan-level TDD gate sequence not applicable.

## Next Phase Readiness

**Stage 1.5c iter-3 is COMPLETE pending Jake's halt review.** Plan 7 is the natural end of 1.5c-IA (per `halt_after: true` + nwrp88 Block N+2 final plan).

**Block N+2 status:**
- Plans 1, 2, 2-amend, 3, 3a-now, 4, 5, 6, 7 — all shipped
- Harness state at 32b19c9: PASS=159, FAIL=0, SKIP=4 (success)
- Plan 7 atomic commit 6d1c568 pushed to remote (Vercel preview build triggered)

**Next workflow step:** `/nightwork-qa` runs (GATE B halt). Per the user's standing rule, end-of-Block /nightwork-qa closes the staleness gap from per-task commits via NIGHTWORK_PRECOMMIT_DISABLE=1.

**After GATE B passes:** M3 phone walk bundled with 1.5b ship gate per CONTEXT D-17 / Q13=A.

**Concerns / blockers:** None. The atomic commit 6d1c568 includes all plan-required artifacts. The .gitignore deviation (Rule 3) is documented + scoped to Plan 7's explicit file requirements.

## Self-Check: PASSED

**Files created/exist verification:**
- `.planning/architecture/ARCHITECTURE.md`: FOUND ✓
- `.planning/architecture/ENTITY-INVENTORY.md`: FOUND ✓
- `.planning/architecture/ROLES-CATALOG.md`: FOUND ✓
- `.planning/architecture/INGESTION-ARCHITECTURE.md`: FOUND ✓
- `.planning/CONTEXT.md`: FOUND ✓ (whitelisted in .gitignore)
- This SUMMARY at `.planning/phases/stage-1.5c-information-architecture/01.5c-7-canonical-docs-smoke-SUMMARY.md`: WRITING NOW

**Commits exist verification:**
- `6d1c568`: FOUND ✓ (atomic 11-file commit: 4 NEW + 6 UPDATED + .gitignore Rule 3 auto-fix)

**Grep verifications passing:**
- `grep -c 'D-040' MASTER-PLAN.md` → 3 (≥1 expected; appended once + 2 references) ✓
- `grep -c 'D-060' MASTER-PLAN.md` → 2 (≥1) ✓
- `grep -c 'D-065' MASTER-PLAN.md` → 2 (≥1) ✓
- `grep -c 'entity_type' CLAUDE.md` → 1 (≥1) ✓
- `grep -c 'src/lib/audit/action-labels.ts' CLAUDE.md` → 1 (≥1) ✓
- `grep -c 'invoice (vendor bills)' CLAUDE.md` → 1 (≥1) ✓
- `grep -cE 'bill_\*|bill_<'  CLAUDE.md` → 0 (=0; concat framing NOT used) ✓
- `grep -c 'Today as home' PHILOSOPHY.md` → 1 (≥1) ✓
- `grep -cE '\*\*Consumers' PHILOSOPHY.md` → 4 (≥4; one per new pattern) ✓
- `grep -c 'Placeholder treatment' PATTERNS.md` → 1 (≥1) ✓
- `grep -cE '\*\*Consumers' PATTERNS.md` → 6 (≥5; one per new pattern + 1 extra) ✓
- `grep -c 'NwPlaceholderCard' COMPONENTS.md` → 4 (≥1) ✓
- `grep -c 'NwPlaceholderCardProps' COMPONENTS.md` → 1 (≥1) ✓
- 8 Principles grep in ARCHITECTURE.md: all 1-8 ≥1 ✓ (iter-2 planner LOW-5 strengthened)
- D-040..D-065 sequential check in MASTER-PLAN.md: all 26 ≥1 ✓ (iter-2 mechanical #8 idempotency verified)
- Privacy gate on `.planning/architecture/*.md` (32-token denylist): 0 hits ✓ (iter-2 security LOW SEC-6/7/8)

---

*Phase: stage-1.5c-information-architecture (Plan 7 — canonical-docs-smoke; Block N+2 final plan per nwrp88/89)*
*Completed: 2026-05-11*
*Halt: `halt_after: true` — Stage 1.5c iter-3 COMPLETE pending Jake's halt review + /nightwork-qa GATE B*
