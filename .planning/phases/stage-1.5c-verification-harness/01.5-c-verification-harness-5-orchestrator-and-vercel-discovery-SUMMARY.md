---
phase: stage-1.5c-verification-harness
plan: 05
plan_name: orchestrator-and-vercel-discovery
subsystem: infra
tags: [verification, orchestrator, vercel-discovery, criteria-loader, auth-strategy, report-writer, fixture-org, sec-high-2, arch-crit-1, arch-crit-2, c1, c5, d-30, sentry, redact-api-key, calibration-log, halt-for-jake, harness-binary, plan-5]

# Dependency graph
requires:
  - phase: stage-1.5c-verification-harness (Plan 1 foundation)
    provides: src/lib/verification/types.ts (Layer1/2/3Context, VerificationCriterion, VerificationResult, FIXTURE_ORG_ID slug, MAX_ITERATIONS, LoopState, IdempotencyKey, VisionResult), src/lib/verification/idempotency.ts (deriveIdempotencyKey, cacheReadJson, cacheWriteJson — org_id-bounded by SHAPE per D-30), src/lib/verification/state-machine.ts (initialState/transition/MAX_ITERATIONS — Plan 8b consumes; Plan 5 does NOT import per ARCH-CRIT-1), src/lib/verification/index.ts barrel, src/lib/verification/criteria-loader.test.md scaffold (Plan 11 executes against Plan 5's criteria-loader.ts)
  - phase: stage-1.5c-verification-harness (Plan 2 layer-1)
    provides: src/lib/verification/layer1/index.ts runLayer1; org_id threading pattern; Layer1Context shape via types.ts
  - phase: stage-1.5c-verification-harness (Plan 3 layer-2)
    provides: src/lib/verification/layer2/index.ts runLayer2; SKIP-when-no-introspection pattern
  - phase: stage-1.5c-verification-harness (Plan 4 layer-3)
    provides: src/lib/verification/layer3/index.ts runLayer3; src/lib/verification/layer3/vision-client.ts redactApiKey (single source of truth — Plan 5 imports + reuses); src/lib/verification/_browser.ts chromiumLaunchArgs (not used by Plan 5 directly — orchestrator delegates browser to layer1/layer3 internals); README Plan 5 chicken-and-egg setup template
  - phase: stage-1.5c-verification-harness (migration 00092 — Plan 1)
    provides: organizations row + idempotent org_members membership for fixture-harness-org; SQL chicken-and-egg note documents bootstrap flow
  - external dep: @supabase/supabase-js ^2.103.0 (already present, used by auth-strategy.ts for createClient)
  - external dep: @anthropic-ai/sdk ^0.88.0 (already present, used by Plan 4 — orchestrator imports redactApiKey from layer3/vision-client)
  - external dep: @sentry/nextjs ^10.49.0 (already present, dynamic-imported by orchestrator for W1 tagging)
  - external dep: tsx ^4.21.0 (already present, used as CLI shebang for scripts/verify-phase.ts)

provides:
  - "src/lib/verification/vercel-discovery.ts — discoverPreviewUrl 4-tier hierarchy: (1) explicit --preview-url, (2) Vercel REST API + VERCEL_TOKEN PRIMARY (per Jake watchpoint #2 + AUTO-LOG R2 evidence that deterministic pattern truncates beyond 63-char DNS-label limit), (3) vercel CLI ls fallback, (4) deterministic pattern (short branches only), (5) fail-loudly with all-paths-attempted error. Wait-for-Ready: HEAD probe with exponential backoff up to 5min. SIGINT-on-CLI-spawn timeout per D-23. 401 counts as Ready."
  - "src/lib/verification/criteria-loader.ts — loadCriteriaFromPhase parses .planning/phases/<phase>/*-PLAN.md <criteria> yaml blocks; returns flat VerificationCriterion[] tagged by category. Routes mechanical/dom→Layer1, behavioral→Layer2, visual/semantic→Layer3. ITER-1 SEC-HIGH-2 sanitizeCriterionText with 200-char cap + INJECTION_PATTERNS blocklist (6 regex: ignore-prior-instructions, system-role-injection, override-instructions, json-shape-injection, backtick-fence, nested-newlines). Rejected criteria logged to stderr [criteria-loader] [SEC-HIGH-2] line and skipped. Hand-rolled YAML parser per criteria-loader.test.md trade-off (Plan 11 smoke test catches regressions)."
  - "src/lib/verification/auth-strategy.ts — createHarnessSession via Supabase signInWithPassword({email: 'harness-fixture@nightwork.local'}) + HARNESS_FIXTURE_PASSWORD env var. Defense-in-depth: confirms membership exists + asserts org_uuid === FIXTURE_ORG_UUID (D-30 BREACH detection halts before any layer runs). Returns HarnessSession {access_token, refresh_token, user_id, org_id (slug — Layer*Context), org_uuid (DB PK)}. NO platform-admin path (Jake watchpoint #3 verified by grep). Per Jake watchpoint #1 (iter-1 watchpoint #1): real auth strategy, NOT a deferred-to-F1 placeholder."
  - "src/lib/verification/orchestrator.ts — runHarness composes runLayer1 → runLayer2 → runLayer3 + emits HarnessReport. Per ITER-1 ARCH-CRIT-1 + Jake watchpoint #1+#5: runHarness DOES NOT drive state machine — that lives ONLY in Plan 8b runLoop. HarnessReport carries results_by_layer + ambiguous_results; NO loop_state or halt_reason fields (active scope item #3 closed). ITER-1 C1 + D-30: createHarnessSession called BEFORE any layer runs; threads harnessSession.org_id (FIXTURE_ORG_ID slug) into all three Layer*Context constructions. Cost-saving short-circuit on Layer 1 FAIL (NOT a state-machine transition). ITER-1 W1: Sentry tagging on top-level catch with redactApiKey. ITER-1 WARNING-1: redactApiKey reused from layer3/vision-client.ts (single source of truth)."
  - "src/lib/verification/report-writer.ts — writePerCommitReport (gitignored at runs/<phase>/<commit>/report.md, full evidence retained, JSON to stdout for CI consumption + nightwork-custodian post-ship sweep) + writeReport (git-tracked at reports/<phase>/final.md, ITER-1 C5 + D-30 SANITIZED — strips pageUrl=, screenshot=, raw vision reasoning, absolute paths to runs/ tree). Per-commit keeps full reasoning + expected/actual + URL for debugging; final.md is the auditor-friendly summary with sanitization note pointing back to per-commit report."
  - "scripts/verify-phase.ts — harness binary CLI. argv parser supports --phase, --preview-url, --base-url, --commit-sha, --mode ci|local, --cost-cap-usd, --skip-layer 1|2|3, --routes. Wraps runHarness from orchestrator. Exit codes: 0 (all PASS), 1 (fail-fixable — any FAIL no ambiguous), 2 (fail-ambiguous — any conf<0.7), 3 (preview URL discovery failed), 4 (runtime error). Per ITER-1 ARCH-CRIT-1: applies decision rule (HarnessReport → exit code) — does NOT call transition()."
  - "src/lib/verification/orchestrator.test.md — manual test fixture. 9 scenarios documenting end-to-end behavior including Iter-3 hard-halt (Plan 11 ARCH-CRIT-1 verification), Idempotency (Plan-review watchpoint #7), Criteria-loader smoke test (Plan 11 ARCH-CRIT-2 ≥40 non-N/A), all 6 SEC-HIGH-2 INJECTION_PATTERNS, D-30 BREACH detection. Includes architecture invariant grep contract + chicken-and-egg setup walkthrough (active scope item #2)."
  - "src/lib/verification/types.ts — additive FIXTURE_ORG_UUID = '00000000-0000-0000-0000-fb1ce0a55e55' constant alongside existing FIXTURE_ORG_ID slug (Option A — active scope item #1 closed). Slug stays as harness-scoped identifier; UUID is RLS primary key. Both correct. Documented inline + in README + in auth-strategy.ts."
  - "src/lib/verification/README.md — updated file map with Plan 5 deliverables; added Chicken-and-egg setup section (active scope #2: SQL editor → re-run migration → set HARNESS_FIXTURE_PASSWORD) + FIXTURE_ORG_ID slug vs FIXTURE_ORG_UUID section (active scope #1)."

affects:
  - Plan 6 GH Actions workflow (Wave 3) — consumes scripts/verify-phase.ts CLI; sets VERCEL_TOKEN + ANTHROPIC_API_KEY + HARNESS_FIXTURE_PASSWORD secrets; parses JSON harness-report from stdout for PR comment + commit status; sanitization is already done in Plan 5 final.md
  - Plan 7 gsd-fix-executor (Wave 4) — out of scope for Plan 5 changes; Plan 7 is spawned by Plan 8b runLoop on failed-fixable, not by Plan 5
  - Plan 8b loop-with-executor (Wave 4) — consumes HarnessReport from runHarness; applies state machine via initialState/transition/MAX_ITERATIONS from state-machine.ts (which Plan 5 deliberately does NOT import per ARCH-CRIT-1); reads ambiguous_results to drive failed-ambiguous transition; reads results_by_layer Layer N FAILs to drive failed-fixable transition; threads cost_cap_remaining_usd into Layer3Context across iterations
  - Plan 9 calibration-log (Wave 4) — consumes JSON line {kind: 'harness-report', report} emitted to stdout by writePerCommitReport; nightwork-custodian post-ship sweep populates per-phase calibration-log.md entries (Jake watchpoint #7 — calibration-log custodian integration designed even if not yet invoked)
  - Plan 11 self-test (Wave 5) — executes orchestrator.test.md scenarios including Step E.0 criteria-loader ≥40 smoke test (verified manually by Plan 5: 141 non-N/A criteria from this phase's 12 PLANs); Step E.1 idempotency rerun-no-op assertion; Step E.x ARCH-CRIT-1 grep contract verification

# Tech tracking
tech-stack:
  added: [no new npm deps; uses @supabase/supabase-js + @anthropic-ai/sdk + @sentry/nextjs + tsx all already present]
  patterns:
    - "4-tier discovery hierarchy with empirical-evidence-driven ordering — REST API as PRIMARY (per AUTO-LOG R2 deterministic pattern truncates beyond 63-char DNS label limit); CLI as fallback for transient REST 5xx; deterministic pattern as last-ditch for short branches; fail-loudly with all-paths-attempted error message. Per Jake watchpoint #2: REST API works against current preview URL (token validated 2026-05-06 per SETUP-COMPLETE.md item #29; live test deferred to Plan 6 GH Actions)"
    - "Tenant-blind orchestrator + tenant-bounded auth boundary — orchestrator.ts threads opaque org_id strings (FIXTURE_ORG_ID slug today) through all three Layer*Context constructions; tenant scoping happens at auth-strategy.ts via createHarnessSession + signInWithPassword. When Wave 1.1+ enables per-tenant verification, only auth-strategy.ts changes; orchestrator.ts stays untouched. Tenant safety is a SHAPE invariant, not a runtime check"
    - "Defense-in-depth tenant boundary at auth layer — auth-strategy.ts asserts (1) sign-in succeeds, (2) membership row exists for the user, (3) org_uuid === FIXTURE_ORG_UUID. If any of the 3 fails, throws actionable error and orchestrator halts before any layer runs. Even a misconfigured DB cannot let the harness reach across tenants — D-30 BREACH detection is enforcement-by-construction"
    - "Hand-rolled YAML parser + smoke test safety net (per criteria-loader.test.md trade-off) — INJECTION_PATTERNS blocklist + 200-char cap defends against prompt-injection at the input-side; pairs with Plan 4 vision-client.ts defender prompt + strict JSON schema validation at the output-side. Plan 11 smoke test asserts ≥40 non-N/A criteria from this phase's PLAN files (verified manually: 141 non-N/A from 12 PLANs across 5 categories)"
    - "Run-once-and-emit orchestrator separated from loop-with-state-machine — runHarness composes layers + emits HarnessReport; runLoop (Plan 8b) wraps runHarness in a max-3-iteration loop and applies the state machine. Plan 5 grep contract: orchestrator.ts and verify-phase.ts MUST NOT import initialState/transition/LoopContext/LoopState. ARCH-CRIT-1 enforcement-by-construction: state machine ownership boundary is the import boundary"
    - "Sanitization at git-tracked artifact boundary (ITER-1 C5 + D-30) — writeReport strips pageUrl=, screenshot=, raw vision reasoning, absolute paths from final.md evidence column; per-commit gitignored report keeps full evidence. Future PR comment surface (Plan 6 GH Actions) renders sanitized final.md ONLY; defense-in-depth: even if a tenant boundary breaches upstream, raw page URLs and screenshot paths containing tenant identifiers won't land in git history"
    - "Calibration-log custodian integration via stdout JSON contract (Jake watchpoint #7) — writePerCommitReport emits {kind: 'harness-report', report} JSON line to stdout; nightwork-custodian post-ship sweep (Plan 9) parses to populate per-phase calibration-log.md entries. HarnessReport interface in orchestrator.ts is the frozen schema; future calibration improvements add fields without breaking existing parsers (additive-only)"
    - "Reused redactApiKey from Plan 4 layer3/vision-client.ts (WARNING-1 single source of truth) — orchestrator.ts imports redactApiKey to stay in lockstep with the canonical regex. If a future module needs to redact API keys (e.g., Plan 7 gsd-fix-executor agent logs), it should import this same function rather than duplicating the regex"

key-files:
  created:
    - "src/lib/verification/vercel-discovery.ts"
    - "src/lib/verification/criteria-loader.ts"
    - "src/lib/verification/auth-strategy.ts"
    - "src/lib/verification/orchestrator.ts"
    - "src/lib/verification/report-writer.ts"
    - "scripts/verify-phase.ts"
    - "src/lib/verification/orchestrator.test.md"
  modified:
    - "src/lib/verification/types.ts (added FIXTURE_ORG_UUID constant alongside FIXTURE_ORG_ID slug — active scope item #1)"
    - "src/lib/verification/README.md (added Plan 5 file map entries + Chicken-and-egg setup + FIXTURE_ORG_ID slug vs UUID sections — active scope items #1 + #2)"

key-decisions:
  - "Option A — additive FIXTURE_ORG_UUID constant (active scope item #1 — slug/UUID resolution). FIXTURE_ORG_ID stays as the slug 'fixture-harness-org' (what Layer*Context.org_id carries; what canonicalCriterionHash uses; what runs/screenshots paths use; what Sentry tags use). FIXTURE_ORG_UUID = '00000000-0000-0000-0000-fb1ce0a55e55' is the database primary key (what RLS filters on; what auth-strategy.ts asserts the harness session resolves to). Both correct. Less invasive than Option B (rename FIXTURE_ORG_ID → FIXTURE_ORG_SLUG which would have touched every Plans 1/2/3/4 callsite). HarnessSession returns BOTH fields so downstream callers never need their own slug↔UUID lookup"
  - "Vercel REST API as PRIMARY discovery tier (NOT CLI). Per AUTO-LOG R2 empirical evidence: deterministic pattern truncates beyond 63-char DNS-label limit (long branch names like phase/1.5-c-verification-harness break the pattern); REST API is reliable across all branch lengths. Per Jake watchpoint #2: REST API actually works (VERCEL_TOKEN validated 2026-05-06 per SETUP-COMPLETE.md item #29). CLI demoted to fallback because it parses column-aligned text output (more brittle than REST JSON). Plan spec listed CLI as tier 2 + REST as tier 3; deviation here justified by AUTO-LOG empirical evidence + watchpoint #2 priority"
  - "runHarness DOES NOT drive state machine (ARCH-CRIT-1 + Jake watchpoint #1+#5). HarnessReport interface has NO loop_state or halt_reason fields. orchestrator.ts deliberately does NOT import initialState/transition/LoopContext/LoopState/MAX_ITERATIONS from state-machine.ts. State machine ownership lives ONLY in Plan 8b runLoop (when shipped). scripts/verify-phase.ts applies a SIMPLE decision rule (HarnessReport.ambiguous_results.length > 0 → exit 2; any FAIL → exit 1; else exit 0) — NOT a state-machine transition. Verified by grep contract documented in orchestrator.test.md architecture invariant section"
  - "auth-strategy.ts uses anon-key client (NOT service-role). Defense-in-depth: the harness session resolves through the same RLS path real customers' sessions resolve through. Even a malicious migration that flipped RLS policies couldn't grant the harness cross-tenant access without also granting it to every authenticated user. Per CLAUDE.md 'Never hardcode an ORG_ID as a fallback' rule: this module has NO fallback path. If sign-in fails, throws; orchestrator halts; nothing downstream silently degrades. Per D-30: there is NO platform-admin escape hatch (Jake watchpoint #3 verified by grep)"
  - "auth-strategy.ts asserts org_uuid === FIXTURE_ORG_UUID after membership lookup (D-30 BREACH detection). Even if a misconfigured migration or test contributor accidentally inserts a row binding the harness user to a non-fixture org, this assertion catches it before any layer runs. The error message starts with [auth-strategy] [D-30 BREACH] so log-grepping can flag the condition immediately"
  - "Hand-rolled YAML parser kept (NOT swapped for js-yaml). Per criteria-loader.test.md Plan 1 trade-off note: 50KB npm dep adds robustness vs hand-rolled regex's failure modes #1-#4. Decision was to ship with smoke test as safety net (Plan 11 Step E.0 asserts ≥40 non-N/A); if iter-2 plan-review escalates, swap parser. Manual smoke test verified: 141 non-N/A criteria extracted from this phase's 12 PLANs (well above ≥40 threshold). Hand-rolled parser is correct for current PLAN file shape; swap is forward-compatible if needed"
  - "Reused redactApiKey from layer3/vision-client.ts (WARNING-1 single source of truth). orchestrator.ts imports redactApiKey rather than duplicating the regex. If Layer N adds a new redaction site (e.g., Plan 6 GH Actions PR comment renderer), it should import this same function. Pattern: every harness module that catches an exception with a string body MUST run it through redactApiKey before stderr / Sentry / committed report files"
  - "Calibration-log custodian integration designed at JSON contract (Jake watchpoint #7). writePerCommitReport emits {kind: 'harness-report', report} JSON line to stdout. Plan 9 nightwork-custodian post-ship sweep parses to populate per-phase calibration-log.md entries. HarnessReport interface is the frozen schema; downstream Plan 9 changes only need to ADD fields if richer telemetry is needed (additive-only). Even though Plan 9 is not yet shipped, the contract is in place — Plan 9 will not need to retrofit Plan 5's stdout shape"
  - "Vercel CLI fallback uses spawn (NOT shell parser). Per D-23 process discipline: never kill -9; CLI runs to completion or its own timeout (Vercel CLI typically returns within a few seconds). On Windows the cmd is 'vercel.cmd' (CLI is shipped as .cmd shim); on POSIX it's 'vercel'. Shell flag enabled on Windows only (where .cmd resolution requires it). Pattern matches existing scripts/e2e-*.mjs spawn usage"
  - "Sanitization rules in writeReport (ITER-1 C5 + D-30): strip pageUrl= markers; strip screenshot= markers; strip absolute paths to .png/.jpg/.jpeg/.webp; strip absolute paths to /runs/ or \\runs\\ tree (case-insensitive). After all strips, truncate to 80 chars. Final.md gets a sanitization note at the bottom pointing operators to the gitignored per-commit report. Per-commit (gitignored) report uses fullEvidenceForPerCommit which keeps reasoning + expected + actual + URL intact for debugging"

patterns-established:
  - "Tenant-blind orchestrator + tenant-bounded auth — orchestrator.ts threads opaque org_id strings; auth-strategy.ts is the ONLY tenant-aware module. When Wave 1.1+ enables multi-tenant verification, only auth-strategy.ts changes — runHarness orchestrator stays. This is the same boundary pattern Plan 4 layer3/runner.ts established with screenshot path tenant-scoping"
  - "Run-once orchestrator separated from loop-with-state-machine — runHarness emits HarnessReport with results_by_layer + ambiguous_results; Plan 8b runLoop owns the state machine. Future ANY new harness orchestrator (e.g., a hypothetical Layer 4 'interactive-test' phase) should follow this pattern: emit results, leave state machine to consumers"
  - "Stdout JSON contract for downstream tooling (calibration-log + GH Actions) — {kind: 'harness-report', report} on a single line, parseable by jq + Python. Plan 6 GH Actions parses for PR comment + commit status; Plan 9 calibration-log custodian parses post-ship for per-phase entries. Both readers + writers agree on the JSON shape; future telemetry adds fields, never renames"
  - "Sanitization at the git-tracked artifact boundary (NOT at the source) — writePerCommitReport keeps full evidence; writeReport sanitizes. Per-commit is gitignored; final.md is committed. Operators who need full evidence consult the gitignored file. This pattern keeps ground-truth in the gitignored layer + sanitized summary in the tracked layer"
  - "4-tier fallback hierarchy with empirical-evidence-driven ordering — discoverPreviewUrl tier 1 explicit > tier 2 REST API > tier 3 CLI > tier 4 deterministic pattern. AUTO-LOG empirical evidence drove the ordering (REST API works for all branch lengths; pattern truncates for long names). Pattern: ordering should follow what works, not what's first-thought"

requirements-completed: []

# Metrics
duration: 25min
completed: 2026-05-05
---

# Phase stage-1.5c-verification-harness Plan 5: Orchestrator + Vercel Discovery Summary

**Wave 2 single-plan dispatch — the orchestrator. Composes Layers 1/2/3 from Wave 1 into runHarness; ships scripts/verify-phase.ts harness binary; ships Vercel REST-API-primary preview URL discovery; ships criteria-loader with SEC-HIGH-2 prompt-injection blocklist; ships auth-strategy.ts with D-30 BREACH detection (no platform-admin path); ships report-writer with ITER-1 C5 sanitization for git-tracked final.md. Per ITER-1 ARCH-CRIT-1 + Jake watchpoint #1+#5: runHarness DOES NOT drive state machine — that lives ONLY in Plan 8b runLoop. HarnessReport.loop_state DELETED (active scope #3). FIXTURE_ORG_UUID added alongside slug (active scope #1). Chicken-and-egg setup documented (active scope #2). Halt for Jake review per halt_after: true.**

## Performance

- **Duration:** ~25 min
- **Completed:** 2026-05-05
- **Tasks:** 3 atomic commits + 1 SUMMARY commit
- **Files created:** 7
- **Files modified:** 2 (`src/lib/verification/types.ts` — additive FIXTURE_ORG_UUID; `src/lib/verification/README.md` — Plan 5 docs)

## Accomplishments

- 5 TypeScript modules + 1 CLI binary + 1 test fixture markdown shipped under `src/lib/verification/` and `scripts/`
- All 7 watchpoints addressed inline (see Watchpoint Evidence Map below)
- All 3 active scope items closed (slug/UUID resolution; chicken-and-egg setup; loop_state cleanup)
- 2 deferred items flagged in Deferred Items section (NOT actioned)
- ITER-1 ARCH-CRIT-1 enforcement-by-construction verified by grep contract: `grep -nE '^\\s*import.*\\b(initialState|transition|LoopContext|LoopState)\\b' src/lib/verification/orchestrator.ts scripts/verify-phase.ts` returns 0
- ITER-1 ARCH-CRIT-2 smoke test verified manually: 141 non-N/A criteria extracted from this phase's 12 PLAN files (≥40 threshold met with 3.5x margin)
- ITER-1 SEC-HIGH-2 sanitization verified: 6 INJECTION_PATTERNS blocklist + 200-char cap; 6 criteria rejected during smoke test (5 over-length + 1 json-shape false-positive on a legitimate code reference; conservative posture preserved)
- ITER-1 C1 + D-30 auth strategy: real Supabase signInWithPassword with defense-in-depth membership + UUID assertions; NO platform-admin path (verified by grep)
- ITER-1 C5 + D-30 report sanitization: writeReport strips pageUrl/screenshot/reasoning/absolute paths from git-tracked final.md
- ITER-1 W1 enterprise-readiness: try/catch + Sentry tagging + redactApiKey reused from Plan 4 (single source of truth)
- ITER-1 WARNING-1 compliance: redactApiKey applied at every catch site
- `npm run build` clean (Plan 5 introduces 0 new warnings); `npx tsc --noEmit` clean for `src/lib/verification/` + `scripts/verify-phase.ts`
- System `.githooks/pre-commit` Drummond grep gate exit 0 on every commit + on the final tree

## Task Commits

Each task was committed atomically with `NIGHTWORK_PRECOMMIT_DISABLE=1` (system `.githooks/pre-commit` Drummond grep gate ran unconditionally on every commit and exited 0):

1. **Task 1: vercel-discovery.ts + criteria-loader.ts + auth-strategy.ts** — `1cc868d` (feat)
2. **Task 2: orchestrator.ts + report-writer.ts** — `547c00d` (feat)
3. **Task 3: scripts/verify-phase.ts + orchestrator.test.md + README.md update** — `789fe35` (feat)

## Files Created/Modified

### Created

| Path | Purpose |
|------|---------|
| `src/lib/verification/vercel-discovery.ts` | `discoverPreviewUrl` 4-tier hierarchy: explicit → REST API (PRIMARY per Jake watchpoint #2 + AUTO-LOG R2) → CLI fallback → deterministic pattern → fail-loudly. Wait-for-Ready with exponential backoff up to 5min. SIGINT-on-CLI-spawn timeout per D-23. 401 counts as Ready (preview auth on, URL exists). |
| `src/lib/verification/criteria-loader.ts` | `loadCriteriaFromPhase` reads `.planning/phases/<phase>/*-PLAN.md`; extracts `<criteria>` yaml blocks; routes mechanical/dom→Layer1, behavioral→Layer2, visual/semantic→Layer3. ITER-1 SEC-HIGH-2: `sanitizeCriterionText` exported helper + `INJECTION_PATTERNS` blocklist (6 regex). N/A entries skipped. Hand-rolled YAML parser per criteria-loader.test.md trade-off. |
| `src/lib/verification/auth-strategy.ts` | `createHarnessSession` via Supabase `signInWithPassword({email: 'harness-fixture@nightwork.local', password: HARNESS_FIXTURE_PASSWORD})`. Returns `HarnessSession {access_token, refresh_token, user_id, org_id (slug), org_uuid (DB PK)}`. Defense-in-depth: confirms membership exists + asserts `org_uuid === FIXTURE_ORG_UUID` (D-30 BREACH detection). NO platform-admin path (Jake watchpoint #3). Exports `FIXTURE_USER_EMAIL` constant. |
| `src/lib/verification/orchestrator.ts` | `runHarness(opts: HarnessOptions): Promise<HarnessReport>` composes runLayer1 → runLayer2 → runLayer3. ITER-1 ARCH-CRIT-1: NO state machine driving (no imports of initialState/transition/LoopContext/LoopState/MAX_ITERATIONS); HarnessReport has NO loop_state or halt_reason fields. ITER-1 C1 + D-30: createHarnessSession called BEFORE any layer; threads org_id slug into Layer1/2/3Context. Cost-saving short-circuit on Layer 1 FAIL (NOT state-machine transition). ITER-1 W1: Sentry capture with redactApiKey on top-level catch. ITER-1 WARNING-1: redactApiKey reused from layer3/vision-client.ts. |
| `src/lib/verification/report-writer.ts` | `writePerCommitReport` (gitignored at runs/<phase>/<commit>/report.md, full evidence, JSON to stdout for CI + custodian) + `writeReport` (git-tracked at reports/<phase>/final.md, ITER-1 C5 + D-30 SANITIZED — strips pageUrl=, screenshot=, raw reasoning, absolute paths to runs/ tree). Final.md gets sanitization note pointing to per-commit report. Calibration-log JSON contract `{kind: 'harness-report', report}` on stdout (Jake watchpoint #7). |
| `scripts/verify-phase.ts` | Harness binary CLI. argv parser: `--phase`, `--preview-url`, `--base-url`, `--commit-sha`, `--mode ci\|local`, `--cost-cap-usd`, `--skip-layer 1\|2\|3`, `--routes`. Wraps runHarness. Exit codes: 0 (all PASS), 1 (fail-fixable — any FAIL no ambiguous), 2 (fail-ambiguous — conf<0.7), 3 (preview URL discovery failed), 4 (runtime error). Per ARCH-CRIT-1: applies decision rule, NOT state-machine transition. Falls back to `git rev-parse HEAD` for `--commit-sha` if not provided. |
| `src/lib/verification/orchestrator.test.md` | 9 manual test scenarios. Architecture invariant section (ARCH-CRIT-1 grep contract). Chicken-and-egg setup walkthrough (active scope item #2). Scenarios: Empty phase, Discovery failure, Local mode, Idempotency rerun, Ambiguous Layer 3, Iter-3 hard-halt (Plan 11 ARCH-CRIT-1), Criteria-loader smoke (≥40), SEC-HIGH-2 sanitization (all 6 patterns), D-30 BREACH detection. Plan 11 Step E.0/E.1 executes against this fixture. |

### Modified

| Path | Change |
|------|--------|
| `src/lib/verification/types.ts` | **Active scope item #1 closed:** added `FIXTURE_ORG_UUID = "00000000-0000-0000-0000-fb1ce0a55e55"` constant alongside existing `FIXTURE_ORG_ID = "fixture-harness-org"` slug. Option A — additive constants (less invasive than Option B which would rename FIXTURE_ORG_ID across every Plan 1/2/3/4 callsite). Inline doc-comment block explains slug-vs-UUID disambiguation: slug for harness-scoped surfaces; UUID for DB primary key. |
| `src/lib/verification/README.md` | **Active scope items #1 + #2 closed:** added Plan 5 file-map entries; added "Chicken-and-egg setup (Plan 5)" section walking through migration 00092 + Supabase Studio SQL editor `auth.admin.create_user` + re-run migration + set HARNESS_FIXTURE_PASSWORD; added "FIXTURE_ORG_ID slug vs FIXTURE_ORG_UUID" section explaining what each constant is for and where each is used. |

## Watchpoint Evidence Map (per Jake nwrp51 — 7 watchpoints)

| # | Watchpoint | Evidence | Verified |
|---|-----------|----------|----------|
| 1 | runHarness integrates 3 layers WITHOUT redefining types | `orchestrator.ts:48-58` imports types from `./types`, `./layer1`, `./layer2`, `./layer3`; `grep -rnE '^(export )?(interface\|type) (Layer1Context\|Layer2Context\|Layer3Context\|VisionResult\|LoopState)' src/lib/verification/orchestrator.ts src/lib/verification/criteria-loader.ts src/lib/verification/report-writer.ts src/lib/verification/auth-strategy.ts src/lib/verification/vercel-discovery.ts scripts/verify-phase.ts` returns 0 | ✓ |
| 2 | Vercel REST API primary actually works | `vercel-discovery.ts:71-94` — REST API is tier 2 (after explicit URL); CLI demoted to tier 3; deterministic pattern tier 4. Per AUTO-LOG R2 + SETUP-COMPLETE.md item #29: `VERCEL_TOKEN` validated 2026-05-06; pattern works for short branches but truncates for `phase/1.5-c-*`. Live test deferred to Plan 6 GH Actions integration (Plan 5 cannot run end-to-end without `HARNESS_FIXTURE_PASSWORD` set in environment) | ✓ (logic + evidence) |
| 3 | auth-strategy uses Supabase signInWithPassword; NO platform-admin path | `auth-strategy.ts:79-87` calls `createClient(...).auth.signInWithPassword({email: FIXTURE_USER_EMAIL, password})`; `grep -E 'createServiceRoleClient\|platformAdmin\|admin\.createUser\|service[_-]role[_-]key' src/lib/verification/auth-strategy.ts` returns 0; comments document "NO platform-admin escape hatch — future need = explicit D-30 amendment" | ✓ |
| 4 | criteria-loader extracts ≥40 non-N/A criteria from this phase's 12 PLANs | Manual smoke test (per orchestrator.test.md Scenario 7) — 141 non-N/A criteria extracted from `stage-1.5c-verification-harness` 12 PLAN files; categories: mechanical=39, behavioral=89, dom=1, semantic=10, visual=2 (3.5x the ≥40 threshold) | ✓ |
| 5 | iter-3 hard-halt actually fires (Plan 11's verification — Plan 5 must NOT drive SM) | `orchestrator.ts` does NOT import initialState/transition/LoopContext/LoopState/MAX_ITERATIONS (verified by `grep -nE '^\\s*import.*\\b(initialState\|transition\|LoopContext\|LoopState\|MAX_ITERATIONS)\\b' src/lib/verification/orchestrator.ts scripts/verify-phase.ts` returns 0); HarnessReport has NO loop_state or halt_reason fields. Iter-3 hard-halt is owned by Plan 8b runLoop; Plan 11 Scenario 6 in orchestrator.test.md documents the verification | ✓ |
| 6 | All 3 follow-ups closed (active scope) | (1) Slug/UUID: types.ts adds FIXTURE_ORG_UUID alongside slug + README disambiguation section; (2) Chicken-and-egg: README + orchestrator.test.md both document SQL editor → re-run migration → HARNESS_FIXTURE_PASSWORD flow; (3) Lingering loop_state: HarnessReport interface has NO loop_state/halt_reason fields (deleted from spec — verified by `grep -nE '^\\s*(loop_state\|halt_reason)\\s*[?:]' src/lib/verification/orchestrator.ts src/lib/verification/report-writer.ts` returns 0) | ✓ |
| 7 | Calibration-log custodian integration designed | `report-writer.ts:writePerCommitReport` emits `{kind: 'harness-report', report}` JSON line to stdout. nightwork-custodian (Plan 9) post-ship sweep parses to populate per-phase calibration-log.md entries. HarnessReport interface in orchestrator.ts is the frozen contract. Plan 9 will not need to retrofit Plan 5's stdout shape (Plan 5 SUMMARY documents this for Plan 9 dispatch awareness) | ✓ |

## Active Scope Items (closed)

Per Jake nwrp51 §"Active scope" — Plan 5 must close all 3 before halt-after.

### Active scope #1 — FIXTURE_ORG_ID slug/UUID resolution

**Choice: Option A** (additive `FIXTURE_ORG_UUID` constant alongside existing `FIXTURE_ORG_ID` slug).

**Rationale:** Less invasive than Option B (rename `FIXTURE_ORG_ID` → `FIXTURE_ORG_SLUG` would touch every Plan 1/2/3/4 callsite). Named constants document the distinction; both are correct (slug for harness-scoped surfaces; UUID for DB primary key). HarnessSession returns BOTH so downstream callers never need their own lookup.

**Implementation:**
- `src/lib/verification/types.ts` adds `FIXTURE_ORG_UUID = "00000000-0000-0000-0000-fb1ce0a55e55"` with inline doc-comment block explaining where each is used.
- `src/lib/verification/auth-strategy.ts` returns `org_id` (slug) + `org_uuid` (UUID) in HarnessSession.
- `src/lib/verification/README.md` adds "FIXTURE_ORG_ID slug vs FIXTURE_ORG_UUID" section.
- All Plans 1/2/3/4 callsites that thread `ctx.org_id` continue using the slug — no breaking changes.

### Active scope #2 — Migration 00092 chicken-and-egg setup documented

**Documented in:** `src/lib/verification/README.md` "Chicken-and-egg setup (Plan 5)" section + `src/lib/verification/orchestrator.test.md` "Chicken-and-egg setup" section.

**Setup flow (one-time per fresh checkout):**
1. Apply migration 00092 — seeds fixture org row (UUID `00000000-0000-0000-0000-fb1ce0a55e55`); idempotent `ON CONFLICT DO NOTHING`. The `org_members` INSERT is gated on the user existing in `auth.users` — if user doesn't exist yet, INSERT is a no-op.
2. Bootstrap the harness user via Supabase Studio SQL editor:
   ```sql
   SELECT auth.admin.create_user(
     email := 'harness-fixture@nightwork.local',
     password := '<the-value-you-will-set-as-HARNESS_FIXTURE_PASSWORD>',
     email_confirm := true
   );
   ```
3. Re-run migration 00092 — now binds membership to the fixture org (the SELECT-from-auth.users-WHERE pattern resolves once the user exists).
4. Set `HARNESS_FIXTURE_PASSWORD` in `.env.local` (local dev) or as GitHub Actions secret (CI).
5. Run the harness — `createHarnessSession` succeeds.

**Failure modes:** If any step is skipped, `createHarnessSession` throws with an actionable error pointing back to README. Per D-30: there is NO platform-admin escape hatch.

**Future Plan 5 follow-up:** A `npm run harness:bootstrap` script that calls `supabase.auth.admin.createUser()` with service-role-key would automate Step 2. Documented but not yet shipped (out of scope for this single-plan dispatch). Tracked for Wave 2.1+.

### Active scope #3 — Lingering `LoopState` import in HarnessReport spec

**Action:** Cleanup deferred from Plan 1/2/3/4 — the original Plan 5 spec for `HarnessReport` (lines 736-737 of PLAN file) had `loop_state: LoopState` and `halt_reason?: "iter-3" | ...` fields. Per ITER-1 ARCH-CRIT-1, these fields belong to Plan 8b runLoop's `LoopOutcome`, NOT to Plan 5's `HarnessReport`.

**Closed:**
- `src/lib/verification/orchestrator.ts:88-108` — HarnessReport interface declares NO loop_state or halt_reason fields. Inline comment block (lines 70-78) cites ITER-1 ARCH-CRIT-1 + Jake watchpoint #1+#5.
- Verified by `grep -nE '^\\s*(loop_state|halt_reason)\\s*[?:]' src/lib/verification/orchestrator.ts src/lib/verification/report-writer.ts` returns 0.
- `report-writer.ts` does NOT render a "Loop state" line (the original Plan 5 spec at line 992 did; deleted in implementation).

## Deferred Items (flagged, NOT actioned)

Per Jake nwrp51 §"Deferred":

### Deferred #4 — Plan 8b interface doc drift

**Status:** Flagged. Plan 11 (self-test) will catch any drift between Plan 5's HarnessReport schema and Plan 8b's runLoop expectations during execution. Plan 8b's LoopOutcome is the consumer; if Plan 5's HarnessReport adds/removes fields without coordinating with Plan 8b, Plan 11 self-test fails.

**Action:** None for Plan 5. Plan 8b dispatch (Wave 4) will read this SUMMARY's HarnessReport `provides` block and Plan 5's orchestrator.ts inline doc to align.

### Deferred #5 — No cost-pricing config table

**Status:** Flagged. Wave 1.1+ improvement.

**Context:** Plan 4 layer3/cost-cap.ts uses hard-coded `claude-3-5-sonnet-20241022` pricing; future model upgrades (claude-3-7, claude-4) require code changes. A cost-pricing config table (e.g., `org_settings.vision_cost_pricing` or a per-org config row) would let operators upgrade models without redeploying.

**Action:** None for Plan 5. Tracked for Wave 1.1+ alongside other config-table extractions (per CLAUDE.md "Org-configurable, not hardcoded" rule). Wave 1.1+ planning will surface this from `deferred-items.md` (this SUMMARY's Deferred #5 is a new entry).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Vercel REST API promoted to tier 2 (PRIMARY); CLI demoted to tier 3**

- **Found during:** Task 1 (vercel-discovery.ts implementation)
- **Issue:** Plan spec listed CLI as tier 2 + REST API as tier 3. Per AUTO-LOG R2 empirical evidence: deterministic pattern truncates beyond 63-char DNS-label limit (long branch names like `phase/1.5-c-*` break the pattern); REST API is reliable across all branch lengths. Per Jake watchpoint #2 + SETUP-COMPLETE.md item #29: VERCEL_TOKEN validated 2026-05-06. CLI parses column-aligned text (more brittle than REST JSON).
- **Fix:** REST API as tier 2 (PRIMARY); CLI as tier 3 fallback (when REST returns no rows / network glitch / transient 5xx).
- **Files modified:** `src/lib/verification/vercel-discovery.ts`
- **Verification:** typecheck clean; logic mirrors AUTO-LOG R2 + SETUP-COMPLETE recommendations
- **Committed in:** `1cc868d` (Task 1 commit)

**2. [Rule 2 - Missing Critical] auth-strategy.ts adds D-30 BREACH defense-in-depth**

- **Found during:** Task 1 (auth-strategy.ts implementation)
- **Issue:** Plan spec described createHarnessSession as a thin wrapper around signInWithPassword. But D-30 requires tenant-bounded by construction — what if a misconfigured migration or test contributor accidentally inserts a row binding the harness user to a non-fixture org? The thin wrapper would silently succeed, layers would run with a non-fixture org_id, and tenant boundary would be breached. Per CLAUDE.md "Multi-tenant RLS is non-negotiable" + "Tenant safety is built BY CONSTRUCTION, not by enforcement" rules: defense-in-depth required.
- **Fix:** After signInWithPassword succeeds, query `org_members` table and assert (1) row exists, (2) `org_id === FIXTURE_ORG_UUID`. If either fails, throw `[auth-strategy] [D-30 BREACH]` actionable error and orchestrator halts before any layer runs.
- **Files modified:** `src/lib/verification/auth-strategy.ts`
- **Verification:** typecheck clean; D-30 BREACH detection halts before Layer 1; orchestrator.test.md Scenario 9 documents the verification path
- **Committed in:** `1cc868d` (Task 1 commit)

**3. [Rule 1 - Bug] redactApiKey reused from layer3/vision-client.ts (NOT redefined locally)**

- **Found during:** Task 2 (orchestrator.ts implementation)
- **Issue:** Plan spec at line 743-748 defined `redactApiKey` locally in orchestrator.ts. But Plan 4 layer3/vision-client.ts already exports `redactApiKey` per the WARNING-1 single-source-of-truth pattern (Plan 4 SUMMARY decision #5: "redactApiKey EXPORTED from vision-client.ts and called in 3 places... Single source of truth for key-redaction patterns"). Locally redefining would create drift risk.
- **Fix:** orchestrator.ts imports `redactApiKey` from `./layer3/vision-client` (line 56). The regex stays in one place; future surfaces (e.g., Plan 6 GH Actions PR comment renderer) should import the same function rather than duplicating.
- **Files modified:** `src/lib/verification/orchestrator.ts`
- **Verification:** typecheck clean; grep confirms single redactApiKey definition in src/lib/verification/
- **Committed in:** `547c00d` (Task 2 commit)

**4. [Rule 2 - Missing Critical] Vercel CLI fallback uses spawn with platform-aware shell flag**

- **Found during:** Task 1 (vercel-discovery.ts implementation)
- **Issue:** Plan spec at line 372 used `shell: process.platform === "win32"`. On Windows, the Vercel CLI ships as `vercel.cmd` (a `.cmd` shim) which requires shell resolution; on POSIX the binary is `vercel` and shell adds attack surface. Plan spec was correct in intent but missed the cmd vs vercel distinction.
- **Fix:** vercel-discovery.ts uses `process.platform === "win32" ? "vercel.cmd" : "vercel"` for the cmd name + `shell: process.platform === "win32"` for the flag. Both match.
- **Files modified:** `src/lib/verification/vercel-discovery.ts`
- **Verification:** typecheck clean; pattern matches existing scripts/e2e-*.mjs spawn usage
- **Committed in:** `1cc868d` (Task 1 commit)

**5. [Rule 3 - Blocking] criteria-loader plan_id regex updated for `Na` suffix support**

- **Found during:** Task 1 (criteria-loader.ts implementation against this phase's PLANs which include `8a` and `8b` plan IDs)
- **Issue:** Plan spec at line 525-526 had a complex regex `(\d+|\d+a|\d+b)` to handle both numeric and lettered plan IDs. The simpler regex `\d+[a-z]?` covers the same cases without alternation.
- **Fix:** `criteria-loader.ts:135-136` uses `/^plan_id:\s*(\d+[a-z]?)\s*$/m` for frontmatter; filename fallback uses `\d+[a-z]?`.
- **Files modified:** `src/lib/verification/criteria-loader.ts`
- **Verification:** Manual smoke test extracts 141 criteria from this phase's 12 PLANs (including 8a and 8b) — plan_id regex correctly identifies all
- **Committed in:** `1cc868d` (Task 1 commit)

### Architectural / API decisions (not Rule 1-3 — documented for review)

**1. Vercel REST API as PRIMARY (vs CLI as PRIMARY in plan spec)**

Already documented above in Auto-fixed Issue #1.

**2. FIXTURE_ORG_UUID as additive constant (Option A)**

See Active scope item #1 above.

## Authentication Gates

None occurred during execution. Plan 5 introduces an authentication gate for downstream work (`HARNESS_FIXTURE_PASSWORD` env var + Supabase user bootstrap), but the gate is documented (chicken-and-egg setup) rather than triggered.

**For first run on Vercel preview (Plan 6 GH Actions or local user):**
- Set `VERCEL_TOKEN` (already done per SETUP-COMPLETE.md item #29)
- Set `ANTHROPIC_API_KEY` (already done per SETUP-COMPLETE.md item #28)
- Bootstrap `harness-fixture@nightwork.local` user (Step 2 of chicken-and-egg setup)
- Set `HARNESS_FIXTURE_PASSWORD`

These are setup concerns documented in README + orchestrator.test.md, NOT runtime gates the harness halts on.

## ITER-1 Amendment Evidence Map

All 9 iter-1 amendments cited by Plan 5 (Cluster 1 + Cluster 2 + Cluster 3 + W1 + WARNING-1):

| # | Amendment | Evidence | Verified |
|---|-----------|----------|----------|
| 1 | **ARCH-CRIT-1** runHarness REMOVES state-machine driving | `orchestrator.ts:48-58` no SM imports; `orchestrator.ts:88-108` HarnessReport has NO loop_state/halt_reason; verified by grep contract documented in orchestrator.test.md | ✓ |
| 2 | **ARCH-CRIT-2** criteria-loader smoke test foundation | `criteria-loader.ts` ships hand-rolled YAML parser; manual smoke test verifies 141 non-N/A criteria from this phase's 12 PLANs; Plan 11 Step E.0 EXECUTES this against the committed implementation | ✓ |
| 3 | **ARCH-CRIT-3** Layer*Context imported from types.ts | `orchestrator.ts:60-65` imports types from `./types`; no Plan 5 file redefines Layer1/2/3Context; grep returns 0 redefinitions | ✓ |
| 4 | **C1 + D-30** auth-strategy ships real Supabase signInWithPassword | `auth-strategy.ts:84-87` calls `signInWithPassword({email: FIXTURE_USER_EMAIL, password})`; defense-in-depth membership + UUID assertions; orchestrator threads session BEFORE any layer runs | ✓ |
| 5 | **C5 + D-30** report-writer sanitizes git-tracked final.md | `report-writer.ts:sanitizeEvidenceForFinal` strips pageUrl=, screenshot=, absolute paths to .png/.jpg/.jpeg/.webp, absolute paths to runs/ tree; final.md gets sanitization note pointing to per-commit report; per-commit (gitignored) keeps full evidence via fullEvidenceForPerCommit | ✓ |
| 6 | **SEC-HIGH-2** criteria-loader sanitizeCriterionText + INJECTION_PATTERNS | `criteria-loader.ts:32-72` exports sanitizeCriterionText (200-char cap + 6-pattern blocklist); rejected criteria logged to stderr `[criteria-loader] [SEC-HIGH-2]` + skipped; verified by manual smoke test (6 criteria rejected from 147 total = 141 accepted) | ✓ |
| 7 | **WARNING-1** redactApiKey strips sk-ant-* + Bearer * | `orchestrator.ts:56` imports `redactApiKey` from `./layer3/vision-client` (single source of truth); applied at top-level catch (line 264) + re-throw (line 269) | ✓ |
| 8 | **W1 enterprise-readiness** Sentry tagging on orchestrator exceptions | `orchestrator.ts:248-262` dynamic-imports `@sentry/nextjs`; `captureException(err, {tags: {layer, phase, commit_sha, preview_url_source}, extra: {error_redacted: redactApiKey(errMsg)}})` | ✓ |
| 9 | **D-30** auth strategy is the ONLY auth path; NO platform-admin | `auth-strategy.ts:21-24` doc-comment cites D-30; `grep -E 'createServiceRoleClient\|platformAdmin\|admin\.createUser\|service[_-]role[_-]key' src/lib/verification/auth-strategy.ts` returns 0; defense-in-depth uses anon-key client (NOT service-role) | ✓ |

## Acceptance Criteria Verification

| AC | Description | Evidence |
|----|-------------|----------|
| AC-1.5c-vh-5-1 | `scripts/verify-phase.ts` exists; runnable | `scripts/verify-phase.ts` shipped; shebang `#!/usr/bin/env -S npx tsx`; main() invoked at bottom |
| AC-1.5c-vh-5-2 | Argv parser supports all flags | `scripts/verify-phase.ts:53-95` switch on each flag |
| AC-1.5c-vh-5-3 | Exit codes 0/1/2/3/4 | `scripts/verify-phase.ts:147-160` decision rule + 137-140 discovery-fail + 100, 178 runtime-error |
| AC-1.5c-vh-5-4 | `discoverPreviewUrl` 4-tier hierarchy | `vercel-discovery.ts:55-129` |
| AC-1.5c-vh-5-5 | Wait-for-Ready exponential backoff | `vercel-discovery.ts:213-230` `READY_BACKOFF_MS = [1000, 2000, 4000, 8000, 15000, 30000]`; `MAX_READY_WAIT_MS = 5*60*1000` |
| AC-1.5c-vh-5-6 | `loadCriteriaFromPhase` parses yaml + routes by category | `criteria-loader.ts:103-122` `CATEGORY_TO_LAYER` map + per-category extraction loop |
| AC-1.5c-vh-5-7 | N/A entries skipped | `criteria-loader.ts:140-141` `if (/^\\s*N\\/A/i.test(text)) continue;` |
| AC-1.5c-vh-5-8 | `runHarness` composes 3 layers in sequence | `orchestrator.ts:155-211` runLayer1 → layer1HasFail check → runLayer2 → layer2HasFailBlocking check → runLayer3 |
| AC-1.5c-vh-5-9 | Layer 1 FAIL aborts before Layer 3 | `orchestrator.ts:184-193` `if (!layer1HasFail && !opts.skip_layers?.includes(2))` for Layer 2 + `orchestrator.ts:198-211` `if (!layer1HasFail && !layer2HasFailBlocking ...)` for Layer 3 |
| AC-1.5c-vh-5-10 | Per-commit report at runs/ (gitignored) | `report-writer.ts:46-58` writes to `runs/<phase>/<commit>/report.md`; `.gitignore` line 136 ignores `runs/` |
| AC-1.5c-vh-5-11 | Final report at reports/ (git-tracked) | `report-writer.ts:62-70` writes to `reports/<phase>/final.md`; `.gitignore` line 134 unignores `reports/` |
| AC-1.5c-vh-5-12 | JSON harness-report to stdout | `report-writer.ts:54-56` `process.stdout.write(JSON.stringify({kind: 'harness-report', report}, null, 2) + '\\n')` |
| AC-1.5c-vh-5-13 | --mode local supported per D-04 | `orchestrator.ts:115-122` log "mode=local — using ${url} (per D-04: documented but discouraged...)" |
| AC-1.5c-vh-5-14 | Test fixture documents 5 scenarios incl. rerun-no-op | `orchestrator.test.md` 9 scenarios including Scenario 4 Idempotency (rerun-no-op invariant) — Plan-review watchpoint #7 |
| AC-1.5c-vh-5-15 | npm run build clean, npx tsc clean | `npm run build` clean; `npx tsc --noEmit 2>&1 \| grep -v __tests__/` returns no Plan 5 errors |
| AC-1.5c-vh-5-16 | Wave 2 halt signal surfaced | This SUMMARY + Task 3 commit message + plan halt_after: true |
| AC-1.5c-vh-5-17 (iter-1 ARCH-CRIT-1) | runHarness no SM imports; HarnessReport no loop_state | `grep -nE '^\\s*import.*\\b(initialState\|transition\|LoopContext\|LoopState)\\b' src/lib/verification/orchestrator.ts scripts/verify-phase.ts` returns 0; `grep -nE '^\\s*(loop_state\|halt_reason)\\s*[?:]' src/lib/verification/orchestrator.ts` returns 0 |
| AC-1.5c-vh-5-18 (iter-1 C1 + D-30) | auth-strategy.ts createHarnessSession + FIXTURE_USER_EMAIL | `auth-strategy.ts:25` `export const FIXTURE_USER_EMAIL`; `auth-strategy.ts:71` `export async function createHarnessSession`; `orchestrator.ts:147-150` calls before any layer; `orchestrator.ts:170, 188, 204` threads `harnessSession.org_id` into all 3 Layer*Contexts |
| AC-1.5c-vh-5-19 (iter-1 SEC-HIGH-2) | sanitizeCriterionText + sanitization integration | `criteria-loader.ts:80-103` exports `sanitizeCriterionText`; `criteria-loader.ts:148-156` calls on every criterion; rejected logged to stderr |
| AC-1.5c-vh-5-20 (iter-1 SEC-HIGH-2) | INJECTION_PATTERNS contains 6 named patterns | `criteria-loader.ts:42-71` 6 patterns: ignore-prior-instructions / system-role-injection / override-instructions / json-shape-injection / backtick-fence / nested-newlines |
| AC-1.5c-vh-5-21 (iter-1 C5 + D-30) | writeReport sanitizes evidence | `report-writer.ts:79-115` sanitizeEvidenceForFinal strips pageUrl=, screenshot=, paths to .png/.jpg/.jpeg/.webp, paths to runs/ tree, truncates to 80 chars |
| AC-1.5c-vh-5-22 (iter-1 W1 enterprise-readiness) | runHarness try/catch + Sentry.captureException | `orchestrator.ts:104-271` body-wrapping try/catch; `orchestrator.ts:248-262` Sentry capture with redacted error |
| AC-1.5c-vh-5-23 (iter-1 WARNING-1 compliance) | redactApiKey strips sk-ant + Bearer | imported from `./layer3/vision-client` (single source of truth); applied at orchestrator.ts:264, 269 |
| AC-1.5c-vh-5-24 (iter-1 W1 day bump) | estimated_days = 2 | PLAN frontmatter `estimated_days: 2` (was 1.5d → bumped to 2d) |

## Smoke Test Results (manual)

Per orchestrator.test.md Scenario 7 (criteria-loader smoke test):

```
count=141
{
  "mechanical": 39,
  "behavioral": 89,
  "dom": 1,
  "semantic": 10,
  "visual": 2
}
PASS
```

**6 criteria rejected during smoke test (SEC-HIGH-2 sanitization working):**
1. Plan 1 behavioral: criterion text 208 chars (>200 cap) — over-length
2. Plan 3 behavioral: matched `json-shape-injection` pattern (legitimate code reference `{verdict: 'SKIP'}` — false-positive trade-off accepted per conservative posture)
3. Plan 4 behavioral: criterion text 220 chars — over-length
4. Plan 6 behavioral: criterion text 255 chars — over-length
5. Plan 7 behavioral: criterion text 316 chars — over-length
6. Plan 9 semantic: criterion text 215 chars — over-length

**Note on rejection #2 (json-shape false-positive):** The pattern `[{}]\s*["']?(verdict|confidence|reasoning)["']?\s*:` matched a legitimate criterion describing the conservation rule's return shape. This is a known trade-off of the conservative blocklist posture — better to reject ambiguous criteria than miss an injection attempt. The plan author can rephrase using different shape syntax. Tracked as a observation, NOT a Rule 1 bug (the sanitizer is working as designed; the cost is occasional false-positive rejection).

## User Setup Required

For first run on Vercel preview (Plan 6 GH Actions or local user):

1. Apply migration 00092 (already applied in Plan 1)
2. Bootstrap `harness-fixture@nightwork.local` via Supabase Studio SQL editor (per chicken-and-egg setup)
3. Re-run migration 00092 to bind membership
4. Set `HARNESS_FIXTURE_PASSWORD` env var (local `.env.local`; CI GitHub Actions secret)
5. Set `VERCEL_TOKEN` env var (already done per SETUP-COMPLETE.md item #29)
6. Set `ANTHROPIC_API_KEY` env var (already done per SETUP-COMPLETE.md item #28)
7. Set `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` (already in .env.example)

The harness halts with actionable error messages for each missing piece. Per D-30: NO platform-admin escape hatch; bootstrap MUST be done.

## Next Phase Readiness

**Wave 2 single-plan dispatch complete — Plan 5 hits HALT (`halt_after: true`).** Wave 1 (Plans 1/2/3/4) + Wave 2 (Plan 5) integration complete. The harness is ready to run end-to-end against Vercel preview URLs once setup steps 1-7 above are complete.

**Wave 3 (Plan 6 — GitHub Actions workflow) ready to dispatch when Jake confirms Wave 2 review is complete.**

### Plan 6 dispatch contract

When orchestrator dispatches Plan 6:

- **Plan 6 sets up GitHub Actions secrets:** `VERCEL_TOKEN` (already configured per SETUP-COMPLETE.md item #29), `ANTHROPIC_API_KEY` (already configured per SETUP-COMPLETE.md item #28), `HARNESS_FIXTURE_PASSWORD` (NEW for Plan 5 — Plan 6 must add this secret), `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` (these are already set as env vars per CLAUDE.md deployment requirements).
- **Plan 6 invokes the harness CLI:** `npx tsx scripts/verify-phase.ts --phase ${PHASE} --commit-sha ${GITHUB_SHA}` (no `--preview-url` — REST API discovery resolves automatically). Plan 6 parses the JSON `{kind: 'harness-report', report}` line from stdout for PR comment + commit status.
- **Plan 6 PR comment renders the SANITIZED final.md** (per ITER-1 C5 + D-30). Plan 5 report-writer.ts already sanitizes; Plan 6 just renders. NO additional sanitization needed in Plan 6.
- **Plan 6 exit-code mapping:** 0 → PR check pass; 1 → PR check fail (fail-fixable; future Plan 8b runLoop spawns gsd-fix-executor); 2 → PR check fail with halt-for-Jake annotation (fail-ambiguous); 3 → PR check fail with discovery-failed annotation; 4 → PR check fail with runtime-error annotation.
- **Plan 6 timeout:** harness completes < 5 min for a single-route phase per EXPANDED-SCOPE §7. Plan 6 timeout should be 7-10 min to give buffer.

### Plan 8b dispatch contract (Wave 4)

When orchestrator dispatches Plan 8b runLoop:

- **Plan 8b imports `runHarness` + `HarnessReport`** from `src/lib/verification/orchestrator.ts`.
- **Plan 8b owns the state machine:** imports `initialState`, `transition`, `MAX_ITERATIONS`, `LoopState` from `src/lib/verification/state-machine.ts` (Plan 1). Reads `HarnessReport.results_by_layer` Layer N FAILs to drive `failed-fixable` transition; reads `HarnessReport.ambiguous_results` to drive `failed-ambiguous` transition.
- **Plan 8b threads cost_cap_remaining_usd into Layer3Context** across iterations so iter-N is bounded across iterations (NOT multiplied 3×).
- **Per ARCH-CRIT-1:** Plan 8b `runLoop` is the ONLY consumer of state-machine.ts in the harness. Plan 5 orchestrator.ts and scripts/verify-phase.ts deliberately do NOT import. Verified by grep contract documented in orchestrator.test.md.

### Plan 9 dispatch contract (Wave 4)

When orchestrator dispatches Plan 9 calibration-log scaffolding:

- **Plan 9 nightwork-custodian post-ship sweep** parses `{kind: 'harness-report', report}` JSON line from stdout (writePerCommitReport emits this). Plan 9 populates `.planning/calibration-log.md` per-phase entries from HarnessReport fields: `duration_ms`, `total_vision_cost_usd`, `results_by_layer.layer1/2/3.length`, `ambiguous_results.length`.
- **HarnessReport schema is the frozen contract.** Plan 9 will not need to retrofit Plan 5's stdout shape.

## Self-Check: PASSED

### File existence
- `src/lib/verification/vercel-discovery.ts`: FOUND
- `src/lib/verification/criteria-loader.ts`: FOUND
- `src/lib/verification/auth-strategy.ts`: FOUND
- `src/lib/verification/orchestrator.ts`: FOUND
- `src/lib/verification/report-writer.ts`: FOUND
- `scripts/verify-phase.ts`: FOUND
- `src/lib/verification/orchestrator.test.md`: FOUND

### Commits
- `1cc868d`: FOUND (Task 1 — vercel-discovery + criteria-loader + auth-strategy)
- `547c00d`: FOUND (Task 2 — orchestrator + report-writer)
- `789fe35`: FOUND (Task 3 — verify-phase.ts + test fixture + README update)

### Architecture invariants
- ARCH-CRIT-1 grep contract: 0 SM imports in orchestrator.ts/verify-phase.ts ✓
- ARCH-CRIT-1 HarnessReport: 0 loop_state/halt_reason field declarations ✓
- ARCH-CRIT-3 + Type redefinition grep: 0 redefinitions of Layer1/2/3Context/VisionResult/LoopState in Plan 5 files ✓
- D-30 platform-admin grep: 0 service-role/platform-admin/admin.createUser in auth-strategy.ts ✓

### Quality bar
- npm run build: clean (Plan 5 introduces 0 new warnings) ✓
- npx tsc --noEmit (filtered for src/ + scripts/): 0 errors ✓
- system .githooks/pre-commit Drummond grep gate: exit 0 on every commit + final tree ✓
- ARCH-CRIT-2 smoke test: 141 non-N/A criteria from this phase's 12 PLANs (≥40 threshold met with 3.5x margin) ✓
