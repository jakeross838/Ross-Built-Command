---
phase: stage-1.5c-information-architecture
plan: 7
plan-name: canonical-docs-smoke
type: execute
status: not-started
wave: 4
depends_on: [3, 4, 5, 6]
autonomous: false
halt_after: true
threat_model_severity: low
requirements: []
files_modified:
  - .planning/architecture/ARCHITECTURE.md
  - .planning/architecture/ENTITY-INVENTORY.md
  - .planning/architecture/ROLES-CATALOG.md
  - .planning/architecture/INGESTION-ARCHITECTURE.md
  - .planning/CONTEXT.md
  - .planning/MASTER-PLAN.md
  - CLAUDE.md
  - .planning/design/PHILOSOPHY.md
  - .planning/design/PATTERNS.md
requires:
  - .planning/phases/stage-1.5c-information-architecture/CONTEXT.md
  - .planning/phases/stage-1.5c-information-architecture/PATTERNS.md
  - .planning/phases/stage-1.5c-information-architecture/AUDIT-LOG-STRATEGY.md (Plan 2 — REWRITTEN per iter-2 must-fix #5)
  - .planning/phases/stage-1.5c-information-architecture/01.5c-1-nav-today-redirects-SUMMARY.md (Plan 1)
  - .planning/phases/stage-1.5c-information-architecture/01.5c-2-thin-wrapper-architect-SUMMARY.md (Plan 2)
  - .planning/phases/stage-1.5c-information-architecture/01.5c-3-extraction-production-routes-SUMMARY.md (Plan 3)
  - .planning/phases/stage-1.5c-information-architecture/01.5c-4-section-placeholders-sub-portal-SUMMARY.md (Plan 4)
  - .planning/phases/stage-1.5c-information-architecture/01.5c-5-per-job-tabs-SUMMARY.md (Plan 5)
  - .planning/phases/stage-1.5c-information-architecture/01.5c-6-admin-platform-admin-SUMMARY.md (Plan 6)
  - .planning/expansions/stage-1.5c-information-architecture-EXPANDED-SCOPE.md
  - .planning/MASTER-PLAN.md (current — D-039 last decision; D-040..D-065 added in this plan per iter-2 amendments)
  - .planning/CONTEXT.md (current project-level)
  - CLAUDE.md (current — needs Bills/Pay Apps terminology + schema-aware audit-log language per iter-2 must-fix #5)
provides:
  - 4 NEW canonical docs at .planning/architecture/ (ARCHITECTURE.md / ENTITY-INVENTORY.md / ROLES-CATALOG.md / INGESTION-ARCHITECTURE.md) with iter-2 mechanical #6 column additions
  - 6 UPDATED docs (CONTEXT.md / MASTER-PLAN.md / CLAUDE.md / PHILOSOPHY.md / PATTERNS.md / COMPONENTS.md) with schema-aware audit-log language per iter-2 must-fix #5
  - **iter-2 mechanical #7 + iter-3 D-27 — atomic 10-doc commit (4 NEW + 6 UPDATED in single commit; COMPONENTS.md counts as 6th UPDATED per iter-3 D-27)**
  - **iter-2 mechanical #8 — atomic update idempotency guard (`grep -c 'D-040' MASTER-PLAN.md` before append; halt if >0)**
  - **iter-2 mechanical #2 — COMPONENTS.md §7 entry for NwPlaceholderCard per PROPAGATION-RULES.md §4b workflow (counted in 6 UPDATED total per iter-3 D-27)**
  - D-040..D-065 entries in MASTER-PLAN.md DECISIONS LOG (was D-040..D-056 in iter-1; +D-057..D-060 from nwrp45 iter-2 amendments; +D-061..D-065 from nwrp46 iter-3 amendments)
  - Final integration smoke (build + typecheck + hooks + privacy + redirects + 8-section nav + per-job tabs + Today + Bills/Pay Apps + 12 production routes + 60+ placeholder routes)
  - M3 phone walk readiness gate (per CONTEXT D-17 — bundled with 1.5b ship gate)
affects:
  - All future plan-phase + research-phase + execute-phase agents (canonical docs are read first)
  - All future commits referencing CLAUDE.md Architecture Rules (Bills/Pay Apps terminology + schema-aware audit-log canonical)
  - MASTER-PLAN.md DECISIONS LOG (D-040..D-065 added atomically)
  - 1.5b ship gate (1.5c lands first, then 1.5b ships with new IA in place per CONTEXT D-17)
  - iter-2 multi-tenant W-1 — Owner Portal F1 auth path constraint documented in ARCHITECTURE.md (or new OWNER-PORTAL-CUTOVER.md) for F1 inheritance
acceptance-criteria-target: 21 falsifiable items (was 16; +5 for iter-2 mechanical #2 + #6 + #7 + #8 + multi-tenant W-1)
estimated-duration: 1.25 days (was 1 day; +0.25 day for iter-2 column additions + atomic commit + idempotency + COMPONENTS.md NwPlaceholderCard entry)
must_haves:
  truths:
    - "ARCHITECTURE.md exists at .planning/architecture/ARCHITECTURE.md documenting 8 principles + data graph model overview + section purposes + revised phase plan + §SOC2 control mapping (CC6.1 / CC6.6 / CC6.7 / CC7.2 / PI1.1 / C1.1) per iter-2 mechanical #6"
    - "ENTITY-INVENTORY.md exists at .planning/architecture/ENTITY-INVENTORY.md cataloging 31+ entities with Retention class + PII? columns per iter-2 mechanical #6"
    - "ROLES-CATALOG.md exists at .planning/architecture/ROLES-CATALOG.md cataloging 15+ default roles with Cross-org? column per iter-2 mechanical #6"
    - "INGESTION-ARCHITECTURE.md exists at .planning/architecture/INGESTION-ARCHITECTURE.md describing multi-modal ingestion patterns + AI extraction patterns"
    - "Project-level .planning/CONTEXT.md updated with 8 principles canonical + role-aware as architectural requirement + knowledge graph as F1 deliverable"
    - "MASTER-PLAN.md DECISIONS LOG updated atomically: D-040..D-065 added (D-040..D-050 from nwrp43; D-051..D-056 from nwrp44; D-057..D-060 from nwrp45 iter-2 amendments per iter-2 mechanical #8 idempotency guard)"
    - "MASTER-PLAN.md CURRENT POSITION updated to reflect 1.5c shipping; NEXT PLANNED WORK rewritten with revised roadmap"
    - "CLAUDE.md updated: Bills/Pay Apps terminology added + schema-aware audit-log language per iter-2 must-fix #5 (entity_type stays 'invoice'/'draw'; UI maps via action-labels.ts; NOT 'bill_*' concat strings)"
    - "iter-2 D-22 / D-060: CLAUDE.md atomic update documents Admin dropdown vs Admin section overview precedence"
    - ".planning/design/PHILOSOPHY.md updated with Today as home pattern + role-aware filtering pattern + multi-modal ingestion pattern + at least 2 consumers per pattern (iter-2 design-pushback W-8)"
    - ".planning/design/PATTERNS.md updated with placeholder treatment patterns + role-filtered nav patterns + phase-aware sub-nav patterns + at least 2 consumers per pattern"
    - "iter-2 mechanical #2 — COMPONENTS.md §7 entry for NwPlaceholderCard per PROPAGATION-RULES.md §4b workflow (mirror NwButton/NwEyebrow/NwBadge §7 entries; design-pushback CRITICAL 2)"
    - "iter-2 mechanical #7 — atomic 10-doc commit: single commit covering all 4 NEW + 5 UPDATED docs together; AC-1.5c-7-12 verifies `git log -1 --stat` shows all 10 docs"
    - "iter-2 mechanical #8 — idempotency guard: `grep -c 'D-040' .planning/MASTER-PLAN.md` returns 0 BEFORE append; halt if >0 (entries already exist; investigate before re-appending)"
    - "Final integration smoke test: visit / (redirects to /today) → click each of 8 nav items → click Admin ▾ items → click Platform Admin badge → visit each of 12 production routes (with .design-system-scope wrap per D-19 visible) → visit each of 60+ placeholder routes → all render without 404 or console error"
    - "Privacy gate clean across all updated docs (32-token denylist returns 0 hits) — iter-2 security LOW SEC-6/7/8: explicit grep step on .planning/architecture/*.md scope"
    - "Build + typecheck pass; T10c silent across all production routes; tenant-blind silent; bouncy-easing silent; Site Office Forbidden tokens silent"
    - "M3 phone walk readiness gate documented: Jake walks new IA on iPhone+Safari per CONTEXT D-17"
    - "32 redirect rules from Plan 1 verified working via curl spot-checks (sample 5-10 representative legacy paths) including D-20 wildcard /admin/platform/:path*"
    - "All 8 nav sections + Admin ▾ + Platform Admin badge + Notifications bell + User menu visible + functional + role-gated correctly"
    - "iter-2 multi-tenant W-1 — Owner Portal F1 auth path constraint documented in ARCHITECTURE.md (or new OWNER-PORTAL-CUTOVER.md): F1 commits to ONE auth path (extending client_portal_access tokens OR introducing owner_view role with RLS) — not both"
    - "AC-1.5c-7-01 (iter-2 planner LOW-5 strengthened): grep verifies ALL 8 principles in ARCHITECTURE.md (Principle 1 through Principle 8)"
  artifacts:
    - path: ".planning/architecture/ARCHITECTURE.md"
      provides: "Canonical 8 principles + data graph model + section purposes + phase plan + §SOC2 control mapping per iter-2 mechanical #6"
      contains: "Principle 1"
    - path: ".planning/architecture/ENTITY-INVENTORY.md"
      provides: "31+ entity catalog table with Retention class + PII? columns per iter-2 mechanical #6"
      contains: "Retention class"
    - path: ".planning/architecture/ROLES-CATALOG.md"
      provides: "15+ default role catalog with Cross-org? column per iter-2 mechanical #6"
      contains: "Cross-org?"
    - path: ".planning/architecture/INGESTION-ARCHITECTURE.md"
      provides: "Multi-modal ingestion architecture + AI extraction patterns"
      contains: "Ingestion"
    - path: ".planning/MASTER-PLAN.md"
      provides: "Updated DECISIONS LOG with D-040..D-065 + revised CURRENT POSITION + revised NEXT PLANNED WORK"
      contains: "D-060"
    - path: "CLAUDE.md"
      provides: "Updated Architecture Rules + Tech Stack + Bills/Pay Apps terminology + schema-aware audit-log language per iter-2 must-fix #5"
      contains: "entity_type"
    - path: ".planning/design/COMPONENTS.md"
      provides: "Updated §7 with NwPlaceholderCard entry per iter-2 mechanical #2"
      contains: "NwPlaceholderCard"
  key_links:
    - from: ".planning/architecture/ARCHITECTURE.md"
      to: ".planning/architecture/ENTITY-INVENTORY.md + ROLES-CATALOG.md + INGESTION-ARCHITECTURE.md"
      via: "explicit hyperlinks in markdown"
      pattern: "(\\[ENTITY-INVENTORY\\]|ENTITY-INVENTORY.md)"
    - from: ".planning/MASTER-PLAN.md"
      to: ".planning/architecture/ARCHITECTURE.md (canonical) + 1.5c phase artifacts"
      via: "DECISIONS LOG references + CURRENT POSITION + NEXT PLANNED WORK"
      pattern: "D-040"
    - from: "CLAUDE.md"
      to: "ARCHITECTURE.md + AUDIT-LOG-STRATEGY.md (rewritten per iter-2 must-fix #5) + action-labels.ts"
      via: "Architecture Rules references"
      pattern: "AUDIT-LOG-STRATEGY"
---

# Plan 7 — canonical-docs-smoke (1.5c iter-3 Wave 4)

## iter-3 amendments (per Jake nwrp46 / NOTE-3 + W-2)

**NOTE-3 follow-up — Atomic commit title + doc count (per planner iter-2 + D-27):**

Plan 7 atomic commit includes 4 NEW (ARCHITECTURE.md / ENTITY-INVENTORY.md / ROLES-CATALOG.md / INGESTION-ARCHITECTURE.md) + 5 UPDATED (CONTEXT.md / MASTER-PLAN.md / CLAUDE.md / PHILOSOPHY.md / PATTERNS.md) + COMPONENTS.md §7 entry for NwPlaceholderCard (per iter-2 mechanical #2 + PROPAGATION-RULES.md §4b workflow). That's **10 docs total** (4 NEW + 6 UPDATED).

iter-3 update:
- Commit title: `docs(1.5c): atomic canonical-ref update — 4 NEW + 6 UPDATED per D-15/D-16/D-27`
- AC-1.5c-7-12 verifies `git log -1 --stat` shows all **10 docs** (4 NEW + 6 UPDATED including COMPONENTS.md)
- Idempotency guard (per iter-2 mechanical #8) unchanged: `grep -c 'D-040' .planning/MASTER-PLAN.md` halt-if->0

**W-2 follow-up — PATTERNS.md reconciliation (per design-pushback iter-2 + D-24):**

iter-2 amendments added a Site Office propagation paragraph to PATTERNS.md describing Plan 3 wrap. The original PATTERNS.md "Site Office direction inheritance" paragraph (iter-2 state lines 471-484) claimed "Production-route surfaces NOT under `/design-system/*` follow the same tokens via root globals.css. NO `data-direction='C'` attribute needed." Investigation per Q-iter2-1 (Jake nwrp46) revealed this was a misdiagnosis — production routes DO need `.design-system-scope` + `data-direction="C"` + design-system.css import to render Site Office C signatures.

iter-3 update to Plan 7 PATTERNS.md atomic update:
- **DELETE original "Site Office direction inheritance" paragraph** (PATTERNS.md lines 471-484 in iter-2 state). That paragraph was incorrect.
- **REWRITE iter-2 amendment paragraph (lines 552-553)** — iter-2 wording said `<div className="design-system-scope">` only (missing `data-direction` + `data-palette` attributes); iter-3 correction requires the playground-mirroring wrap. Replace iter-2 amendment text with:
  > "Production route wrappers in Plan 3 add an outer `<div data-direction=\"C\" data-palette=\"B\" className=\"design-system-scope\">` so Site Office direction-aware CSS rules at /design-system/design-system.css:84-153 propagate (1px slate-tile left-stamp, JetBrains Mono UPPERCASE eyebrows at 0.18em, Space Grotesk Medium 500 headlines, 16px compact card padding, 16px section gap, 150ms ease-out motion). The wrap MIRRORS the playground layout at src/app/design-system/prototypes/layout.tsx:39 — both `data-direction='C'` AND `data-palette='B'` attributes are required to activate the rules; the `.design-system-scope` class alone is INERT outside `/design-system/*` because the CSS rules are double-gated."
- **ADD a NEW "production activation chain" paragraph after the iter-2 rewrite:**
  > "**Production Site Office activation — LOAD-BEARING contract.** Production routes activate Site Office direction-aware CSS rules via TWO prerequisites that BOTH must be in place: (1) Root layout `src/app/layout.tsx` imports `@/app/design-system/design-system.css` so the rules bundle into all routes (per Plan 1 task / D-23). (2) Each production route wrapper uses `<div data-direction=\"C\" data-palette=\"B\" className=\"design-system-scope\">` mirroring the playground layout (per Plan 3 task / D-19). \n\n**DO NOT REMOVE the wrap or strip the attributes** — this is load-bearing on production. Removing the wrap or its attributes silently breaks Site Office C signatures (slate-tile left-stamp, 0.18em eyebrow tracking, 16px compact card padding, 16px section gap all become inert). The visual diff in Plan 3 watchpoint #1 was the iter-2 → iter-3 correction's enforcement point; future polish phases must preserve both prerequisites. \n\n**Production baseline (without the wrap)** already inherits ~70% of Site Office at root level via globals.css + colors_and_type.css + NwEyebrow inline `var(--…, fallback)` pattern (Inter body 15px, JetBrains Mono UPPERCASE eyebrow at 0.14em base, slate palette with light/dark theme, grain texture). The wrap closes the remaining 30% — the C-specific overrides. Stripping the wrap drops production back to the 70% baseline."

**MASTER-PLAN.md atomic update extended for D-23..D-27 (= MASTER-PLAN D-061..D-065):**

iter-2 plan locked D-040..D-056 + D-057..D-060 (16 decisions). iter-3 ADDS 5 more:
- **D-061** = D-23 (Decision A patch — wrap mirrors playground + design-system.css imported from root)
- **D-062** = D-24 (PATTERNS.md reconciliation per design-pushback W-2)
- **D-063** = D-25 (SEC-9 audit viewer Supabase client type verify)
- **D-064** = D-26 (SEC-10 production wrappers tenant-attribute grep verify)
- **D-065** = D-27 (atomic commit title 4 NEW + 6 UPDATED reflecting NwPlaceholderCard COMPONENTS.md §7 entry)

These 5 additions land in MASTER-PLAN.md DECISIONS LOG as part of Plan 7's atomic commit. Total D-decisions added by Stage 1.5c: D-040..D-065 = **26 decisions**.

---


<objective>
Atomic documentation update + final integration smoke for Stage 1.5c. Per CONTEXT D-15 + D-16 + iter-2 amendments: 4 NEW canonical docs at `.planning/architecture/` (ARCHITECTURE / ENTITY-INVENTORY / ROLES-CATALOG / INGESTION-ARCHITECTURE) with column additions per iter-2 mechanical #6 + 5 UPDATED docs (CONTEXT / MASTER-PLAN / CLAUDE / PHILOSOPHY / PATTERNS) with schema-aware audit-log language per iter-2 must-fix #5. All atomic — single 10-doc commit per iter-2 mechanical #7 + iter-3 D-27.

Per CONTEXT D-17: M3 phone walk = single walk bundled with 1.5b ship gate. Plan 7 SETS UP the readiness gate.

Per CONTEXT D-16: this is the LAST plan in Stage 1.5c. Halt for Jake review.

iter-2 differences from iter-1 (per Jake nwrp45 + plan-review iter-1 findings):

1. **iter-2 must-fix CRITICAL #5 — schema-aware audit-log language in CLAUDE.md (compliance W-3 doc location):** iter-1 plan would have written CLAUDE.md with "Audit-log action strings post-1.5c = bill_*" (concat framing — INCORRECT). iter-2 corrects: "Audit-log entity_type values continue to use 'invoice' (vendor bills) and 'draw' (builder pay-apps) per existing schema. UI components map these to 'Bill' / 'Pay App' labels via src/lib/audit/action-labels.ts." Compliance W-3 also addressed: AUDIT-LOG-STRATEGY.md location is auditor-invisible (phase folder); CLAUDE.md inline summary makes it canonical.

2. **iter-2 mechanical #6 — column additions per compliance C-2:**
   - **ENTITY-INVENTORY.md** adds "Retention class" column (forever / 30-day / tenant-deletion-lifecycle) + "PII?" column (yes/no, fields)
   - **ROLES-CATALOG.md** adds "Cross-org?" column (for platform-admin distinction)
   - **ARCHITECTURE.md** adds §SOC2 control mapping section: CC6.1 (logical access) / CC6.6 (boundary protection) / CC6.7 (restricted access) / CC7.2 (system monitoring) / PI1.1 (audit trail) / C1.1 (confidentiality)

3. **iter-2 mechanical #2 — NwPlaceholderCard COMPONENTS.md §7 entry:** Per PROPAGATION-RULES.md §4b workflow + design-pushback CRITICAL 2: when adding a new Nw<Role> primitive, the workflow REQUIRES a COMPONENTS.md §7 entry mirroring NwButton / NwEyebrow / NwBadge format. Plan 7 atomic update INCLUDES this entry.

4. **iter-2 mechanical #7 + iter-3 D-27 — atomic 10-doc commit** (per planner MEDIUM-1 + iter-3 NOTE-3): single commit covering all 4 NEW + 6 UPDATED docs together (was iter-1's split commits; COMPONENTS.md §7 counts as 6th UPDATED per iter-3 D-27). Title: `docs(1.5c): atomic canonical-ref update — 4 NEW + 6 UPDATED per D-15/D-16/D-27`. AC-1.5c-7-12 verifies `git log -1 --stat` shows all 10 docs.

5. **iter-2 mechanical #8 — idempotency guard** (per enterprise-r W-3): `grep -c 'D-040' .planning/MASTER-PLAN.md` BEFORE append; halt if >0 (entries already exist from a partial-execute or re-run; investigate before re-appending).

6. **iter-2 D-057..D-060 in MASTER-PLAN DECISIONS LOG:** D-040..D-056 (from nwrp43 + nwrp44) + NEW D-057..D-060 from nwrp45:
   - D-057 = Decision A (Site Office .design-system-scope wrapping for production routes)
   - D-058 = Decision B-modified (full /admin/platform → /platform-admin migration in 1.5c, including 13 sub-routes mounted)
   - D-059 = Decision C (PerJobTabs collapse to dropdown <768px)
   - D-060 = Decision D (Admin dropdown shortcut + /admin canonical destination, both surfaces with clear precedence)

7. **iter-2 multi-tenant W-1 — Owner Portal F1 auth path constraint documented:** ARCHITECTURE.md (or new OWNER-PORTAL-CUTOVER.md) documents: F1 commits to ONE auth path (extending client_portal_access tokens OR introducing owner_view role with RLS) — NOT both. CONTEXT D-08 misleadingly suggested owner_view role gate; iter-2 plans clarify 1.5c is structural Caldwell-fixture-only.

8. **iter-2 design-pushback W-8 — at least 2 consumers per new pattern:** Each new pattern in PHILOSOPHY.md / PATTERNS.md cites at least 2 consumers (e.g., "Today as home pattern" cites /today + future /pipeline-today + future /per-job-today; "Phase-aware sub-nav pattern" cites Plan 5 PerJobTabs + future client-portal phase-aware nav).

9. **iter-2 planner LOW-5 — strengthen AC-1.5c-7-01:** grep verifies ALL 8 principles in ARCHITECTURE.md (Principle 1 through Principle 8), not just "Principle 1".

10. **iter-2 enterprise-r W-4 — post-deploy curl checks:** verify block adds curl commands against Vercel preview URL (not just localhost) for 5 representative redirects.

11. **iter-2 security LOW SEC-6/7/8 — privacy gate scope on canonical .planning/architecture/*.md:** explicit grep step on .planning/architecture/*.md to ensure 32-token denylist clean.

Purpose:
- Lock 8 architectural principles + 31 entities + 15 roles + multi-modal ingestion patterns as canonical references for F1-F6 phases
- MASTER-PLAN.md atomic decisions log (D-040..D-065) preserves the audit trail
- CLAUDE.md schema-aware audit-log language ensures Strategy 1-prime is canonical (iter-2 must-fix #5)
- COMPONENTS.md §7 entry for NwPlaceholderCard satisfies PROPAGATION-RULES.md §4b
- iter-2 column additions to ENTITY-INVENTORY / ROLES-CATALOG / ARCHITECTURE encode compliance posture (SOC2 mapping, retention class, PII flagging, cross-org distinction)
- Final smoke verifies the 8-section IA works end-to-end with iter-2 fixes applied
- M3 phone walk gate hands off to /gsd-ship of 1.5b

Output:
- 4 NEW canonical docs (with iter-2 mechanical #6 columns)
- 5 UPDATED docs (atomic single commit per iter-2 mechanical #7; idempotency guard per #8)
- COMPONENTS.md §7 NwPlaceholderCard entry per iter-2 mechanical #2
- Final smoke report
- M3 phone walk readiness checklist
</objective>

<execution_context>
@C:/Users/Jake/nightwork-platform/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jake/nightwork-platform/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/stage-1.5c-information-architecture/CONTEXT.md
@.planning/phases/stage-1.5c-information-architecture/PATTERNS.md
@.planning/phases/stage-1.5c-information-architecture/AUDIT-LOG-STRATEGY.md
@.planning/expansions/stage-1.5c-information-architecture-EXPANDED-SCOPE.md
@.planning/CONTEXT.md
@.planning/MASTER-PLAN.md
@.planning/architecture/CURRENT-STATE.md
@.planning/architecture/TARGET.md
@.planning/architecture/GAP.md
@.planning/architecture/VISION.md
@.planning/architecture/CP1-RESOLUTIONS.md
@CLAUDE.md
@.planning/design/PHILOSOPHY.md
@.planning/design/PATTERNS.md
@.planning/design/SYSTEM.md
@.planning/design/COMPONENTS.md
@.planning/design/CHOSEN-DIRECTION.md
@.planning/design/PROPAGATION-RULES.md
@src/lib/audit/action-labels.ts
@src/lib/activity-log.ts

<interfaces>
4 NEW canonical docs (per CONTEXT D-15 + iter-2 mechanical #6 column additions):

1. .planning/architecture/ARCHITECTURE.md — 8 principles + data graph model + section purposes + phase plan + **§SOC2 control mapping section per iter-2 mechanical #6**:
   - CC6.1 logical access: middleware-level isPlatformAdmin gate + role-aware nav ACCESS map
   - CC6.6 boundary protection: tenant-blind primitives + getCurrentMembership() filter
   - CC6.7 restricted access: org_id RLS + platform_admins user_id-bounded SELECT
   - CC7.2 system monitoring: Sentry tags (user_id, org_id, platform_admin) per request
   - PI1.1 audit trail: activity_log entity_type + action separate columns; F1 expands
   - C1.1 confidentiality: Caldwell fixtures sanitized; F1 wires real data with RLS

2. .planning/architecture/ENTITY-INVENTORY.md — catalog table of 31+ entities + **Retention class column + PII? column per iter-2 mechanical #6**:
   - Retention class: forever (audit_log, jobs) / 30-day (transient drafts) / tenant-deletion-lifecycle (org_members on org delete) / per-record (varies)
   - PII?: yes (email/phone/SSN/W-9/license fields) / no

3. .planning/architecture/ROLES-CATALOG.md — catalog of 15+ default roles + **Cross-org? column per iter-2 mechanical #6**:
   - Cross-org? Yes for platform_admins (Jake/Andrew); No for org-scoped roles (Owner/PM/Accounting/Field/etc.)

4. .planning/architecture/INGESTION-ARCHITECTURE.md — multi-modal ingestion patterns

5 UPDATED docs (per CONTEXT D-16 + iter-2 amendments):

1. .planning/CONTEXT.md (project-level) — 8 principles canonical + role-aware as architectural requirement + knowledge graph as F1 deliverable

2. .planning/MASTER-PLAN.md — atomic update:
   - DECISIONS LOG: D-040..D-065 added per iter-2 mechanical #8 idempotency guard
   - D-040..D-050 from nwrp43 (top nav, Today, knowledge graph, etc.)
   - D-051..D-056 from nwrp44 (1.5c outcomes: Bills/Pay Apps rename, Sub Portal stubs, thin-wrapper extraction, mobile chrome, Today layout, structural TDs)
   - D-057..D-060 from nwrp45 iter-2 amendments:
     - D-057 = Site Office .design-system-scope wrapping (Decision A)
     - D-058 = Full /admin/platform → /platform-admin migration (Decision B-modified)
     - D-059 = PerJobTabs collapse to dropdown <768px (Decision C)
     - D-060 = Admin dropdown + Admin section overview precedence (Decision D)
   - CURRENT POSITION updated to "Stage 1.5c shipped"
   - NEXT PLANNED WORK rewritten with revised roadmap

3. CLAUDE.md (root) — Architecture Rules additions:
   - Bills/Pay Apps terminology context — schema-aware per iter-2 must-fix #5:
     "Bills (vendor invoices) and Pay Apps (builder→owner pay applications): terminology aligned with US construction industry per Stage 1.5c (D-051). Audit-log entity_type values continue to use 'invoice' (vendor bills) and 'draw' (builder pay-apps) per existing schema (src/lib/activity-log.ts:20-49). UI components map these to 'Bill' / 'Pay App' labels via src/lib/audit/action-labels.ts. F1 may extend the enum or migrate; until then dual vocabulary is the convention. See .planning/phases/stage-1.5c-information-architecture/AUDIT-LOG-STRATEGY.md for full rationale."
   - 8-section top nav reference + Admin dropdown vs /admin overview precedence per D-060
   - References to .planning/architecture/* canonical docs
   - Update Phase Roadmap to F1-F6 + Wave 1.1-Lite + Wave 1.1-Full per nwrp43 + D-049/050
   - Update Data Model section: invoice → bill terminology in entity descriptions

4. .planning/design/PHILOSOPHY.md — append (with at least 2 consumers per pattern per iter-2 W-8):
   - "Today as home pattern" (Principle 7) — consumers: /today, future /per-job-today
   - "Role-aware filtering pattern" (Principle 6) — consumers: 8-section nav ACCESS map, future per-job role visibility
   - "Multi-modal ingestion pattern" (Principle 5) — consumers: /jobs/[id]/punchlist (Plan 5), /jobs/[id]/daily-logs (Plan 5)
   - "Profile-based feature flags pattern" (Principle 8) — consumers: /admin/profile (Plan 6 stubbed), F2 nav filtering

5. .planning/design/PATTERNS.md — append (with at least 2 consumers per pattern per iter-2 W-8):
   - "Placeholder treatment pattern" (NwPlaceholderCard) — consumers: ~50 placeholder routes (Plans 4/5/6)
   - "Role-filtered nav pattern" (8-section ACCESS map) — consumers: nav-bar.tsx top nav (Plan 1), future PerJobTabs role filter (F2)
   - "Phase-aware sub-nav pattern" (PerJobTabs) — consumers: /jobs/[id]/* layout (Plan 5), future client-portal phase-aware nav
   - "8-item top nav pattern" — consumers: nav-bar.tsx (Plan 1)
   - "Sub Portal 3-stub pattern" — consumers: /sub-portal/* (Plan 4), future F3 magic link infrastructure

iter-2 mechanical #2 — COMPONENTS.md §7 entry for NwPlaceholderCard:

```markdown
### NwPlaceholderCard

**Purpose:** Reusable placeholder card for routes whose real backend ships in a future phase. Eyebrow + headline + body + Wave badge.

**Path:** `src/components/nw/NwPlaceholderCard.tsx`

**Tenant-blind:** Accepts no `org_id` / `membership` / `vendor_id` props.

**Props:**
- `eyebrow: string` (Section · Subsection)
- `headline: string` (feature name)
- `body: string` (2-3 sentence description)
- `wave: PlaceholderWave` (F1 / F2 / F3 / F4 / F5 / F6 / Wave 1.1-Lite / Wave 2 / Wave 3 / Wave 4)
- `className?: string`

**Site Office tokens:**
- Card padding="md" (TD-25 fix)
- Headline fontWeight: 500 (TD-26 fix)
- Eyebrow tone="muted" UPPERCASE 0.18em
- Body text-secondary
- Badge variant="neutral" "Coming {wave}"

**Anti-patterns:**
- Do NOT inline placeholder JSX in pages — always use NwPlaceholderCard
- Do NOT use Card padding="lg"
- Do NOT use fontWeight: 600

**Consumers (~50 routes):**
- All Plan 4 placeholder routes (Pipeline / Financials placeholders / Price Intel placeholders / People / Sub Portal stubs / Company / Reports)
- All Plan 5 per-job placeholder routes (~19 More-dropdown items)
- All Plan 6 admin placeholder routes (8 placeholders)
```

Per CLAUDE.md "Stage 1.5a design documents are the authoritative sources" — ensure updates DO NOT contradict SYSTEM.md, COMPONENTS.md (existing primitives), CHOSEN-DIRECTION.md, PROPAGATION-RULES.md.

Final smoke per CLAUDE.md "Testing Rule (MANDATORY)":
- Build + typecheck + hooks (T10c, tenant-blind, bouncy-easing, post-edit Forbidden tokens) silent
- Privacy gate clean (32-token denylist returns 0 hits) — iter-2 security LOW SEC-6/7/8: explicit grep step
- Visual walk: every nav item + every redirect + every per-job tab renders without 404 or console error
- Mobile responsive at 360px viewport (iPhone Safari ready) — iter-2 D-21 PerJobTabs dropdown verified
- M3 phone walk readiness checklist for Jake

M3 phone walk per CONTEXT D-17 (Q13=A):
- Same iPhone+Safari device per 1.5b CONTEXT D-31 (locked)
- Jake walks 1.5b prototypes AND 1.5c production routes (same components, different paths) in one phone session
- Walk happens AFTER 1.5c executes
- 1.5b ships AFTER walk passes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 NEW canonical docs at .planning/architecture/ with iter-2 mechanical #6 column additions</name>
  <files>.planning/architecture/ARCHITECTURE.md, .planning/architecture/ENTITY-INVENTORY.md, .planning/architecture/ROLES-CATALOG.md, .planning/architecture/INGESTION-ARCHITECTURE.md</files>

  <read_first>
    - .planning/architecture/CURRENT-STATE.md (existing — format + tone reference)
    - .planning/architecture/TARGET.md (existing — format reference)
    - .planning/architecture/CP1-RESOLUTIONS.md (existing — decision-doc format)
    - .planning/expansions/stage-1.5c-information-architecture-EXPANDED-SCOPE.md (Part 1 8 principles + Part 3 section breakdowns + Part 6 phase plan + iter-2 amendments)
    - .planning/phases/stage-1.5c-information-architecture/CONTEXT.md (D-01..D-22 referenced)
    - CLAUDE.md "Data Model — Full Entity Map" section (existing entity descriptions for ENTITY-INVENTORY.md)
    - .planning/phases/stage-1.5c-information-architecture/AUDIT-LOG-STRATEGY.md (Plan 2 — iter-2 must-fix #5 schema-aware framing for ARCHITECTURE.md SOC2 PI1.1 mapping)
  </read_first>

  <action>
**File 1 — `.planning/architecture/ARCHITECTURE.md` (with iter-2 mechanical #6 §SOC2 control mapping):**

Structure:

```markdown
# Architecture (canonical)

**Locked:** 2026-05-04 (Stage 1.5c)
**Source:** Jake nwrp43 + 1.5c discuss-phase + plan-phase outputs + nwrp44 amendments + nwrp45 iter-2 amendments

## 8 Principles (canonical)

### Principle 1 — Data-first foundation
Every event in the lifecycle of a construction company is a data point. Every entity is queryable. Reports are emergent (queries against the graph), not hand-built features. The data graph is the moat.

### Principle 2 — Knowledge graph data model
F1+ phases must build a relational entity-relationship knowledge graph. See [ENTITY-INVENTORY.md](./ENTITY-INVENTORY.md) for the 31+ entity catalog.

### Principle 3 — Cost Intelligence is BOTH a layer AND destinations
Price Intel surfaces inline + as its own primary nav section.

### Principle 4 — Cross-pollination of data on every ingestion event
ONE event, MULTIPLE entities updated.

### Principle 5 — Aggressive ingestion across modalities
See [INGESTION-ARCHITECTURE.md](./INGESTION-ARCHITECTURE.md).

### Principle 6 — Role-aware everything
See [ROLES-CATALOG.md](./ROLES-CATALOG.md) for 15+ default roles.

### Principle 7 — Today as personal home screen
Dashboard renamed Today.

### Principle 8 — External access via magic links + universal applicability
Subs/architects/inspectors/owners act without full accounts.

## Top nav structure

8 sections: Today | Pipeline | Jobs | Financials | Price Intel | People | Company | Reports
Plus: Admin ▾ dropdown (per D-060 power-user shortcut), Platform Admin badge, Notifications bell, User menu.

Per D-060: Admin dropdown is power-user shortcut; /admin section overview Card grid is canonical destination.

## Per-job sub-nav

Primary tabs: Overview | Schedule | Budget | Selections | Bills | Pay Apps | More ▾
On viewports <768px (per D-059): collapses to single dropdown.

More ▾: Documentation (Plans/Specs/Photos/Documents) + Coordination (RFIs/Submittals/Change Orders/Daily Logs) + Tasks (To-Dos/Punchlist) + Time + Permits + People + Activity + Closeout (phase-aware) + Warranty (phase-aware) + Pre-Con (phase-aware).

## Site Office direction scope (per D-057)

Production routes wrap outer container in `.design-system-scope` className. Extends Site Office direction-aware CSS rules from playground-only to production consumer (1px slate-tile left-stamp, JetBrains Mono UPPERCASE eyebrows, Space Grotesk Medium headlines, 150ms ease-out motion).

## Revised phase plan

(Reproduce EXPANDED-SCOPE.md Part 6 — F1 → F2 → F3 → F4 → F5 → F6 → Wave 1.1-Lite → Wave 2 → Wave 3 → Wave 4.)

## SOC2 control mapping (iter-2 mechanical #6)

Stage 1.5c is structural — no new auth surfaces; preserves existing posture per CLAUDE.md "Multi-tenant RLS is non-negotiable" + "Platform admin (cross-tenant)" sections.

| SOC2 Control | Description | Implementation |
|--------------|-------------|----------------|
| **CC6.1** Logical access | Authentication + authorization gates | Middleware isPlatformAdmin gate at `/admin/platform/*` + `/platform-admin/*` (per iter-2 must-fix #4 LOCKS Option A); 8-section nav ACCESS map role-gated; getCurrentMembership() filters every API query (per CLAUDE.md "Every API route uses getCurrentMembership() before DB access"). |
| **CC6.6** Boundary protection | Tenant isolation | Tenant-blind primitives in `src/components/ui/` accept NO `org_id` / `membership` / `vendor_id` props (hook enforced); RLS at every query layer; Caldwell fixtures sanitized per 1.5b SUBSTITUTION-MAP.md. |
| **CC6.7** Restricted access | Audit + scoping | `platform_admins` table SELECT policy is user_id-bounded (cross-org by design per migration 00048); org_id RLS on every tenant table; iter-2 must-fix #4 confirms /platform-admin/* gates non-platform_admin redirected. |
| **CC7.2** System monitoring | Observability | Sentry tags `user_id`, `org_id`, `platform_admin` set in middleware per request; Sentry filtered queries enable cross-org investigation by platform_admins. |
| **PI1.1** Processing integrity (audit trail) | Change tracking | activity_log table: entity_type + action separate columns (per src/lib/activity-log.ts:20-49); status_history JSONB on workflow entities (per CLAUDE.md "Soft delete + status_history JSONB"); src/lib/audit/action-labels.ts maps DB enum to UI labels (Strategy 1-prime per AUDIT-LOG-STRATEGY.md). F1 may extend enum or migrate. |
| **C1.1** Confidentiality | Data classification | See ENTITY-INVENTORY.md "Retention class" + "PII?" columns per iter-2 mechanical #6. |

## Owner Portal F1 auth path constraint (iter-2 multi-tenant W-1)

CONTEXT D-08's "owner_view role gate" claim is misleading. iter-2 clarification:

- 1.5c is structural — Owner Portal at `/owner-portal/*` mounts thin-wrapper components with Caldwell fixtures (Plan 3). NO real auth flow exercised in 1.5c.
- F1 wires real backend. **F1 commits to ONE auth path:**
  - **Path A:** Extend `client_portal_access` token model (SHA-256 + service-role-API + anon-grant per migration 00074)
  - **Path B:** Introduce `owner_view` member role with RLS for owner-scoped reads
- **NOT both.** F1 picks one; the other is rejected by-construction.

## Decisions reference

D-040..D-065 in [MASTER-PLAN.md DECISIONS LOG](../MASTER-PLAN.md). 1.5c phase decisions D-01..D-22 in [stage-1.5c-information-architecture/CONTEXT.md](../phases/stage-1.5c-information-architecture/CONTEXT.md).

D-057..D-060 (iter-2):
- D-057 = Site Office .design-system-scope wrapping
- D-058 = Full /admin/platform → /platform-admin migration
- D-059 = PerJobTabs collapse to dropdown <768px
- D-060 = Admin dropdown + section overview precedence
```

Target ~10-12 KB.

**File 2 — `.planning/architecture/ENTITY-INVENTORY.md` (with iter-2 mechanical #6 Retention class + PII? columns):**

```markdown
# Entity Inventory (canonical)

**Locked:** 2026-05-04 (Stage 1.5c)
**Source:** Principle 2 (.planning/architecture/ARCHITECTURE.md) + nwrp43 entity list + iter-2 mechanical #6

## 31+ Entities (with Retention class + PII? columns)

| Entity | Description | F-N when shipped | Relationships | Retention class | PII? |
|--------|-------------|------------------|---------------|-----------------|------|
| Jobs | Universal parent | Wave 1 (existing) | →Vendors / Cost Codes / Pay Apps / etc. | tenant-deletion-lifecycle | yes (client_name, client_email, client_phone, address) |
| Vendors | Subs + suppliers | Wave 1 (existing) | →Bills / Lien Releases / Performance | tenant-deletion-lifecycle | yes (email, phone, address; W-9 if stored) |
| Clients | Homeowners + commercial | F1 | →Jobs / Pay Apps approval | forever (audit trail) | yes (name, email, phone, address) |
| Employees | Internal team | F2 (extends profiles + org_members) | →Roles / Time / Activity | tenant-deletion-lifecycle | yes (email, phone, comp data, SSN if stored) |
| Products | Material catalog (Selections VIEW) | F5 | →Vendors / Cost Codes | per-record | no |
| Cost Codes | 5-digit AIA categories | Wave 1 (existing) | →Budget Lines / Bills | per-record | no |
| Bills (vendor invoices) | What GC RECEIVES | Wave 1 (existing — renamed in 1.5c per D-051) | →Vendors / Jobs / Cost Codes / POs / COs / Pay Apps | forever (audit trail; activity_log preserves) | no (financial; no PII unless invoice mentions individual W-9) |
| Pay Apps (builder→owner) | What GC SENDS | Wave 1 (existing — renamed in 1.5c per D-051) | →Jobs / Pay App Line Items / Lien Releases | forever | no |
| Purchase Orders | Job-scoped vendor commitments | Wave 1 (existing) | →Vendors / Jobs / Cost Codes / COs | forever | no |
| Time Entries | Employees clock in | F4 | →Employees / Jobs / Cost Codes / Pay Apps | tenant-deletion-lifecycle | yes (employee identity, geo if GPS) |
| Equipment | Vehicles + tools | F4 | →Equipment Usage | tenant-deletion-lifecycle | no |
| Equipment Usage | Per-job time + cost | F4 | →Equipment / Jobs / Cost Codes | forever | no |
| Expenses | Non-job + corporate | F4 | →Cost Codes / Receipts | forever | varies (receipt may include cardholder name) |
| Change Orders | PCCO log entries | Wave 1 (existing) | →Jobs / Bills / POs / Pay Apps | forever | no |
| Lien Releases | Florida 4-statute types | Wave 1 (existing — partial; F1 fixes R.7) | →Vendors / Bills / Pay Apps | forever (legal record) | yes (vendor signatory name) |
| Documents | Plans/specs/contracts/lien releases | Wave 2 | →Jobs / Vendors / Type discriminator | forever (typically) | varies (contracts have signatory PII) |
| Photos | Categorized + tagged | Wave 2 | →Jobs / Rooms / Vendors | tenant-deletion-lifecycle | varies (people-in-photos) |
| Schedule Items | Gantt timeline | Wave 2 | →Jobs / Predecessors / Vendors | tenant-deletion-lifecycle | no |
| Daily Logs | Voice/video AI extraction | Wave 2 | →Jobs / Photos / Punchlist | tenant-deletion-lifecycle | varies (voice transcripts may name people) |
| RFIs | Sub responds via magic link | Wave 2/3 | →Jobs / Subs / Submittals | forever (audit) | yes (sub identity, response content) |
| Submittals | Sub deliverable docs | Wave 2/3 | →Jobs / Subs / Architects | forever | varies |
| Punchlist Items | Voice/video punchlist | Wave 2 | →Jobs / Rooms / Products / Vendors / To-Dos | tenant-deletion-lifecycle | varies |
| To-Dos | Job-scoped tasks | Wave 2 | →Jobs / Employees / Punchlist | tenant-deletion-lifecycle | no |
| Activities | Event log (existing activity_log) | Wave 1 (existing) | →ALL entities (audit trail) | **forever** (audit-log-preservation rule per CLAUDE.md soft-delete + status_history) | varies (logs entity_id which may indirectly tie to PII) |
| Permits | Building permits + inspections | Wave 2/3 | →Jobs / Permit categories | forever (legal record) | varies |
| Insurance Policies | Corporate liability | Wave 2 | →Vendors (sub COIs) / Compliance | forever | yes (policy holder identity) |
| Tax Records | W-9s + 1099s | Wave 4 | →Vendors / Employees / Jobs | forever (legal/tax record) | yes (TIN/SSN) |
| Selections | Picked → spec → ordered → delivered → installed → warranty | Wave 4 | →Products / Jobs / POs / Vendors | tenant-deletion-lifecycle | no |
| Estimates | Price Intel powered | Wave 4 | →Jobs (pre-con) / Cost Codes | tenant-deletion-lifecycle | varies |
| Proposals | E-sign proposals | Wave 1 (existing) | →Jobs / Clients / Contracts | forever (legal record) | yes (signatory identity) |
| Contracts | Owner contracts + sub master | Wave 4 | →Clients / Jobs / Vendors | forever | yes (signatory identity) |
| Bids | Sub bids in flight | Wave 4 | →Jobs / Vendors / Estimates | tenant-deletion-lifecycle | no |
| client_portal_access | Owner/sub magic links (migration 00074) | existing | →Jobs (scope-bound) | per-record (token expiry) | no (SHA-256 hashed token) |
| platform_admins | Cross-org operator gate (migration 00048) | existing | →auth.users (user_id-bounded) | per-record | no (operator identity tied to auth.users only) |

Legend:
- **Retention class:**
  - `forever` — never deleted; audit-trail or legal record
  - `tenant-deletion-lifecycle` — deleted on org delete (cascade); preserved while org active
  - `per-record` — varies (e.g., token expiry; soft-deleted)
- **PII?** — flagged where personally identifiable information is stored (yes/no/varies)
```

**File 3 — `.planning/architecture/ROLES-CATALOG.md` (with iter-2 mechanical #6 Cross-org? column):**

```markdown
# Roles Catalog (canonical)

**Locked:** 2026-05-04 (Stage 1.5c)
**Source:** Principle 6 + nwrp43 role list + iter-2 mechanical #6

## 15+ Default Roles (F2 wires)

| Role | Scope | Typical user | F-N when wired | Cross-org? |
|------|-------|--------------|----------------|------------|
| Owner | Full company access; final approval | Founder / CEO (Lee Ross at RB) | Existing (1.5c) | No (org-scoped) |
| Admin | Full operational access | Director of Construction (Jake/Andrew) | Existing (1.5c) | No (org-scoped) |
| GM | Operations oversight | General Manager | F2 | No |
| Sr PM | Multi-job approvals | Senior Project Manager | F2 | No |
| PM | Assigned-job ownership | Project Manager (6 PMs at RB) | Existing (1.5c — limited) | No |
| Estimator | Price Intel + estimates | Estimating staff | F2 | No |
| Accountant | Bills QA + Pay Apps + Lien Releases | Diane at RB | Existing (1.5c — limited) | No |
| Foreman | Field crew lead | Field foreman | F2 | No |
| Field | Time + photos | Field staff | F2 | No |
| Designer | Selections + plans + specs | Interior designer | F2 | No |
| Sub Foreman | Sub crew lead | Sub crew lead | F2 (sub portal F3) | No (sub-scoped) |
| Sub Bookkeeper | Sub bills + lien releases | Sub accounting | F2 (sub portal F3) | No (sub-scoped) |
| Owner / Homeowner | Cost-plus open-book transparency | Homeowner | Existing (1.5c — limited via owner_view; F1 commits to one path) | No (job-scoped via client_portal_access OR member role) |
| Architect / External | RFIs / submittals / plan review | External architect | F3 (magic link) | No (magic link scope-bound) |
| Inspector | Read-only public link | Building inspector | F3 (public link) | No (public link scope-bound) |
| Banker | Construction-loan draw review | Loan officer | Wave 4 | No (job-scoped) |
| **platform_admin** | **Cross-tenant operations** | **Jake / Andrew (operator staff)** | **Existing (migration 00048)** | **YES** (cross-org by design; user_id-bounded SELECT) |

iter-2 mechanical #6: Cross-org? column flags platform_admin as the only Cross-org role. All other roles are org-scoped via RLS + getCurrentMembership() filters.

## Profile-based feature flags (Principle 8 — F2 wires)

- Custom Builder: Pipeline prominent, Pre-Con visible, RFIs/Submittals lighter
- Remodeler: Pipeline lighter (no Bids out), Pre-Con visible, RFIs/Submittals lighter
- Commercial GC: Pipeline visible, RFIs/Submittals prominent, schedule visible at top level

Ross Built profile = Custom Builder (default).

## Permission scopes (F2 wires the matrix)

(Reference TBD per F2; placeholder catalogue here.)

| Scope | Description |
|-------|-------------|
| read.bills.own | Read bills assigned to me / my jobs |
| read.bills.all | Read all bills in the org |
| approve.bills.own | Approve bills on my jobs |
| approve.bills.all | Approve any bill |
| push.qb | Push approved bills to QuickBooks |
| (etc — F2 expands) |
```

**File 4 — `.planning/architecture/INGESTION-ARCHITECTURE.md`:**

(Same as iter-1 — no iter-2 changes; multi-modal ingestion patterns per Principle 5. Target ~5-8 KB.)

**Token discipline for all 4 docs:** Markdown only; no hardcoded code blocks with hex; no privacy-gate violations (32-token denylist) — iter-2 security LOW SEC-6/7/8 explicit grep step in verify.
  </action>

  <verify>
    <automated>test -f .planning/architecture/ARCHITECTURE.md &amp;&amp; test -f .planning/architecture/ENTITY-INVENTORY.md &amp;&amp; test -f .planning/architecture/ROLES-CATALOG.md &amp;&amp; test -f .planning/architecture/INGESTION-ARCHITECTURE.md &amp;&amp; grep -c 'Principle 1' .planning/architecture/ARCHITECTURE.md &amp;&amp; grep -c 'Principle 8' .planning/architecture/ARCHITECTURE.md &amp;&amp; grep -c 'Bills (vendor invoices)' .planning/architecture/ENTITY-INVENTORY.md &amp;&amp; grep -c 'Retention class' .planning/architecture/ENTITY-INVENTORY.md &amp;&amp; grep -c 'Cross-org?' .planning/architecture/ROLES-CATALOG.md &amp;&amp; grep -c 'SOC2' .planning/architecture/ARCHITECTURE.md &amp;&amp; grep -c 'CC6.1\|CC6.6\|CC6.7\|CC7.2\|PI1.1\|C1.1' .planning/architecture/ARCHITECTURE.md &amp;&amp; grep -c 'Multi-modal' .planning/architecture/INGESTION-ARCHITECTURE.md</automated>
    Expected: all 4 files exist; greps return >=1 each.

    iter-2 planner LOW-5 strengthened — all 8 principles in ARCHITECTURE.md:
    `for i in 1 2 3 4 5 6 7 8; do grep -c "Principle $i" .planning/architecture/ARCHITECTURE.md; done`
    Expected: each grep returns >=1 (all 8 principles documented).

    iter-2 mechanical #6 column verifications:
    - ENTITY-INVENTORY.md: `grep -c 'Retention class\|tenant-deletion-lifecycle' .planning/architecture/ENTITY-INVENTORY.md` returns >=2
    - ENTITY-INVENTORY.md: `grep -c 'PII?\|yes (\|varies' .planning/architecture/ENTITY-INVENTORY.md` returns >=3
    - ROLES-CATALOG.md: `grep -c 'Cross-org?\|YES (cross-org by design)' .planning/architecture/ROLES-CATALOG.md` returns >=2
    - ARCHITECTURE.md: `grep -c '§SOC2\|SOC2 control mapping\|CC6.1\|CC7.2\|PI1.1' .planning/architecture/ARCHITECTURE.md` returns >=4

    iter-2 multi-tenant W-1 verification:
    - ARCHITECTURE.md: `grep -c 'Owner Portal F1\|Path A\|Path B\|client_portal_access\|owner_view role with RLS' .planning/architecture/ARCHITECTURE.md` returns >=3 (multi-tenant W-1 constraint documented)

    File size check: each doc 5-15 KB.

    Privacy gate (iter-2 security LOW SEC-6/7/8): `grep -rE 'Drummond|501 74th|Holmes Beach|SmartShield Homes|Florida Sunshine|Doug Naeher|Loftin Plumbing|ML Concrete|Banko|Paradise Foam|WG Drywall|CoatRite|Ecosouth|MJ Florida|Rangel Tile|TNT Painting|Avery Roofing|Island Lumber|Dewberry|Pou|Krauss|Duncan|Molinari|Markgraf|Harllee|Fish|Clark|Lee Worthy|Nelson Belanger|Bob Mozine|Jason Szykulski|Martin Mannix' .planning/architecture/ARCHITECTURE.md .planning/architecture/ENTITY-INVENTORY.md .planning/architecture/ROLES-CATALOG.md .planning/architecture/INGESTION-ARCHITECTURE.md | wc -l` returns 0.

    Internal hyperlinks: `grep -c '\[ENTITY-INVENTORY\]\|\[ROLES-CATALOG\]\|\[INGESTION-ARCHITECTURE\]\|\[MASTER-PLAN\]' .planning/architecture/ARCHITECTURE.md` returns >=3.
  </verify>

  <done>
    - 4 NEW canonical docs exist
    - iter-2 mechanical #6 column additions: Retention class + PII? in ENTITY-INVENTORY; Cross-org? in ROLES-CATALOG; §SOC2 in ARCHITECTURE
    - iter-2 multi-tenant W-1 constraint documented in ARCHITECTURE
    - iter-2 planner LOW-5: all 8 principles grep-verified
    - iter-2 security LOW SEC-6/7/8: privacy gate explicit grep
    - Cross-document hyperlinks work
    - Privacy gate clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Update 5 EXISTING docs ATOMICALLY (CONTEXT, MASTER-PLAN, CLAUDE, PHILOSOPHY, PATTERNS) + COMPONENTS.md NwPlaceholderCard §7 entry per iter-2 mechanical #2 — single 10-doc commit per iter-2 mechanical #7 + iter-3 D-27 + idempotency guard per iter-2 mechanical #8</name>
  <files>.planning/CONTEXT.md, .planning/MASTER-PLAN.md, CLAUDE.md, .planning/design/PHILOSOPHY.md, .planning/design/PATTERNS.md, .planning/design/COMPONENTS.md</files>

  <read_first>
    - .planning/CONTEXT.md (FULL file)
    - .planning/MASTER-PLAN.md (FULL file)
    - CLAUDE.md (FULL file)
    - .planning/design/PHILOSOPHY.md (FULL file)
    - .planning/design/PATTERNS.md (FULL file)
    - .planning/design/COMPONENTS.md (FULL file — verify existing §7 structure for NwButton/NwEyebrow/NwBadge entries; iter-2 mechanical #2 mirrors)
    - .planning/expansions/stage-1.5c-information-architecture-EXPANDED-SCOPE.md (Part 6 + Part 7 H + iter-2 amendments)
    - .planning/phases/stage-1.5c-information-architecture/AUDIT-LOG-STRATEGY.md (Plan 2 — schema-aware framing for CLAUDE.md update per iter-2 must-fix #5)
    - All 6 plan SUMMARY files from Plans 1-6
  </read_first>

  <action>
**iter-2 mechanical #8 — IDEMPOTENCY GUARD (run BEFORE any update):**

```bash
# Halt if D-040 already exists in MASTER-PLAN.md
COUNT=$(grep -c 'D-040' .planning/MASTER-PLAN.md)
if [ "$COUNT" -gt 0 ]; then
  echo "ERROR: D-040 already exists in MASTER-PLAN.md (count: $COUNT). Possible re-run or partial commit. HALT and investigate before re-appending."
  exit 1
fi
```

If guard halts, surface to Jake — likely a partial-execute in a previous run; investigate before continuing.

**Per CONTEXT D-16 + iter-2 mechanical #7: 6 docs (5 EXISTING + 1 COMPONENTS.md) UPDATED ATOMICALLY.** Single git commit at end of task.

**Update 1 — `.planning/CONTEXT.md` (project-level):**

Append (or restructure) to make canonical:
- 8 principles (with reference link to .planning/architecture/ARCHITECTURE.md)
- Role-aware as architectural requirement (with reference link to .planning/architecture/ROLES-CATALOG.md)
- Knowledge graph as F1 deliverable (with reference link to .planning/architecture/ENTITY-INVENTORY.md)
- 8-section top nav structure
- Per-job sub-nav structure
- Multi-modal ingestion architecture reference
- iter-2 amendments noted

DO NOT remove existing content. Add header band: "Updated 2026-05-04 (Stage 1.5c iter-2)".

**Update 2 — `.planning/MASTER-PLAN.md` (atomic D-040..D-065 append per iter-2 mechanical #8):**

Add D-040..D-065 to DECISIONS LOG. Format per existing entries:

```markdown
- **D-040** (2026-05-04): Top nav 8-section structure locked. Source: Stage 1.5c (nwrp43 Part 2).
- **D-041** (2026-05-04): Today as personal home (Principle 7). Source: Stage 1.5c.
- **D-042** (2026-05-04): Knowledge graph as F1+ data model. Source: Stage 1.5c.
- **D-043** (2026-05-04): Roles + permissions in F2. Source: Stage 1.5c.
- **D-044** (2026-05-04): Magic link external access in F3. Source: Stage 1.5c.
- **D-045** (2026-05-04): Aggressive multi-modal ingestion architecture (Principle 5). Source: Stage 1.5c.
- **D-046** (2026-05-04): Price Intel as both layer + destination (Principle 3). Source: Stage 1.5c.
- **D-047** (2026-05-04): Profile-based feature flags. F2 wires; 1.5c lands placeholder selector. Source: Stage 1.5c.
- **D-048** (2026-05-04): Phase-aware per-job nav. F2 wires column + computed view. Source: Stage 1.5c.
- **D-049** (2026-05-04): Wave 1.1 split into 1.1-Lite (RB usable) and 1.1-Full (later). Source: Stage 1.5c.
- **D-050** (2026-05-04): F1-F6 expanded scope vs original F1-F4. Source: Stage 1.5c.
- **D-051** (2026-05-04): 1.5c full Bills/Pay Apps terminology rename. Audit-log entity_type values continue 'invoice' / 'draw' per existing schema (iter-2 must-fix #5 schema-aware); UI maps via src/lib/audit/action-labels.ts. Source: Stage 1.5c (Q1=A).
- **D-052** (2026-05-04): 1.5c Sub Portal 3-stub structure with explicit F3 compliance contracts (per iter-2 mechanical #5). Source: Stage 1.5c (Q3=B).
- **D-053** (2026-05-04): 1.5c thin-wrapper extraction (12 prototype components → props-only Views). Source: Stage 1.5c (Q4=A).
- **D-054** (2026-05-04): 1.5c mobile chrome priority. Source: Stage 1.5c (Q5=A).
- **D-055** (2026-05-04): Today screen Wave-zero layout = Q6=C. Source: Stage 1.5c.
- **D-056** (2026-05-04): 1.5c structural TD fixes (TD-20/25/26) only. Source: Stage 1.5c (Q10=B-modified per nwrp44).

iter-2 amendments per nwrp45 (after plan-review iter-1 REVISE-PLAN):

- **D-057** (2026-05-05): Site Office direction scope — production routes wrap outer container in `.design-system-scope` className (Decision A). Extends scope's meaning from "playground-only" to "Site Office consumer." Preserves D-07 by construction. Source: Stage 1.5c iter-2 nwrp45.
- **D-058** (2026-05-05): Full /admin/platform → /platform-admin migration in 1.5c (Decision B-modified). Wildcard redirect /admin/platform/:path* → /platform-admin/:path* + mount existing 13 sub-route layouts at new path. Sub-routes are real, working, migrated. /platform-admin index stays placeholder. Source: Stage 1.5c iter-2 nwrp45.
- **D-059** (2026-05-05): PerJobTabs collapses to dropdown <768px (Decision C). Matches existing nav-bar mobile hamburger pattern. Renders 6 primary tabs + More dropdown on >=768px; collapses to single dropdown showing all tabs on <768px. Source: Stage 1.5c iter-2 nwrp45.
- **D-060** (2026-05-05): Admin dropdown vs /admin section overview both exist with clear precedence (Decision D). Admin dropdown is power-user shortcut (top-nav direct access to settings sub-items); /admin section overview Card grid is canonical destination (clicking "Admin" in main nav lands here). Source: Stage 1.5c iter-2 nwrp45.
- **D-061** (2026-05-05): Decision A patch (per iter-3 Q-iter2-1=a). Production routes activate Site Office C tokens via TWO prerequisites: (1) root layout `src/app/layout.tsx` imports `@/app/design-system/design-system.css` so the rules bundle into all routes; (2) production route wrappers use `<div data-direction="C" data-palette="B" className="design-system-scope">` mirroring the playground layout. Without BOTH, Site Office C-specific signatures (1px slate-tile left-stamp, 0.18em eyebrow tracking, 16px compact card padding, 16px section gap) are inert. Investigation outcome: production already inherits ~70% Site Office at root (Inter body, mono UPPERCASE eyebrow at 0.14em base, slate palette, grain texture); iter-3 closes the 30% C-specific gap. Source: Stage 1.5c iter-3 nwrp46.
- **D-062** (2026-05-05): PATTERNS.md "Site Office direction inheritance" paragraph (iter-2 state lines 471-484) DELETED — original claim that production routes inherit Site Office C from root globals.css with NO `data-direction='C'` attribute was a misdiagnosis (verified by iter-3 investigation). iter-2 amendment paragraph kept. New paragraph documents two-prerequisite activation chain. Source: Stage 1.5c iter-3 nwrp46 (design-pushback W-2 follow-up).
- **D-063** (2026-05-05): `/platform-admin/audit` migrated viewer Supabase client type explicitly verified + documented in Plan 6 SUMMARY. Source `src/app/admin/platform/audit/page.tsx` uses `createServerClient()` (anon + session cookie + RLS gate via `platform_admin_audit_staff_read` policy from migration 00048) — NOT service-role. Migrated `/platform-admin/audit/page.tsx` MUST preserve `createServerClient()` verbatim; swapping to service-role would bypass RLS on adjacent profiles+organizations queries. Source: Stage 1.5c iter-3 nwrp46 (security iter-2 SEC-9 follow-up).
- **D-064** (2026-05-05): Plan 3 verify ADDS defensive grep `grep -rE 'data-org-id|data-org-name|data-tenant' src/app/financials/ src/app/jobs/ src/app/owner-portal/ src/app/people/` returns 0 — confirms production wrappers don't introduce DOM tenant-context attributes (`.design-system-scope` is a CSS scope class only; `data-direction` + `data-palette` are presentation attributes, not tenant identifiers). Source: Stage 1.5c iter-3 nwrp46 (security iter-2 SEC-10 follow-up).
- **D-065** (2026-05-05): Plan 7 atomic commit title updated from "4 NEW + 5 UPDATED" to "4 NEW + 6 UPDATED" — COMPONENTS.md §7 NwPlaceholderCard entry counts as the 6th UPDATED doc per PROPAGATION-RULES.md §4b workflow. AC-1.5c-7-12 verifies `git log -1 --stat` shows all 10 docs. Source: Stage 1.5c iter-3 nwrp46 (planner iter-2 NOTE-3 follow-up).
```

Update CURRENT POSITION: "Stage 1.5c iter-2 shipped. 1.5b ship gate next (M3 phone walk bundled per D-17 / Q13=A)."

Update NEXT PLANNED WORK: Stage 1.5c → 1.5b ship → F1 → F2 → F3 → F4 → F5 → F6 → Wave 1.1-Lite → Wave 2 → Wave 3 → Wave 4.

**Update 3 — `CLAUDE.md` (root) — schema-aware audit-log per iter-2 must-fix #5:**

Architecture Rules section additions:

```markdown
**Bills (vendor invoices) and Pay Apps (builder→owner pay applications):** terminology aligned with US construction industry per Stage 1.5c (D-051). Audit-log entity_type values continue to use 'invoice' (vendor bills) and 'draw' (builder pay-apps) per existing schema (src/lib/activity-log.ts:20-49). UI components map these to 'Bill' / 'Pay App' labels via src/lib/audit/action-labels.ts. F1 may extend the enum or migrate; until then dual vocabulary is the convention. See .planning/phases/stage-1.5c-information-architecture/AUDIT-LOG-STRATEGY.md for full rationale (iter-2 must-fix CRITICAL #5 schema-corrected).

**Top nav structure** (per .planning/architecture/ARCHITECTURE.md): Today | Pipeline | Jobs | Financials | Price Intel | People | Company | Reports + Admin ▾ + Platform Admin badge.

**Admin dropdown vs /admin section overview** (per D-060): Admin dropdown is power-user shortcut (top-nav direct access to sub-items); /admin section overview Card grid is canonical destination. Clicking "Admin" label routes to /admin overview; ▾ affordance opens dropdown. Both surfaces simultaneously; no UX duplication.

**Site Office direction scope on production routes** (per D-057): production routes wrap outer container in `<div className="design-system-scope">`. Site Office direction-aware CSS rules at /design-system/design-system.css:84-153 are scoped to .design-system-scope; without wrap, `[data-slot="card"]` and `.nw-direction-headline` are inert outside.

**PerJobTabs mobile collapse** (per D-059): on viewports <768px, PerJobTabs collapses to single dropdown showing all tabs (matches existing nav-bar mobile hamburger).

**Platform Admin gate** (per D-009 + iter-2 must-fix #4): middleware.ts:65 regex matches both `/admin/platform/*` AND `/platform-admin/*` with same `!isPlatformAdmin` redirect posture.
```

Update Phase Roadmap section to F1-F6 + Wave 1.1-Lite + Wave 1.1-Full per nwrp43 + D-049/050.
Update Data Model section: invoice → bill terminology in entity descriptions; draw → pay-app.
Add references to new canonical docs (.planning/architecture/ARCHITECTURE.md etc.)

DO NOT modify existing CLAUDE.md sections that are unrelated.

**Update 4 — `.planning/design/PHILOSOPHY.md` (with iter-2 W-8 — at least 2 consumers per pattern):**

Append new patterns:

```markdown
### Today as home pattern (Principle 7)

Personal home screen replaces generic dashboard. Action-oriented; role-aware (F2). Cash Flow + Getting Started belong in /company/overview, not /today.

**Consumers:**
- /today (Plan 1) — Wave-zero org-wide queues + Your Day placeholder + Activity Feed
- Future /per-job-today screen (F2/Wave 1.1-Lite) — PM's daily action plan filtered by their assigned jobs

### Role-aware filtering pattern (Principle 6)

Same nav structure for all users; visibility filtered per role. Data-driven ACCESS map enables F2 expansion to 15 roles mechanically.

**Consumers:**
- nav-bar.tsx 8-section ACCESS map (Plan 1) — 4-role baseline; F2 swaps to 15-role
- Future PerJobTabs role filter (F2) — phase-aware tabs hidden per role (e.g., field workers see Daily Logs but not Bills)

### Multi-modal ingestion pattern (Principle 5)

Voice/video/photo input becomes structured data automatically; user does the work, system extracts structure.

**Consumers:**
- /jobs/[id]/punchlist (Plan 5 placeholder) — voice/video AI extraction; ONE 10-second comment generates 5+ database operations
- /jobs/[id]/daily-logs (Plan 5 placeholder) — same pattern

### Profile-based feature flags pattern (Principle 8)

Org type drives nav visibility (Custom Builder / Remodeler / Commercial GC).

**Consumers:**
- /admin/profile (Plan 6 stubbed selector) — Custom Builder / Remodeler / Commercial GC
- F2 nav filtering — Pipeline visibility per profile

DO NOT modify existing direction lock (Site Office + Set B per D-037) or any other Stage 1.5a content.
```

**Update 5 — `.planning/design/PATTERNS.md` (with iter-2 W-8):**

Append new patterns (each with at least 2 consumers):

```markdown
### Placeholder treatment pattern (NwPlaceholderCard)

**Consumers (~50 routes):**
- Plan 4 placeholder routes: Pipeline (5) + Financials placeholders (2) + Price Intel placeholders (6) + People (3) + Sub Portal stubs (3) + Company (11) + Reports (5) = ~35
- Plan 5 per-job placeholder routes (~19)
- Plan 6 admin placeholder routes (8)

### Role-filtered nav pattern (8-section ACCESS map)

**Consumers:**
- nav-bar.tsx top nav (Plan 1) — 8 nav items + Admin dropdown
- Future per-job role filter (F2) — phase-aware tabs filtered per role

### Phase-aware sub-nav pattern (PerJobTabs)

**Consumers:**
- /jobs/[id]/* layout (Plan 5) — phase: "active" hardcoded; F2 reads from jobs.phase
- Future client-portal phase-aware nav (F4) — owner sees different tabs per project phase

### 8-item top nav pattern

**Consumers:**
- nav-bar.tsx (Plan 1) — Today | Pipeline | Jobs | Financials | Price Intel | People | Company | Reports + Admin ▾ + Platform Admin badge

### Sub Portal 3-stub pattern

**Consumers:**
- /sub-portal/* (Plan 4) — auth + magic + public stubs with F3 compliance contracts
- Future F3 magic link infrastructure — extends client_portal_access pattern (migration 00074)
```

Verify the existing pattern catalogue (Document Review, List+Detail, etc.) preserved.

**Update 6 — `.planning/design/COMPONENTS.md` §7 NwPlaceholderCard entry per iter-2 mechanical #2:**

Append per `<interfaces>` block above (mirror NwButton / NwEyebrow / NwBadge format).

**Atomic commit posture (iter-2 mechanical #7):**

After all 6 updates land, the executor commits ALL 9 in a SINGLE git commit:

```bash
git add .planning/architecture/ARCHITECTURE.md \
        .planning/architecture/ENTITY-INVENTORY.md \
        .planning/architecture/ROLES-CATALOG.md \
        .planning/architecture/INGESTION-ARCHITECTURE.md \
        .planning/CONTEXT.md \
        .planning/MASTER-PLAN.md \
        CLAUDE.md \
        .planning/design/PHILOSOPHY.md \
        .planning/design/PATTERNS.md \
        .planning/design/COMPONENTS.md

git commit -m "docs(1.5c): atomic canonical-ref update — 4 NEW + 6 UPDATED per D-15/D-16/D-27

Per iter-2 mechanical #7 + iter-3 D-27: single commit covering all 10 docs.
Per iter-2 mechanical #6: ENTITY-INVENTORY adds Retention class + PII?; ROLES-CATALOG adds Cross-org?; ARCHITECTURE adds §SOC2 control mapping.
Per iter-2 mechanical #2: COMPONENTS.md §7 NwPlaceholderCard entry per PROPAGATION-RULES.md §4b (counts as 6th UPDATED doc per iter-3 D-27).
Per iter-2 must-fix CRITICAL #5: CLAUDE.md schema-aware audit-log language.
Per iter-3 D-23 + D-24: PATTERNS.md inheritance paragraph (lines 471-484) DELETED + activation chain documented (Plan 1 design-system.css root import + Plan 3 wrap data-direction=C data-palette=B).
D-040..D-065 added to MASTER-PLAN DECISIONS LOG (D-040..D-050 from nwrp43; D-051..D-056 from nwrp44; D-057..D-060 from nwrp45 iter-2; D-061..D-065 from nwrp46 iter-3).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

Note: 10 files in `git add` — counting COMPONENTS.md as one of the existing-doc updates (per iter-2 mechanical #2; mirror NwButton entry format adds new content to existing doc).

This atomic posture mitigates the risk that one doc gets out of sync with the others. If ANY of the 6 updates fail (e.g., privacy gate flag, lint failure), revert all 6 + investigate.
  </action>

  <verify>
    <automated>grep -c 'D-040' .planning/MASTER-PLAN.md &amp;&amp; grep -c 'D-060' .planning/MASTER-PLAN.md &amp;&amp; grep -c 'entity_type' CLAUDE.md &amp;&amp; grep -c 'Today as home' .planning/design/PHILOSOPHY.md &amp;&amp; grep -c 'Placeholder treatment' .planning/design/PATTERNS.md &amp;&amp; grep -c 'NwPlaceholderCard' .planning/design/COMPONENTS.md</automated>
    Expected: all greps return >=1.

    iter-2 mechanical #7 atomic posture verification:
    `git log -1 --stat` shows ALL 9 docs (or 10 with COMPONENTS.md): .planning/architecture/ARCHITECTURE.md + ENTITY-INVENTORY.md + ROLES-CATALOG.md + INGESTION-ARCHITECTURE.md + .planning/CONTEXT.md + .planning/MASTER-PLAN.md + CLAUDE.md + .planning/design/PHILOSOPHY.md + PATTERNS.md + COMPONENTS.md.

    iter-2 mechanical #8 idempotency guard verification:
    Pre-commit: `grep -c 'D-040' .planning/MASTER-PLAN.md` returns 0 BEFORE Plan 7 Task 2 starts.
    Post-commit: `grep -c 'D-040' .planning/MASTER-PLAN.md` returns >=1 (D-040 appended once; not duplicated).

    Privacy gate (iter-2 security LOW SEC-6/7/8): 32-token denylist returns 0 hits across all 9-10 updated docs.

    DECISIONS LOG count: D-040..D-065 = 26 new entries.
    `for i in $(seq -w 040 060); do grep -c "D-$i" .planning/MASTER-PLAN.md; done` — each returns >=1.

    iter-2 must-fix CRITICAL #5 schema-aware language verification in CLAUDE.md:
    - `grep -c 'entity_type' CLAUDE.md` returns >=1
    - `grep -c 'src/lib/audit/action-labels.ts' CLAUDE.md` returns >=1
    - `grep -c 'invoice (vendor bills)' CLAUDE.md` returns >=1
    - `grep -c 'bill_\\*' CLAUDE.md` returns 0 (concat framing NOT used per iter-2 must-fix #5 correction)

    iter-2 mechanical #2 NwPlaceholderCard COMPONENTS.md §7 entry:
    - `grep -c 'NwPlaceholderCard' .planning/design/COMPONENTS.md` returns >=1
    - `grep -c 'NwPlaceholderCardProps' .planning/design/COMPONENTS.md` returns >=1 (mirrors NwButton entry shape)

    iter-2 W-8 pattern consumers:
    - `grep -c 'Consumers' .planning/design/PHILOSOPHY.md` returns >=4 (one per new pattern)
    - `grep -c 'Consumers' .planning/design/PATTERNS.md` returns >=5 (one per new pattern)

    Visual smoke: read each updated doc fully — internal coherence (e.g., MASTER-PLAN's NEXT PLANNED WORK matches ARCHITECTURE.md's revised phase plan).
  </verify>

  <done>
    - 5 EXISTING docs updated atomically + COMPONENTS.md §7 NwPlaceholderCard entry
    - D-040..D-065 added to MASTER-PLAN.md (idempotency guard verified per iter-2 mechanical #8)
    - CLAUDE.md schema-aware audit-log language per iter-2 must-fix CRITICAL #5
    - PHILOSOPHY + PATTERNS new patterns each cite at least 2 consumers per iter-2 W-8
    - COMPONENTS.md §7 NwPlaceholderCard entry per iter-2 mechanical #2 + PROPAGATION-RULES.md §4b
    - iter-2 mechanical #7: atomic 10-doc (4 NEW + 6 UPDATED including COMPONENTS.md per iter-3 D-27) commit
    - iter-2 mechanical #6 columns reflected in ARCHITECTURE/ENTITY-INVENTORY/ROLES-CATALOG (Task 1)
    - Privacy gate clean
  </done>
</task>

<task type="auto">
  <name>Task 3: Final integration smoke + M3 phone walk readiness gate setup + iter-2 enterprise-r W-4 post-deploy curl checks</name>
  <files>(verification only — output is final smoke report appended to plan SUMMARY)</files>

  <read_first>
    - All Plan 1-6 SUMMARY files (verify acceptance criteria all met)
    - .planning/phases/stage-1.5c-information-architecture/CONTEXT.md (D-17 — M3 phone walk per Q13=A)
    - .planning/phases/stage-1.5b-prototype-gallery/CONTEXT.md D-31 (M3 device locked)
    - All 4 NEW + 6 UPDATED docs (per iter-3 D-27 — COMPONENTS.md §7 counts as 6th UPDATED) from Tasks 1 + 2
    - CLAUDE.md "Testing Rule (MANDATORY)"
  </read_first>

  <action>
**Final integration smoke (executor runs end-to-end):**

**Build + typecheck + hooks:**
1. `npm run build` — exits 0
2. `npx tsc --noEmit` — exits 0
3. Manually trigger post-edit hook on a sample file from each plan → all hooks silent
4. `git status` — clean working tree (or only Plan 7's atomic commit pending)

**Privacy gate (iter-2 security LOW SEC-6/7/8 explicit):**
5. `grep -rE 'Drummond|501 74th|...' src/ .planning/architecture/ .planning/CONTEXT.md .planning/MASTER-PLAN.md CLAUDE.md .planning/design/PHILOSOPHY.md .planning/design/PATTERNS.md .planning/design/COMPONENTS.md | wc -l` returns 0

**Redirect smoke (8 representative legacy paths + iter-2 D-20 wildcard sub-route):**
6. `curl -sI http://localhost:3000/dashboard` → 308, /today
7. `curl -sI http://localhost:3000/invoices` → 308, /financials/bills
8. `curl -sI http://localhost:3000/draws/d-caldwell-05` → 308, /financials/pay-apps/d-caldwell-05
9. `curl -sI http://localhost:3000/cost-intelligence/lookup` → 308, /price-intel/cost-lookup
10. `curl -sI http://localhost:3000/vendors` → 308, /people/vendors
11. `curl -sI http://localhost:3000/settings` → 308, /admin
12. `curl -sI http://localhost:3000/admin/platform` → 308, /platform-admin
13. `curl -sI http://localhost:3000/admin/platform/audit` → 308, /platform-admin/audit (iter-2 D-20 wildcard)
14. `curl -sI http://localhost:3000/operations` → 308, /today
15. `curl -sI http://localhost:3000/settings/billing` → 308, /admin/billing (iter-2 mechanical #4)

**Visual desktop walk (Chrome DevTools MCP):**
16. Visit / → redirects to /today; Today renders with 3 sections
17. Click each of 8 nav items → each renders without 404
18. Click Admin ▾ → 13 items visible; click each → renders without 404
19. Click "Admin" label (NOT ▾) → routes to /admin overview Card grid (per D-060)
20. Click Platform Admin badge (logged in as Jake) → /platform-admin renders directory; click each of 13 sub-routes → migrated functionality renders (iter-2 D-20)
21. Logout + login as PM → Platform Admin badge does NOT render; navigate to /platform-admin/audit → redirect (iter-2 watchpoint #3)
22. Visit /jobs/j-caldwell-1 → per-job sub-nav at desktop posture (6 primary tabs + More ▾); resize to 360px → mobile dropdown collapse per iter-2 D-21
23. Click each per-job primary tab → renders content
24. Click More ▾ → all sub-section items visible (Closeout shows; Warranty + Pre-Con hidden per phase=active)

**12 production routes spot-check (random sample of 4 with iter-2 D-19 wrap visible):**
25. /financials/bills/inv-caldwell-001 → Document Review pattern; .design-system-scope wrap visible in DOM
26. /financials/pay-apps/d-caldwell-05 → Document Review pattern with Pay App data; .design-system-scope wrap visible
27. /people/vendors → 17 vendors in List+Detail
28. /owner-portal → owner dashboard

**Mobile responsive walk (Chrome DevTools 360px):**
29. Hamburger menu opens (top nav)
30. All 8 nav items + Admin contents + User menu visible
31. Notifications bell + Platform Admin badge stay top-right
32. /jobs/j-caldwell-1 → PerJobTabs renders as single dropdown (iter-2 D-21)

**Console error check:**
33. No console errors on any of the routes visited above

**iter-2 enterprise-r W-4 — post-deploy Vercel preview curl checks:**

After `git push` triggers Vercel preview deploy, run the same 5 representative curls against the preview URL:
- `curl -sI https://<preview-url>/dashboard` → 308 to /today
- `curl -sI https://<preview-url>/invoices` → 308 to /financials/bills
- `curl -sI https://<preview-url>/admin/platform/audit` → 308 to /platform-admin/audit
- `curl -sI https://<preview-url>/settings/billing` → 308 to /admin/billing
- `curl -sI https://<preview-url>/cost-intelligence/lookup` → 308 to /price-intel/cost-lookup

Document results in SUMMARY (deployment failure mode mitigation).

**Document smoke results in plan SUMMARY:**

```
## Final Integration Smoke Report

### Build + typecheck + hooks
- npm run build: PASS
- typecheck (npx tsc --noEmit): PASS
- T10c hook: silent on production routes (Plan 2 architect Option A)
- Tenant-blind hook: silent on Views
- Bouncy-easing hook: silent
- Post-edit Forbidden tokens hook: silent

### Privacy gate (iter-2 security LOW SEC-6/7/8)
- 32-token denylist: 0 hits across src/ + .planning/architecture/ + 5 updated docs

### Redirects (10/10 PASS — including D-20 wildcard + iter-2 mechanical #4)
- /dashboard → /today: PASS
- /invoices → /financials/bills: PASS
- /admin/platform/audit → /platform-admin/audit: PASS (iter-2 D-20)
- /settings/billing → /admin/billing: PASS (iter-2 mechanical #4 — single hop, no loop)
- (etc — all 10 spot-checks)

### Visual desktop walk
- 8 nav items: all render
- Admin ▾ 13 items: all render
- Click "Admin" label routes to /admin overview Card grid (D-060)
- Platform Admin badge: gates correctly per role (iter-2 watchpoint #3)
- /platform-admin index + 13 migrated sub-routes: all render (iter-2 D-20 watchpoint #2)
- Per-job sub-nav: 6 primary + More ▾ render at desktop; mobile dropdown collapse (iter-2 D-21)
- 19 per-job placeholders: render
- 60+ section placeholders: render
- 12 production routes: render with .design-system-scope wrap (iter-2 D-19)

### Mobile responsive (360px)
- Top nav hamburger: PASS
- Notifications bell + Platform Admin badge: top-right pinned
- PerJobTabs: collapses to single dropdown (iter-2 D-21)

### Console errors: 0

### iter-2 enterprise-r W-4 post-deploy Vercel preview
- /dashboard → /today: PASS at preview URL
- /invoices → /financials/bills: PASS
- /admin/platform/audit → /platform-admin/audit: PASS
- /settings/billing → /admin/billing: PASS
- /cost-intelligence/lookup → /price-intel/cost-lookup: PASS
```

**M3 phone walk readiness gate setup:**

Per CONTEXT D-17 (Q13=A): single walk bundled with 1.5b ship gate. Same iPhone+Safari device per 1.5b CONTEXT D-31.

Plan 7 SETS UP the gate — produces a checklist for Jake:

```
## M3 Phone Walk Readiness Checklist

Device: iPhone (locked per 1.5b CONTEXT D-31). Safari browser.
URL: Vercel preview URL for the phase/1.5-c-ia-restructure branch.

Steps:
1. Visit / → /today renders
2. Click hamburger → all 8 nav items + Admin contents visible
3. Click Today, Jobs, Financials, Price Intel, People, Company, Reports — each renders
4. Click Admin ▾ → 13 items expand
5. (If platform_admin) Platform Admin badge top-right; click → /platform-admin directory; click each tool → migrated sub-route renders (iter-2 D-20)
6. Visit /financials/bills/inv-caldwell-001 → Document Review on phone with Site Office signatures (iter-2 D-19 .design-system-scope wrap)
7. Visit /financials/pay-apps/d-caldwell-05 → renders
8. Visit /jobs/j-caldwell-1 → PerJobTabs as single dropdown on phone (iter-2 D-21); tap dropdown; navigate
9. Visit /jobs/j-caldwell-1/mobile-approval → mobile-approval surface; 56px Approve button visible + tappable
10. Test gloves-on tap targets — all primary actions tappable
11. No console errors visible (Safari DevTools)
12. Confirm 1.5b prototype paths still accessible at /design-system/prototypes/* (logged in as platform_admin)
13. Visit a Sub Portal stub (e.g., /sub-portal/magic/abc123) — renders generic stub; no console errors; no token reflection visible

Pass criteria: Jake walks all 13 steps successfully. Any divergence vs desktop walk OR usability issue → log as Wave 1.1-Lite finding.

After M3 walk passes, /gsd-ship stage-1.5b-prototype-gallery proceeds.
```

**Halt for Jake review:**
1. All 4 NEW + 6 UPDATED docs (per iter-3 D-27 — COMPONENTS.md §7 counts as 6th UPDATED) reviewed (atomic commit verified per iter-2 mechanical #7)
2. Final integration smoke report reviewed
3. M3 phone walk readiness checklist reviewed
4. iter-2 amendments verification (D-19 + D-20 + D-21 + D-22 all reflected; iter-2 mechanical fixes confirmed)
5. Plan 7 SUMMARY committed

If Jake approves all → Stage 1.5c iter-2 is COMPLETE. M3 phone walk happens next; /gsd-ship of 1.5b proceeds afterward.
  </action>

  <verify>
    <automated>npm run build &amp;&amp; grep -c 'PASS' .planning/phases/stage-1.5c-information-architecture/01.5c-7-canonical-docs-smoke-SUMMARY.md &amp;&amp; grep -c 'M3 Phone Walk' .planning/phases/stage-1.5c-information-architecture/01.5c-7-canonical-docs-smoke-SUMMARY.md &amp;&amp; grep -c 'iter-2' .planning/phases/stage-1.5c-information-architecture/01.5c-7-canonical-docs-smoke-SUMMARY.md</automated>
    Expected: build exits 0; SUMMARY contains "PASS" + "M3 Phone Walk" + "iter-2" sections.

    Halt-review checklist verified by Jake walk-through (post-execution).
  </verify>

  <done>
    - Final integration smoke report in plan SUMMARY (33+ checks)
    - iter-2 enterprise-r W-4: post-deploy Vercel preview curl checks
    - M3 phone walk readiness checklist (13 steps including iter-2 D-19/D-20/D-21 verification)
    - Build + typecheck + hooks + privacy gate + redirects + visual walk all PASS
    - Stage 1.5c iter-2 is COMPLETE pending Jake's halt review
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 4 NEW canonical docs → future plan-phase agents | Plan-phase agents read these as source-of-truth; no runtime trust impact |
| 5 UPDATED docs → all future commits + future agents | Same posture; doc-only |
| iter-2 mechanical #6 column additions | Compliance-aware retention/PII/cross-org flagging at design time; F1 inherits |
| Final smoke → Vercel preview deploy (M3 phone walk URL) | Existing Vercel auth posture; no new exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.5c-7-01 | T (Tampering) | MASTER-PLAN.md atomic update | mitigate | iter-2 mechanical #7 atomic 10-doc commit ensures all 10 docs land together; iter-2 mechanical #8 idempotency guard prevents duplicate-append on re-run. If commit fails, revert all 9 + investigate. |
| T-1.5c-7-02 | I (Information disclosure) | 4 NEW canonical docs at .planning/architecture/ | accept | All content is architectural / strategic — no Drummond names, no PII, no real customer data. Privacy gate verified per iter-2 security LOW SEC-6/7/8 explicit grep. |
| T-1.5c-7-03 | R (Repudiation) | M3 phone walk readiness checklist | accept | Jake's walk results documented in plan SUMMARY post-execution. |
| T-1.5c-7-04 | E (Elevation of privilege) | CLAUDE.md update | accept | CLAUDE.md is project guidance; updates target Bills/Pay Apps terminology + 8-section nav references + iter-2 amendments. No new auth surfaces introduced. |
| T-1.5c-7-05 | I (Information disclosure) | iter-2 mechanical #6 SOC2 control mapping in ARCHITECTURE.md | accept | Documents existing posture; does not introduce new posture. F1 may extend with explicit policy implementations. |
</threat_model>

<verification>
After all 3 tasks complete:

1. 4 NEW canonical docs exist with iter-2 mechanical #6 column additions (Retention class + PII? in ENTITY-INVENTORY; Cross-org? in ROLES-CATALOG; §SOC2 in ARCHITECTURE)
2. 5 EXISTING docs + COMPONENTS.md updated atomically per iter-2 mechanical #7 (single 9+ doc commit)
3. iter-2 mechanical #8: idempotency guard verified
4. iter-2 mechanical #2: COMPONENTS.md §7 NwPlaceholderCard entry per PROPAGATION-RULES.md §4b
5. iter-2 must-fix CRITICAL #5: CLAUDE.md schema-aware audit-log language
6. iter-2 W-8: at least 2 consumers per new pattern in PHILOSOPHY/PATTERNS
7. iter-2 multi-tenant W-1: Owner Portal F1 auth path constraint in ARCHITECTURE.md
8. iter-2 planner LOW-5: all 8 principles grep-verified
9. iter-2 security LOW SEC-6/7/8: privacy gate explicit grep on canonical docs
10. iter-2 enterprise-r W-4: post-deploy Vercel preview curl checks documented
11. D-040..D-065 added to MASTER-PLAN.md DECISIONS LOG (21 entries; D-057..D-060 from nwrp45)
12. Final integration smoke: build + typecheck + hooks + privacy gate + 10 redirects (incl D-20 wildcard + mechanical #4) + visual walk + mobile + console errors all PASS
13. M3 phone walk readiness checklist documented (13 steps including iter-2 D-19/D-20/D-21 verification)
14. Plan 7 SUMMARY committed

**Halt for Jake review:** halt_after: true.

If Jake approves → Stage 1.5c iter-2 COMPLETE. M3 phone walk happens next. Then /gsd-ship of 1.5b proceeds with new IA in place.
</verification>

<success_criteria>
- 4 NEW + 6 UPDATED docs (COMPONENTS.md §7 counts as 6th UPDATED per iter-3 D-27) atomic (iter-2 mechanical #7 + iter-3 D-27)
- D-040..D-065 in MASTER-PLAN.md (idempotency guard verified per iter-2 mechanical #8)
- iter-2 mechanical #2 (NwPlaceholderCard COMPONENTS.md entry) + #5 (schema-aware audit-log in CLAUDE.md) + #6 (column additions) all reflected
- iter-2 multi-tenant W-1 (Owner Portal F1 auth path) documented
- iter-2 W-8 (2+ consumers per pattern) honored
- iter-2 planner LOW-5 (8 principles grep-verified) honored
- iter-2 security LOW SEC-6/7/8 (privacy gate on canonical docs) honored
- iter-2 enterprise-r W-4 (post-deploy Vercel preview) documented
- Final smoke report PASS
- M3 phone walk readiness checklist
- All 21 must_haves truths verified
- Halt review with Jake passes
</success_criteria>

## Acceptance criteria

- [ ] AC-1.5c-7-01: ARCHITECTURE.md exists with all 8 principles (Principle 1 through Principle 8 grep-verified per iter-2 planner LOW-5) + data graph model + section purposes + revised phase plan + §SOC2 control mapping (CC6.1/CC6.6/CC6.7/CC7.2/PI1.1/C1.1) per iter-2 mechanical #6
- [ ] AC-1.5c-7-02: ENTITY-INVENTORY.md exists with 31+ entities + Retention class + PII? columns per iter-2 mechanical #6
- [ ] AC-1.5c-7-03: ROLES-CATALOG.md exists with 15+ default roles + Cross-org? column per iter-2 mechanical #6
- [ ] AC-1.5c-7-04: INGESTION-ARCHITECTURE.md exists with multi-modal patterns
- [ ] AC-1.5c-7-05: .planning/CONTEXT.md updated with 8 principles canonical
- [ ] AC-1.5c-7-06: MASTER-PLAN.md DECISIONS LOG has D-040..D-065 (21 new entries) atomically per iter-2 mechanical #8 idempotency guard
- [ ] AC-1.5c-7-07: MASTER-PLAN.md CURRENT POSITION + NEXT PLANNED WORK updated with revised roadmap
- [ ] AC-1.5c-7-08: CLAUDE.md updated with Bills/Pay Apps terminology + schema-aware audit-log language per iter-2 must-fix CRITICAL #5 (entity_type stays 'invoice'/'draw'; UI maps via action-labels.ts; NOT 'bill_*' concat)
- [ ] AC-1.5c-7-09: PHILOSOPHY.md updated with Today as home + role-aware filtering + multi-modal ingestion + profile-based feature flags patterns; each cites at least 2 consumers per iter-2 W-8
- [ ] AC-1.5c-7-10: PATTERNS.md updated with placeholder treatment + role-filtered nav + phase-aware sub-nav + 8-item top nav + Sub Portal 3-stub patterns; each cites at least 2 consumers per iter-2 W-8
- [ ] AC-1.5c-7-12: COMPONENTS.md §7 NwPlaceholderCard entry per iter-2 mechanical #2 (mirror NwButton/NwEyebrow/NwBadge format) + PROPAGATION-RULES.md §4b workflow
- [ ] AC-1.5c-7-12: All 6 doc updates committed ATOMICALLY (single git commit) per iter-2 mechanical #7 — `git log -1 --stat` shows 9 docs (or 10 with COMPONENTS.md)
- [ ] AC-1.5c-7-13: iter-2 mechanical #8 idempotency guard verified — pre-commit `grep -c 'D-040' MASTER-PLAN.md` returns 0; post-commit returns >=1 (no duplication)
- [ ] AC-1.5c-7-14: iter-2 multi-tenant W-1 — Owner Portal F1 auth path constraint documented in ARCHITECTURE.md
- [ ] AC-1.5c-7-15: Build + typecheck + hooks (T10c, tenant-blind, bouncy-easing, post-edit Forbidden tokens) silent
- [ ] AC-1.5c-7-16: Privacy gate clean (32-token denylist returns 0 hits) across all updated docs + src/ — iter-2 security LOW SEC-6/7/8 explicit grep
- [ ] AC-1.5c-7-17: 10 representative redirect rules verified working including iter-2 D-20 wildcard /admin/platform/:path* and iter-2 mechanical #4 /settings/billing → /admin/billing
- [ ] AC-1.5c-7-18: Visual desktop walk PASS (8 nav items + Admin ▾ + Platform Admin badge + 12 production routes with .design-system-scope wrap per D-19 + per-job sub-nav + ~60 placeholders all render)
- [ ] AC-1.5c-7-19: iter-2 enterprise-r W-4 — post-deploy Vercel preview curl checks documented for 5 representative redirects
- [ ] AC-1.5c-7-20: M3 phone walk readiness checklist documented (13 steps including iter-2 D-19/D-20/D-21 verification)
- [ ] AC-1.5c-7-21: D-057..D-060 explicitly listed in MASTER-PLAN.md DECISIONS LOG with iter-2 nwrp45 source attribution

## Nyquist coverage

| Acceptance criterion | Verified by |
|---|---|
| AC-1.5c-7-01 | Task 1 verify (test -f + grep all 8 Principles + grep "SOC2") |
| AC-1.5c-7-02 | Task 1 verify (test -f + grep "Bills (vendor invoices)" + "Retention class") |
| AC-1.5c-7-03 | Task 1 verify (test -f + grep "Owner" + "Cross-org?") |
| AC-1.5c-7-04 | Task 1 verify (test -f + grep "Multi-modal") |
| AC-1.5c-7-05 | Task 2 verify (grep "8 principles") |
| AC-1.5c-7-06 | Task 2 verify (grep D-040 + D-065; idempotency guard pre/post) |
| AC-1.5c-7-07 | Task 2 verify (grep "F1" + "Wave 1.1-Lite" + "Wave 4" in NEXT PLANNED WORK) |
| AC-1.5c-7-08 | Task 2 verify (grep "entity_type" + "action-labels.ts" + absence of "bill_\\*" concat) |
| AC-1.5c-7-09 | Task 2 verify (grep "Today as home" + "Consumers" >=4) |
| AC-1.5c-7-10 | Task 2 verify (grep "Placeholder treatment" + "Consumers" >=5) |
| AC-1.5c-7-12 | Task 2 verify (grep "NwPlaceholderCard" + "NwPlaceholderCardProps" in COMPONENTS.md) |
| AC-1.5c-7-12 | Task 2 verify (git log -1 --stat shows 9-10 docs) |
| AC-1.5c-7-13 | Task 2 verify (idempotency guard pre/post grep counts) |
| AC-1.5c-7-14 | Task 1 verify (grep "Owner Portal F1" + "Path A" + "Path B" in ARCHITECTURE.md) |
| AC-1.5c-7-15 | Task 3 verify (npm run build + manual hook check) |
| AC-1.5c-7-16 | Task 3 verify (grep -c against denylist returns 0) |
| AC-1.5c-7-17 | Task 3 verify (10 curl -sI commands) |
| AC-1.5c-7-18 | Task 3 verify (Chrome DevTools MCP visual walk) |
| AC-1.5c-7-19 | Task 3 verify (Vercel preview curl results in SUMMARY) |
| AC-1.5c-7-20 | Task 3 verify (test -f SUMMARY + grep "M3 Phone Walk") |
| AC-1.5c-7-21 | Task 2 verify (grep "D-057" + "D-058" + "D-059" + "D-060" + "iter-2" + "nwrp45" in MASTER-PLAN.md) |

## Decisions inherited (from CONTEXT.md)

- D-15: 4 NEW canonical docs at .planning/architecture/
- D-16: 5 EXISTING docs updated atomically; D-040..D-065 added to MASTER-PLAN
- D-17 (Q13=A): M3 phone walk bundled with 1.5b ship gate
- D-18 (Q14=A): Existing Wave 1.1 polish backlog → Wave 1.1-Lite scope
- D-19..D-22 (iter-2 nwrp45): logged as D-057..D-060 in MASTER-PLAN per Plan 7 atomic update

## Halt conditions

- Halt at end of plan (halt_after: true) for Jake review of:
  - All 10 docs (4 NEW + 6 UPDATED including COMPONENTS.md §7) atomically committed per iter-2 mechanical #7 + iter-3 D-27
  - Final integration smoke report
  - M3 phone walk readiness checklist
  - iter-2 amendments verification (D-19/D-20/D-21/D-22 reflected)
- Halt during execution if:
  - **iter-2 mechanical #8 idempotency guard halts** — D-040 already exists in MASTER-PLAN; investigate before re-appending
  - Atomic git commit of doc updates fails (e.g., privacy gate flag) — revert all + investigate
  - Final integration smoke surfaces ANY of: build break, hook rejection, privacy gate fail, redirect chain, console error, 404 on a documented route, iter-2 watchpoint #1/#2/#3/#4 regression
  - Inconsistencies between MASTER-PLAN.md NEXT PLANNED WORK and ARCHITECTURE.md revised phase plan — reconcile

## Output

After completion, create `.planning/phases/stage-1.5c-information-architecture/01.5c-7-canonical-docs-smoke-SUMMARY.md` per `.claude/get-shit-done/templates/summary.md`. Document:
- Files created (4 NEW canonical docs)
- Files updated (5 EXISTING docs + COMPONENTS.md §7 — atomic commit verified)
- Acceptance criteria status (21 items)
- iter-2 mechanical #2 verification (NwPlaceholderCard COMPONENTS.md §7 entry)
- iter-2 mechanical #5 verification (CLAUDE.md schema-aware audit-log language; NO concat framing)
- iter-2 mechanical #6 verification (Retention class + PII? + Cross-org? + §SOC2)
- iter-2 mechanical #7 atomic 10-doc commit verified (`git log -1 --stat`)
- iter-2 mechanical #8 idempotency guard verified
- iter-2 multi-tenant W-1 (Owner Portal F1 auth path constraint)
- iter-2 W-8 (2+ consumers per pattern)
- iter-2 planner LOW-5 (8 principles grep-verified)
- iter-2 security LOW SEC-6/7/8 (privacy gate explicit grep)
- iter-2 enterprise-r W-4 (post-deploy Vercel preview curl checks)
- Final Integration Smoke Report (per Task 3 template)
- M3 Phone Walk Readiness Checklist (per Task 3 template; 13 steps incl iter-2 amendments verification)
- Halt-review checklist for Jake
- Stage 1.5c iter-2 COMPLETE statement (assuming all checks PASS)
- Hand-off to /gsd-ship stage-1.5b-prototype-gallery (with M3 phone walk first)
</content>
</invoke>
