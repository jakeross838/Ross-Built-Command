---
phase: stage-1.5c-information-architecture
plan: 6
plan-name: admin-platform-admin
type: execute
status: not-started
wave: 3
depends_on: [1, 3]
autonomous: false
halt_after: true
threat_model_severity: medium
requirements: []
files_modified:
  - src/app/admin/page.tsx
  - src/app/admin/users/page.tsx
  - src/app/admin/roles/page.tsx
  - src/app/admin/permissions/page.tsx
  - src/app/admin/org-chart/page.tsx
  - src/app/admin/approval-workflows/page.tsx
  - src/app/admin/notification-rules/page.tsx
  - src/app/admin/cost-codes/page.tsx
  - src/app/admin/templates/page.tsx
  - src/app/admin/integrations/page.tsx
  - src/app/admin/billing/page.tsx
  - src/app/admin/workflow-customization/page.tsx
  - src/app/admin/profile/page.tsx
  - src/app/platform-admin/page.tsx
  - src/app/platform-admin/layout.tsx
  - src/app/platform-admin/audit/page.tsx
  - src/app/platform-admin/organizations/page.tsx
  - src/app/platform-admin/organizations/[id]/page.tsx
  - src/app/platform-admin/users/page.tsx
  - src/app/platform-admin/users/[id]/page.tsx
  - src/app/platform-admin/feedback/page.tsx
  - src/app/platform-admin/feedback/[id]/page.tsx
  - src/app/platform-admin/support/page.tsx
  - src/app/platform-admin/support/[id]/page.tsx
  - src/app/platform-admin/items/page.tsx
  - src/app/platform-admin/items/[id]/page.tsx
  - src/app/platform-admin/cost-intelligence/page.tsx
  - src/middleware.ts
requires:
  - .planning/phases/stage-1.5c-information-architecture/CONTEXT.md
  - .planning/phases/stage-1.5c-information-architecture/PATTERNS.md
  - .planning/phases/stage-1.5c-information-architecture/01.5c-1-nav-today-redirects-PLAN.md (Wave 1; D-20 wildcard /admin/platform/:path* → /platform-admin/:path*)
  - .planning/phases/stage-1.5c-information-architecture/01.5c-3-extraction-production-routes-PLAN.md (Wave 2)
  - src/components/nw/NwPlaceholderCard.tsx (Plan 1 hoisted per iter-2 mechanical #1)
  - src/app/settings/page.tsx (existing — analog for /admin)
  - src/app/settings/team/page.tsx (existing — analog for /admin/users)
  - src/app/settings/integrations/page.tsx (existing — analog for /admin/integrations)
  - src/app/settings/billing/page.tsx (existing — analog for /admin/billing)
  - src/app/settings/cost-codes/page.tsx (existing — analog for /admin/cost-codes)
  - **src/app/admin/platform/page.tsx + 12 sub-route files (existing — sources for full migration per D-20)**
  - src/middleware.ts (lines 65, 72 — Platform admin gate; iter-2 must-fix #4 LOCKS Option A regex extension to also match /platform-admin/*)
provides:
  - 13-item Admin section at /admin/* (5 real-logic mounts + 8 placeholders)
  - **/platform-admin/* full migration per iter-2 D-20: 13 sub-routes mounted (audit, organizations, organizations/[id], users, users/[id], feedback, feedback/[id], support, support/[id], items, items/[id], cost-intelligence) + index placeholder + layout**
  - middleware.ts regex extension to gate /platform-admin/* with same isPlatformAdmin posture (iter-2 must-fix CRITICAL #4 LOCKS Option A)
affects:
  - All /settings/* paths 301-redirect to /admin/* (per Plan 1's redirect map)
  - All /admin/platform/* paths 301-redirect to /platform-admin/* (per Plan 1's D-20 wildcard redirect)
  - Admin ▾ dropdown in top nav (Plan 1) links to all 13 admin items
  - Platform Admin badge (Plan 1) → /platform-admin renders directory of migrated tools
  - 13 existing /admin/platform/* sub-route layouts FUNCTIONALLY MIGRATED to /platform-admin/* (iter-2 D-20 watchpoint #2)
  - Middleware enforces isPlatformAdmin gate at /platform-admin/* (iter-2 watchpoint #3 — auth bypass test)
acceptance-criteria-target: 14 falsifiable items (was 9; +5 for D-20 sub-route migration + watchpoints #2 + #3 + getCurrentMembership preservation)
estimated-duration: 1.5 days (was 0.5 day; +1d for D-20 full migration scope per Jake nwrp45)
must_haves:
  truths:
    - "Admin section has 13 routes per CONTEXT D-15 / Part 7 F: /admin (overview) + /admin/users + /admin/roles + /admin/permissions + /admin/org-chart + /admin/approval-workflows + /admin/notification-rules + /admin/cost-codes + /admin/templates + /admin/integrations + /admin/billing + /admin/workflow-customization + /admin/profile"
    - "/admin (overview) renders Card grid linking to all 12 sub-routes per CONTEXT D-22 (canonical destination; Admin dropdown in top nav is the power-user shortcut)"
    - "/admin/users mounts existing /settings/team logic with terminology rename (Team → Users in user-visible labels) — getCurrentMembership preserved per iter-2 mechanical #10"
    - "/admin/integrations mounts existing /settings/integrations logic — getCurrentMembership preserved"
    - "/admin/billing mounts existing /settings/billing logic — getCurrentMembership preserved"
    - "/admin/cost-codes mounts existing /settings/cost-codes logic — getCurrentMembership preserved"
    - "/admin/roles + /admin/permissions + /admin/org-chart placeholders describe F2 implementation (15-role library + permission matrix + visual org chart)"
    - "/admin/approval-workflows + /admin/notification-rules placeholders describe F3 engines"
    - "/admin/profile placeholder shows the stubbed selector copy: 'Custom Builder / Remodeler / Commercial GC — coming F2' (per CONTEXT D-10 / Q12=A)"
    - "/admin/templates + /admin/workflow-customization placeholders describe Wave 3 / F3 future-state"
    - "/platform-admin/page.tsx exists as a placeholder showing directory of migrated tools (per iter-2 D-20 — index stays placeholder; sub-routes are real)"
    - "iter-2 D-20 / D-058 (Decision B-modified) — 13 sub-routes MIGRATED from /admin/platform/* to /platform-admin/*: audit, organizations, organizations/[id], users, users/[id], feedback, feedback/[id], support, support/[id], items, items/[id], cost-intelligence (existing layout + functionality; not just routed)"
    - "iter-2 watchpoint #2 — Plan 6 explicitly smokes each migrated sub-route confirming functional + not just routed: navigate to /platform-admin/audit (audit log table loads), /platform-admin/organizations (org list loads), /platform-admin/users (user list loads), etc."
    - "iter-2 must-fix CRITICAL #4 LOCKS Option A — middleware.ts:65 regex extended from `/admin/platform/` to ALSO match `/platform-admin/*` with same `!isPlatformAdmin` redirect posture"
    - "iter-2 watchpoint #3 — auth bypass test: PM (non-platform-admin) authenticated session navigating to /platform-admin/audit MUST receive redirect (NOT successful render)"
    - "iter-2 mechanical #10 — getCurrentMembership preservation per re-mounted route (4 Admin REAL-LOGIC mounts + 13 Platform Admin REAL-LOGIC migrations) per grep diff"
    - "Build + typecheck + hooks silent across all ~30 files; privacy gate clean"
  artifacts:
    - path: "src/app/admin/page.tsx"
      provides: "Admin section overview Card grid linking to 12 sub-routes (canonical destination per D-22)"
      exports: ["default"]
    - path: "src/app/admin/users/page.tsx"
      provides: "Users management (renamed from /settings/team) — getCurrentMembership preserved"
      exports: ["default"]
    - path: "src/app/admin/profile/page.tsx"
      provides: "Org Profile placeholder showing stubbed Custom Builder / Remodeler / Commercial GC selector"
      exports: ["default"]
    - path: "src/app/platform-admin/page.tsx"
      provides: "Platform Admin index placeholder showing directory of migrated tools (per iter-2 D-20 — index stays placeholder)"
      exports: ["default"]
    - path: "src/app/platform-admin/layout.tsx"
      provides: "Platform Admin layout (mirror of existing /admin/platform/layout.tsx)"
      exports: ["default"]
    - path: "src/app/platform-admin/audit/page.tsx"
      provides: "Audit log viewer migrated from /admin/platform/audit (REAL-LOGIC; getCurrentMembership preserved)"
      exports: ["default"]
    - path: "src/app/platform-admin/organizations/page.tsx + [id]/page.tsx"
      provides: "Cross-org organization list + detail migrated from /admin/platform/organizations/* (REAL-LOGIC)"
      exports: ["default"]
    - path: "src/app/platform-admin/users/page.tsx + [id]/page.tsx"
      provides: "Cross-org user list + detail migrated from /admin/platform/users/* (REAL-LOGIC)"
      exports: ["default"]
    - path: "src/app/platform-admin/feedback/page.tsx + [id]/page.tsx"
      provides: "Feedback list + detail migrated from /admin/platform/feedback/* (REAL-LOGIC)"
      exports: ["default"]
    - path: "src/app/platform-admin/support/page.tsx + [id]/page.tsx"
      provides: "Support tickets migrated from /admin/platform/support/* (REAL-LOGIC)"
      exports: ["default"]
    - path: "src/app/platform-admin/items/page.tsx + [id]/page.tsx"
      provides: "Cost Intelligence items migrated from /admin/platform/items/* (REAL-LOGIC)"
      exports: ["default"]
    - path: "src/app/platform-admin/cost-intelligence/page.tsx"
      provides: "Cost Intelligence ops migrated from /admin/platform/cost-intelligence (REAL-LOGIC)"
      exports: ["default"]
    - path: "src/middleware.ts"
      provides: "Regex extension at line 65 to gate /platform-admin/* with same isPlatformAdmin posture (iter-2 must-fix CRITICAL #4 Option A LOCKED)"
      contains: "/platform-admin/"
  key_links:
    - from: "src/app/admin/users/page.tsx"
      to: "src/app/settings/team/page.tsx"
      via: "logic re-mount with terminology rename (getCurrentMembership preserved per iter-2 mechanical #10)"
      pattern: "(Team UI label → Users)"
    - from: "src/app/admin/page.tsx"
      to: "All 12 sub-routes via Card grid"
      via: "Link components"
      pattern: "/admin/users"
    - from: "src/app/platform-admin/audit/page.tsx + 12 other migrated sub-routes"
      to: "src/app/admin/platform/audit/page.tsx + 12 other source files"
      via: "logic re-mount (per iter-2 D-20 — full migration; getCurrentMembership preserved per iter-2 mechanical #10)"
      pattern: "platform_admin"
    - from: "src/middleware.ts"
      to: "Platform admin gate at /platform-admin/*"
      via: "regex extension at line 65 (iter-2 must-fix CRITICAL #4 Option A)"
      pattern: "platform-admin"
---

# Plan 6 — admin-platform-admin (1.5c iter-3 Wave 3)

## iter-3 amendment (per Jake nwrp46 / SEC-9)

**SEC-9 follow-up — `/platform-admin/audit` Supabase client type verification (per security iter-2 MEDIUM finding + D-25):**

iter-2 mechanical #10 grep diff (`getCurrentMembership|org_id|requireMembership` preservation) returns "zero diff" vacuously for `/platform-admin/audit` because the audit log viewer uses neither `getCurrentMembership` nor `org_id` — it queries `platform_admin_audit` cross-org via the existing RLS gate. The grep diff doesn't confirm the client type is preserved.

**Source-file inspection result (verified by security iter-3 reviewer):** `src/app/admin/platform/audit/page.tsx` uses `createServerClient()` from `@/lib/supabase/server` — that's the **anon/session-cookie client**, NOT service-role. Cross-org reads work because the `platform_admin_audit` table has the `platform_admin_audit_staff_read` RLS policy gated on `app_private.is_platform_admin()` (per migration 00048). Service-role is NOT used for this table.

**CRITICAL:** the migrated `src/app/platform-admin/audit/page.tsx` MUST preserve `createServerClient()` verbatim. Swapping to `createServiceRoleClient()` would bypass RLS on the profiles + organizations look-up queries inside the same file — that would be a regression.

Plan 6 Task 2 EXTENDED verify steps for `/platform-admin/audit` specifically:

1. **Source verified:** `src/app/admin/platform/audit/page.tsx` uses `createServerClient()` (anon + session cookie + RLS gate). Plan 6 SUMMARY documents this verbatim — NOT as service-role.

2. **Migrated `src/app/platform-admin/audit/page.tsx` MUST preserve `createServerClient()` verbatim** — same import path (`@/lib/supabase/server`), same client builder call, same query shape, same `platform_admin_audit_staff_read` RLS gate posture.

3. **Cross-org row smoke test** (extends watchpoint #2):
   - Logged in as PM (non-platform-admin): navigate to `/platform-admin/audit` → middleware redirects per watchpoint #3 (auth bypass test). Audit rows NOT visible to PM (RLS would also block, but middleware fires first).
   - Logged in as Jake (platform-admin): navigate to `/platform-admin/audit` → audit rows from MULTIPLE orgs visible (the `platform_admin_audit_staff_read` RLS policy permits cross-org SELECT for `is_platform_admin()` users). Confirms RLS posture preserved.

4. **Plan 6 SUMMARY documents the Supabase client type explicitly** so future F1 / Wave 1.1-Lite work doesn't accidentally swap to service-role (which would bypass RLS on adjacent profiles/organizations queries — a regression).

This SEC-9 fix is contained to Plan 6 Task 2's existing scope; no new tasks added; estimate impact: +15 min for Plan 6 (inspection + verify documentation).

---


<objective>
Build the 13-item Admin section + Platform Admin section with FULL migration of 13 existing /admin/platform/* sub-routes to /platform-admin/* (per iter-2 D-20 / D-058 Decision B-modified). Existing /settings/* routes are renamed + remounted at /admin/* (legacy paths redirect via Plan 1's `next.config.mjs`). New placeholders describe F2 / F3 future-states. Per iter-2 must-fix CRITICAL #4: middleware.ts:65 regex EXTENDED to gate /platform-admin/* with same isPlatformAdmin posture (Option A LOCKED).

This plan is parallel-safe with Plan 4 + Plan 5 — different file sets, all in Wave 3.

**Halt after Plan 6** so Jake can review the Platform Admin migration (iter-2 watchpoint #2: sub-routes functional, not just routed; iter-2 watchpoint #3: auth bypass test) before Plan 7.

iter-2 differences from iter-1:

1. **iter-2 must-fix CRITICAL #3 / D-20 / D-058 (Decision B-modified) — Full /admin/platform → /platform-admin migration in 1.5c:** iter-1 plan only created /platform-admin/page.tsx placeholder; Jake/Andrew would have lost access to 13 existing sub-routes (audit log viewer, cross-org tools) until Wave 1.1-Lite. Jake nwrp45 resolved: Plan 6 mounts all 13 existing sub-route layouts at /platform-admin/{audit,organizations,organizations/[id],users,users/[id],feedback,feedback/[id],support,support/[id],items,items/[id],cost-intelligence}. Sub-routes are REAL, working, migrated (not just routed). /platform-admin/page.tsx index stays placeholder showing directory of available tools. Plan 1 wildcard redirect /admin/platform/:path* → /platform-admin/:path* mechanically catches existing bookmarks/links. ~1d additional scope.

2. **iter-2 must-fix CRITICAL #4 / SEC-1 / compliance W-1 — middleware.ts:65 LOCKS Option A:** iter-1 plan left middleware gating as "Option A or B" (regex extension OR page-level check). plan-review iter-1 security BLOCKING SEC-1 + compliance W-1 require: LOCK Option A. Plan 6 Task 2 extends `src/middleware.ts:65` from `pathname.startsWith("/admin/platform/")` to ALSO match `/platform-admin/*` with the same `!isPlatformAdmin` redirect posture. NO page-level fallback (Option B rejected — middleware-level is the existing posture per CLAUDE.md "Platform admin (cross-tenant)" section).

3. **iter-2 watchpoint #2 — sub-route functionality verify:** Plan 6 Task 2 explicitly smokes each migrated sub-route (navigate to /platform-admin/audit, /platform-admin/organizations, /platform-admin/users, /platform-admin/feedback, /platform-admin/support, /platform-admin/items, /platform-admin/cost-intelligence; each must render existing layout + functionality, not just route).

4. **iter-2 watchpoint #3 — auth bypass test:** Plan 6 Task 2 final smoke: log in as a PM (non-platform-admin), navigate to /platform-admin/audit; MUST receive redirect to /dashboard or /today (NOT successful render). If render observed, middleware regex extension is incomplete; halt + reconcile.

5. **iter-2 mechanical #10 — getCurrentMembership preservation per re-mounted route:** For each of 4 Admin REAL-LOGIC mounts (users/integrations/billing/cost-codes) + 13 Platform Admin REAL-LOGIC migrations, run grep diff: zero diff expected (or documented divergence reasoning).

6. **iter-2 D-22 / D-060 (Decision D) — Admin dropdown vs Admin section overview both exist with clear precedence:** Plan 6 /admin/page.tsx is the canonical destination (clicking "Admin" in top nav lands here per Plan 1's link). Admin dropdown is power-user shortcut. Plan 7 atomic CLAUDE.md update documents the precedence.

Purpose:
- Admin ▾ dropdown items (Plan 1's AdminDropdown) all resolve to existing routes (no 404s).
- /settings/* logic preserved (no functional regression) — just remounted at /admin/*.
- F2/F3 future-state placeholders describe the IA so reviewers can evaluate.
- Platform Admin section: index placeholder + 13 functional sub-routes per iter-2 D-20 (Jake/Andrew never lose access).
- Middleware gates /platform-admin/* with same isPlatformAdmin posture per iter-2 must-fix #4 Option A LOCKED.
- Auth bypass test confirms gate works per iter-2 watchpoint #3.

Output:
- 13 admin page.tsx files (5 real-logic + 8 placeholders)
- 1 /platform-admin/page.tsx index placeholder + 1 layout.tsx + 13 migrated sub-route page.tsx files (per iter-2 D-20)
- 1 middleware.ts regex extension (iter-2 must-fix CRITICAL #4 Option A LOCKED)
</objective>

<execution_context>
@C:/Users/Jake/nightwork-platform/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Jake/nightwork-platform/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/stage-1.5c-information-architecture/CONTEXT.md
@.planning/phases/stage-1.5c-information-architecture/PATTERNS.md
@.planning/phases/stage-1.5c-information-architecture/01.5c-1-nav-today-redirects-PLAN.md
@.planning/phases/stage-1.5c-information-architecture/01.5c-3-extraction-production-routes-PLAN.md
@.planning/phases/stage-1.5c-information-architecture/01.5c-4-section-placeholders-sub-portal-PLAN.md
@.planning/expansions/stage-1.5c-information-architecture-EXPANDED-SCOPE.md
@CLAUDE.md
@.planning/design/SYSTEM.md
@.planning/design/COMPONENTS.md
@.planning/design/PATTERNS.md
@src/components/nw/Card.tsx
@src/components/nw/Eyebrow.tsx
@src/components/nw/Badge.tsx
@src/components/nw/NwPlaceholderCard.tsx
@src/app/settings/page.tsx
@src/app/settings/team/page.tsx
@src/app/settings/integrations/page.tsx
@src/app/settings/billing/page.tsx
@src/app/settings/cost-codes/page.tsx
@src/app/admin/platform/page.tsx
@src/app/admin/platform/layout.tsx
@src/app/admin/platform/audit/page.tsx
@src/app/admin/platform/organizations/page.tsx
@src/app/admin/platform/organizations/[id]/page.tsx
@src/app/admin/platform/users/page.tsx
@src/app/admin/platform/users/[id]/page.tsx
@src/app/admin/platform/feedback/page.tsx
@src/app/admin/platform/feedback/[id]/page.tsx
@src/app/admin/platform/support/page.tsx
@src/app/admin/platform/support/[id]/page.tsx
@src/app/admin/platform/items/page.tsx
@src/app/admin/platform/items/[id]/page.tsx
@src/app/admin/platform/cost-intelligence/page.tsx
@src/middleware.ts

<interfaces>
Admin section (per CONTEXT D-15 / Part 7 F):

  /admin                        — overview (NEW; canonical destination per D-22)
  /admin/users                  — REAL-LOGIC (mount /settings/team)
  /admin/roles                  — placeholder (F2)
  /admin/permissions            — placeholder (F2)
  /admin/org-chart              — placeholder (F2)
  /admin/approval-workflows     — placeholder (F3)
  /admin/notification-rules     — placeholder (F3)
  /admin/cost-codes             — REAL-LOGIC (mount /settings/cost-codes)
  /admin/templates              — placeholder (Wave 3)
  /admin/integrations           — REAL-LOGIC (mount /settings/integrations)
  /admin/billing                — REAL-LOGIC (mount /settings/billing)
  /admin/workflow-customization — placeholder (F3)
  /admin/profile                — placeholder (F2 — stubbed Custom Builder / Remodeler / Commercial GC selector)

Platform Admin section (per CONTEXT D-09 + iter-2 D-20 / D-058 full migration):

  /platform-admin                          — index placeholder (NEW; directory of migrated tools)
  /platform-admin/layout.tsx               — layout (NEW; mirror of /admin/platform/layout.tsx)
  /platform-admin/audit                    — REAL-LOGIC (migrated from /admin/platform/audit)
  /platform-admin/organizations            — REAL-LOGIC (migrated from /admin/platform/organizations)
  /platform-admin/organizations/[id]       — REAL-LOGIC (migrated)
  /platform-admin/users                    — REAL-LOGIC (migrated from /admin/platform/users)
  /platform-admin/users/[id]               — REAL-LOGIC (migrated)
  /platform-admin/feedback                 — REAL-LOGIC (migrated)
  /platform-admin/feedback/[id]            — REAL-LOGIC (migrated)
  /platform-admin/support                  — REAL-LOGIC (migrated)
  /platform-admin/support/[id]             — REAL-LOGIC (migrated)
  /platform-admin/items                    — REAL-LOGIC (migrated)
  /platform-admin/items/[id]               — REAL-LOGIC (migrated)
  /platform-admin/cost-intelligence        — REAL-LOGIC (migrated)

  Total: 13 migrated sub-routes (12 page.tsx files + 1 layout.tsx) + 1 index placeholder

Plan 1's redirect map handles legacy paths:
  /settings           → /admin
  /settings/team      → /admin/users
  /settings/integrations → /admin/integrations
  /settings/billing   → /admin/billing
  /settings/cost-codes → /admin/cost-codes
  (other /settings/* paths redirect to /admin per Plan 1's wildcard rule)
  /admin/platform     → /platform-admin
  /admin/platform/:path* → /platform-admin/:path* (Plan 1 wildcard per iter-2 D-20)

Existing middleware.ts:65 (Platform admin gate):
  Per pre-execution audit:
    if (pathname === "/admin/platform" || pathname.startsWith("/admin/platform/")) {
      if (!user) { /* redirect to /login */ }
      if (!isPlatformAdmin) { /* redirect to /dashboard */ }
    }

  iter-2 must-fix CRITICAL #4 LOCKS Option A — extend regex to ALSO match /platform-admin/*:
    if (pathname === "/admin/platform" || pathname.startsWith("/admin/platform/") ||
        pathname === "/platform-admin" || pathname.startsWith("/platform-admin/")) {
      if (!user) { /* redirect to /login */ }
      if (!isPlatformAdmin) { /* redirect to /dashboard */ }
    }

NwPlaceholderCard reuse from Plan 1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 13 admin routes (4 real-logic mounts + 8 placeholders + 1 overview) — getCurrentMembership preserved per iter-2 mechanical #10</name>
  <files>src/app/admin/page.tsx, src/app/admin/users/page.tsx, src/app/admin/roles/page.tsx, src/app/admin/permissions/page.tsx, src/app/admin/org-chart/page.tsx, src/app/admin/approval-workflows/page.tsx, src/app/admin/notification-rules/page.tsx, src/app/admin/cost-codes/page.tsx, src/app/admin/templates/page.tsx, src/app/admin/integrations/page.tsx, src/app/admin/billing/page.tsx, src/app/admin/workflow-customization/page.tsx, src/app/admin/profile/page.tsx</files>

  <read_first>
    - src/app/settings/page.tsx (full file — analog for /admin overview if structure transfers)
    - src/app/settings/team/page.tsx (full file — to mount at /admin/users)
    - src/app/settings/integrations/page.tsx (full file)
    - src/app/settings/billing/page.tsx (full file)
    - src/app/settings/cost-codes/page.tsx (full file)
    - src/components/nw/NwPlaceholderCard.tsx (Plan 1 hoisted)
    - src/app/pipeline/page.tsx (Plan 4 section overview pattern)
    - src/app/admin/page.tsx (existing — verify if exists; if so, replace with overview)
  </read_first>

  <action>
**Half A — REAL-LOGIC mounts (4 files) with iter-2 mechanical #10 preservation:**

For each, COPY the rendering logic from /settings/* and rename to /admin/*. UI labels retain their existing semantic.

1. **`/admin/users/page.tsx`** — copy from `src/app/settings/team/page.tsx`. Rename "Team" → "Users" in user-visible labels. Logic + state shape unchanged. Component file imports + variable names retain `team*` per CONTEXT D-01 scope clarification.
   - **iter-2 mechanical #10 verify:** `diff <(grep -E 'getCurrentMembership|org_id|requireMembership' src/app/settings/team/page.tsx | sort) <(grep -E ... src/app/admin/users/page.tsx | sort)` returns zero diff. Document in SUMMARY: REAL-LOGIC re-mount.

2. **`/admin/integrations/page.tsx`** — copy from `src/app/settings/integrations/page.tsx`. Page title stays "Integrations". Logic + state unchanged. + iter-2 mechanical #10 verify.

3. **`/admin/billing/page.tsx`** — copy from `src/app/settings/billing/page.tsx`. Page title stays "Billing". Logic + state unchanged. + iter-2 mechanical #10 verify.

4. **`/admin/cost-codes/page.tsx`** — copy from `src/app/settings/cost-codes/page.tsx`. Page title stays "Cost Codes". Logic + state unchanged. + iter-2 mechanical #10 verify.

**Half B — PLACEHOLDER mounts (8 files + 1 overview):**

5. **`/admin/page.tsx` (overview, canonical destination per D-22)** — section overview Card grid. RESTRUCTURE existing (if /admin/page.tsx exists from earlier code) OR create new.

   Per CONTEXT D-22 (D-060): Admin dropdown is power-user shortcut; /admin overview is canonical destination. Plan 1's nav-bar links "Admin" label to /admin (this overview); ▾ affordance opens dropdown.

   Suggested grid items (with Wave badges where placeholder):
   ```
   Org Settings (overview) — current page
   Users — REAL (manage org_members)
   Roles — Coming F2
   Permissions — Coming F2
   Org Chart — Coming F2
   Approval Workflows — Coming F3
   Notification Rules — Coming F3
   Cost Codes — REAL
   Templates — Coming Wave 3
   Integrations — REAL
   Billing — REAL
   Workflow Customization — Coming F3
   Profile (Org Type) — Coming F2
   ```

   The overview is interactive — each card is a Link to its sub-route. Show Wave badge for placeholders only (real-logic items don't show "Coming" badges).

6. **`/admin/roles/page.tsx`** — placeholder. Body: "15 default roles will live here, customizable per org. Default library: Owner, GM, Sr PM, PM, Estimator, Accountant, Foreman, Field, Designer, Sub Foreman, Sub Bookkeeper, Owner/Homeowner, Architect/External, Inspector, Banker." Wave: F2.

7. **`/admin/permissions/page.tsx`** — placeholder. Body: "Permissions matrix UI — granular per-role permissions. Read / Write / Approve / Admin per entity type." Wave: F2.

8. **`/admin/org-chart/page.tsx`** — placeholder. Body: "Visual org chart — drives reporting + approval routing + notification routing + permission inheritance per Principle 6." Wave: F2.

9. **`/admin/approval-workflows/page.tsx`** — placeholder. Body: "Approval workflow engine — customizable per company." Wave: F3.

10. **`/admin/notification-rules/page.tsx`** — placeholder. Body: "Notification routing engine — who gets notified for what events." Wave: F3.

11. **`/admin/templates/page.tsx`** — placeholder. Body: "Document + email templates." Wave: Wave 3.

12. **`/admin/workflow-customization/page.tsx`** — placeholder. Body: "Workflow customization — adjust per-company business rules." Wave: F3.

13. **`/admin/profile/page.tsx`** — placeholder with stubbed selector per CONTEXT D-10 / Q12=A. Disabled grid demonstrates eventual selector shape (Custom Builder / Remodeler / Commercial GC) without doing actual filtering.

**Per existing /admin/page.tsx (if exists):** verify before writing. If existing /admin/page.tsx mounts something specific, restructure to be the section overview Card grid. Backup any existing logic in SUMMARY for review.
  </action>

  <verify>
    <automated>npm run build &amp;&amp; ls src/app/admin/page.tsx src/app/admin/users/page.tsx src/app/admin/roles/page.tsx src/app/admin/permissions/page.tsx src/app/admin/org-chart/page.tsx src/app/admin/approval-workflows/page.tsx src/app/admin/notification-rules/page.tsx src/app/admin/cost-codes/page.tsx src/app/admin/templates/page.tsx src/app/admin/integrations/page.tsx src/app/admin/billing/page.tsx src/app/admin/workflow-customization/page.tsx src/app/admin/profile/page.tsx 2>&amp;1 | grep -c 'page.tsx'</automated>
    Expected: build exits 0; ls returns 13.

    iter-2 mechanical #10 — getCurrentMembership preservation per 4 REAL-LOGIC mounts:
    For each REAL-LOGIC mount, run grep diff:
    - `diff <(grep -E 'getCurrentMembership|org_id|requireMembership' src/app/settings/team/page.tsx | sort) <(grep -E 'getCurrentMembership|org_id|requireMembership' src/app/admin/users/page.tsx | sort)` — zero diff
    - (repeat for integrations, billing, cost-codes)
    SUMMARY documents per-route status.

    Real-logic mount verification:
    - `/admin/users/page.tsx` size resembles `/settings/team/page.tsx` (logic preserved)
    - `/admin/integrations/page.tsx` size resembles `/settings/integrations/page.tsx`
    - `/admin/billing/page.tsx` size resembles `/settings/billing/page.tsx`
    - `/admin/cost-codes/page.tsx` size resembles `/settings/cost-codes/page.tsx`

    Placeholder verification:
    - `grep -c 'NwPlaceholderCard' src/app/admin/roles/page.tsx` returns >=1 (consume Plan 1 hoisted)
    - Same for /admin/permissions, /admin/org-chart, /admin/approval-workflows, /admin/notification-rules, /admin/templates, /admin/workflow-customization

    /admin/profile shows stubbed selector:
    - `grep -c 'Custom Builder' src/app/admin/profile/page.tsx` returns >=1
    - `grep -c 'Remodeler' src/app/admin/profile/page.tsx` returns >=1
    - `grep -c 'Commercial GC' src/app/admin/profile/page.tsx` returns >=1
    - `grep -c 'pointer-events-none' src/app/admin/profile/page.tsx` returns >=1 (disabled state)

    Hex check across all 13 files: 0 hardcoded hex.

    Visual smoke (Chrome DevTools MCP):
    - Visit /admin — section overview Card grid renders with 13 cards (per D-22 canonical destination)
    - Click /admin/users — renders Users page (existing /settings/team logic re-mounted)
    - Click /admin/roles — placeholder
    - Click /admin/profile — stubbed selector with 3 disabled options
    - Visit legacy /settings → 308 redirects to /admin (per Plan 1's redirect map)
  </verify>

  <done>
    - 13 admin routes exist (1 overview + 4 real-logic + 8 placeholders)
    - Real-logic mounts preserve /settings/* functionality at /admin/* paths
    - iter-2 mechanical #10: getCurrentMembership preservation verified per 4 re-mounted routes
    - /admin/profile shows stubbed Custom Builder / Remodeler / Commercial GC selector
    - All placeholders consume NwPlaceholderCard from Plan 1
    - iter-2 D-22: /admin is canonical destination (Card grid); Admin dropdown is power-user shortcut
    - Build passes
    - Privacy gate clean
    - Hooks silent
  </done>
</task>

<task type="auto">
  <name>Task 2: Platform Admin section — full migration of 13 sub-routes per iter-2 D-20 / D-058 (Decision B-modified) + middleware.ts regex extension LOCKS Option A per iter-2 must-fix CRITICAL #4 + watchpoint #2 + watchpoint #3</name>
  <files>src/app/platform-admin/page.tsx, src/app/platform-admin/layout.tsx, src/app/platform-admin/audit/page.tsx, src/app/platform-admin/organizations/page.tsx, src/app/platform-admin/organizations/[id]/page.tsx, src/app/platform-admin/users/page.tsx, src/app/platform-admin/users/[id]/page.tsx, src/app/platform-admin/feedback/page.tsx, src/app/platform-admin/feedback/[id]/page.tsx, src/app/platform-admin/support/page.tsx, src/app/platform-admin/support/[id]/page.tsx, src/app/platform-admin/items/page.tsx, src/app/platform-admin/items/[id]/page.tsx, src/app/platform-admin/cost-intelligence/page.tsx, src/middleware.ts</files>

  <read_first>
    - src/app/admin/platform/page.tsx (existing — analog for /platform-admin index; or stays at /admin/platform if existing has substantive logic — verify)
    - **src/app/admin/platform/layout.tsx (FULL file — to migrate to /platform-admin/layout.tsx)**
    - **src/app/admin/platform/audit/page.tsx (FULL file — to migrate to /platform-admin/audit/page.tsx)**
    - **src/app/admin/platform/organizations/page.tsx (FULL file — to migrate)**
    - **src/app/admin/platform/organizations/[id]/page.tsx (FULL file — to migrate)**
    - **src/app/admin/platform/users/page.tsx + [id]/page.tsx (FULL files — to migrate)**
    - **src/app/admin/platform/feedback/page.tsx + [id]/page.tsx (FULL files — to migrate)**
    - **src/app/admin/platform/support/page.tsx + [id]/page.tsx (FULL files — to migrate)**
    - **src/app/admin/platform/items/page.tsx + [id]/page.tsx (FULL files — to migrate)**
    - **src/app/admin/platform/cost-intelligence/page.tsx (FULL file — to migrate)**
    - src/middleware.ts (FULL file — verify isPlatformAdmin gate at lines 65, 72; iter-2 must-fix #4 extends to /platform-admin/*)
    - src/components/nw/NwPlaceholderCard.tsx (Plan 1 hoisted — for /platform-admin/page.tsx index)
    - .planning/phases/stage-1.5c-information-architecture/CONTEXT.md (D-09 — Platform Admin badge target; D-20 — full migration)
    - .planning/expansions/stage-1.5c-information-architecture-EXPANDED-SCOPE.md Part 7 G (Platform Admin badge spec) + iter-2 amendments (D-20)
  </read_first>

  <action>
**iter-2 D-20 / D-058 (Decision B-modified) — Full /admin/platform → /platform-admin migration:**

Per Jake nwrp45 resolution: Plan 6 mounts ALL 13 existing /admin/platform/* sub-routes at /platform-admin/* paths. Sub-routes are real, working, migrated — Jake/Andrew never lose access to audit / orgs / users / feedback / support / items / cost-intelligence. /platform-admin/page.tsx index stays as placeholder showing directory.

**Step A — Read all 13 source files at /admin/platform/* in full.**

Inventory:
- /admin/platform/page.tsx — existing index (likely placeholder or simple landing)
- /admin/platform/layout.tsx — existing layout
- /admin/platform/audit/page.tsx — audit log viewer (REAL functionality)
- /admin/platform/organizations/page.tsx + [id]/page.tsx — cross-org org list + detail
- /admin/platform/users/page.tsx + [id]/page.tsx — cross-org user list + detail
- /admin/platform/feedback/page.tsx + [id]/page.tsx — feedback list + detail
- /admin/platform/support/page.tsx + [id]/page.tsx — support tickets
- /admin/platform/items/page.tsx + [id]/page.tsx — Cost Intelligence items
- /admin/platform/cost-intelligence/page.tsx — Cost Intelligence ops

For each, document the existing functionality + getCurrentMembership / org_id / requireMembership / supabase service-role usage.

**Step B — Migrate 13 files from /admin/platform/* to /platform-admin/***.

Approach: COPY each source file's full content to the new location, updating internal references where needed.

Per-file migration:
1. /admin/platform/layout.tsx → /platform-admin/layout.tsx (verbatim copy; update any internal Link components from /admin/platform/* to /platform-admin/*)
2. /admin/platform/audit/page.tsx → /platform-admin/audit/page.tsx (verbatim copy; preserve audit log query logic)
3. /admin/platform/organizations/page.tsx → /platform-admin/organizations/page.tsx (verbatim copy)
4. /admin/platform/organizations/[id]/page.tsx → /platform-admin/organizations/[id]/page.tsx
5-12. Same pattern for users, feedback, support, items (each with page.tsx + [id]/page.tsx)
13. /admin/platform/cost-intelligence/page.tsx → /platform-admin/cost-intelligence/page.tsx

**iter-2 mechanical #10 — preserve getCurrentMembership() verbatim per migrated route:**

For each migrated file, run grep diff:
`diff <(grep -E 'getCurrentMembership|org_id|requireMembership' src/app/admin/platform/<sub-route>/page.tsx | sort) <(grep -E 'getCurrentMembership|org_id|requireMembership' src/app/platform-admin/<sub-route>/page.tsx | sort)` — zero diff expected.

For platform_admin queries (e.g., supabase.from("platform_admins").select), preserve exactly. Document any divergence in SUMMARY.

**Internal reference updates:**

Within migrated files, update internal Link / navigation references:
- Search for `/admin/platform` strings within migrated files
- Replace with `/platform-admin` where they reference the section root
- Preserve external API references (e.g., /api/admin/platform — Plan 6 doesn't change API paths in 1.5c; F1 may rename API routes later)

Per Plan 1's redirect map, /admin/platform/:path* → /platform-admin/:path* wildcard catches existing user bookmarks. Plan 1's existing 4 platform-admin redirects also point at /platform-admin/cost-intelligence (already updated).

**Step C — Create /platform-admin/page.tsx index placeholder showing directory.**

Per iter-2 D-20: index page itself stays as placeholder showing directory of available tools.

```typescript
// src/app/platform-admin/page.tsx
//
// Platform Admin index per CONTEXT D-09 + iter-2 D-20 (Decision B-modified).
// Index is placeholder showing directory; sub-routes (audit, organizations, etc.)
// are REAL-LOGIC migrated from /admin/platform/*.
//
// Per CLAUDE.md "Platform admin (cross-tenant)" section: existing posture preserved.

import Link from "next/link";
import Card from "@/components/nw/Card";
import Eyebrow from "@/components/nw/Eyebrow";
import Badge from "@/components/nw/Badge";

const PLATFORM_TOOLS = [
  { href: "/platform-admin/audit", label: "Audit log", desc: "Cross-org audit trail viewer", live: true },
  { href: "/platform-admin/organizations", label: "Organizations", desc: "All orgs across the platform", live: true },
  { href: "/platform-admin/users", label: "Users", desc: "Cross-org user search + impersonation", live: true },
  { href: "/platform-admin/feedback", label: "Feedback", desc: "User feedback inbox", live: true },
  { href: "/platform-admin/support", label: "Support", desc: "Support tickets across orgs", live: true },
  { href: "/platform-admin/items", label: "Cost Intelligence Items", desc: "Cross-org cost-code item registry", live: true },
  { href: "/platform-admin/cost-intelligence", label: "Cost Intelligence Ops", desc: "CI engine controls + verification queue", live: true },
];

export default function PlatformAdminIndexPage() {
  return (
    <div className="px-6 py-8 max-w-[1200px] mx-auto">
      <div className="mb-6">
        <Eyebrow tone="muted" className="mb-2">Platform Admin</Eyebrow>
        <h1 className="text-[28px] mb-1" style={{ fontFamily: "var(--font-space-grotesk)", fontWeight: 500, color: "var(--text-primary)" }}>
          Cross-org operations
        </h1>
        <p className="text-[14px]" style={{ color: "var(--text-secondary)" }}>
          For Jake / Andrew and future operations staff. Existing platform_admins gate enforced via middleware (iter-2 must-fix CRITICAL #4 — /platform-admin/* gated with same isPlatformAdmin posture).
        </p>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-2 gap-5">
        {PLATFORM_TOOLS.map((tool) => (
          <Link key={tool.href} href={tool.href} className="block">
            <Card padding="md">
              <Eyebrow tone="accent" className="mb-2">{tool.label}</Eyebrow>
              <p className="text-[13px] mb-2" style={{ color: "var(--text-secondary)" }}>{tool.desc}</p>
              <Badge variant="success">Live</Badge>
            </Card>
          </Link>
        ))}
      </div>
    </div>
  );
}
```

**Step D — Extend middleware.ts:65 regex (iter-2 must-fix CRITICAL #4 LOCKS Option A).**

Per pre-execution audit (verified 2026-05-04):

```typescript
// src/middleware.ts:65 (BEFORE)
if (pathname === "/admin/platform" || pathname.startsWith("/admin/platform/")) {
  if (!user) {
    const loginUrl = request.nextUrl.clone();
    loginUrl.pathname = "/login";
    loginUrl.searchParams.set("redirect", "/admin/platform");
    return NextResponse.redirect(loginUrl);
  }
  if (!isPlatformAdmin) {
    const url = request.nextUrl.clone();
    url.pathname = "/dashboard";
    url.search = "";
    return NextResponse.redirect(url);
  }
  // Authorized — fall through to response.
}
```

**iter-2 must-fix CRITICAL #4 LOCKS Option A — extend regex:**

```typescript
// src/middleware.ts:65 (AFTER — iter-2 must-fix CRITICAL #4 Option A LOCKED)
//
// Per iter-2 must-fix CRITICAL #4 (security BLOCKING SEC-1 + compliance W-1):
// extend regex to ALSO match /platform-admin/* with same isPlatformAdmin posture.
// Plan 1's wildcard redirect /admin/platform/:path* → /platform-admin/:path* is
// supported; Plan 6 migrated 13 sub-routes; this gate ensures auth bypass test
// passes (PM/accountant authenticated session navigating to /platform-admin/audit
// MUST receive redirect to /dashboard; iter-2 watchpoint #3).
if (
  pathname === "/admin/platform" ||
  pathname.startsWith("/admin/platform/") ||
  pathname === "/platform-admin" ||
  pathname.startsWith("/platform-admin/")
) {
  if (!user) {
    const loginUrl = request.nextUrl.clone();
    loginUrl.pathname = "/login";
    // Use the actual pathname for redirect param so user lands at the right place after login
    loginUrl.searchParams.set("redirect", pathname);
    return NextResponse.redirect(loginUrl);
  }
  if (!isPlatformAdmin) {
    const url = request.nextUrl.clone();
    url.pathname = "/dashboard";
    url.search = "";
    return NextResponse.redirect(url);
  }
  // Authorized — fall through to response.
}
```

**Step E — iter-2 watchpoint #2 verification (sub-route functional, not just routed).**

After migration, smoke-test each migrated sub-route as Jake/Andrew (platform_admin role):

1. Visit /platform-admin → renders index placeholder with 7 directory cards
2. Click "Audit log" → /platform-admin/audit renders audit log table with real data (or empty state if no audit rows; verify no console errors)
3. Click "Organizations" → /platform-admin/organizations renders org list (or empty state)
4. Visit /platform-admin/organizations/<some-org-id> → renders org detail page
5. Click "Users" → /platform-admin/users renders user search/list
6. Visit /platform-admin/users/<some-user-id> → renders user detail page
7. Click "Feedback" → /platform-admin/feedback renders feedback list
8. Visit /platform-admin/feedback/<some-id> → renders feedback detail
9. Click "Support" → /platform-admin/support renders support ticket list
10. Visit /platform-admin/support/<some-id> → renders support ticket detail
11. Click "Cost Intelligence Items" → /platform-admin/items renders item list
12. Visit /platform-admin/items/<some-id> → renders item detail
13. Click "Cost Intelligence Ops" → /platform-admin/cost-intelligence renders CI ops page

For each sub-route: confirm content renders + no console errors + matches existing /admin/platform/<same-path> behavior.

Document watchpoint #2 results in SUMMARY.

**Step F — iter-2 watchpoint #3 verification (auth bypass test).**

Critical security test:
1. Log out
2. Log in as a user with role 'pm' (NOT a platform_admin; create test user if needed via existing org_members table — or use existing test PM credentials)
3. Manually navigate to /platform-admin/audit (e.g., paste URL in browser)
4. Confirm: middleware redirects to /dashboard (or /today post-Plan-1)
5. Confirm: NOT a successful render of audit log

Repeat at /platform-admin/organizations, /platform-admin/users — all should redirect.

If any successful render observed for non-platform_admin user, middleware regex extension is broken — halt + reconcile.

Document watchpoint #3 results in SUMMARY.

**Step G — Existing /admin/platform/* files cleanup (deferred to F1):**

Plan 6 does NOT delete /admin/platform/* source files. Plan 1's redirect map handles legacy paths. F1 / Wave 1.1-Lite cleanup deletes the originals once all consumer references migrate.

Document in SUMMARY: "Source files at /admin/platform/* preserved as redirect targets per Plan 1 wildcard. F1 / Wave 1.1-Lite deletes originals after consumer reference audit."
  </action>

  <verify>
    <automated>npm run build &amp;&amp; ls src/app/platform-admin/page.tsx src/app/platform-admin/layout.tsx src/app/platform-admin/audit/page.tsx src/app/platform-admin/organizations/page.tsx src/app/platform-admin/organizations/[id]/page.tsx src/app/platform-admin/users/page.tsx src/app/platform-admin/users/[id]/page.tsx src/app/platform-admin/feedback/page.tsx src/app/platform-admin/feedback/[id]/page.tsx src/app/platform-admin/support/page.tsx src/app/platform-admin/support/[id]/page.tsx src/app/platform-admin/items/page.tsx src/app/platform-admin/items/[id]/page.tsx src/app/platform-admin/cost-intelligence/page.tsx 2>&amp;1 | grep -c 'page.tsx\|layout.tsx'</automated>
    Expected: build exits 0; ls returns 14 (12 page.tsx + 1 layout.tsx + 1 index page.tsx).

    iter-2 must-fix CRITICAL #4 verification (middleware.ts regex extension):
    - `grep -c '/platform-admin' src/middleware.ts` returns >=2 (in regex check)
    - Verify the extension at line 65 with iter-2 mechanical change
    - Build passes (no syntax errors in middleware.ts)

    iter-2 mechanical #10 — getCurrentMembership preservation per migrated route (13 routes):
    For each migrated file, run grep diff:
    - `diff <(grep -E 'getCurrentMembership|org_id|requireMembership' src/app/admin/platform/audit/page.tsx | sort) <(grep -E 'getCurrentMembership|org_id|requireMembership' src/app/platform-admin/audit/page.tsx | sort)` — zero diff
    - (repeat for organizations, organizations/[id], users, users/[id], feedback, feedback/[id], support, support/[id], items, items/[id], cost-intelligence)
    SUMMARY documents per-route status.

    iter-2 watchpoint #2 verification (sub-route functional smoke):
    - As platform_admin: visit each of 13 sub-routes; confirm content renders + no console errors
    - SUMMARY documents per-route status (PASS / FAIL with details)

    iter-2 watchpoint #3 verification (auth bypass test):
    - As PM (non-platform_admin): visit /platform-admin/audit, /platform-admin/organizations, /platform-admin/users
    - Confirm: middleware redirects to /dashboard (or /today)
    - Confirm: NOT successful render
    - SUMMARY documents per-test status

    Manual smoke (Chrome DevTools MCP):
    - Logged in as Jake (platform_admin) — visit /platform-admin → renders directory of 7 tools (all marked Live)
    - Click each tool → renders existing functionality at new path
    - Visit legacy /admin/platform → 308 redirects to /platform-admin (per Plan 1 redirect map)
    - Visit legacy /admin/platform/audit → 308 redirects to /platform-admin/audit (per Plan 1 wildcard)

    Hex check + tenant-blind check + privacy gate per usual.
  </verify>

  <done>
    - /platform-admin/page.tsx index placeholder + /platform-admin/layout.tsx + 13 migrated sub-route page.tsx files
    - middleware.ts:65 regex extended per iter-2 must-fix CRITICAL #4 Option A LOCKED
    - iter-2 mechanical #10: getCurrentMembership preservation verified per 13 migrated routes
    - iter-2 watchpoint #2: each sub-route functionally smoked; results in SUMMARY
    - iter-2 watchpoint #3: auth bypass test confirms PM gets redirect (NOT render); results in SUMMARY
    - Existing /admin/platform/* files preserved as redirect targets (deletion deferred to F1)
    - Platform Admin badge from Plan 1 navigates to /platform-admin index
    - Build passes
    - Privacy gate clean
    - Hooks silent
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /admin/* routes → existing role gate | owner / admin role required per Plan 1's ACCESS map; middleware enforces |
| /platform-admin/* → platform_admins gate | iter-2 must-fix CRITICAL #4 Option A LOCKED — middleware.ts:65 regex extended to gate /platform-admin/* with same isPlatformAdmin posture |
| Real-logic re-mounts (/admin/users etc.) → original /settings/* logic | Same RLS, same auth, same audit-log writes; just renamed UI labels (iter-2 mechanical #10 preserves getCurrentMembership verbatim) |
| Platform Admin migrated sub-routes (/platform-admin/audit etc.) → original /admin/platform/* logic | Same posture preserved; iter-2 mechanical #10 verifies per route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.5c-6-01 | E (Elevation of privilege) | /platform-admin/* gate | mitigate | iter-2 must-fix CRITICAL #4 LOCKS Option A — middleware.ts:65 regex extended. iter-2 watchpoint #3: auth bypass test confirms non-platform_admin redirected. SUMMARY documents test results. |
| T-1.5c-6-02 | I (Information disclosure) | /platform-admin index placeholder | accept | Index content is generic (directory of tools); no sensitive cross-org data. |
| T-1.5c-6-03 | T (Tampering) | /admin/* real-logic mounts | accept | /admin/users + /admin/integrations + /admin/billing + /admin/cost-codes preserve /settings/* logic verbatim. iter-2 mechanical #10 verifies preservation per grep diff. |
| T-1.5c-6-04 | T (Tampering) | /platform-admin/* migrated sub-routes | mitigate | iter-2 D-20 full migration preserves existing /admin/platform/* logic verbatim. iter-2 mechanical #10 verifies preservation per grep diff (13 routes). iter-2 watchpoint #2 verifies functional behavior post-migration. |
| T-1.5c-6-05 | R (Repudiation) | /admin/* placeholder routes | accept | Placeholders are read-only; no audit-log writes. |
| T-1.5c-6-06 | S (Spoofing) | Admin section overview links | accept | Section overview Card grid links to /admin/* sub-routes via Next.js Link components. Tampering with Link href just routes elsewhere; existing role gates apply. |
| T-1.5c-6-07 | I (Information disclosure) | Cross-org audit log access at /platform-admin/audit | mitigate | Existing audit log query in source file uses service-role client + isPlatformAdmin gate (preserved per iter-2 mechanical #10). Only platform_admins can access. iter-2 watchpoint #3 confirms non-platform_admin gets redirect. |
</threat_model>

<verification>
After Task 1 + Task 2 complete:

1. 13 admin routes exist (4 real-logic + 8 placeholders + 1 overview)
2. Existing /settings/* logic preserved at /admin/* paths (iter-2 mechanical #10 verified)
3. /admin/profile shows stubbed Custom Builder / Remodeler / Commercial GC selector (disabled)
4. /admin/page.tsx is canonical destination per D-22 (Card grid linking to 12 sub-routes)
5. /platform-admin/page.tsx index placeholder + 13 migrated sub-routes exist (iter-2 D-20 / D-058 full migration)
6. middleware.ts:65 regex extended per iter-2 must-fix CRITICAL #4 Option A LOCKED
7. iter-2 mechanical #10: getCurrentMembership preservation verified per re-mounted route (4 Admin + 13 Platform Admin = 17 routes)
8. iter-2 watchpoint #2: each migrated /platform-admin sub-route functionally smoked; results documented
9. iter-2 watchpoint #3: auth bypass test confirms non-platform_admin redirected; results documented
10. Plan 1's redirect map for /settings/* + /admin/platform/* → /admin/* + /platform-admin/* all resolve
11. Build + typecheck + hooks silent
12. Privacy gate clean across all ~30 new files

**Halt for Jake review:** halt_after: true. Jake reviews:
- iter-2 watchpoint #2 results (13 sub-routes functional)
- iter-2 watchpoint #3 results (auth bypass test passes)
- iter-2 mechanical #10 results (getCurrentMembership preserved)
- Spot-check 2-3 migrated /platform-admin/* routes as platform_admin
- Spot-check auth bypass: log out, log in as PM, attempt /platform-admin/audit; confirm redirect

If Jake approves → Wave 4 (Plan 7) spawns. If Jake wants changes → revise + re-halt.
</verification>

<success_criteria>
- 13 admin routes + 1 platform-admin index placeholder + 13 migrated platform-admin sub-routes (per iter-2 D-20)
- Existing /settings/* logic preserved per iter-2 mechanical #10
- Existing /admin/platform/* logic migrated to /platform-admin/* per iter-2 D-20 + mechanical #10
- /admin/profile stubbed selector documents F2 future-state
- middleware.ts regex extension LOCKS Option A per iter-2 must-fix CRITICAL #4
- iter-2 watchpoint #2 (sub-route functional) + #3 (auth bypass test) verified
- All 14 must_haves truths verified
- Build + typecheck + hooks + privacy gate clean
</success_criteria>

## Acceptance criteria

- [ ] AC-1.5c-6-01: 13 admin routes exist per CONTEXT D-15 / Part 7 F (1 overview + 4 real-logic + 8 placeholders)
- [ ] AC-1.5c-6-02: /admin/users mounts /settings/team logic; UI labels say Users; iter-2 mechanical #10 grep diff zero
- [ ] AC-1.5c-6-03: /admin/integrations + /admin/billing + /admin/cost-codes mount existing /settings/* logic; iter-2 mechanical #10 verified per route
- [ ] AC-1.5c-6-04: /admin (overview) renders Card grid linking to all 12 sub-routes (per D-22 canonical destination)
- [ ] AC-1.5c-6-05: 8 placeholder admin routes (roles, permissions, org-chart, approval-workflows, notification-rules, templates, workflow-customization, profile) describe F2 / F3 / Wave 3 future-states; consume NwPlaceholderCard from Plan 1
- [ ] AC-1.5c-6-06: /admin/profile shows disabled stubbed Custom Builder / Remodeler / Commercial GC selector per Q12=A
- [ ] AC-1.5c-6-07: /platform-admin/page.tsx index placeholder + /platform-admin/layout.tsx exist
- [ ] AC-1.5c-6-08: iter-2 D-20 / D-058 — 13 sub-routes migrated from /admin/platform/* to /platform-admin/*: audit, organizations, organizations/[id], users, users/[id], feedback, feedback/[id], support, support/[id], items, items/[id], cost-intelligence (12 page.tsx files + 1 layout.tsx)
- [ ] AC-1.5c-6-09: iter-2 mechanical #10 — getCurrentMembership preservation verified per 13 migrated /platform-admin sub-routes (grep diff zero per route)
- [ ] AC-1.5c-6-10: iter-2 must-fix CRITICAL #4 Option A LOCKED — middleware.ts:65 regex extended to match /platform-admin/* with same !isPlatformAdmin redirect posture
- [ ] AC-1.5c-6-11: iter-2 watchpoint #2 verified — each migrated /platform-admin sub-route functionally smoked: audit, organizations, users, feedback, support, items, cost-intelligence each render existing layout + functionality (not just routed)
- [ ] AC-1.5c-6-12: iter-2 watchpoint #3 verified — auth bypass test: PM (non-platform_admin) authenticated session navigating to /platform-admin/audit MUST receive redirect to /dashboard (NOT successful render)
- [ ] AC-1.5c-6-13: iter-2 D-22 — /admin canonical destination + Admin dropdown power-user shortcut precedence documented (Plan 7 atomic CLAUDE.md update inherits)
- [ ] AC-1.5c-6-14: Build + typecheck pass; T10c silent; tenant-blind silent; bouncy-easing silent; privacy gate clean

## Nyquist coverage

| Acceptance criterion | Verified by |
|---|---|
| AC-1.5c-6-01 | Task 1 verify (ls returns 13) |
| AC-1.5c-6-02 | Task 1 verify (file size resembles /settings/team + grep "Users" + iter-2 mechanical #10 grep diff) |
| AC-1.5c-6-03 | Task 1 verify (per-file grep + iter-2 mechanical #10) |
| AC-1.5c-6-04 | Task 1 verify (visual: section overview links to all 12) |
| AC-1.5c-6-05 | Task 1 verify (per-placeholder grep NwPlaceholderCard + Wave badge) |
| AC-1.5c-6-06 | Task 1 verify (grep "Custom Builder" + "Remodeler" + "Commercial GC" + "pointer-events-none") |
| AC-1.5c-6-07 | Task 2 verify (test -f /platform-admin/page.tsx + layout.tsx) |
| AC-1.5c-6-08 | Task 2 verify (ls returns 14 files in /platform-admin) |
| AC-1.5c-6-09 | Task 2 verify (grep diff per route — 13 grep diffs) |
| AC-1.5c-6-10 | Task 2 verify (grep "/platform-admin" in middleware.ts returns >=2) |
| AC-1.5c-6-11 | Task 2 verify (manual smoke as platform_admin; SUMMARY documents per-route PASS/FAIL) |
| AC-1.5c-6-12 | Task 2 verify (manual auth bypass test as PM; SUMMARY documents PASS/FAIL) |
| AC-1.5c-6-13 | Task 1 verify (visual: clicking "Admin" label routes to /admin overview) + Plan 7 atomic CLAUDE.md update |
| AC-1.5c-6-14 | Plan-level (npm run build + privacy gate grep) |

## Decisions inherited (from CONTEXT.md)

- D-09: Platform Admin badge top-right for Jake/Andrew (Plan 1) — destination at /platform-admin per CONTEXT D-09
- D-10: Today screen role-aware logic SKELETON only; profile-based feature flags placeholder at /admin/profile per Q12=A
- D-15: 4 NEW canonical docs + 5 UPDATED docs (Plan 7); /admin/* structure per Part 7 F lands here
- **D-20 (= MASTER-PLAN D-058) — iter-2:** Full /admin/platform → /platform-admin migration in 1.5c (Decision B-modified); 13 sub-routes mounted at /platform-admin/* with REAL-LOGIC; index stays placeholder
- **D-22 (= MASTER-PLAN D-060) — iter-2:** Admin dropdown shortcut + /admin canonical destination both exist with clear precedence; /admin/page.tsx is canonical destination Card grid

## Halt conditions

- Halt at end of plan (halt_after: true) for Jake review of iter-2 D-20 full migration + watchpoint #2 + watchpoint #3 + iter-2 mechanical #10 results
- Halt during execution if:
  - Existing /settings/* logic doesn't transfer cleanly (e.g., context provider scope issues)
  - Existing /admin/page.tsx has critical logic that can't be replaced with section overview — surface to Jake
  - **iter-2 must-fix CRITICAL #4** — middleware.ts regex extension breaks build OR existing /admin/platform gate (regression test); halt + reconcile
  - **iter-2 watchpoint #2** — any migrated /platform-admin sub-route fails to render or has console errors; halt + investigate
  - **iter-2 watchpoint #3** — auth bypass test succeeds (non-platform_admin lands at /platform-admin/audit without redirect); CRITICAL halt — middleware regex extension is broken
  - **iter-2 mechanical #10** — getCurrentMembership grep diff shows divergence in any REAL-LOGIC re-mount or migrated route without documented reasoning; halt + reconcile
  - Hook rejection on any new file
  - Privacy gate fail

## Output

After completion, create `.planning/phases/stage-1.5c-information-architecture/01.5c-6-admin-platform-admin-SUMMARY.md` per `.claude/get-shit-done/templates/summary.md`. Document:
- Files created (13 admin + 14 platform-admin = 27 + middleware.ts modification)
- Acceptance criteria status (14 items)
- Real-logic vs placeholder counts per section
- iter-2 D-20 full migration: 13 sub-routes + layout + index migrated; per-route status
- iter-2 must-fix CRITICAL #4: middleware.ts regex extension diff
- iter-2 mechanical #10: getCurrentMembership grep diff per 17 routes (4 Admin + 13 Platform Admin)
- iter-2 watchpoint #2: per-sub-route functional smoke results
- iter-2 watchpoint #3: auth bypass test results (PM session navigating to /platform-admin/audit)
- Backed-up existing /admin/page.tsx content if replaced
- Existing /admin/platform/* preserved as redirect targets (F1 cleanup)
- Hook silence + build / privacy gate pass
- Halt-review checklist for Jake
</content>
</invoke>

<criteria>

```yaml
criteria:
  mechanical:
    - "npm run build clean (0 errors)"
    - "npx tsc --noEmit returns 0 errors in src/"
    - "All hooks silent on every commit (T10a-T10d, post-edit drift, Drummond grep)"
    - "Privacy gate clean across admin + platform-admin route files"
    - "src/middleware.ts contains both /admin/platform and /platform-admin path-gate (regex extension Option A LOCKED per iter-2 #4)"
    - "13 /admin/* routes created (page.tsx exists for /admin + 12 sub-routes)"
    - "14 /platform-admin/* surfaces created (1 layout + 1 index + 12 migrated sub-routes)"
    - "REAL-LOGIC re-mounts at /admin/users + /admin/billing + /admin/cost-codes preserve getCurrentMembership() (grep diff zero against /settings/team /settings/billing /settings/cost-codes sources)"
    - "Plan 1 D-20 wildcard /admin/platform/:path* → /platform-admin/:path* redirect still present in next.config.mjs"
  dom:
    - 'Page /admin contains text "Admin"'
    - 'Page /admin/users contains text "Team"'
    - 'Page /admin/billing contains text "Billing"'
    - 'Page /admin/cost-codes contains text "Cost"'
  visual:
    - "Page /admin: Card grid overview surface visible (replaces prior 5-line redirect stub) with admin tool entries rendered as Site Office Cards"
    - "Page /admin/roles: NwPlaceholderCard or coming-feature surface visible with role-management context messaging"
  behavioral:
    - "Middleware regex matches BOTH /admin/platform/audit and /platform-admin/audit paths with identical isPlatformAdmin gate"
    - "Shared component platform-sidebar.tsx active-state matcher works under both prefixes (dual-prefix awareness verified per iter-2 D-058)"
    - "/platform-admin/audit createServerClient() preserved verbatim (NOT service-role) — SEC-9 compliance"
  semantic:
    - "Page /admin: admin tools clearly organized by category (Team / Billing / Cost Codes vs F2-deferred roles / permissions); placeholder surfaces communicate which Wave they ship in"
```

</criteria>
