---
phase: stage-1.5c-information-architecture
plan: 6
subsystem: ui
tags: [next-app-router, middleware, platform-admin, rls, isPlatformAdmin, design-tokens, slate]

# Dependency graph
requires:
  - phase: stage-1.5c-information-architecture
    provides: Plan 1 redirect map (next.config.mjs) + NwPlaceholderCard primitive + AdminDropdown
  - phase: stage-1.5c-information-architecture
    provides: Plan 3 production routes pattern (.design-system-scope wrapper)
provides:
  - 13 admin routes at /admin/* (3 REAL-LOGIC + 10 placeholders) + Card grid overview
  - /platform-admin/* full migration (12 sub-route page.tsx + 1 layout + 1 index = 14 surfaces)
  - middleware.ts regex extension gating /platform-admin/* with same isPlatformAdmin posture
  - Shared component updates (platform-sidebar / audit-row / end-impersonation-button)
    pointing at canonical /platform-admin/* paths
affects:
  - All /settings/* paths (resolved by Plan 1 redirect map)
  - All /admin/platform/* paths (resolved by Plan 1 wildcard + middleware gates both prefixes)
  - Admin dropdown in top nav (Plan 1) — all 13 items resolve to existing routes
  - Platform Admin badge (Plan 1) — destination /platform-admin renders directory of migrated tools

# Tech tracking
tech-stack:
  added: []  # no new dependencies
  patterns:
    - "13-route Admin section with overview Card grid as canonical destination (D-22)"
    - "Real-logic re-mount pattern: copy entry-point page.tsx + reuse client component verbatim"
    - "Middleware regex extension pattern: dual-prefix path matching for migration transition"
    - "Dual-aware shared component: platform-sidebar handles both new and legacy URL prefixes"

key-files:
  created:
    - "src/app/admin/page.tsx (overview Card grid — replaces redirect-loop stub)"
    - "src/app/admin/users/page.tsx (REAL-LOGIC, mounts /settings/team)"
    - "src/app/admin/billing/page.tsx (REAL-LOGIC, mounts /settings/billing)"
    - "src/app/admin/cost-codes/page.tsx (REAL-LOGIC, mounts /settings/cost-codes)"
    - "src/app/admin/{roles,permissions,org-chart,approval-workflows,notification-rules,templates,integrations,workflow-customization,profile}/page.tsx (placeholders)"
    - "src/app/platform-admin/page.tsx (NEW index placeholder — directory of 6 tools)"
    - "src/app/platform-admin/layout.tsx (migrated from /admin/platform/layout.tsx)"
    - "src/app/platform-admin/{audit,organizations,users,feedback,support,cost-intelligence}/page.tsx + [id]/page.tsx (12 migrated sub-routes)"
    - "src/app/platform-admin/items/[id]/page.tsx (migrated; no source page.tsx exists)"
  modified:
    - "src/middleware.ts (regex extension for /platform-admin/* gating)"
    - "src/components/admin/platform-sidebar.tsx (NAV_ITEMS + dual-prefix active matcher)"
    - "src/components/admin/audit-row.tsx (link targets updated)"
    - "src/components/end-impersonation-button.tsx (default redirect + pre-existing hex fix)"

key-decisions:
  - "D-22 (= MASTER-PLAN D-060): /admin overview is canonical destination, AdminDropdown is power-user shortcut; overview Card grid implemented per spec"
  - "Plan 1 introduced /admin/page.tsx redirect to /settings/company creating a loop (also redirected to /admin) — Rule 1 fix: replace 5-line redirect with overview Card grid"
  - "Plan listed /admin/integrations as REAL-LOGIC but /settings/integrations doesn't exist — Rule 3 fix: render as F3 placeholder describing third-party connection registry"
  - "iter-2 D-20 / D-058 Decision B-modified: 12 sub-routes (not 13 — items list page.tsx doesn't exist in source) migrated to /platform-admin/* with REAL-LOGIC preserved"
  - "iter-2 must-fix CRITICAL #4 Option A LOCKED: middleware.ts regex extension; redirect param now uses actual pathname instead of hardcoded /admin/platform"
  - "Shared components (platform-sidebar / audit-row / end-impersonation-button) updated to point at /platform-admin/* canonical paths; sidebar matcher dual-aware for transition period"
  - "API paths /api/admin/platform/* preserved unchanged (per Plan 6 explicit non-scope)"
  - "/admin/platform/* source pages PRESERVED as redirect targets per Plan 1 wildcard; F1 / Wave 1.1-Lite cleans up"
  - "SEC-9: /platform-admin/audit uses createServerClient() (anon + session cookie + RLS via platform_admin_audit_staff_read policy) — preserved verbatim; NOT service-role"

patterns-established:
  - "Pattern: REAL-LOGIC re-mount — new page.tsx imports client component from old path (TeamSettings, BillingActions, CostCodesManager) so type imports via ./page resolve to original location"
  - "Pattern: NwPlaceholderCard for future-state surfaces (consumed by 8 admin placeholders + plan-wide pattern)"
  - "Pattern: Disabled selector grid (pointer-events-none opacity-60) for stubbed UX preview without behavior"
  - "Pattern: Dual-prefix middleware gate for staged path migration (covers both legacy + new prefix during transition window)"

requirements-completed: []  # Plan frontmatter requirements: [] — no IDs to mark

# Metrics
duration: 95min
completed: 2026-05-11
---

# Phase stage-1.5c-information-architecture Plan 6: admin-platform-admin Summary

**Admin section overview Card grid + 3 REAL-LOGIC re-mounts (Users/Billing/Cost Codes) + 10 placeholders + full /admin/platform → /platform-admin migration (14 surfaces) + middleware.ts regex extension gating /platform-admin/\* with same isPlatformAdmin posture per iter-2 must-fix CRITICAL #4 LOCKS Option A**

## Performance

- **Duration:** 95 min
- **Started:** 2026-05-11T (Plan 6 spawn)
- **Completed:** 2026-05-11T (post-Task-2 push)
- **Tasks:** 2 (Task 1: 13 admin routes; Task 2: 14 platform-admin surfaces + middleware + shared components)
- **Files modified:** 31 (13 new admin + 14 new platform-admin + 4 modified — middleware, platform-sidebar, audit-row, end-impersonation-button + 1 deferred-items.md doc edit)

## Accomplishments

- **Resolved /admin redirect loop** (Plan 1 introduced /settings → /admin and /admin → /settings/company creating infinite loop; Plan 6 /admin/page.tsx is now the canonical overview destination per D-22)
- **3 REAL-LOGIC re-mounts with verbatim-preserved getCurrentMembership posture** (Users/Billing/Cost Codes; iter-2 mechanical #10 zero-diff verified)
- **/platform-admin/\* migration with 12 sub-routes + layout + index placeholder** — Jake/Andrew never lose access to audit log, cross-org tools, feedback inbox, support inbox, cost-intelligence ops per iter-2 D-20 / D-058
- **middleware regex extension (iter-2 must-fix CRITICAL #4 Option A LOCKED)** — both /admin/platform/\* and /platform-admin/\* gated identically; redirect param uses actual pathname for correct post-login destination
- **Shared component dual-awareness** — platform-sidebar correctly highlights active item whether rendered in /admin/platform/\* (legacy, still exists) or /platform-admin/\* (new canonical) layout

## Task Commits

Each task was committed atomically:

1. **Task 1: 13 admin routes (3 REAL-LOGIC + 10 placeholders)** — `a9f7eb9` (feat)
2. **Task 2: /platform-admin migration + middleware regex (iter-2 D-20 + #4)** — `3a9d816` (feat)

**Plan metadata:** _added in final commit_ (this SUMMARY.md + STATE.md update)

## Files Created/Modified

### Created — Admin section (13 files)

- `src/app/admin/page.tsx` — Overview Card grid (12 tiles linking to sub-routes; replaces 5-line redirect-loop stub)
- `src/app/admin/users/page.tsx` — REAL-LOGIC re-mount of /settings/team (TeamSettings client reused)
- `src/app/admin/billing/page.tsx` — REAL-LOGIC re-mount of /settings/billing (BillingActions reused; text-white → text-nw-white-sand per Slate hook)
- `src/app/admin/cost-codes/page.tsx` — REAL-LOGIC re-mount of /settings/cost-codes (CostCodesManager reused)
- `src/app/admin/roles/page.tsx` — F2 placeholder (15-role library spec)
- `src/app/admin/permissions/page.tsx` — F2 placeholder (permission matrix spec)
- `src/app/admin/org-chart/page.tsx` — F2 placeholder (visual org chart spec)
- `src/app/admin/profile/page.tsx` — F2 placeholder with stubbed Custom Builder / Remodeler / Commercial GC selector (disabled via pointer-events-none)
- `src/app/admin/approval-workflows/page.tsx` — F3 placeholder
- `src/app/admin/notification-rules/page.tsx` — F3 placeholder
- `src/app/admin/workflow-customization/page.tsx` — F3 placeholder
- `src/app/admin/integrations/page.tsx` — F3 placeholder (Rule 3 fix: /settings/integrations source doesn't exist)
- `src/app/admin/templates/page.tsx` — Wave 3 placeholder

### Created — Platform Admin section (14 files)

- `src/app/platform-admin/page.tsx` — NEW index placeholder (6 directory cards marked LIVE)
- `src/app/platform-admin/layout.tsx` — migrated verbatim from /admin/platform/layout.tsx (uses existing @/components/admin/platform-sidebar)
- `src/app/platform-admin/audit/page.tsx` — migrated (REAL-LOGIC; SEC-9: createServerClient preserved verbatim)
- `src/app/platform-admin/organizations/page.tsx` + `[id]/page.tsx` — migrated cross-org list + detail
- `src/app/platform-admin/users/page.tsx` + `[id]/page.tsx` — migrated cross-org user search + detail
- `src/app/platform-admin/feedback/page.tsx` + `[id]/page.tsx` — migrated feedback inbox
- `src/app/platform-admin/support/page.tsx` + `[id]/page.tsx` — migrated support tickets
- `src/app/platform-admin/items/[id]/page.tsx` — migrated item detail (no items list page.tsx exists in source; Plan 1 redirects /admin/platform/items → /platform-admin/cost-intelligence)
- `src/app/platform-admin/cost-intelligence/page.tsx` — migrated CI ops (6 tabs: items / pricing / extractions / classifications / conversions / bootstrap)

### Modified — 4 files

- `src/middleware.ts` — regex extended from `/admin/platform[/]` to ALSO match `/platform-admin[/]`; redirect param now uses actual pathname (iter-2 must-fix CRITICAL #4 Option A LOCKED)
- `src/components/admin/platform-sidebar.tsx` — NAV_ITEMS hrefs migrated to `/platform-admin/*`; active-state matcher recognizes BOTH new and legacy prefixes (dual-awareness for transition period)
- `src/components/admin/audit-row.tsx` — org + user detail link targets migrated to `/platform-admin/{organizations,users}/{id}`
- `src/components/end-impersonation-button.tsx` — default fallback redirect changed from `/admin/platform` to `/platform-admin`; pre-existing `#FFF8F3` hex (line 35) replaced with `var(--nw-white-sand)` to satisfy post-edit hook

## Decisions Made

### D-22 (= MASTER-PLAN D-060) — /admin canonical destination implemented

Per CONTEXT D-22 / Decision D: clicking "Admin" in top nav routes to `/admin` (the canonical Card grid overview); the ▾ dropdown (Plan 1's AdminDropdown) is the power-user shortcut. Plan 7 atomic CLAUDE.md update will document this precedence formally. Implemented as Card grid with 12 tiles (3 marked LIVE, 9 marked Coming F2/F3/Wave 3).

### iter-2 mechanical #10 — getCurrentMembership preservation verified

**Admin section (3 REAL-LOGIC mounts):** grep diff zero per route (comments excluded):
- `/admin/users` vs `/settings/team` — ZERO DIFF
- `/admin/billing` vs `/settings/billing` — ZERO DIFF
- `/admin/cost-codes` vs `/settings/cost-codes` — ZERO DIFF

**Platform Admin (12 migrated sub-routes):**
- 11 of 12: ZERO DIFF (verbatim preservation)
- `users/[id]`: documented divergence — the regex `org_id` matches `${m.org_id}` JS variable inside hardcoded URL paths (`/admin/platform/organizations/${m.org_id}` → `/platform-admin/organizations/${m.org_id}`). Substantive logic (`getCurrentMembership`, `org_id` table-filter calls) preserved verbatim. The "diff" is solely URL-path rewriting per Plan 6 Task 2 Step B.

### iter-2 must-fix CRITICAL #4 Option A LOCKED

`src/middleware.ts:130` (pre-existing platform-admin gate at line ~130 of pre-edit file) extended from:

```ts
if (pathname === "/admin/platform" || pathname.startsWith("/admin/platform/")) {
```

to:

```ts
if (
  pathname === "/admin/platform" ||
  pathname.startsWith("/admin/platform/") ||
  pathname === "/platform-admin" ||
  pathname.startsWith("/platform-admin/")
) {
```

Same `!isPlatformAdmin` redirect-to-/dashboard posture for both prefixes. Additionally: the login-redirect param now uses actual pathname instead of hardcoded `"/admin/platform"` so users land at the correct destination after login (e.g., `/platform-admin/audit?redirect=...` no longer drops them at `/admin/platform`).

### Stubbed selector pattern (Q12=A)

`/admin/profile/page.tsx` renders a disabled 3-tile grid (`pointer-events-none opacity-60`) showing the eventual Custom Builder / Remodeler / Commercial GC selector. F2 wires this to `org_settings`; until then it's a UX preview.

### Source files preserved per Plan 6 Step G

`/admin/platform/*` source files NOT deleted. Plan 1's wildcard redirect (`/admin/platform/:path* → /platform-admin/:path*`) catches old bookmarks. F1 / Wave 1.1-Lite cleanup deletes originals once consumer reference audit confirms no remaining inbound links.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Resolved /admin redirect loop introduced by Plan 1**

- **Found during:** Pre-execution audit of `src/app/admin/page.tsx`
- **Issue:** Existing `/admin/page.tsx` (5 lines) was `redirect("/settings/company")`. Plan 1's redirect map in `next.config.mjs` includes `/settings/company → /admin` (line 111). Result: visiting `/admin` → redirect to `/settings/company` → redirect to `/admin` → infinite loop. Browsers would terminate after redirect-chain rate limiting, leaving the user on an error screen.
- **Fix:** Replaced the 5-line redirect with the Plan 6-specified section overview Card grid (canonical destination per D-22). Loop resolved.
- **Files modified:** `src/app/admin/page.tsx`
- **Verification:** Build green; `/admin` now serves the Card grid; no redirect chain triggered.
- **Committed in:** `a9f7eb9` (Task 1 commit)

**2. [Rule 3 - Blocking] /admin/integrations rendered as placeholder (no source to mount)**

- **Found during:** Task 1 pre-execution `read_first` audit
- **Issue:** Plan 6's `must_haves.truths` and `read_first` list `/settings/integrations` as the analog source for the REAL-LOGIC mount at `/admin/integrations`. Verified via `find src/app -name integrations -type d`: NO such directory exists in the current codebase. Plan 1's redirect map (`next.config.mjs:103`) does include `/settings/integrations → /admin/integrations`, but the source page never existed (the plan assumed it did).
- **Fix:** Rendered `/admin/integrations` as an F3 placeholder describing the future third-party connection registry (QuickBooks, Buildertrend, Stripe credentials + sync settings). Stripe billing UI itself lives at `/admin/billing` (real-logic). The overview Card grid marks it Coming F3 (not Live).
- **Files modified:** `src/app/admin/integrations/page.tsx`, `src/app/admin/page.tsx` (overview tile status changed from "live" to "placeholder")
- **Verification:** Build green; route resolves; consistent with adjacent F3 placeholders.
- **Committed in:** `a9f7eb9` (Task 1 commit)
- **Plan impact:** Admin section has 3 REAL-LOGIC mounts (not 4 as plan stated) + 10 placeholders + 1 overview = 13 total routes (count matches plan).

**3. [Rule 2 - Missing Critical Token Discipline] text-white → text-nw-white-sand in /admin/billing**

- **Found during:** Post-edit hook block on `/admin/billing/page.tsx` write
- **Issue:** Source file `/settings/billing/page.tsx` contains `text-white` on the "Compare Plans" CTA. Per Slate token discipline (CLAUDE.md UI rules + Nightwork standing rules), `text-white` is forbidden; Nightwork uses `text-nw-white-sand`. The verbatim copy inherited the pre-existing violation.
- **Fix:** Replaced `text-white` with `text-nw-white-sand` in the new `/admin/billing` file. (The original at `/settings/billing/page.tsx` still has the violation — it's pre-existing in code outside Plan 6 scope and will resolve when the legacy file is deleted in F1 cleanup.)
- **Files modified:** `src/app/admin/billing/page.tsx`
- **Verification:** Hook silent on /admin/billing/page.tsx; visual fallback resolves correctly.
- **Committed in:** `a9f7eb9` (Task 1 commit)

**4. [Rule 2 - Missing Critical Token Discipline] Pre-existing #FFF8F3 hex in end-impersonation-button.tsx**

- **Found during:** Post-edit hook block on `end-impersonation-button.tsx` edit (Task 2)
- **Issue:** Pre-existing hardcoded hex `color: "#FFF8F3"` on line 35 of `end-impersonation-button.tsx`. NOT introduced by Plan 6 — predates the file's appearance in this work. My Task 2 edit only touched lines 18-21 (default redirect path). Post-edit hook scans whole file after any Edit, blocking the commit.
- **Fix:** Pragmatic inline replacement — `#FFF8F3` → `var(--nw-white-sand)` (same visual result; token-correct). Logged in `.planning/phases/stage-1.5c-information-architecture/deferred-items.md` per scope-boundary rule for transparency since the fix was technically out of Plan 6's nominal scope.
- **Files modified:** `src/components/end-impersonation-button.tsx`, `.planning/phases/stage-1.5c-information-architecture/deferred-items.md`
- **Verification:** Hook silent; build green.
- **Committed in:** `3a9d816` (Task 2 commit)

**5. [Rule 3 - Blocking] /platform-admin/items/page.tsx not migrated (source doesn't exist)**

- **Found during:** Task 2 source-file enumeration via `find src/app/admin/platform -type f -name '*.tsx'`
- **Issue:** Plan 6 lists `/platform-admin/items/page.tsx + [id]/page.tsx` in scope (12 sub-routes total). Source enumeration revealed `/admin/platform/items/` contains only `[id]/page.tsx` (no items list `page.tsx`). Plan 1's `next.config.mjs:43` redirects `/admin/platform/items → /platform-admin/cost-intelligence` — i.e., the items list lives at the Cost Intelligence Ops page (items tab), not at its own route. The plan assumed a list page exists but it doesn't.
- **Fix:** Migrated only what exists — `/platform-admin/items/[id]/page.tsx`. Updated back-link in items/[id] from `/admin/platform/items` (which redirected to /platform-admin/cost-intelligence) to direct `/platform-admin/cost-intelligence` link (skips redirect hop). Plan 6 SUMMARY count: 12 migrated sub-routes (not 13) + 1 layout + 1 index = 14 platform-admin surfaces.
- **Files modified:** `src/app/platform-admin/items/[id]/page.tsx` (back-link updated)
- **Verification:** Build green; back-link clickable, lands at cost-intelligence with no redirect hop.
- **Committed in:** `3a9d816` (Task 2 commit)

---

**Total deviations:** 5 auto-fixed (1 bug fix, 1 blocking-source-missing, 2 token-discipline, 1 plan-scope-mismatch)

**Impact on plan:** All auto-fixes preserve the spirit of the plan while resolving real codebase state. Two deviations (Rule 3 fixes for `/admin/integrations` and `/platform-admin/items`) revealed that Plan 6 was authored against an assumed source-file inventory that doesn't fully match the current codebase. Recommendation for Jake: confirm whether `/settings/integrations` was deliberately not built (in which case Plan 6 placeholder is correct), or whether F1 should build it.

## Issues Encountered

- **Source file enumeration revealed plan-spec gaps** — `/settings/integrations` and `/admin/platform/items/page.tsx` were both listed in Plan 6 but don't exist. Resolved via Rule 3 (auto-fix blocking issue). No halt required.
- **Post-edit hook flagged pre-existing token violations in inherited code** — handled per scope-boundary rule (fixed inline + logged to deferred-items).
- **/admin/platform → /platform-admin migration: shared components needed dual-awareness** — `platform-sidebar.tsx` needed to handle both prefixes since the OLD `/admin/platform/*` pages still render (deletion deferred to F1). Implemented dual-prefix active-state matcher.

## iter-2 Watchpoint #2 Status — Sub-route functional verification

**Watchpoint #2:** "Sub-routes functional, not just routed."

**Status:** **Build + typecheck PASS** — all 12 migrated sub-routes compile and render at their new paths (verified via `npm run build` output: 12 /platform-admin routes in the dynamic-route table; 0 build errors; 0 typecheck errors).

**Manual smoke testing (Chrome DevTools MCP) was not executed in this Plan 6 run** because:
1. The plan has `halt_after: true` — Jake's review at GATE A is the human verification step.
2. The harness phase (Stage 1.5c-VH) added an automated post-Plan-6 verification pipeline that will exercise these routes in Layer 1 (DOM) + Layer 2 (mechanical) + Layer 3 (vision against PLAN-file criteria) on a Vercel preview deploy.
3. Per CLAUDE.md hybrid posture: "For verification-eligible surfaces (every UI change post-stage-1.5c-verification-harness): the harness's Layer 3 vision against PLAN-file `<criteria>` is the canonical pass/fail."

**Halt-review checklist for Jake (watchpoint #2 manual confirmation):**
- [ ] Log in as Jake (platform_admin) → visit `/platform-admin` → confirm directory of 6 tools renders + all marked LIVE
- [ ] Click each card → confirm sub-route renders existing functionality (`/platform-admin/audit` shows audit log table; `/platform-admin/organizations` shows org list; etc.)
- [ ] Verify cross-org access: `/platform-admin/audit` should show audit rows from MULTIPLE orgs (RLS posture preserved via `platform_admin_audit_staff_read` policy)
- [ ] Verify legacy bookmarks still work: visit `/admin/platform/audit` → 308 redirects to `/platform-admin/audit` (Plan 1 wildcard)
- [ ] Verify sidebar highlights correctly when rendered in either layout context

## iter-2 Watchpoint #3 Status — Auth bypass test

**Watchpoint #3:** "PM (non-platform_admin) authenticated session navigating to /platform-admin/audit MUST receive redirect (NOT successful render)."

**Status:** **Middleware regex extension verified at code-level** — both build output and source inspection confirm the regex now includes `/platform-admin` and `/platform-admin/*` with the same `!isPlatformAdmin → redirect to /dashboard` posture. The middleware short-circuits BEFORE any page rendering occurs.

**Manual smoke testing checklist for Jake:**
- [ ] Log out as platform_admin
- [ ] Log in as a PM (non-platform_admin user)
- [ ] Manually navigate to `/platform-admin/audit` (paste URL in browser)
- [ ] **Expected:** redirect to `/dashboard` (or `/today` per Plan 1 mapping)
- [ ] **NOT expected:** successful render of audit log
- [ ] Repeat at `/platform-admin/organizations`, `/platform-admin/users`, `/platform-admin/feedback`, `/platform-admin/support`, `/platform-admin/cost-intelligence` — all should redirect

If any successful render observed for non-platform_admin user, the middleware regex extension has a flaw — halt and escalate.

## SEC-9 Compliance — /platform-admin/audit Supabase client type

Per iter-3 amendment (SEC-9 follow-up):

**Source-file inspection (verified):** `src/app/admin/platform/audit/page.tsx` uses `createServerClient()` from `@/lib/supabase/server` — the anon/session-cookie client with RLS, NOT service-role. Cross-org reads work because `platform_admin_audit` table has the `platform_admin_audit_staff_read` RLS policy gated on `app_private.is_platform_admin()` (migration 00048).

**Migrated file (verified):** `src/app/platform-admin/audit/page.tsx` preserves `createServerClient()` verbatim — same import path (`@/lib/supabase/server`), same builder call, same query shape, same RLS gate posture. **NOT swapped to service-role.**

**Why this matters:** Swapping to `createServiceRoleClient()` would bypass RLS on the adjacent profiles + organizations look-up queries inside the same file (lines 96-109). Those queries currently filter by `platform_admin_audit_staff_read` indirectly (admins can read all profile/org metadata when fetching cross-org audit context). Service-role would BYPASS RLS entirely — a regression.

**iter-2 mechanical #10 grep diff for audit:** ZERO DIFF (verbatim preservation verified at lines 1-388 of `/platform-admin/audit/page.tsx`).

## User Setup Required

None — no external service configuration introduced. Existing Stripe + Supabase auth + RLS posture preserved.

## Next Phase Readiness

- **Ready for GATE A (halt-after) Jake review** — both Tasks shipped + pushed; build + typecheck green; iter-2 mechanical #10 verified; middleware regex extension code-verified.
- **Ready for post-Plan-6 harness validation** — the verification harness can target:
  - 13 new /admin/* routes (functional + visual)
  - 14 new /platform-admin/* surfaces (functional + visual + cross-org RLS verification)
  - Middleware regex extension (auth bypass test as PM)
  - Shared component dual-awareness (sidebar active-state in both prefix contexts)
- **Plan 7 follow-on:** atomic CLAUDE.md update documents D-22 precedence; final canonical-docs smoke.

## Known Stubs

None introduced. The `/admin/profile` stubbed selector is intentional per Q12=A (F2 wires real behavior); the rest are placeholder Cards consuming NwPlaceholderCard with explicit Wave badges.

## Threat Flags

None — Plan 6 introduces no new network endpoints, auth paths, file access patterns, or schema changes at trust boundaries. Existing platform_admins gate (migrations 00048 + 00049) preserved; middleware regex extension EXTENDS the gate (does not weaken or bypass it).

## TDD Gate Compliance

N/A — Plan 6 is `type: execute` (not `type: tdd`). No TDD gate enforcement required.

## Self-Check: PASSED

Verified post-write (2026-05-11):

- **All 27 created files exist on disk** — 13 admin (page.tsx + 12 sub-routes) + 14 platform-admin (page.tsx + layout.tsx + 12 migrated sub-routes)
- **All 4 modified files exist on disk** — middleware.ts, platform-sidebar.tsx, audit-row.tsx, end-impersonation-button.tsx
- **Both commits found in git log** — `a9f7eb9` (Task 1), `3a9d816` (Task 2)
- **Build green** — 25 admin routes + 12 platform-admin routes appearing in Next.js build output; no errors; no warnings flagged
- **Typecheck clean** — `npx tsc --noEmit` exit 0
- **Hex check clean** — 0 hardcoded hex in /admin/* + /platform-admin/* trees
- **iter-2 mechanical #10 verified** — 14 of 15 routes ZERO DIFF; 1 documented divergence (users/[id] URL-path rewriting per Plan 6 Task 2 Step B)
- **Middleware regex extension verified** — `grep -c '/platform-admin' src/middleware.ts` returns 7 (includes 4 new pathname checks + redirect param + 2 comment references)

---

*Phase: stage-1.5c-information-architecture*
*Completed: 2026-05-11*
